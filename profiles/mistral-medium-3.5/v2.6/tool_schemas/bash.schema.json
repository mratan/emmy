{
  "type": "function",
  "function": {
    "name": "bash",
    "description": "Run a bash command (YOLO default; per CLAUDE.md). Output truncated to head+tail of 50 lines each side. Timeout default 60000ms. Denylist applies (rm -rf /, fork bomb, plus user-configured patterns).",
    "parameters": {
      "type": "object",
      "properties": {
        "command": { "type": "string" },
        "cwd": { "type": "string" },
        "timeout_ms": { "type": "number" }
      },
      "required": ["command"],
      "additionalProperties": false
    }
  }
}
