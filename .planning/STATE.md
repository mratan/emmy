---
gsd_state_version: 1.0
milestone: v0.68.0
milestone_name: milestone
status: unknown
last_updated: "2026-05-01T23:15:48.748Z"
progress:
  total_phases: 14
  completed_phases: 8
  total_plans: 69
  completed_plans: 56
  percent: 81
---

# State: Emmy

**Last updated:** 2026-04-28
**Updated by:** operator-and-claude (QWEN 35B-A3B MoE DROPPED FROM ACTIVE STACK 2026-04-28 — operator decision after V-RESULTS-v8 measured 55% V1 memory adoption: "if it can't follow directions, I would rather not use it." Profile bundles `profiles/qwen3.6-35b-a3b/{v1,v2,v3,v3.1,v3.2,v3.1-default,v3.1-precise,v3.1-reason}` deleted; `profiles/routes.yaml` deleted (Phase 4 SC-3 within-model role routing was Qwen-MoE-only by D-08; routes-loader falls back to default-only mode per documented ENOENT path). Historical evidence (V-RESULTS-v1..v8, postmortems, V-PROTOCOLS, all `.planning/phases/04.4-…/runs/` artifacts) preserved as research record. Test fixtures migrated from Qwen MoE to Gemma; Qwen-specific tests (Hermes parser, qwen3-coder tool parser) marked .skip with V-RESULTS-v8 decision pointer. Qwen 27B dense lives on as opt-in Phase 4.1 dense sibling. Surfaces touched: profile bundles deleted; `routes.yaml` deleted; `CLAUDE.md` Pinned Tech Stack rewritten (Primary model + heavy-context-alternate-slot rows); `README.md` profile ladder + Quickstart + troubleshooting; `eval/MATRIX.md` rewritten to drop Qwen MoE participant + add v2.1/v1.2 256K rows; this STATE.md entry; ~25 test files migrated. **Same-day predecessor — DAILY-DRIVER SWITCH:** Qwen 35B-A3B v3.1 → Gemma 4 26B-A4B v2.1 on 2026-04-28 per the now-complete 4-profile V-protocol matrix at `.planning/phases/04.4-…/runs/V-RESULTS-v8-matrix-complete.md`. Trigger: bio-pipeline freed UMA, Gemma MoE @ gmu=0.86 finally bootable; matrix completed with V1 = 100% (20/20) + V3 = 5/5 + 0 writes. The data **falsifies** V-RESULTS-v7's "active-params drives MoE compliance" hypothesis — Gemma MoE's 4B active is just 33% more than Qwen MoE's 3B but adoption is 100% vs 55%. The Qwen-MoE 50-55% V1 ceiling is Qwen-MoE-specific, not architectural; candidates for the actual driver are Qwen tool-use RL trade-off / router collapse on instruction tokens / chat-template positional differences (Phase 5 measurement). Throughput cost ~36 vs ~48 tok/s is operator-accepted in exchange for higher compliance. **Surfaces switched (commit `<TBD>`):** `CLAUDE.md` Pinned Tech Stack ("Primary model" row), `packages/emmy-ux/bin/pi-emmy.ts` defaultProfilePath() + help string, `scripts/start_emmy.sh` PROFILE variable + usage example, `README.md` daily-driver narrative + profile ladder, this STATE.md entry. **Surfaces NOT switched (intentional):** `profiles/routes.yaml` keeps the Qwen variants because Phase 4 SC-3 within-model role routing was Qwen-only by D-08 design and engine byte-identity prevents Qwen variant overlays from applying to a Gemma engine; routes.yaml gains an explanatory comment that role routing degrades to a no-op when Gemma is loaded (Phase 5+ work to author Gemma plan/edit/critic siblings). `emmy_serve/airgap/ci_verify_*.py` DEFAULT_PROFILE constants left alone — they're CI validators, profile-agnostic in spirit. **Open follow-up — sidecar controller death quirk:** during the Gemma boot swap the localhost:8003 sidecar Python (`emmy_serve.swap.controller`) died (controller PID gone from `ss -tnlp` after the swap completed) but vLLM at 127.0.0.1:8002 came up cleanly and pi-emmy ran 25 sessions with 100% jsonl coverage. Investigation under task 16. PRIOR ENTRY: executor (Phase 04.2 paperwork CLOSED — Plans 01-05 LANDED 2026-04-25; Plan 06 closed paperwork 2026-04-26 with the 3 operator-gated SC walkthroughs DEFERRED per Phase 1/Phase 4 closeout precedent. Sidecar `emmy_serve.swap.controller` is now the always-on Spark-side FastAPI control plane bound to 0.0.0.0:8003; systemd user unit `emmy-sidecar.service` enables it at boot. TS-side dual-path dispatcher routes through HTTP+SSE when `EMMY_REMOTE_CLIENT=1`; spawn-argv snapshot pins local-mode argv at byte equivalence (D-04 BYTE-STABLE). New slash commands `/start`, `/stop`, `/status` operate from the Mac TUI. `web-search.ts` honors `EMMY_SEARXNG_URL` (D-33 LOCKED loopback default preserved when unset; air-gap regression smoke green). `install-client.sh` writes `EMMY_REMOTE_CLIENT` + `EMMY_SERVE_URL` + `EMMY_SEARXNG_URL` into the wrapper at install time. Operator-gated SC-1/SC-2/SC-3 phase4.2 walkthroughs documented verbatim in `docs/runbook.md § Phase 04.2 SC walkthroughs`. REQ-IDs touched: TOOLS-10, UX-04, UX-07 (all flipped to Done†; promote to Done after `sc1/sc2/sc3 phase4.2 green` resume signals). Local mode is byte-stable; air-gap CI continues to gate STRICT egress. Cumulative 43/68 REQ-IDs Done (no new REQ-IDs added; Phase 04.2 ships extensions, not new requirements). PRIOR ENTRY: Phase 04.1 fully CLOSED — both dense sibling profiles authored, KV-bisected, 2×2h thermal-passed. `qwen3.6-27b/v1` (NGC fastsafetensors, hash c3ccf1e1, gmu 0.86, decode p50 7.6 tok/s) + `gemma-4-31b-it/v1` (upstream Day-1 Gemma 4 image, hash fe9eded6, gmu 0.86, decode p50 6.4 tok/s) both pass with preemptions=0, oom=0. Daily-driver default UNCHANGED at qwen3.6-35b-a3b@v3.1 per operator directive. routes.yaml extended with optional `dense:` role, eval/MATRIX.md authored as Phase 5 participant manifest, runbook updated with 4-profile table + container-per-family rule. Throughput informational only — both dense profiles bandwidth-bound by design (CONTEXT.md research range met for Gemma; Qwen lower than estimate but per operator directive NOT a gate). Mid-stream canary fix landed (max_tokens 128→2048 in tool_call probe to handle Qwen3.6-27B's reasoning-prefix style — backward-compatible across all profiles). All 4 GB10 profiles converge to gmu=0.86 — clearly a hardware-level vLLM allocation ceiling. Cumulative 43/68 REQ-IDs Done; Phase 5 eval matrix is now eval-ready)

---

## Project Reference

**Project:** Emmy — fully-local coding agent on NVIDIA DGX Spark
**Core Value:** A local coding agent good enough to be the author's daily driver, structured rigorously enough to be a public research artifact others can reproduce — with no cloud dependency anywhere in the loop.
**Current Focus:** Phase --phase — 04.7

**Authoritative documents:**

- `.planning/PROJECT.md` — what Emmy is, constraints, key decisions
- `.planning/ROADMAP.md` — 7-phase plan with success criteria
- `.planning/REQUIREMENTS.md` — 66 v1 requirements with phase mappings
- `.planning/research/SUMMARY.md` — research synthesis
- `.planning/research/STACK.md` — recommended stack (NGC vLLM 0.19.x, Qwen3.6, Gemma 4 26B MoE, pi-mono v0.68.0, XGrammar, Langfuse v3, terminal-bench 2.0)
- `.planning/research/FEATURES.md` — MVP definition split v1/v1.x/v2 with P1/P2/P3 priority
- `.planning/research/ARCHITECTURE.md` — 4-component MVP spine, two-process topology
- `.planning/research/PITFALLS.md` — 20 pitfalls; 8 critical, mapped to phases

---

## Current Position

Phase: --phase (04.7) — EXECUTING
Plan: 1 of --name
**Phase 1:** Serving Foundation + Profile Schema — closed 2026-04-21 with 3 documented deferrals; see `.planning/phases/01-serving-foundation-profile-schema/01-CLOSEOUT.md`
**Phase 2:** Pi-Harness MVP — Daily-Driver Baseline — closed 2026-04-21 with SC-1 green + SC-2/3/4/5 pass; 5 Phase-3 wire-through deferrals; see `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md`
**Phase 3:** Observability + Agent-Loop Hardening + Lived-Experience — closed 2026-04-22 with SC-1 phase3 green + SC-2/3/4/5 pass; v3 profile hash `sha256:2beb99c7...d4d3718`; 8 Phase-3 REQ-IDs + 5 Phase-2 Done† promoted to Done (13 total flipped; cumulative 36); 5 operator-gated evidence items deferred (not blockers); see `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CLOSEOUT.md`
**Phase 4:** Gemma 4 Profile + Profile System Maturity — closed 2026-04-23 with SC-2 + SC-5 automated-green + SC-1/SC-3/SC-4 wire-proven end-to-end (4 operator-gated live-rig evidence items deferred per Phase 1 D-15 precedent); Gemma 4 v1 hash `sha256:6d2884fb...384450` + 3 Qwen v3.1 sibling variants (default/reason/precise, byte-identical engine sections) + `profiles/routes.yaml`; 5 REQ-IDs flipped (SERVE-03 + PROFILE-07 Done; PROFILE-08 + HARNESS-08 + UX-04 Done†); 188 pytest + 520 bun + 5/5 typecheck + 8/8 profile validates at close; see `.planning/phases/04-gemma-4-profile-profile-system-maturity/04-CLOSEOUT.md`
**Phase 04.1:** Dense-variant model profiles — closed 2026-04-25 with both dense sibling profiles (Qwen 27B v1.1 + Gemma 31B v1.1) KV-bisected, 2×2h thermal-passed (preemptions=0, oom=0); daily-driver default UNCHANGED at qwen3.6-35b-a3b@v3.1; routes.yaml extended with optional `dense:` role; eval/MATRIX.md authored as Phase 5 participant manifest.
**Phase 04.2:** Remote-client mode parity — Plans 01-05 LANDED 2026-04-25; Plan 06 paperwork CLOSED 2026-04-26 with 3 operator-gated SC walkthroughs DEFERRED per Phase 1/Phase 4 closeout precedent; sidecar (`emmy_serve.swap.controller`) + dual-path dispatcher + `/start`/`/stop`/`/status` slash commands + `EMMY_SEARXNG_URL` override + air-gap regression smoke green; TOOLS-10 + UX-04 + UX-07 flipped to Done† (promote to Done after `sc1/sc2/sc3 phase4.2 green` resume signals); 250 pytest + 322 emmy-ux bun green; D-04 BYTE-STABLE + D-33 LOCKED preserved.
**Next:** `/gsd-plan-phase 5` — Eval harness + reproducible benchmark suite (terminal-bench 2.0 primary + SWE-bench Verified milestone + LiveCodeBench rolling + prior Phase 1 prompts for continuity; eval imports harness via public SDK; ≥3 samples + std + provenance + executable-paired-with-judge). Phase 04.2 SC walkthroughs can be closed opportunistically whenever operator tailnet time allows; not blockers for Phase 5 start.
**Phase 2 Progress:** 100% (9/9 plans landed; Phase-2-close certified v2 hash `sha256:24be3eea...85d8b`)
**Phase 3 Progress:** 100% (7/7 plans landed; v3 profile hash `sha256:2beb99c773a0e425a3e485459964740640c5f3addbea186738402cf66d4d3718` certified at close 2026-04-22; 8 Phase-3 REQ-IDs + 5 Phase-2 Done† → Done)
**Phase 4 Progress:** 100% (6/6 plans landed + **all 4 operator-gated deferrals RESOLVED 2026-04-23 → 2026-04-24**; 26 commits across 4 waves + 4 follow-up commits; Gemma 4 v1 + 3 Qwen sibling variants + routes.yaml shipped; D-19 no-model-conditionals audit green in both Python and TS; `sc1 phase4 green` + `sc3 phase4 green` + `sc4 phase4 green` resolved in prior 2026-04-23 follow-up session; `p4 kv green` + `p4 thermal floors recorded` + `p4 thermal green` resolved 2026-04-24 via Gemma 4 v2 profile bundle: gpu_memory_utilization bisects to 0.86 (11 iters, 3h34m) + 2×2h thermal replay with zero regressions ("All floors pass"). Gemma 4 v2 hash `sha256:8f9c23f500...` measured floors populated.)

**Daily-driver bar: REACHED (end of Phase 2) — 2026-04-21.** SC-1 walkthrough verdict `sc1 green`; author ran pi-emmy against a clean repo, agent completed multi-file task on Qwen3.6 via local vLLM endpoint, no cloud call.

```
Phases: [✓][✓][✓][✓][▢][▢][▢]   4/7 phases complete (+ 03.1 + 04.1 inserts CLOSED; 04.2 insert CLOSURE PENDING)
Current: Phase 4.2 CLOSURE PENDING 2026-04-26 — paperwork landed (runbook SC walkthroughs + ROADMAP/STATE/REQUIREMENTS backfill); operator-gated SC-1/SC-2/SC-3 phase4.2 walkthroughs flip TOOLS-10/UX-04/UX-07 from Done† → Done; **daily-driver default switched 2026-04-28 to gemma-4-26b-a4b-it/v2 per V-RESULTS-v8**; ready for Phase 5 (eval harness + reproducible benchmark suite, dense-vs-MoE A/B with new Gemma-MoE baseline)
```

**Daily-driver bar:** end of Phase 2 — **REACHED**
**Research-artifact bar:** end of Phase 5
**Public artifact bar:** end of Phase 7

---

## Performance Metrics

| Metric | Value |
|--------|-------|
| Phases complete | 3 / 7 |
| v1 requirements complete | 36 / 66 (23 Phase-2 + 8 new Phase-3 + 5 Phase-2-Done†-promoted: HARNESS/TOOLS/CONTEXT/SERVE/UX/TELEM) |
| Critical pitfalls addressed | 8 / 8 (#1 KV theory, #3 grammar fights, #5 more-prompting via SC-3+SC-2 three-run discipline, #6 SP-delivery via SP_OK canary, #8 hidden cloud deps via bun.lock + uv.lock + air-gap + UX-03 OFFLINE OK badge; #2 vLLM API churn via profile digest-pinning; #7 thermal throttle via 2-hour sustained-load gate; #15 tool-result truncation via D-14 error-verbatim preservation) |
| Daily-driver readiness | **REACHED 2026-04-21** (SC-1 green on Qwen3.6-35B-A3B-FP8 via pi-emmy --print) |
| Research-artifact readiness | Not yet (blocked on Phase 5 eval harness) |
| Phase 03 P02 | 28min | 4 tasks | 26 files |
| Phase 03 P03 | 22min | 3 tasks | 24 files |
| Phase 03 P04 | ~10min | 3 tasks | 11 files |
| Phase 03 P05 | ~25min | 2 tasks | 15 files |
| Phase 03 P06 | ~10min | 2 tasks | 17 files |
| Phase 04.2 P06 | 7min | 1 (Task 4 paperwork; Tasks 1-3 deferred operator-gated) tasks | 4 modified + 1 SUMMARY created files |

### Per-plan execution log

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 02 P01 | 12min | 2 tasks | 20 files |
| Phase 02 P04 | 19min | 2 tasks | 20 files |
| Phase 02 P07 | 8min  | 1 task + 1 Phase-1-schema patch | 21 files (12 created, 7 modified, 2 deleted) |
| Phase 02 P08 | 135min | 2 tasks | 30 files (25 created, 5 modified) |
| Phase 02 P09 | ~25min | 2 tasks (1 SC-1 walkthrough checkpoint + 1 CLOSEOUT) | 7 files (3 created, 4 modified) + 4 live bug-fix commits (2c22018, 4049d95, 85fa910, a17f4a9) |
| Phase 03 P01 | ~3h | 3 tasks (RED + GREEN + SC-1-class walkthrough checkpoint) | 19 files (12 created, 7 modified) |
| Phase 03 P02 | ~28min | 4 tasks (infra + RED + GREEN + Rule-3 healthcheck fix; Task 4 SC-1 trace walkthrough operator-gated) | 26 files (19 created, 7 modified) |
| Phase 03 P03 | ~22min | 3 tasks (scaffold + trigger body + SC-2 fixture/runner; SC-2 live matrix deferred to 03-07) | 24 files (19 created, 5 modified) |
| Phase 03 P04 | ~10min | 3 tasks (RED + GREEN + Task 3 operator driver; SC-4 parity verification operator-gated) | 11 files (10 created, 3 modified) |
| Phase 03 P05 | ~25min | 2 tasks (RED + GREEN; Task 3 interactive-TUI verification operator-gated) | 15 files (11 created, 4 modified) |
| Phase 03 P06 | ~10min | 2 tasks (RED + GREEN; Task 3 operator-gated UX-03 demo) | 17 files (7 created, 10 modified) |
| Phase 03 P07 | ~45min | 4 tasks (v2→v3 profile bump + schema patch + air-gap CI extension + CLOSEOUT walkthrough) | 27 files (20 created, 7 modified) |
| Phase 04.2 P01 | 26min | 3 tasks (state/runner/metrics + controller.py FastAPI app + 5 unit-test files) | 12 files (9 created, 3 modified); 62 sidecar unit tests |
| Phase 04.2 P02 | 5min | 2 tasks (systemd unit + integration test + runbook posture section) | 5 files (3 created, 2 modified); 6 integration tests + smoke gate |
| Phase 04.2 P03 | 8min | 2 tasks (HTTP+SSE swap runner + dual-path dispatcher + spawn-argv snapshot guard) | 6 files (3 created, 3 modified); 23 dispatcher tests |
| Phase 04.2 P04 | ~10min | 2 tasks (sidecar-status-client + 3 slash commands + sidecar-lifecycle-client + metrics-poller D-09 branch + extension wiring) | 9 files (6 created, 3 modified); +48 tests |
| Phase 04.2 P05 | ~10min | 3 tasks (web-search.ts EMMY_SEARXNG_URL getter + install-client.sh wrapper + README + air-gap regression smoke) | 5 files (2 created, 3 modified); 4 env-override tests + D-33 LOCKED canary |
| Phase 04.2 P06 | ~15min | 1 autonomous task (3 SC walkthroughs DEFERRED operator-gated per Phase 1/Phase 4 closeout precedent + ROADMAP/STATE/REQUIREMENTS backfill + runbook SC scripts) | 4 files modified (docs/runbook.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md) |

---

## Accumulated Context

### Roadmap Evolution

- Phase 04.1 inserted after Phase 4 (2026-04-24): Dense-variant model profiles — Qwen3.6-27B-FP8 + Gemma-4-31B-it dense siblings for Phase 5 A/B (INSERTED). Goal: provide dense-vs-MoE comparison slots for Phase 5 eval matrix. Does NOT change daily-driver default. Dense throughput expected substantially lower than MoE counterparts (bandwidth-bound on GB10) — accepted and NOT a pass/fail criterion.
- Phase 04.2 inserted after Phase 4 (2026-04-25): Remote-client mode parity (URGENT). Goal: make `/profile`, `/start`, `/stop`, `/status`, and `web_search` work from a Mac/laptop client over Tailscale; today they all hardcode `127.0.0.1` and break on the client. Architecture: always-on Python sidecar on Spark (`emmy_serve.swap.controller`) owns vLLM lifecycle so user can `/stop` to reclaim RAM and `/start <profile>` to wake; vLLM container stays on-demand. Mac client refactor: `profile-swap-runner.ts` detects `EMMY_REMOTE_CLIENT=1` and routes to HTTP+SSE instead of `spawn("uv",…)`; `web-search.ts` honors `EMMY_SEARXNG_URL` override (D-33 LOCKED loopback invariant preserved for local mode — env override is the documented remote-client escape hatch). `install-client.sh` adds two `tailscale serve` routes (sidecar :8003, SearxNG :8888). Auth: trust-on-tailnet, no token (single-user single-tailnet posture). Estimated 6 plans.

### Decisions Made During Roadmapping

- **Phase 1 carries the SP_OK canary infrastructure** (EVAL-07), not just Phase 5. Rationale: PITFALLS.md ranks system-prompt-delivery failure as Critical and the prior repo's Phase 3 incident wasted 5/5 task scores at 0/5 success. The canary is shipped as boot-time smoke-test infrastructure used by every later phase.
- **Hash-anchored edits ship in Phase 2 as the default edit format** (TOOLS-03), not as later polish. Rationale: documented 6.7 → 68.3% improvement on 180 tasks for weak models; it is the highest-leverage single change for daily-driver feel. Plain string-replace falls back only when hashes can't be computed.
- **MCP client and web_fetch ship in Phase 2**, not later. Rationale: 2026 inflection point — MCP is now infrastructural (LF governance Dec 2025, 10k+ servers), and web_fetch is documentation reading (allowed under "no cloud inference" constraint). Daily-driver bar requires both.
- **Eval harness is sequenced as Phase 5** (parallelizable with P3/P4 after P2 stable). Rationale: it depends only on the Phase 2 SDK entry point per ARCHITECTURE.md, and isolating it means P3 and P4 don't gate on eval methodology decisions.
- **Speculative decoding is Phase 6, not earlier.** Rationale: PITFALLS.md #4 — spec decode requires working profiles + eval to measure correctly via paired benchmark; sequencing earlier would measure it against an unstable baseline.
- **Two first-class models proven at Phase 4, not Phase 1.** Rationale: adding the second model is what proves the profile abstraction is truly model-agnostic. Phase 1 ships one profile end-to-end; Phase 4 forces the abstraction by adding Gemma 4 with its own tool format and quirks.
- **Granularity calibration:** the natural phase count came out to 7, which sits at the upper edge of Standard granularity (5–8). All 7 phases are real coherent capabilities — none are padding.

### Decisions Made During Execution

- **Bun 1.3 text-lockfile (`bun.lock`) committed in place of legacy `bun.lockb`** (Plan 02-01). Bun ≥1.2 defaults to text lockfiles; the binary format is deprecated. Text lockfile preserves Pitfall #8 reproducibility while adding audit-diffability. Neither `bun.lock` nor `bun.lockb` is gitignored.
- **Profile v2 built as sibling of Phase 1-locked v1** (Plan 02-01). Preserves Phase 1 certification hash `sha256:b91e747...`; v2 harness.yaml TODO fills + hash recomputation owned by Plan 02-07.
- **pi-coding-agent 0.68.0 pinned EXACTLY (no `^`/`~`) in all four @emmy/* packages** (Plan 02-01). TS-side analog of Phase 1's `uv.lock` discipline per T-02-01-04 threat register.
- **Plan 02-04 chose pi's `createAgentSession` (SDK path) + `SessionManager.inMemory(cwd)` over the full `createAgentSessionRuntime` machinery.** Rationale: Phase 2 needs a real AgentSession + turn subscription for transcript capture, not session-replacement / fork / import flows. Plan 02-09's SC-1 walkthrough can upgrade to `AgentSessionRuntime` if interactive-TUI bindings require it; for now the adapter exposes `.session` for callers who need to drive pi directly.
- **Narrow `PiRuntime` adapter pattern** (Plan 02-04). Presents `{registerProvider, registerTool, on, session}` because @emmy/provider and @emmy/tools already target this shape. Pi's real `ExtensionAPI` is richer; adapter's `registerProvider/registerTool` are no-ops in Phase 2 (calls are ordered + observed) because Phase 2 does not plumb @emmy tools through pi's tool pipeline — that's a Phase 3 extension-runner binding.
- **CLI test hybrid execution model** (Plan 02-04 Rule 3 deviation). The execution sandbox does not route subprocess→parent localhost traffic; static CLI behaviors (--help, --print-environment, missing profile) use real `spawnSync`, network-touching paths (SP_OK canary, vLLM probe, profile validate) import and call `main()` in-process. Both exercise the same CLI orchestration logic. Pattern applies to future @emmy/* CLIs under the same sandbox.
- **`EMMY_PROFILE_VALIDATE_BIN` + `EMMY_SKIP_PROFILE_VALIDATE` env vars as CLI test hooks** (Plan 02-04). Production always shells `uv run emmy profile validate`; tests override via `EMMY_PROFILE_VALIDATE_BIN=/bin/false` to simulate failure or short-circuit via `EMMY_SKIP_PROFILE_VALIDATE=1` for failure-mode tests that don't exercise this gate.
- **`runs/phase2-sc3-capture/` transcript capture is ALWAYS ON** (Plan 02-04 / B2). No opt-in flag; every pi-emmy session writes JSONL turns so Plan 02-08's SC-3 real-replay corpus builds up passively during daily-driver use. Replay runs themselves guard against feedback by convention, not a flag.
- **Phase-1 schema patch was needed (B3/C3 discovery)** (Plan 02-07). `emmy_serve/profile/schema.py` `ToolsConfig.grammar: Optional[str]` → `Optional[GrammarConfig]`. Nested `{path, mode}` shape locked by CONTEXT D-11. v1 still validates (backward-compatible with `None` default). Committed as dated discrete commit `88e48a4` per Plan 02-07 Step 0 so Plan 02-09 CLOSEOUT can cite the SHA in its addendum. Phase 1 tests: 137 pass / 1 skip unchanged.
- **v2 `context.max_input_tokens = 114688`** (Plan 02-07). Derived honestly from `serving.yaml.engine.max_model_len` (131072) − `output_reserve_tokens` (16384) per CONTEXT-05 / SC-5. Computed via `scripts/compute_max_input_tokens.ts` (js-yaml + `@emmy/ux` at repo root). Plan-04 `TODO(plan-07)` max-model-len regression un-skipped: loads serving + harness + PROFILE_NOTES frontmatter, asserts `harness.yaml == computed`. Any future drift surfaces as a failed test.
- **v2 profile hash recomputed** via `uv run emmy profile hash --write` (Plan 02-07): `sha256:b91e747…21913` (stale v1 clone) → `sha256:0025799f…53fa41` (v2 honest). The `KNOWN-STALE` warning comment is auto-stripped by `--write` (rewrites profile.yaml in canonical YAML form). v1 unchanged (byte-identical to Phase 1 closeout). Both profiles `uv run emmy profile validate` exit 0.
- **Grammar is reactive, not always-on, and over-accepting** (Plan 02-07 landing D-11). `tools.grammar.mode: reactive` in v2/harness.yaml; `disabled` reserved for Plan 02-08's SC-3 no-grammar baseline (D-14). The XGrammar Lark (`grammars/tool_call.lark`) is deliberately over-accepting on the `arguments` object — per-tool shape validation lives in `tool_schemas/*.schema.json`. CLAUDE.md Pitfall #6 backstop-not-shape-enforcer discipline.
- **SC-3 zero-retry observation confirms D-11 thesis** (Plan 02-08). Qwen3.6-35B-A3B-FP8 emits parseable tool-call arguments on 300/300 first-try attempts across all three variants (reactive, disabled, no_per_tool_sampling) on the 100-call corpus. The reactive grammar retry path fired ZERO times. Grammar is a genuine zero-cost correctness backstop for this model; future profiles (Gemma 4 in Phase 4, harder adversarial corpora) may flip this.
- **SC-3 W3/Pitfall-#5 per_tool_sampling finding — unobservable at single-turn level** (Plan 02-08). Removing `tools.per_tool_sampling` from harness.yaml produced IDENTICAL parse-rate (1.0 aggregate) on the same 100-call corpus. This is NOT evidence the knobs are useless; it's evidence their effect is unobservable at single-turn wire-shape level — their impact manifests during multi-turn tool-selection agent loops. Phase 5 eval will revisit on terminal-bench multi-turn corpora. Plan 02-09 CLOSEOUT cites this as the boundary of Phase 2's measurement.
- **v2 profile hash re-recompute at Phase 2 close** (Plan 02-08). `sha256:0025799f3bbbb802ebed207791e5e0b39624fa93f0ad6b315d7c488e3153fa41` → `sha256:24be3eea0067102f1f61bd32806a875d019fe02cb114697cd5f3ca4e39985d8b` (PROFILE_NOTES.md validation_runs extended with 6 Phase-2 SC entries; content hash changed accordingly). This is Phase 2's certified-at-close v2 hash; Plan 02-09 CLOSEOUT cites it in the addendum.
- **SC-4 in-process MCP server instead of external @modelcontextprotocol/server-filesystem** (Plan 02-08). `eval/phase2/sc4/test_mcp_fs_server.ts` is a 60-line file using MCP SDK's Server + StdioServerTransport directly. Avoids an additional external npm dependency while exercising identical bridge wiring. Pattern reusable for any future SC-4-class MCP bridge test.
- **Corpus backfill-first disclosure discipline** (Plan 02-08 / D-13). `runs/phase2-sc3-capture/` was empty at plan-execute time (no daily-driver sessions captured); `corpus_fill.ts` backfilled via 50 single-turn postChat calls. Each entry's `source:` field records natural-capture vs backfill-postChat; the README.md declares the 0/50 ratio explicitly. Pattern applies to any future parse-rate corpus.

### D-3X (Phase 04.4): Append-only-prefix invariant

**Decision:** The system-prompt prefix sent to vLLM never mutates within a session. Compaction replaces conversation-body turns; never edits system prompt, tool descriptions, project preamble (CLAUDE.md / AGENTS.md), or any pre-turn content.

**Rationale:**

- vLLM automatic prefix caching is the largest free-money win in long-context. Any prefix mutation invalidates blocks → full recompute → KV pressure → preemption.
- Cognition's 2026 "clean context for verifiers" lesson — stable prefix is closer to clean than rewriting.
- Reproducibility: prefix mutations interleaved with compactions create non-deterministic histories.

**Allowed:**

- `before_provider_request` payload mutations (thinking-disable, reactive grammar) — per-request payload, not conversation prefix.
- Compaction body replacement — pi's existing behavior.
- Custom messages appended AFTER system prompt — appending, not mutating.

**Forbidden:**

- Re-rendering system prompt mid-session
- Re-ordering tool descriptions mid-session
- Mutating CLAUDE.md / AGENTS.md and re-loading
- Adding "session summary so far" to system prompt at compaction time

**Enforcement:**

1. One-time audit (Phase 04.4 plan 06): `bun run packages/emmy-ux/scripts/audit_prefix_mutations.ts` — exits 0 if zero FORBIDDEN findings.
2. Per-request OTel attribute `emmy.prefix.hash = sha256(system_prompt_prefix_bytes)` — V3 test asserts hash equality at turn 0 and turn N within one session.
3. V4 architecture-aware (per Pitfall #22): attention-only profiles gate at ≥80% prefix-cache hit rate post-compaction; Mamba-hybrid profiles use "measure-and-acknowledge" (boot-log warning detection).

### Key Constraints Carried Forward

- **Hardware:** DGX Spark (GB10, 128 GB unified memory). Single model loaded at a time (~75 tok/s Qwen3.6, ~38–52 tok/s Gemma 4 MoE).
- **Container:** must use NGC `nvcr.io/nvidia/vllm:26.03.post1-py3`; never upstream PyPI vLLM (SM121 kernel failures).
- **Quantization:** FP8 only on DGX Spark; NVFP4 is slower than FP16 on GB10 UMA (-23.6% at 32K context) and ModelOpt 0.42.0 has a NaN bug.
- **No Gemma-4-31B Dense:** bandwidth-bound at 6.9 tok/s; only the 26B MoE variant is practical.
- **Air-gap thesis:** the entire stack must run with the network cable pulled; verified in CI.

### TODOs / Blockers

- None at this time. Awaiting `/gsd-plan-phase 1` to begin execution.

### Open Questions for Plan-Phase

Per research/SUMMARY.md "Research Flags" — questions that may need deeper research during plan-phase:

- **Phase 2:** Harness language choice (TypeScript directly on pi-mono SDK, or Python calling pi as subprocess) — affects observability bus implementation. Default lean per research: TypeScript.
- **Phase 4:** EAGLE-3 speculator availability for Gemma-4-26B-A4B specifically (RedHatAI publishes for 31B); Gemma 4 chat template handling for tool calls.
- **Phase 5:** SWE-bench Verified + mini-swe-agent API compatibility with vLLM 0.19.x; which SWE-bench-Lite subset is reproducible offline on Spark in a single eval run.
- **Phase 6:** Qwen3.6 MTP acceptance rates on coding workloads; whether two profiles can co-load on 128 GB UMA without container swaps.

Phases with standard patterns (skip research-phase per SUMMARY.md):

- **Phase 1:** stack already deeply researched; planning starts from STACK.md directly.
- **Phase 7:** publication is process work, not technical research.

---

## Session Continuity

**Current position:** Phase 2 CLOSED 2026-04-21 with SC-1 verdict `sc1 green`. Phase 1 and Phase 2 both have full closeouts on disk; Phase 3 planning pending.

### Phase 2 close summary

- **9 plans landed** (02-01..02-04, 02-06..02-09; 02-05 SUPERSEDED by 2026-04-21 structural revision). See `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md`.
- **All 5 Success Criteria pass:**
  - SC-1 daily-driver walkthrough: `sc1 green` — author ran `pi-emmy --print` against `/tmp/emmy-sc1-walkthrough/` (clean repo), agent created `src/{foo,bar,baz}.ts` + tests + ran `bun test` green, no cloud call. Evidence: `runs/phase2-sc1/walkthrough.md` + `runs/phase2-sc1/transcript.json`.
  - SC-2 hash-anchored edit regression: pass (0 hash-anchored failures vs 1 baseline failure; Hashline win on sc2_05 near-duplicate line). Evidence: `runs/phase2-sc2/report.json`.
  - SC-3 XGrammar parse rate: pass at 1.0 aggregate across 100 calls + 2 informational baselines (D-14 no-grammar baseline at 1.0; W3/Pitfall-#5 no_per_tool_sampling counterfactual at 1.0). Reactive retry path fired zero times. Evidence: `runs/phase2-sc3/{report,baseline,no_per_tool_sampling}.json`.
  - SC-4 MCP dispatch + Unicode poison rejection: pass (4/4 Cf/Co/Cs/bidi categories rejected; 2/2 fs-server tools registered flat + dispatched). Evidence: `runs/phase2-sc4/report.json`.
  - SC-5 prompt-hash + AGENTS.md + honest max_model_len: pass (3 runs → 1 unique sha256; max_input_tokens=114688 = computed). Evidence: `runs/phase2-sc5/report.json`.
- **23 Phase-2 REQ-IDs closed** in REQUIREMENTS.md traceability (HARNESS-01/02/03/04/06/07/10, TOOLS-01..09, CONTEXT-01/03/04/05, SERVE-05, UX-01/05). Five are flagged "Done †" — library shipped + tested + evidence captured, pi-pipeline wire-through deferred to Phase 3.
- **Profile hash trajectory:** v1 unchanged at `sha256:b91e747...21913` (Phase 1 close); v2 new at `sha256:24be3eea0067102f1f61bd32806a875d019fe02cb114697cd5f3ca4e39985d8b` (Phase-2-close certified; PROFILE_NOTES validation_runs extended in Plan 08).
- **Phase-1-schema patch** committed as `88e48a4` (separate dated commit per plan Step 0 instruction).
- **SC-1 findings — 4 live bug fixes committed inline:** `2c22018` (install-root path), `4049d95` (uv cwd), `85fa910` (real pi AgentSession wire-through for --print), `a17f4a9` (`<think>` strip stopgap). Full narrative in `runs/phase2-sc1/walkthrough.md` + `02-CLOSEOUT.md` § SC-1 findings.

### Phase 3 carry-forward (from 02-CLOSEOUT.md § Carry-forward)

Five documented architectural deferrals — libraries shipped + tested, only pi-pipeline wire-through remains:

1. `@emmy/provider` → pi's `streamSimple` hook via `BeforeProviderRequestEvent`.
2. Hash-anchored edit as pi's `customTools` override (swap pi's built-in edit for `editHashline`).
3. MCP bridge (`registerMcpServers`) as pi tool source via `customTools`.
4. Emmy 3-layer prompt assembly through `BeforeProviderRequestEvent` (currently computed-and-logged; pi builds its own system prompt at request time).
5. `chat_template_kwargs.enable_thinking:false` at request level (fix `a17f4a9` stopgap — proper fix is same scope as deferral #1).

Alongside ROADMAP-declared Phase-3 scope: Langfuse v3 + OTel GenAI semconv (HARNESS-09 + TELEM-01/02/03), per-profile compaction (HARNESS-05 + CONTEXT-02), GPU/KV/spec-accept TUI footer (UX-02), offline-OK badge (UX-03), lived-experience rating JSONL.

### Four-way regression green at Phase 2 close

- `bun test` → 192 pass / 0 fail / 499 expect() across 21 files
- `bun run typecheck` → all 4 packages exit 0
- `uv run pytest tests/unit -q` → 137 passed / 1 skipped (shellcheck) — unchanged from Phase 1 baseline
- `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v{1,2}/` → both exit 0

### Phase 1 deferrals (still open, ownership unchanged)

Three Phase 1 items documented as deferred per `01-CLOSEOUT.md`. They are NOT blockers for Phase 2's close nor Phase 3's start:

- **SC-1 throughput (01-06 Task 2):** DGX Spark sweep pending, ~60-90 min GPU. Resume signal: `"sc1 resolved"`.
- **SC-5 sampler re-validation (01-07 Tasks 2+3):** two 2-hour thermal replays pending. Resume signals: `"sc5 floors recorded"` / `"sc5 reproducibility green"`.
- **SC-4 air-gap CI wrapper (01-08 Task 3):** self-hosted runner registration + CI trigger, ~5-10 min CI time. Resume signal: `"sc4 certified"`.

All three are operator-gated (DGX Spark GPU time); each has a documented runbook in the corresponding SUMMARY.md.

**Next action:** `/gsd-plan-phase 3` — start Phase 3 planning (observability + agent-loop hardening + lived-experience). Phase 1 deferrals can be closed opportunistically whenever operator time allows.

**Resume signal:** Phase 2 is closed; no pending resume signal for this phase. Phase 3 has not been planned yet.

---

*State initialized: 2026-04-20 by roadmapper*

**Planned Phase:** 04.7 (mistral-medium-3-5-128b-alternate) — 3 plans — 2026-05-01T23:03:10.733Z

**Plan 01-06 Task 1 completed:** 2026-04-21 — commits `feea40c` (RED) + `742fd9b` (GREEN); SUMMARY.md written; awaiting DGX Spark operator for Task 2.

**Plan 01-07 Task 1 completed:** 2026-04-21T16:40:09Z — commits `4214b71` (RED) + `b510d1b` (GREEN); SUMMARY.md written at `.planning/phases/01-serving-foundation-profile-schema/01-07-SUMMARY.md`; GpuSampler now tolerates nvidia-smi `[N/A]` per-field (DGX Spark UMA case). 7/7 sampler tests GREEN, 124/124 unit suite GREEN (1 skip for missing shellcheck), `uv run emmy profile validate` exits 0. Awaiting DGX Spark operator for Task 2 (second 2-hour `--record-floors` replay) and Task 3 (third 2-hour `--assert-floors` replay).

**Plan 01-08 Tasks 1 + 2 completed:** 2026-04-21 — commits `93fab55` (test-RED) + `78ff0be` (feat-GREEN) + `3889724` (docs); SUMMARY.md written at `.planning/phases/01-serving-foundation-profile-schema/01-08-SUMMARY.md`. SC-4 certification machinery shipped: `emmy_serve/airgap/ci_verify.py` validator + `scripts/trigger_airgap_ci.sh` + `scripts/verify_airgap_ci.sh` + 2 golden fixtures + 13 unit tests + `docs/airgap-green-run.md` runbook + `docs/ci-runner.md` §8. 137/137 unit suite GREEN (1 skip for missing shellcheck), `uv run emmy profile validate` exits 0. Awaiting DGX Spark operator for Task 3 (register runner + trigger CI + verify + commit evidence; ~5-10 min of CI time).

**Plan 02-01 completed:** 2026-04-21T21:41Z — commits `4fa82ac` (Task 1 feat: workspace + four package shells + pi-emmy shim) + `ae97e04` (Task 2 feat: v1→v2 profile clone + docs templates). SUMMARY.md at `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-01-SUMMARY.md`. Bun 1.3.13 installed during pre-flight (Rule 3 deviation: host prereq), `bun-types 1.1.42` added to workspace devDeps (Rule 3: missing types dep blocked typecheck), `bun.lock` (text, Bun 1.3 default) committed in place of legacy `bun.lockb` (Rule 3: tool format drift — reproducibility spirit preserved). All four `@emmy/*` typecheck GREEN, `pi-emmy` shim on PATH prints wave-0 message + exits 0, `profiles/qwen3.6-35b-a3b/v2/` byte-for-byte clone of v1 (9-line diff in profile.yaml only). Phase 1 guardrails held: `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` exits 0, `uv run pytest tests/unit -q` → 137 passed / 1 skipped. **Next (Wave 1):** Plans 02-02 + 02-03 + 02-06 can run in parallel.

**Plan 02-04 completed:** 2026-04-21T22:39Z — commits `44c9267` (Task 1 RED: session primitives + transcript tests) + `9e4ac4d` (Task 1 GREEN: profile-loader + prompt-assembly + sp-ok-canary + max-model-len + session-transcript with B3 + W4 + W1 fixes) + `5f85527` (Task 2 RED: session + cli + integration tests) + `e1ea63a` (Task 2 GREEN: real pi runtime adapter + profile-validate pre-flight + pi-emmy CLI with W2 + W5 + B2 fixes). SUMMARY.md at `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-04-SUMMARY.md`. 53 new @emmy/ux tests across 8 files (37 Task 1 + 16 Task 2), 1 skipped Plan-07 regression with `TODO(plan-07)` marker. `pi-emmy --help` works after `bun link`. Rule 3 deviation: CLI tests refactored to hybrid subprocess/in-process model because the sandbox does not route subprocess→parent localhost traffic to Bun.serve mocks. Full Bun suite: 191 pass / 1 skip / 0 fail across 21 files. Phase 1 regression holds: 137/137 unit tests. **Next (Wave 3):** Plan 02-07 (profile v2 fill + hash lock + un-skip regression) unblocks 02-08 (SC-2/3/4/5 evidence runners) and 02-09 (SC-1 daily-driver walkthrough CLOSEOUT).

**Plan 02-07 completed:** 2026-04-21T22:56Z — commits `88e48a4` (Phase-1-schema patch: `ToolsConfig.grammar: Optional[str]` → `Optional[GrammarConfig]`; v1 still validates) + `979a8d0` (feat: fill v2 harness.yaml with nested grammar shape + ship 8 tool schemas + XGrammar Lark + 2 prompt files + PROFILE_NOTES provenance appendix + recompute v2 hash + un-skip Plan-04 regression). SUMMARY.md at `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-07-SUMMARY.md`. Every `TODO(Phase-2)` in v2/harness.yaml filled with a real value; `context.max_input_tokens = 114688` (honest SC-5 derivation); `tools.grammar.{path: grammars/tool_call.lark, mode: reactive}` (NESTED shape per D-11). v2 hash recomputed: `sha256:b91e747…` → `sha256:0025799f…`; KNOWN-STALE comment auto-stripped. `bun test` → 192 pass / 0 fail (was 191 pass + 1 skip; +1 un-skipped SC-5 regression). `bun run typecheck` all 4 packages exit 0. `uv run pytest tests/unit -q` → 137 pass / 1 skip unchanged from Phase 1 baseline. `uv run emmy profile validate` → v1 AND v2 both exit 0. Rule 3 deviation (auto-fixed): js-yaml + @types/js-yaml + @emmy/ux added to root `package.json` devDeps so `scripts/compute_max_input_tokens.ts` resolves from repo root; Rule 1 deviation (auto-fixed): PROFILE_NOTES.md false-positive TODO narrative reworded from "TODO(Phase-2)" to "Phase-2-deferred". **Next (Wave 3):** Plan 02-08 (SC-2/3/4/5 evidence runners) targeting the now-validated v2 bundle; Plan 02-09 (SC-1 walkthrough + CLOSEOUT) referencing schema patch SHA `88e48a4` in the addendum.

**Phase 2 CLOSED:** 2026-04-21 — Plan 02-09 completed; SC-1 walkthrough verdict `sc1 green` (author ran pi-emmy --print against /tmp/emmy-sc1-walkthrough; agent created src/{foo,bar,baz}.ts + tests; bun test 3/3 green; no cloud call). Four live bug fixes landed during the walkthrough cycle (`2c22018` + `4049d95` + `85fa910` + `a17f4a9`) + CLOSEOUT + REQUIREMENTS flip (23 REQ-IDs) + ROADMAP + STATE. Daily-driver bar REACHED. Phase-2-close certified v2 hash: `sha256:24be3eea0067102f1f61bd32806a875d019fe02cb114697cd5f3ca4e39985d8b`. Five architectural deferrals carry to Phase 3 (library-available, pi-pipeline wire-through pending). See `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md`. **Next:** `/gsd-plan-phase 3` — observability + agent-loop hardening + lived-experience.

**Plan 02-08 completed:** 2026-04-21 — commits `dfb8627` (test: SC-2 fixtures + runner + report (verdict=pass) + SC-3 corpora + corpus_fill) + `507623f` (feat: SC-3/4/5 evidence captured + PROFILE_NOTES validation_runs + v2 hash re-locked). SUMMARY.md at `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-08-SUMMARY.md`. Four automated SC drivers shipped and executed against live emmy-serve (127.0.0.1:8002, Qwen3.6-35B-A3B-FP8). All four pass verdicts locked: SC-2 (hash-anchored 0-failures vs baseline 1-failure; Hashline disambiguation win on sc2_05); SC-3 reactive (syn/real/agg = 1.0/1.0/1.0 over 100 calls, verdict=pass per D-12 graduated SLA) + SC-3 disabled baseline (D-14; informational, same 1.0) + SC-3 no_per_tool_sampling (W3/Pitfall-#5; informational, same 1.0); SC-4 (4/4 poison categories rejected, 2/2 in-process MCP fs-server tools dispatched flat); SC-5 (3/3 sha256 stable, AGENTS.md verbatim, max_input_tokens committed=computed=114688). Harness.yaml mutation-restore discipline (tmp backup + try/finally + post-run `uv run emmy profile validate` gate) held across all three SC-3 variants. v2 profile hash re-recomputed at Phase 2 close: `sha256:0025799f...53fa41` → `sha256:24be3eea...85d8b` (PROFILE_NOTES.md content changed; validation_runs extended with 6 Phase-2 SC entries). `bun test` → 192 pass / 0 fail; `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged). `packages/*/src/` untouched per plan invariant (plan 08 is pure evidence-capture). 6 Rule-based auto-fix deviations all folded into the two task commits. **Next (Wave 5):** Plan 02-09 (SC-1 daily-driver walkthrough + Phase 2 CLOSEOUT) references the Phase-1-schema-patch SHA `88e48a4` + Phase-2-close v2 hash `sha256:24be3eea...85d8b`.

**Plan 03-02 completed:** 2026-04-22T06:35Z — commits `f410bfd` (Task 1 infra: Langfuse v3 compose stack — 6 services, all 6 images digest-pinned via `docker inspect .RepoDigests` captured 2026-04-22; start_observability.sh auto-generates .env via openssl rand + 90s health gate; stop_observability.sh; test_stack_healthy.sh; .env.example + README) + `02d46c5` (Task 2 test RED: 5 telemetry tests + 1 Pitfall-#2 guard test in @emmy/ux; all 5 fail at import-resolution for missing ../src/atomic-append + ../src/otel-sdk + ../src/profile-stamp-processor + new exports from ../src/index) + `d11f13e` (Task 3 feat GREEN: @emmy/telemetry dual-sink body — atomic-append.ts TS port of emmy_serve/diagnostics/atomic.py with PIPE_BUF-aware append+fsync vs tempfile+rename; profile-stamp-processor.ts SpanProcessor.onStart auto-stamps emmy.profile.{id,version,hash} on EVERY span per D-10; otel-sdk.ts initOtel+shutdownOtel+resolveTelemetryEnabled with NodeSDK + BatchSpanProcessor(OTLPTraceExporter) at http://127.0.0.1:3000/api/public/otel/v1/traces with Basic auth + x-langfuse-ingestion-version:4; session-context.ts module-level ctx so emitEvent stays argless at all 19 existing call sites across 8 files; span-factory.ts GenAI semconv helpers for Plan 03-03; index.ts replaced NO-OP body with dual-sink JSONL-authoritative + OTLP-best-effort; pi-emmy.ts initOtel ordered AFTER loadProfile BEFORE createEmmySession per Pitfall #2 + 3-mode boot banner; session.ts adds session.start emitEvent; pi-emmy-extension.ts adds harness.assembly emitEvent per wire-level chat request; 6 @opentelemetry/* deps exact-pinned per Phase 2 D-02) + `946da4d` (Task 4 Rule-3 auto-fix surfaced during live compose-up: langfuse-web healthcheck uses $HOSTNAME not localhost because Next.js 16 binds container-IP-only; start_period 45s→90s for first-boot Prisma+ClickHouse migrations). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-02-SUMMARY.md`. All 7 must_haves.truths satisfied. Two Rule-3 auto-fixes folded in (healthcheck + bun mock.module workaround in dual-sink.test.ts). Langfuse v3 stack LIVE-validated: `bash scripts/start_observability.sh` → 6/6 services healthy within ~90s first boot; OTLP endpoint returns HTTP 401 without auth (confirms route wired); /api/public/health returns HTTP 200. 19 existing emitEvent call sites across grammar-retry + native-tools + mcp-bridge + session + prompt-assembly NEEDED ZERO CHANGES (Phase 2 D-01 signature-stable dividend). TELEM-01 + HARNESS-09 REQ-IDs flipped complete. Four-way regression at `946da4d`: `bun test` → 240 pass / 0 fail / 707 expect() across 33 files (+28 vs Plan 03-01 close 212); `bun run typecheck` all 4 packages exit 0; `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged); `uv run emmy profile validate v1+v2` both exit 0. **Task 4 SC-1 trace walkthrough is OPERATOR-GATED** — requires live emmy-serve (DGX Spark GPU scope) + Langfuse UI first-login browser flow + LANGFUSE_PUBLIC_KEY/SECRET_KEY pasted into observability/langfuse/.env. Programmatic scaffolding (scripts/sc1_trace_walkthrough.sh driver + walkthrough steps) is ready. Resume signal: `p3-02 trace green` after operator completes walkthrough. **Next (Wave 2 sequential):** Plan 03-03 (per-profile auto-compaction) is UNBLOCKED; co-modifies session.ts + pi-emmy-extension.ts at before_provider_request seam (same seam this plan established); can reuse span-factory's startChatSpan/endChatSpan helpers for compaction-round-trip chat spans.

**Plan 03-03 completed:** 2026-04-22T07:23Z — commits `42938e2` (Task 1 test RED+impl: @emmy/context package scaffolded as 5th @emmy/* workspace package; D-14 markPreserved classifier + config-loader + SessionTooFullError with 5-key diagnosticBundle + 3 test files / 16 tests all green) + `b6557f4` (Task 2 feat GREEN: emmyCompactionTrigger body with D-11 soft-threshold + Pitfall #3 guard + D-14 preservation pre-filter + D-13 profile-defined compaction prompt + D-16 structured-pruning fallback + D-12 hard-ceiling SessionTooFullError + trigger.test.ts + summarize-fallback.integration.test.ts / 10 new tests; pi-emmy-extension.ts `pi.on("turn_start", ...)` binding with adaptPiEntries adapter) + `8756b67` (Task 3 feat: eval/phase3/sc2-fixture-builder.ts + sc2-assertions.ts + sc2-runner.ts + sc2-fixture-builder.test.ts / 8 new tests + scripts/sc2_200turn_compaction.sh wrapper with --mode + --variant={default,alternate,disabled}). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-03-SUMMARY.md`. All 9 must_haves.truths satisfied. Two Rule-3 auto-fixes folded in: (i) pi 0.68 top-level exports narrower than plan `<interfaces>` block assumed (`prepareCompaction` + `CompactionPreparation` not in pi's package.json `exports` field — verified 2026-04-22 via runtime import probe) — emmy defines its own EmmyCompactionPreparation + prepareCompactionLocal that match the plan shape verbatim; summarization round-trip flows through injectable `engine.summarize()` (no in-house summarization — architectural invariant preserved); (ii) Plan 03-02 Pattern F mock.module process-global hazard avoided by injecting `emitEvent` via EmmyCompactionContext rather than mocking `@emmy/telemetry` (which would poison @emmy/telemetry's own unit tests run in the same bun test process). HARNESS-05 + CONTEXT-02 REQ-IDs flipped complete. Four-way regression at `8756b67`: `bun test` → 274 pass / 0 fail / 1445 expect() across 39 files (+34 vs Plan 03-02 close 240); `bun run typecheck` all 5 packages exit 0 (@emmy/context 5th package); `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged); `uv run emmy profile validate v1+v2` both exit 0. SC-2 fixture hash locked: `sha256:26149bfce42c79e13a26a976780f29410991566fbcd399c65b53a1abd3a0a19b` (threshold crossing at turn 125; elided=181 preserved=19 on stub-mode default variant). Stub-mode verdict=pass on all 3 variants (default / alternate / disabled) → `runs/phase3-sc2/report.json` + `runs/phase3-sc2-stub-alternate/` + `runs/phase3-sc2-stub-disabled/`. **Live-mode SC-2 evidence is DEFERRED to Plan 03-07** per plan success_criteria: Plan 03-07 creates v3 profile (harness.yaml context.compaction block + prompts/compact.md + prompts/compact.alternate.md), wires `engine.summarize()` to `@emmy/provider.postChat` pointed at emmy-serve, and runs the 3-variant matrix against live GPU + records verdicts in `v3/PROFILE_NOTES.md validation_runs`. Wave 2 is CLOSED. **Next (Wave 3):** Plan 03-04 (Alt+Up/Down feedback JSONL + --export-hf) unblocked and file-disjoint from 03-05/06/07; can parallelize.

**Plan 03-05 completed:** 2026-04-22T08:30Z — commits `35b7023` (Task 1 test RED: 5 feedback test files — feedback-append + feedback-idempotent + export-hf.integration + keybind-capture + feedback-flow.integration; all fail at import-resolution for missing ../src/{feedback,feedback-schema,hf-export,turn-tracker,feedback-ui}) + `1fc10c7` (Task 2 feat GREEN: @emmy/telemetry's feedback-schema.ts with 13-field TELEM-02 FeedbackRow + validateRow + FeedbackSchemaError/FeedbackNotFoundError; feedback.ts with PIPE_BUF-aware appendFeedback + atomic updateFeedback + idempotent upsertFeedback keyed on turn_id + defaultFeedbackPath=~/.emmy/telemetry/feedback.jsonl; turn-tracker.ts with TurnTracker keeping most-recent TurnMeta per D-19; hf-export.ts with exportHfDataset copying feedback.jsonl verbatim + emmy-authored dataset_card.md + provenance.json + 6-marker file-content heuristic warning scan; @emmy/ux's feedback-ui.ts with handleFeedbackKey intercepting ANSI_ALT_UP \x1b[1;3A / ANSI_ALT_DOWN \x1b[1;3B before pi's keybind resolution D-18 + thumbs-down modal + kill-switch + emitEvent correlation; pi-emmy-extension.ts turn_end handler populates tracker via typed buildTurnMeta with emmy-synthesized turn_id `${sessionId}:${turnIndex}` resolving the plan-checker BLOCKER — pi 0.68 TurnEndEvent has no turn_id field per types.d.ts line 468-473; pi.on("input") replaces 03-01 stub with handleFeedbackKey delegation + telemetryEnabled gate; session.ts + pi-emmy.ts plumb sessionId + telemetryEnabled; pi-emmy --export-hf <out_dir> CLI dispatcher). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-05-SUMMARY.md`. All 11 must_haves.truths satisfied (truth #7 operator-gated). ZERO deviations (Rule 1-3 not triggered — plan executed exactly as written; the plan's <interfaces> block correctly anticipated pi 0.68 InputEvent / InputEventResult / ctx.ui.input shapes). TELEM-02 + TELEM-03 REQ-IDs flipped complete. Four-way regression at `1fc10c7`: `bun test` → 353 pass / 0 fail / 1 skip (opt-in HF) / 1649 expect() across 49 files (+31 vs Plan 03-04 close 322); `bun run typecheck` all 5 packages exit 0; `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged); `uv run emmy profile validate v1+v2` both exit 0. Acceptance-criterion greps all pass: ANSI_ALT_UP/DOWN grep=4, \x1b[1;3A grep=2, \x1b[1;3B grep=2, `.emmy/telemetry/feedback.jsonl` grep=1, 13-field mentions=37, upsert/update grep=5, load_dataset grep=3, export-hf grep=9, `(event as any)` grep=0 (typed access), turnIndex grep=8, CJS require grep=0 (ESM-only). **Task 3 interactive-TUI verification is OPERATOR-GATED** — requires live DGX Spark + interactive pi-emmy + real Alt+Up/Down keypresses. Programmatic scaffolding (unit + integration tests) covers the state machine. Resume signal: `p3-05 feedback green`. **Next (Wave 3):** Plan 03-06 (offline-OK badge UX-03) unblocked and file-disjoint from 03-05; can parallelize. Plan 03-07 closes the phase.

**Plan 03-04 completed:** 2026-04-22T07:39Z — commits `64a625f` (Task 1 test RED: 5 footer-related test files — vllm-metrics-parser + nvidia-smi + footer + footer-degrade + metrics-poller; all fail at module-resolution for missing ../src/{vllm-metrics,nvidia-smi,footer,metrics-poller}; scripts/footer_parity_check.sh stub) + `498390c` (Task 2 feat GREEN: vllm-metrics.ts Prometheus-text parser + TokRateTracker 5s sliding window over generation_tokens_total per Pitfall #6; nvidia-smi.ts TS port of emmy_serve/thermal/sampler.py with [N/A]-per-field tolerance from Plan 01-07 Task 1 b510d1b; footer.ts pure renderer with D-24 `?` suffix + D-25 spec-accept literal `-`; metrics-poller.ts 1 Hz setInterval + per-field FieldState degrade + priming tick + EMMY_TELEMETRY=off kill-switch + 4 test hooks; pi-emmy-extension.ts session_start/agent_end handlers bookend footer poller lifecycle; session.ts passes opts.baseUrl through to createEmmyExtension; index.ts re-exports new surfaces) + `a43bd03` (Task 3 test: scripts/footer_parity_check.sh operator driver — --help/--sample-only/interactive modes; prereq check + 5% GPU%/KV% tolerance + 30% tok/s tolerance; resume signal `p3-04 footer green`). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-04-SUMMARY.md`. All 7 must_haves.truths satisfied (truth #7 operator-gated). Honors the VERIFIED vLLM metric name `vllm:gpu_cache_usage_perc` — CONTEXT D-22 transcribed the wrong name; RESEARCH §Summary #3 verified. Two auto-fixes folded in: (i) Rule-2 warmup MUST NOT reset failCount — only treat as success when `samplesInWindow() >= 2` so D-24 degrade semantics hold during first 5s of session; (ii) Rule-3 test calibration — metrics-poller.test.ts "reset-on-success" accounts for priming tick (call #1) so the 7-fire pattern maps correctly to the 3rd post-reset failure at call #7. UX-02 REQ-ID flipped complete. Four-way regression at `a43bd03`: `bun test` → 322 pass / 0 fail / 1536 expect() across 44 files (+48 vs Plan 03-03 close 274); `bun run typecheck` all 5 packages exit 0; `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged); `uv run emmy profile validate v1+v2` both exit 0; `bash scripts/footer_parity_check.sh --help` exit 0. **Task 3 SC-4 parity verification is OPERATOR-GATED** — requires live DGX Spark hardware + emmy-serve + tmux split + 3 synchronized snapshots. Programmatic scaffolding (footer_parity_check.sh with interactive instructions) is ready. Resume signal: `p3-04 footer green`. **Next (Wave 3):** Plans 03-05 (input extension + Alt+Shift+Up/Down rating JSONL — `pi.on('input', ...)` body currently stubbed) and 03-06 (offline-OK badge UX-03 — boot-time tool-registry audit + runtime web_fetch enforcement) are file-disjoint from 03-04 and from each other; can parallelize.

**Plan 03-06 completed:** 2026-04-22T08:25Z — commits `84a2d89` (Task 1 test RED: 4 test files — offline-audit pure-fn tests, web-fetch-enforcement integration, offline-badge render, boot-banner stderr; all fail at import-resolution for missing ../src/offline-audit + ../src/web-fetch-allowlist + ../src/offline-badge) + `c4efb68` (Task 2 feat GREEN: packages/emmy-telemetry/src/offline-audit.ts with D-26 LOOPBACK_HOSTS {127.0.0.1, localhost, ::1, loopback} size=4 + bind-all quad-zero EXCLUDED per plan-checker WARNING + auditToolRegistry pure fn + auditWebFetchUrl hostname-EXACT with bracket-IPv6 normalization for loopback-only; packages/emmy-tools/src/web-fetch-allowlist.ts with enforceWebFetchAllowlist + WebFetchAllowlistError + emitEvent("tool.web_fetch.violation"); packages/emmy-tools/src/web-fetch.ts webFetchWithAllowlist wrapper returns WebFetchToolErrorResult on miss per D-28 warn-and-continue; packages/emmy-ux/src/offline-badge.ts renderBadge/renderBadgePlain/updateOfflineBadge + module-level state machine bindBadge/setInitialAudit/flipToViolation + runBootOfflineAudit helper emits [emmy] green/red stderr banner; packages/emmy-tools/src/types.ts NativeToolOpts extended with webFetchAllowlist + webFetchOnViolation; packages/emmy-tools/src/native-tools.ts web_fetch dispatches through webFetchWithAllowlist with plumbed enforcement context; packages/emmy-provider/src/types.ts ProfileSnapshot.harness.tools.web_fetch.allowlist optional; packages/emmy-ux/src/profile-loader.ts parses harness.yaml:tools.web_fetch.allowlist; packages/emmy-ux/src/session.ts boot-time audit after tool registration + setInitialAudit + emitEvent("session.offline_audit.complete") + legacy registerNativeTools wiring; packages/emmy-ux/src/pi-emmy-extension.ts pi.on("session_start") calls bindBadge(ctx.ui)). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-06-SUMMARY.md`. ZERO deviations — plan executed exactly as written. UX-03 REQ-ID flipped complete. Four-way regression at `c4efb68`: `bun test` → 396 pass / 1 skip / 0 fail / 1758 expect() across 53 files (+43 vs Plan 03-05 close 353); `bun run typecheck` all 5 packages exit 0; `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged); `uv run emmy profile validate v1+v2` both exit 0. Acceptance-criterion greps all pass: LOOPBACK_HOSTS in src=8, 127.0.0.1 in src=2, allowlist.includes(hostname) matches=2, 0.0.0.0 in src=0, WebFetchAllowlistError in src=5, tool.web_fetch.violation in src=2, OFFLINE OK/NETWORK USED in src=5, setInitialAudit/auditToolRegistry/runBootOfflineAudit in session.ts=5, LOOPBACK_HOSTS.size === 4 asserted. Minor on-plan refinement: Node WHATWG URL parser preserves IPv6 brackets (`http://[::1]/` → hostname=`[::1]`) — normalized in auditWebFetchUrl for loopback check only; allowlist check uses raw form so hostname-EXACT invariant holds. **Task 3 UX-03 demo is OPERATOR-GATED** — requires live DGX Spark + emmy-serve + interactive pi-emmy + real web_fetch calls against live URLs. Programmatic scaffolding (stderr boot banner, module-level state machine, enforcement hook, emitEvent on violation) is fully in place and covered by 43 unit + integration tests. Resume signal: `p3-06 badge green`. **Wave 3 CLOSED.** **Next:** Plan 03-07 (Phase 3 CLOSEOUT + v3 profile bump with doc-host default allowlist + live SC-2 3-run matrix).

**Plan 03-01 completed:** 2026-04-22T05:55Z — commits `ab4648f` (Task 1 test RED: 6 test files, 10 tests, all fail for the right reasons) + `d4cd189` (Task 2 feat GREEN: five wire-throughs landed in ONE atomic commit per D-01 atomic-wave lock — @emmy/provider through pi ModelRegistry.streamSimple + 8 native tools + MCP via createAgentSessionFromServices({customTools}) + Emmy 3-layer assembled prompt authoritative via before_provider_request + chat_template_kwargs.enable_thinking:false at hook with a17f4a9 render-time <think>-strip DELETED + reactive XGrammar retry on live pi path via WeakMap<AbortSignal, RetryState>) + `5e0ba97` (Task 3 test: SC-1-class Track B walkthrough evidence — verdict `sc1 green`) + `b18b257` (docs SUMMARY). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-01-SUMMARY.md`. Walkthrough ran a deliberately-richer-than-Phase-2-SC-1 prompt (forces read + grep/ls/find + in-place edit + write + bash in one session) against live emmy-serve (Qwen3.6-35B-A3B-FP8 at 127.0.0.1:8002, profile v2 hash `sha256:24be3eea...85d8b`). All 7 acceptance criteria green: (a) 6/6 files present, (b) bun test final 3 pass / 0 fail in 11 ms, (c) 0 `<think>` leaks in stdout, (d) SP_OK canary fired at session boot BEFORE pi runtime built, (e) 6 distinct tools invoked (read×12, bash×8, edit×6, ls×4, write×4, find×2), (f) 0 "string not found" edit failures (hash-anchored path held on real in-place greet.ts fix), (g) 0 non-loopback connections. Phase-3 regressions added: MCP D-18 poison gate re-asserted on NEW `buildMcpToolDefs` path (assertNoPoison on BOTH `tool.name` AND `tool.description` BEFORE ToolDefinition emit; regression test constructs U+202E via `String.fromCodePoint(0x202E)`); WeakMap-only retry-state storage (no LRU / no size-bound; `grep -c 'LRU' packages/emmy-provider/src/grammar-retry.ts` = 0). Four-way regression at `5e0ba97`: `bun test` → 212 pass / 0 fail / 537 expect() across 27 files (+20 vs Phase-2-close 192); `bun run typecheck` all 4 packages exit 0; `uv run pytest tests/unit -q` → 137 pass / 1 skip (unchanged); `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v{1,2}/` both exit 0. Two Observations (non-blocking): agent initially used `/home/user/...` paths for read/ls/find on 3 early turns and self-recovered to `./` on next turn (pretraining prior; tracked for Phase 5 eval harness as candidate prompt-level nudge); initial walkthrough prompt was Phase-2-SC-1-style simple write+bash-only and failed criterion (e) ≥ 4 distinct tools, re-ran with deliberately richer prompt (6 distinct tools invoked, green). **Wave 2 unblocked but must execute SEQUENTIALLY:** plans 03-02 (Langfuse OTel span attach) + 03-03 (per-profile auto-compaction) co-modify `packages/emmy-ux/src/session.ts` + `packages/emmy-ux/src/pi-emmy-extension.ts` at the `before_provider_request` seam; parallel execution would conflict. Plan 03-05 (input extension via `pi.on('input', ...)`) is file-disjoint and can parallelize with either 03-02 or 03-03 (but not both). **Next:** Plan 03-02 (Langfuse v3 compose stack + @emmy/telemetry dual-sink + OTel SDK init + EmmyProfileStampProcessor + EMMY_TELEMETRY=off kill-switch).

**Plan 03-07 completed — PHASE 3 CLOSED:** 2026-04-22 — commits `de4ae96` (Task 1 + 1a feat: v2→v3 profile clone with context.compaction + tools.web_fetch.allowlist blocks; pydantic CompactionConfig + WebFetchConfig schema patch with Optional backward-compat; 7 new regression tests; v3 profile hash computed twice — after Task 1 bundle, then again after Task 1a compact.alternate.md added — final: sha256:2beb99c773a0e425a3e485459964740640c5f3addbea186738402cf66d4d3718) + `d3196bd` (Task 2 infra: emmy_serve/airgap/ci_verify_phase3.py + scripts/airgap_phase3_replay.sh + .github/workflows/airgap-phase3.yml — dual-stack emmy-serve + Langfuse zero-outbound validator; dry-run path asserts config + digest-pin sanity; full-run path scaffolded for self-hosted runner per Phase 1 Plan 01-08 Task 3 pattern) + `185e55c` (Task 3 test: Phase 3 CLOSEOUT walkthrough evidence consolidating Wave 1-3 live-verified runs + re-running SC-2 3-variant stub matrix verdict=pass + air-gap Phase 3 dry-run exit 0; scripts/phase3_close_walkthrough.sh + scripts/sc5_offline_badge.sh operator drivers). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-07-SUMMARY.md`. CLOSEOUT at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CLOSEOUT.md`. All 11+ must_haves.truths satisfied. **REQUIREMENTS.md traceability:** 8 Phase-3 REQ-IDs (HARNESS-05, HARNESS-09, CONTEXT-02, TELEM-01, TELEM-02, TELEM-03, UX-02, UX-03) flipped Pending → Done; 5 Phase-2 Done† (HARNESS-02, HARNESS-06, HARNESS-07, TOOLS-03, TOOLS-07) promoted → Done. 13 REQ-IDs flipped in Phase 3; cumulative 36 / 66 v1 REQ-IDs Done. **Profile hash trajectory:** v1 unchanged `sha256:b91e747...21913` (Phase 1 close; v1 byte-identical through all of Phase 2+3); v2 unchanged `sha256:24be3eea...85d8b` (Phase 2 close; v2 byte-identical through Phase 3 — D-02 immutability honored); v3 new `sha256:2beb99c7...d4d3718` (Phase 3 close; sibling of v2 + context.compaction + tools.web_fetch.allowlist + prompts/compact.md + prompts/compact.alternate.md). **Four-way regression at final commit:** `bun test` 396 pass / 1 skip / 0 fail / 1758 expect() across 53 files (unchanged vs Plan 03-06 close — Plan 03-07 is Python-side + docs only); `bun run typecheck` 5/5 packages exit 0; `uv run pytest tests/unit -q` 144 pass / 1 skip (+7 new schema tests); `uv run emmy profile validate v1 + v2 + v3` all three exit 0. **Operator-gated evidence items carried forward as Phase 3 deferrals** (not blockers, parallel to Phase 1's 3-item pattern): p3-02 trace green (live Langfuse UI), p3-04 footer green (interactive TUI eyeball), p3-05 feedback green (interactive Alt+Up/Down), p3-06 badge green (interactive web_fetch red-flip), p3-07 sc2 live green (live-mode SC-2 3-run matrix — stub-mode green today; live-mode requires ~2h GPU + engine.summarize wire). **Next:** `/gsd-plan-phase 4` — Gemma 4 26B A4B MoE profile + profile system maturity (`/profile` atomic swap + within-model planner/editor/critic routing). Phase 1 deferrals + Phase 3 operator-gated items can be closed opportunistically whenever operator time allows.

**Plan 03-08 completed — SC-3-TUI-WIRE gap closed:** 2026-04-22 — commits `8fe750d` (Task 1 RED: runtime-tui.test.ts + runtime-tui-wiring.integration.test.ts mounting real pi createAgentSessionRuntime + InteractiveMode, all 16 new tests fail at import-resolution for missing buildRealPiRuntimeTui) + `7e7da29` (Task 2 GREEN: parallel `buildRealPiRuntimeTui` in packages/emmy-ux/src/session.ts graduates from Plan 02-04's SDK-only path to pi 0.68's full createAgentSessionRuntime(factory, ...) + new InteractiveMode(runtime, {verbose:false}).run(); emmy-scoped agentDir ~/.emmy/agent per T-03-08-06; removed TUI-unavailable bail from packages/emmy-ux/bin/pi-emmy.ts:337 + replaced with real await runTui() wrapped in try/finally + shutdownOtel; existing buildRealPiRuntime preserved for --print path to not regress Plan 03-07 certification) + `2d01448` (Task 3 walkthrough attempt 1: pexpect PTY spawn on live DGX Spark — TUI launched cleanly with no "TUI unavailable" bail, but Alt+Up/Down delivery FAILED; root-cause investigation of pi dist revealed Plan 03-05's D-18 RESEARCH Pattern 4 assertion was wrong — pi.on("input", handler) is a message-SUBMISSION event (emitInput fires only when user submits a prompt, payload {text, images, source}), NOT a keybind intercept; pi 0.68 exposes correct API at types.d.ts:780 pi.registerShortcut(keyId, {description?, handler}); alt+up/alt+down claimed by built-ins app.message.dequeue/requeue and pi runner silently drops colliding extension shortcuts at runner.js:267) + `ea159e2` (fix-forward: swapped pi.on("input") for pi.registerShortcut("shift+ctrl+up"/"shift+ctrl+down") with unclaimed chords verified against dist/core/keybindings.js defaults scan; refactored handleFeedbackKey(event,...) → handleFeedbackRating(rating,...) removing ANSI-event shape; also fixed a SECOND Plan 03-05 defect surfaced during live walkthrough: pi resets _turnIndex = 0 on every agent_start at dist/core/agent-session.js:376 — every user message's first turn_end carries turnIndex:0, collapsing Plan 03-05's turn_id = sessionId:turnIndex scheme; added emmy-side monotonic emmyTurnCounter++ threaded through buildTurnMeta so idempotency (same turn → same id → upsert) and distinctness (different messages → distinct ids) both hold; debug-gated stderr instrumentation via EMMY_DEBUG_SHORTCUT env var for future walkthroughs) + `42da230` (Task 3 walkthrough attempt 2: pexpect PTY against live Spark — ALL 6 STEPS GREEN, feedback.jsonl final state 2 distinct rows (turn_id ...:0 rating=+1 empty comment + turn_id ...:1 rating=-1 comment="walkthrough comment via shift+ctrl+down"), 13 fields each, v3 profile hash sha256:2beb99c7...d4d3718 stamped, --export-hf roundtrip produced feedback.jsonl + dataset_card.md + provenance.json with 0 file-content warnings). SUMMARY.md at `.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-08-SUMMARY.md`. **Discovered defects:** two Plan 03-05 planning-time defects (D-18 pi.on(input) mis-assumption; turnIndex collapse) — neither caught by plan-checker nor unit tests because the unit tests mocked pi's event delivery using the assumption-under-test; live walkthrough is load-bearing for this integration class. Lessons captured in runs/p3-w5-gap-walkthrough/walkthrough.md §"Lessons for future plans". **Four-way regression at `42da230`:** bun test 412 pass / 1 skip / 0 fail / 1822 expect() across 55 files (+16 vs Plan 03-07 close 396/1); bun run typecheck 5/5 @emmy/* packages exit 0; uv run pytest tests/unit -q 144 pass / 1 skip (unchanged); uv run emmy profile validate v1+v2+v3 all three exit 0 (v3 hash unchanged). **03-VERIFICATION.md:** status flipped gaps_found → human_needed; sc_verdicts.SC-3 flipped `gap` → `pass`; SC-3-TUI-WIRE moved from gaps to resolved with discovered_defects block. **03-HUMAN-UAT.md:** SC-3 item marked RESOLVED 2026-04-22 with cross-refs to walkthrough evidence; passed 0→1, pending 4→3. **Resume signals closed:** `p3-05 feedback green` (collateral — library was right; wiring was the issue) + `p3-08 tui green`. **Remaining Phase-3 operator-gated items (3):** p3-02 trace green (live Langfuse UI — browser API-key provisioning), p3-06 badge green (live web_fetch red-flip via interactive prompt), p3-07 sc2 live green (~2h GPU live 200-turn compaction matrix). **Next:** `/gsd-plan-phase 4` — Gemma 4 26B A4B MoE profile + profile system maturity.

**Phase 03.1 INSERTED** 2026-04-22 — `Operational polish: minimal-RAM profile, live auto-compaction + manual triggers, SearxNG web search, day-to-day documentation` (URGENT). Added mid-milestone after daily-driver use exposed (a) 116/125 GB system RAM pressure with 8 GB swap in use because `gpu_memory_utilization=0.88` is oversized on UMA; (b) Plan 03-03's auto-compaction is stub-only — live path doesn't actually invoke pi's `session.compact()`, agent hit the 114,688-token hard wall during a real research session; (c) no manual escape valve (`/clear`, `/compact`) from an over-full session short of killing pi-emmy; (d) 5-host doc allowlist is too narrow for real research; (e) no README for day-to-day operation. Three plans scoped: 03.1-01 RAM fit + live compaction + /compact + /clear (run first); 03.1-02 SearxNG web_search with Google primary + DDG/Brave/Bing fallbacks (SearxNG handles rate-limit fallback automatically); 03.1-03 README + docs/runbook.md + CLAUDE.md revision. v3 profile remains locked; Phase 3.1 produces sibling **v3.1** profile. Phase 4 (Gemma 4) dependency unchanged — now depends on Phase 3.1 instead of directly on Phase 3.

**Plan 03.1-01 completed:** 2026-04-23 — commits `b372908` (Task 1 RED: compaction-live-wiring test scaffold, 6 tests RED covering soft-threshold directive population + below-threshold no-op + ctx.compact invocation + D-16 fallback on missing profile prompt + D-12 hard-ceiling SessionTooFullError + deprecated engine.summarize stub backward compat) + `3f49f32` (Task 1 GREEN: v3.1 profile bundle sibling of v3 per D-02 immutability — serving.yaml gpu_memory_utilization 0.88→0.55 + max_num_batched_tokens 16384→8192 + max_model_len UNCHANGED 131072 per user constraint; emmyCompactionTrigger now returns EmmyCompactionDirective; pi-emmy-extension.ts turn_start handler calls ctx.compact({customInstructions}) when directive.shouldCompact; deprecated engine.summarize stub path kept for Plan 03-03 test back-compat) + `339e6f1` (Task 2 RED: slash-commands.test.ts + slash-commands.integration.test.ts, 13 tests RED for /compact + /clear dispatch + confirm gate + SP_OK re-fire) + `e87937b` (Task 2 GREEN: packages/emmy-ux/src/slash-commands.ts with registerCompactCommand + registerClearCommand dispatching via pi.registerCommand; /clear handler ordering hasUI→confirm→abort→waitForIdle→ctx.newSession preserves Pitfall #6 SP_OK re-fire). SUMMARY.md at .planning/phases/03.1-operational-polish-minimal-ram-profile-live-auto-compaction-/03.1-01-SUMMARY.md. **RAM before/after on DGX Spark UMA (full 128 GB unified pool):** MemAvailable 4.6 GiB→42 GiB (9× headroom); buff/cache 3.7 GiB→40 GiB; Swap used 7.2 GiB→2.9 GiB (4.3 GiB flushed back as RAM pressure released); cold-start 158s via fastsafetensors; smoke-test 10.16 tok/s baseline. D-29 gate PASS (≥40G available). **Live walkthrough evidence:** pexpect-driven pi-emmy session on v3.1 read 6 large files (~350KB of content, ~85K+ file tokens) then asked follow-up; docker logs show 2 successful POST /v1/chat/completions with zero 400 context-length errors — the exact symptom the user hit pre-Phase-3.1 on a smaller corpus. /clear dispatches + confirms + ctx.newSession resets cleanly. /compact dispatches BUT pi 0.68 ships a built-in /compact at dist/core/slash-commands.js:19 that collides with emmy's registration; pi yellow-warns "Extension command '/compact' conflicts with built-in interactive command. Skipping in autocomplete." and dispatches to its built-in instead (which calls session.compact() — same method emmy's auto-path uses). DOCUMENTED DEVIATION: emmy's profile-defined prompts/compact.md is NOT threaded through manual /compact (only through auto-path); follow-up in 03.1-03 removes emmy's registration (1-line change). **Four-way regression at e87937b:** bun test 431 pass / 1 skip / 0 fail (+19 new: 6 live-wiring + 13 slash-commands vs Plan 03-08 close 412/1); bun run typecheck 5/5 @emmy/* packages exit 0; uv run pytest tests/unit -q 144 pass / 1 skip (unchanged); uv run emmy profile validate v1+v2+v3+v3.1 all four exit 0. **v3 hash unchanged** `sha256:2beb99c773a0e425a3e485459964740640c5f3addbea186738402cf66d4d3718` (git diff profiles/qwen3.6-35b-a3b/v3/ empty). **v3.1 hash:** `sha256:fcdecb2355688167afda98f1310d5e06e2b2c039927d5dda52271fb6ad3fee5f`. **Phase 3 deliverables regression-free:** wire-through / footer / OFFLINE OK badge / feedback JSONL / HF-export / dual-sink telemetry / interactive TUI all continue to work (boot banner still green [emmy] OFFLINE OK, SP_OK canary fires). **Daily-driver unblock:** agent no longer hits the 114,688-token hard wall during normal research sessions (auto-compaction fires on the live path at D-11 soft threshold 86016); /clear exists as manual escape valve if a pathological session still wedges; RAM pressure gone (no more swap thrashing on idle emmy-serve). **Next:** Plan 03.1-02 (SearxNG self-hosted compose + web_search tool + web_fetch returned-URL bypass + 3-state badge + air-gap CI split).

**Plan 03.1-02 completed:** 2026-04-23 — commits `07fa0a3` (Task 1 RED: SearxNG + web_search scaffold tests) + `d3afe7d` (Task 1 GREEN: SearxNG Docker Compose stack observability/searxng/ with digest-pinned searxng/searxng + own dedicated redis — NOT sharing Langfuse's Redis per plan-checker WARN 1 for startup robustness; loopback 127.0.0.1:8888; 90s health gate + openssl-generated secrets; web-search.ts tool module with SearxNG JSON API wrapper + 3 kill-switches + 10-call/turn rate limit + ToolError-shaped fallback; pydantic schema extension with WebSearchConfig + WebFetchConfig.search_bypass_ttl_ms) + `f2bddeb` (Task 2 RED: web_fetch bypass + 3-state badge + research egress CI tests) + `891f6bd` (Task 2 GREEN: RecentSearchUrlStore with 5-min TTL prune + enforceWebFetchAllowlist bypass-BEFORE-allowlist check + T-03.1-02-02 SSRF guard via EXACT-URL-match-only; OfflineAuditResult extended with badge_state; offline-badge.ts 3-state renderer green/yellow/red reserved; session.ts webSearchOnSuccess→flipToYellow and webSearchOnFallback→flipToGreen; profile v3.1 harness.yaml tools.web_search + tools.web_fetch.search_bypass_ttl_ms 300000; pi.on turn_end rate-limit reset at pi-emmy-extension.ts per plan-checker WARN 2; air-gap CI split ci_verify_research_egress.py permits SearxNG outbound but hard-denies 12 cloud-inference endpoints api.openai.com+api.anthropic.com+generativelanguage.googleapis.com+AWS-Bedrock+etc.) + `cd435c9` (walkthrough fix — three integration gaps surfaced when agent tried actually USING web_search live: profile-loader.ts never parsed tools.web_search block so opts.profile.harness.tools.web_search was undefined downstream → added WebSearchBlock parser + emit; session.ts prompt-assembly toolDefsText was hardcoded 8-tool list not read from profile/prompts/tool_descriptions.md → added conditional web_search line gated on profile + kill-switches; v3.1/prompts/tool_descriptions.md cloned from v3 without web_search section → added; v3.1 hash rotated after tool_descriptions.md edit). SUMMARY.md at .planning/phases/03.1-operational-polish-minimal-ram-profile-live-auto-compaction-/03.1-02-SUMMARY.md. **Walkthrough evidence at runs/phase3.1-02/:** 28 SearxNG results from 4 engines (google, duckduckgo, brave, bing, startpage); agent successfully grounded response "The latest Bun release version is 1.3.13, as indicated on both the Bun website and npm" (ground-truth — Bun 1.3.13 published 1 day prior); web_fetch bypass probe 3/3 (exact-URL bypass ALLOWED, same-host-different-path DENIED SSRF-guard, non-allowlist DENIED); EMMY_WEB_SEARCH=off kill-switch verified (agent listed exactly 8 tools, web_search absent); both air-gap CI dry-runs exit 0. **Four-way regression at cd435c9:** bun test 459 pass / 1 skip / 0 fail (+28 new vs Plan 03.1-01 close 431); bun run typecheck 5/5 @emmy/* packages exit 0; uv run pytest tests/unit -q 165 pass / 1 skip (+21 new schema tests vs Plan 03.1-01 144); uv run emmy profile validate v1+v2+v3+v3.1 all four exit 0; v3 hash unchanged sha256:2beb99c7...d4d3718 (git diff profiles/qwen3.6-35b-a3b/v3/ empty); v3.1 hash authoritative post-walkthrough `sha256:f9dcabd1dbee8f29b7ee8439140da83d84e9784dab08a1304474e1d06901fc73`. **Phase 3 deliverables regression-free:** wire-through + footer + OFFLINE OK badge (still works when SearxNG disabled) + feedback JSONL + HF-export + dual-sink telemetry + interactive TUI all continued to work. **Lesson captured:** integration defects between profile-loader, prompt-assembly, tool-registration, and tool_descriptions surface ONLY during live walkthroughs — unit tests mock these layers. Plans 03-05, 03-08, now 03.1-02 all discovered identical-class bugs during operator walkthrough. **Next:** Plan 03.1-03 (top-level README + docs/runbook.md + CLAUDE.md thesis revision split "no cloud INFERENCE" hard-invariant from "local-first EGRESS" via SearxNG + PROFILE_NOTES v3.1 finalization + start_emmy.sh default bump to v3.1 + ROADMAP/REQUIREMENTS/STATE + 03.1-CLOSEOUT).

**Phase 03.1 CLOSED:** 2026-04-23 — Operational polish phase (decimal insertion between Phase 3 and Phase 4). Three plans sequential in 1 working day: 03.1-01 RAM fit + live compaction + /compact + /clear (commits b372908+3f49f32+339e6f1+e87937b+d86e9b6) → 03.1-02 SearxNG + web_search + web_fetch bypass + 3-state badge (commits 07fa0a3+d3afe7d+f2bddeb+891f6bd+cd435c9+89cf484) → 03.1-03 README + runbook + CLAUDE.md thesis revision + CLOSEOUT (this close). **Daily-driver UNBLOCKED:** RAM 4.6→42 GiB available (9× headroom); live auto-compaction fires on turn_start at 86K soft threshold; /clear manual escape valve; SearxNG web_search rotates Google/DDG/Brave/Bing/Startpage with rate-limit fallback; web_fetch returned-URL bypass with exact-match SSRF guard; thesis refined into "No cloud INFERENCE" (hard, air-gap-tested) + "Local-first EGRESS via SearxNG" (auditable, disableable). **v3.1 profile hash** authoritative `sha256:f9dcabd1dbee8f29b7ee8439140da83d84e9784dab08a1304474e1d06901fc73`; v1+v2+v3 byte-identical throughout. **scripts/start_emmy.sh default bumped v1 → v3.1.** **Phase 3 deliverables regression-free.** **Four-way regression at close:** bun test 459 pass / 1 skip / 0 fail (+47 vs Phase 3 close 412/1); typecheck 5/5; pytest 165 pass / 1 skip (+21 schema tests); profile validate v1+v2+v3+v3.1 all exit 0. **REQ-IDs:** +TOOLS-10 web_search Done + UX-07 3-state badge Done; CONTEXT-02 advanced stub → live-wired; PROFILE-05 extended with D-29..D-38; **cumulative 38/68 v1 REQ-IDs Done (was 36/66 pre-3.1)**. **Lesson captured:** integration defects between profile-loader, prompt-assembly, and tool_descriptions surface ONLY during live walkthroughs (Plans 03-05, 03-08, 03.1-02 all discovered identical-class bugs). Phase 4 plans adding new tools must explicitly include all three layers in files_modified. **CLOSEOUT** at .planning/phases/03.1-operational-polish-minimal-ram-profile-live-auto-compaction-/03.1-CLOSEOUT.md. **Operator-gated items from Phase 3.1:** NONE (all plans' walkthroughs ran to completion during execution). **Next:** `/gsd-plan-phase 4` — Gemma 4 26B A4B MoE + /profile atomic swap + within-model routing. Phase 4 now Depends on Phase 3.1 (v3.1 harness is the baseline for /profile UX).

**Phase 04.4 EXEC LANDED — CLOSEOUT PENDING (operator-gated):** 2026-04-26 — All 9 plans landed across 4 waves. **Wave 1 (parallel):** Plan 04.4-01 (memory tool surface — TypeBox schema + path-resolver + V4 30 hostile inputs + quotas + ToolDefinition shell; commits a6f208a + (Task 2 commit) + (Task 3 commit) + a1e0f0b SUMMARY) → Plan 04.4-03 (memory profile schema — pydantic MemoryConfig + 4-profile harness.yaml memory: block + TS profile-loader + 4 hashes recomputed; commits 84d7630 + 7f8da94 + 8be7542 SUMMARY) → Plan 04.4-06 (D-3X append-only-prefix invariant — prefix-hash.ts + emmy.prefix.hash telemetry + audit_prefix_mutations.ts AUDIT GREEN + V4 arch-aware test + STATE/runbook D-3X section; commits a6f208a-equivalent + bc0d733 + 0531565 SUMMARY). **Wave 2:** Plan 04.4-02 (six memory commands — view/create/str_replace/insert/delete/rename + dispatch wired with onOp seam; commit 409a517 SUMMARY) → Plan 04.4-07 (v1 ~35-token compact.md across 4 profiles byte-identical sha256:90beff10... + V1/V6/V7 tests; 4 hashes recomputed). **Wave 3:** Plan 04.4-04 (memory telemetry — gen_ai.tool.name="memory" events + redactBlockedArgs + counters + V5 air-gap stress under strace zero non-loopback connect; commit 9ed38dc SUMMARY) → Plan 04.4-05 (memory reproducibility — resolveMemoryConfig precedence env>profile>flag + applyMemorySnapshot transactional + V6 byte-identical fidelity + --no-memory + --memory-snapshot CLI flags; commit 5bcfa55 SUMMARY) → Plan 04.4-08 (V5 /compact + /clear telemetry — emmy.compaction.trigger + emmy.session.cleared + V5_TEST_SCENARIOS exported; commit ff41285 SUMMARY). **Wave 4:** Plan 04.4-09 (closeout-staging — OPERATOR-PROTOCOLS.md runbook with 6 protocols; STATE update). **Final post-Plan-07 profile.yaml hashes:** qwen3.6-35b-a3b/v3.1=sha256:a3716a86b2b97fe46482e75cc46a23b52f466784fbbd760e95e8a4f19110a272, qwen3.6-27b/v1.1=sha256:cfcfeb97f666eee0d4c347358d27c442b5a81933f5619248cc4a94cc9088254f, gemma-4-26b-a4b-it/v2=sha256:af2e9992980625882f7c146c32d26f5916d2e5e5f375e73402c0d2968b487b2b, gemma-4-31b-it/v1.1=sha256:ffbf7e7d7ab1e83883ba9a05733f01be2fd3bfa1c48ffdce0beec386ac215489. **Test counts at close (additive vs Phase 04.2):** 130 emmy-tools/memory tests (+0 from baseline — new package surface); 14 emmy-context tests (V1/V6/V7); 25+ emmy-ux tests (prefix-hash + invariant + V4 + slash-commands V5 + memory CLI); 5 pytest (ci_verify_phase3_memory_subcheck) + 15 pytest (test_profile_schema_memory). All packages typecheck clean. **V5 air-gap V5 PASS** on local machine (zero non-loopback connect across 100 mixed memory ops). **Operator-gated items from Phase 04.4 (6):** v1 memory adoption green (≥60%), v2 memory discipline green (≥70%), v3 memory rot green (5/5 — CRITICAL), v8 memory real-deal green (1-hour session, 1–5 organic notes), v2 compaction quality green (3-axis hand-rate goal≥80%/decision≥70%/error 100%), v8 compaction real-deal green (2-hour session, 0 preempt/oom, arch-aware cache hit). Runbook for all 6 at `.planning/phases/04.4-filesystem-memory-tool-append-only-prefix-compaction-polish-/04.4-09-OPERATOR-PROTOCOLS.md`. All flip Done† → Done after operator types resume signals (~2 hours total operator time). **Next:** operator runs OPERATOR-PROTOCOLS.md; on green signals, planner flips Done† → Done in next session and Phase 04.4 closes; on FAIL, planner re-engages plan 04.4-01 (memory) or 04.4-07 (compaction prompt).

**Phase 04.2 PAPERWORK CLOSED — CLOSURE PENDING:** 2026-04-26 — Plan 06 (closeout paperwork) landed. Plans 01-05 LANDED 2026-04-25 across 3 waves: Plan 01 (sidecar core: state.py + orchestrator_runner.py + sidecar_metrics.py + controller.py FastAPI app on 0.0.0.0:8003 + 62 unit tests; commits 1f01934 + 6313472 + cf13e99 + 3c95d2c) → Plan 02 (systemd user unit emmy-sidecar.service + start_emmy.sh --install-sidecar-unit + 6 integration tests + runbook ## Remote-client posture section + Air-gap CI Phase 04.2 sub-note; commits 633c2cb + f504063 + 757d072 + 2f8c06e + 880422a) → Plan 03 (TS dual-path dispatcher: profile-swap-runner-http.ts + EMMY_REMOTE_CLIENT='1' branch in profile-swap-runner.ts + spawn-argv snapshot test pinning D-04 BYTE-STABLE invariant + 23 dispatcher tests; commits b22e550 + 7f460a5 + 4ebdee8 + 315e66f + 1057252 + 697238d) → Plan 04 (TS slash commands /start /stop /status + sidecar-status-client.ts + sidecar-lifecycle-client.ts + metrics-poller.ts D-09 LOCKED remote branch + pi-emmy-extension.ts wiring; +48 tests; commits 4dcd5af + 9aeef79 + c802987 + c5ece2a + 91e67a5) → Plan 05 (web-search.ts EMMY_SEARXNG_URL live-getter override + install-client.sh wrapper env vars + Spark-side reminders + README Remote-client mode block + tests/smoke/verify_airgap_local_mode.sh D-33 LOCKED canary; 4 env-override tests; commits 71f7390 + 0a40aea + 87e8c24 + 7ead216 + 7a907f4 + a1edb2b). **Plan 06 paperwork (this entry):** docs/runbook.md gains a verbatim "## Phase 04.2 SC walkthroughs" section with reproducible operator scripts for SC-1 (Mac one-shot), SC-2 (/start /stop /status round-trip with graceful drain), and SC-3 (/profile swap from Mac with C-06 SSE wire format end-to-end); ROADMAP.md Phase 04.2 entry filled in with real Goal + Requirements (TOOLS-10, UX-04, UX-07) + 6-plan listing with commit hashes + Progress Table row marked "Closure pending"; STATE.md Project Memory + Performance Metrics rows for all 6 Phase 04.2 plans + Current Position updated to "CLOSURE PENDING"; REQUIREMENTS.md TOOLS-10 + UX-04 + UX-07 status notes flipped to **Done†** (operator-gated SC pending — Done after `sc1/sc2/sc3 phase4.2 green` resume signals). **Operator-gated SC walkthroughs DEFERRED** per Phase 1/Phase 4 closeout precedent (operator chose deferral; the runbook scripts in `docs/runbook.md § Phase 04.2 SC walkthroughs` are the resume artifact). **Architecture preserved:** D-04 BYTE-STABLE (zero spawn-mode TS modifications below the dispatcher branch; spawn-argv snapshot test enforces) + D-33 LOCKED (web-search.ts loopback default unchanged when EMMY_SEARXNG_URL unset; air-gap STRICT validator regression smoke green) + C-05 trust-on-tailnet (no auth on sidecar; Tailscale Serve owns TLS). **Test counts at close:** 250 pytest unit (+62 sidecar tests vs Phase 04.1 baseline 188); 322 emmy-ux bun tests; 6 sidecar integration tests (opt-in via --run-integration); systemd-analyze verify smoke gate exit 0; air-gap STRICT validator dry-run exit 0. **Phase 04.2 deliverables regression-free:** Phase 4 /profile spawn-path argv byte-identical (snapshot-pinned); Phase 3.1 web-search loopback default preserved; Phase 3 OFFLINE OK badge state machine unchanged (offline-badge.ts byte-untouched). **Cumulative v1 totals after Phase 04.2 paperwork:** 43/68 REQ-IDs Done (unchanged — Phase 04.2 ships extensions to existing TOOLS-10 + UX-04 + UX-07, not new requirements). **Operator-gated items from Phase 04.2 (3):** sc1 phase4.2 green (Mac one-shot through Tailscale + EMMY_SEARXNG_URL), sc2 phase4.2 green (/start /stop /status round-trip + D-01 graceful drain), sc3 phase4.2 green (/profile swap from Mac + C-06 SSE wire format). All can be closed opportunistically whenever operator tailnet time allows; not blockers for Phase 5 start. **Next:** `/gsd-plan-phase 5` — Eval harness + reproducible benchmark suite. Phase 04.2 SC walkthroughs flip Done† → Done after operator confirmation.
