---
phase: 02-pi-harness-mvp-daily-driver-baseline
plan: 08
type: execute
wave: 4
depends_on: [02-07]
files_modified:
  - eval/phase2/sc2/fixtures/sc2_task_01.json
  - eval/phase2/sc2/fixtures/sc2_task_02.json
  - eval/phase2/sc2/fixtures/sc2_task_03.json
  - eval/phase2/sc2/fixtures/sc2_task_04.json
  - eval/phase2/sc2/fixtures/sc2_task_05.json
  - eval/phase2/sc2/fixtures/README.md
  - eval/phase2/sc2/run_sc2.ts
  - eval/phase2/sc3/corpus/synthetic.jsonl
  - eval/phase2/sc3/corpus/real_replay.jsonl
  - eval/phase2/sc3/run_sc3.ts
  - eval/phase2/sc3/README.md
  - eval/phase2/sc3/corpus_fill.ts
  - eval/phase2/sc4/run_sc4.ts
  - eval/phase2/sc5/run_sc5.ts
  - runs/phase2-sc2/.gitkeep
  - runs/phase2-sc3/.gitkeep
  - runs/phase2-sc3-capture/.gitkeep
  - runs/phase2-sc4/.gitkeep
  - runs/phase2-sc5/.gitkeep
  - profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md
  - profiles/qwen3.6-35b-a3b/v2/profile.yaml
autonomous: true
requirements:
  - HARNESS-04
  - HARNESS-07
  - SERVE-05
  - TOOLS-03
  - TOOLS-07
must_haves:
  truths:
    - "SC-2 report.json shows 0 hash-anchored 'string not found' failures across ≥5 prior-repo tasks; baseline string-replace path has ≥1 failure (regression Hashline solves)"
    - "SC-3 report.json shows synthetic ≥98% AND real-replay ≥95% AND aggregate ≥97% parse rate with grammar reactive (D-12 graduated SLA)"
    - "SC-3 no-grammar baseline captured on SAME 100-call corpus (grammar.mode=disabled) in baseline.json"
    - "SC-3 third run captured WITHOUT per_tool_sampling in v2 harness.yaml (temporarily removed) → runs/phase2-sc3/no_per_tool_sampling.json — W3 fix + Pitfall #5 before/after discipline"
    - "SC-3 real-replay corpus has exactly 50 entries sampled from runs/phase2-sc3-capture/session-*.jsonl (B2 capture feed from Plan 04); if <50 sessions captured at plan execution time, corpus_fill.ts runs pi-emmy --print 50× with rotating prompts to backfill"
    - "SC-4 report shows MCP filesystem server registers correctly with flat name; all 4 poison categories (Cf/Co/Cs/bidi-override) rejected with named PoisonError"
    - "SC-5 report shows prompt.sha256 byte-stable across 3 runs; AGENTS.md verbatim in assembled text; AGENTS.md tokens > 0; max_input_tokens consistent with computed value"
    - "PROFILE_NOTES validation_runs updated; profile.yaml hash RE-recomputed after the PROFILE_NOTES.md edit; `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0 at plan close"
  artifacts:
    - path: "eval/phase2/sc2/fixtures/*.json"
      provides: "5-7 SC-2 task fixtures (prior-repo CODE_01..CODE_05, plus synthetic augmentation per D-10 if edit-coverage <3/5)"
    - path: "runs/phase2-sc2/report.json"
      provides: "SC-2 evidence: per-task pass/fail for hash-anchored vs baseline edit paths"
    - path: "runs/phase2-sc3/report.json"
      provides: "SC-3 evidence: 100-call parse rate on grammar-on path (D-12 graduated SLA)"
    - path: "runs/phase2-sc3/baseline.json"
      provides: "SC-3 no-grammar baseline (D-14 counterfactual)"
    - path: "runs/phase2-sc3/no_per_tool_sampling.json"
      provides: "SC-3 counterfactual WITHOUT per_tool_sampling (W3 fix + Pitfall #5 before/after)"
    - path: "runs/phase2-sc4/report.json"
      provides: "SC-4 evidence: MCP filesystem server dispatch + 4 poison category fixtures"
    - path: "runs/phase2-sc5/report.json"
      provides: "SC-5 evidence: prompt.sha256 stability + AGENTS.md token count > 0 + max_input_tokens consistent"
  key_links:
    - from: "eval/phase2/sc3/corpus/real_replay.jsonl"
      to: "runs/phase2-sc3-capture/session-*.jsonl (B2 feed from Plan 04)"
      via: "50 sampled turns; corpus_fill.ts backfills via pi-emmy --print if capture directory is short"
      pattern: "runs/phase2-sc3-capture"
    - from: "eval/phase2/sc3/run_sc3.ts --grammar reactive|disabled"
      to: "profiles/qwen3.6-35b-a3b/v2/harness.yaml tools.grammar.mode"
      via: "runner temporarily flips mode to 'disabled' for baseline; resets to 'reactive' after"
      pattern: "grammar.*mode"
    - from: "runs/phase2-sc3/no_per_tool_sampling.json"
      to: "profiles/qwen3.6-35b-a3b/v2/harness.yaml tools.per_tool_sampling"
      via: "runner temporarily removes the key for this third run (W3)"
      pattern: "per_tool_sampling"
---

<objective>
Run the four automated SC verification drivers against real emmy-serve:
- **SC-2:** hash-anchored edit regression on 5 prior-repo coding tasks (+ synthetic augmentation per D-10 if edit-coverage <3/5). `verdict=pass` iff hash-anchored has 0 "string not found" failures AND baseline string-replace has ≥1 (proves the regression Hashline solves).
- **SC-3:** parse-rate over a 100-call corpus (50 synthetic + 50 real-replay from B2 capture feed). Runs THREE times:
  1. Grammar reactive + per_tool_sampling populated → `runs/phase2-sc3/report.json`
  2. Grammar DISABLED, per_tool_sampling populated → `runs/phase2-sc3/baseline.json` (D-14 no-grammar baseline)
  3. Grammar reactive + per_tool_sampling REMOVED → `runs/phase2-sc3/no_per_tool_sampling.json` (W3 Pitfall #5 before/after discipline)
  `verdict=pass` iff synthetic ≥98% AND real-replay ≥95% AND aggregate ≥97% on Run 1 (D-12 graduated SLA).
- **SC-4:** MCP filesystem server dispatches identically to native; 4 poison categories (Cf, Co, Cs, bidi-override) reject with named PoisonError. `verdict=pass` iff all 4 rejected AND fs-server listTools/callTool works.
- **SC-5:** prompt.sha256 byte-stable across 3 runs on a fixture repo; AGENTS.md verbatim in assembled text; AGENTS.md tokens_approx > 0; harness.yaml.context.max_input_tokens equals the computed value.

Then update `profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md` with the validation_runs hashes and re-recompute the profile hash (PROFILE_NOTES.md changed, so the content hash changes).

Purpose: This is the evidence-capture plan. It does NOT modify source code (`packages/*/src/` stays frozen) — only fixtures, runners, and output artifacts. SC-1 is NOT in this plan; it's Plan 09's human-verify checkpoint.

Output:
- 5-7 SC-2 fixtures + SC-2 runner + runs/phase2-sc2/report.json
- SC-3 synthetic + real-replay corpora + SC-3 runner + 3 output files (report, baseline, no_per_tool_sampling)
- SC-4 runner + runs/phase2-sc4/report.json (4 poison categories + fs-server dispatch rows)
- SC-5 runner + runs/phase2-sc5/report.json
- Corpus-fill helper (`eval/phase2/sc3/corpus_fill.ts`) for when B2 capture directory is short
- Updated PROFILE_NOTES.md validation_runs + re-recomputed v2 profile hash

This plan is Wave 4: depends on Plan 07 (v2 profile must validate before these runs make sense). Plan 09 follows in Wave 5 with the SC-1 human-verify + CLOSEOUT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md
@/data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-PATTERNS.md
@/data/projects/emmy/CLAUDE.md
@/data/projects/emmy/profiles/qwen3.6-35b-a3b/v2/harness.yaml
@/data/projects/emmy/packages/emmy-ux/src/session.ts
@/data/projects/emmy/packages/emmy-ux/bin/pi-emmy.ts
@/data/projects/emmy/packages/emmy-ux/src/session-transcript.ts
@/data/projects/emmy/packages/emmy-provider/src/grammar-retry.ts
@/data/projects/emmy/packages/emmy-tools/src/mcp-poison-check.ts

<source_excerpts>
From CONTEXT.md §D-10, D-12, D-13, D-14 + §specifics (SC-4 poison fixtures, SC-5 regression).
From CLAUDE.md Pitfall #5: "Every time a Phase 2 decision adjusts the system prompt, per-tool sampling, or retry policy, the change MUST be measured against the full SC-3 corpus, not a subset."

Prior-repo fixtures (lifted per D-10):
- `/data/projects/setup_local_opencode/validation/eval_tasks.py` — CODE_01..CODE_05
- `/data/projects/setup_local_opencode/validation/PHASE1_RESULTS_QWEN3.json` — baseline scores

Report schema (every SC runner emits):
```json
{
  "sc": "SC-2",
  "phase": "02",
  "profile": { "id": "qwen3.6-35b-a3b", "version": "v2", "hash": "sha256:..." },
  "started_at": "2026-04-2XT...Z",
  "ended_at": "2026-04-2XT...Z",
  "verdict": "pass" | "fail",
  "metrics": { },
  "rows": [ ],
  "environment": { }
}
```

Runner commands:
```
bun eval/phase2/sc2/run_sc2.ts --profile profiles/qwen3.6-35b-a3b/v2 --base-url http://127.0.0.1:8002 --out runs/phase2-sc2/
bun eval/phase2/sc3/run_sc3.ts --profile profiles/qwen3.6-35b-a3b/v2 --variant reactive --out runs/phase2-sc3/report.json
bun eval/phase2/sc3/run_sc3.ts --profile profiles/qwen3.6-35b-a3b/v2 --variant disabled --out runs/phase2-sc3/baseline.json
bun eval/phase2/sc3/run_sc3.ts --profile profiles/qwen3.6-35b-a3b/v2 --variant no_per_tool_sampling --out runs/phase2-sc3/no_per_tool_sampling.json
bun eval/phase2/sc4/run_sc4.ts --profile profiles/qwen3.6-35b-a3b/v2 --out runs/phase2-sc4/report.json
bun eval/phase2/sc5/run_sc5.ts --profile profiles/qwen3.6-35b-a3b/v2 --out runs/phase2-sc5/report.json
```
</source_excerpts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SC-2 fixtures + SC-2 runner + SC-3 corpus (synthetic + real-replay via B2 feed + corpus_fill backfill)</name>
  <read_first>
    - /data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md §D-10, D-13
    - /data/projects/setup_local_opencode/validation/eval_tasks.py (entire — CODE_01..CODE_05 lift)
    - /data/projects/setup_local_opencode/validation/PHASE1_RESULTS_QWEN3.json (baseline scores)
    - /data/projects/emmy/packages/emmy-ux/src/session-transcript.ts (from Plan 04 — reads `runs/phase2-sc3-capture/`)
    - /data/projects/emmy/packages/emmy-tools/src/edit-hashline.ts (from Plan 03 — SC-2 hash-anchored path)
    - /data/projects/emmy/air_gap/session.jsonl (Phase 1 real-turn replay — shape reference for synthetic.jsonl)
  </read_first>
  <action>
**Step 1 — SC-2 fixtures (edit-coverage audit per D-10).**

Read `/data/projects/setup_local_opencode/validation/eval_tasks.py`. Extract CODE_01..CODE_05. For each task tag `exercises_edit: true|false` based on whether the prompt requires ≥1 edit to a pre-existing file.

If <3 of 5 exercise edit, augment with synthetic tasks:
- `sc2_task_synthetic_rename.json` — rename a symbol across 2-3 files
- `sc2_task_synthetic_whitespace.json` — change indentation in a dense function
- `sc2_task_synthetic_dup_lines.json` — file with 2 identical `dup\ndup\nend` lines; agent must disambiguate via HashResolutionError path

Write final 5-7 fixtures to `eval/phase2/sc2/fixtures/sc2_task_*.json` with shape:
```json
{
  "task_id": "sc2_01",
  "source": "prior-repo CODE_01 | synthetic",
  "exercises_edit": true,
  "prompt": "...",
  "expected_rubric": "...",
  "notes": "..."
}
```

Write `eval/phase2/sc2/fixtures/README.md` documenting which tasks are lifted vs synthetic (D-10 disclosure).

**Step 2 — SC-2 runner (`eval/phase2/sc2/run_sc2.ts`).**

```
1. Load 5-7 fixtures.
2. For each fixture run TWO pi-emmy sessions:
   (a) default hash-anchored `edit` tool
   (b) baseline: a modified session that registers a plain string-replace tool as "edit"
       (runner mocks registerNativeTools to override the edit registration)
3. Count per session:
   - Edit-tool invocation count
   - "string not found" failures (StaleHashError/HashResolutionError for hash-anchored; "old_string not found" for plain)
   - Task completion verdict (regex on final model output OR asserted rubric)
4. Emit runs/phase2-sc2/report.json:
   {
     sc: "SC-2",
     verdict: "pass" | "fail",
     rows: [{task_id, hash_anchored: {invocations, string_not_found_failures, completed}, baseline: {...}}],
     metrics: {hash_anchored_string_not_found_failures_total, baseline_string_not_found_failures_total, delta_failures},
     profile, environment, started_at, ended_at
   }
5. verdict = "pass" iff hash_anchored_string_not_found_failures_total === 0
   AND baseline_string_not_found_failures_total >= 1
```

Pre-flight: emmy-serve reachable on :8002 with Qwen3.6. If not, exit 4 with diagnostic.

Run. Commit report + fixtures.

**Step 3 — SC-3 synthetic half (50 calls) at `eval/phase2/sc3/corpus/synthetic.jsonl`.**

Hand-author 50 entries covering:
- 6 per native tool × 8 tools = 48 calls
- 2 MCP-client-synthetic (crafted tool-call-shaped turns referencing plausible MCP tool names, to stress the parser without requiring a real MCP load)

Each entry:
```json
{"id": "syn_01", "turn": [{"role":"system","content":"..."},{"role":"user","content":"read /tmp/x"}], "expected_tool": "read", "adversarial_shape": "normal|long_path|nested_args|unicode_filename|empty_arg"}
```

Adversarial shapes per D-13: long paths (>300 chars), nested JSON args (3 levels), unicode filenames, empty-string arg values, edge-case numeric values.

**Step 4 — SC-3 real-replay half (50 turns) via B2 capture feed + corpus_fill backfill.**

Check `ls runs/phase2-sc3-capture/session-*.jsonl 2>/dev/null | wc -l`. Aggregate all captured turns across all sessions.

- **If ≥50 tool-call turns are available:** sample 50 (mix of tools, not all `read`) into `eval/phase2/sc3/corpus/real_replay.jsonl`. Done.
- **If <50 available (e.g., Phase 2 dev ran short):** run `eval/phase2/sc3/corpus_fill.ts` to backfill. This helper:
  1. Uses a rotating set of 10 real prompts from `eval_tasks.py` (code + literature prompts).
  2. Invokes `pi-emmy --print "<prompt>"` against real emmy-serve 50 times (rotating prompts).
  3. Each invocation writes a transcript to `runs/phase2-sc3-capture/` (B2 feed — existing Plan 04 machinery).
  4. After the 50 invocations complete, aggregates turns and samples 50 into `real_replay.jsonl`.

  ```typescript
  // eval/phase2/sc3/corpus_fill.ts (sketch)
  #!/usr/bin/env bun
  import { execFileSync } from "node:child_process";
  const PROMPTS = [/* 10 prompts lifted from eval_tasks.py */];
  for (let i = 0; i < 50; i++) {
    const p = PROMPTS[i % PROMPTS.length]!;
    try { execFileSync("pi-emmy", ["--print", p], { encoding: "utf8", stdio: "inherit", timeout: 120_000 }); }
    catch (e) { console.error(`[fill] iteration ${i} failed; continuing:`, e); }
  }
  // Then aggregate runs/phase2-sc3-capture/*.jsonl into 50 sampled real_replay.jsonl entries.
  ```

This step ensures `real_replay.jsonl` has exactly 50 lines before SC-3 runs. Document in README.md whether the corpus was sampled naturally or backfilled (and how many of each).

**Step 5 — Commit.** `test(02-08): SC-2 fixtures + runner + SC-3 corpora (synthetic + real-replay with backfill)`.
  </action>
  <verify>
    <automated>test -f /data/projects/emmy/runs/phase2-sc2/report.json &amp;&amp; grep -c '"verdict": "pass"' /data/projects/emmy/runs/phase2-sc2/report.json &amp;&amp; test $(wc -l &lt; /data/projects/emmy/eval/phase2/sc3/corpus/synthetic.jsonl) -eq 50 &amp;&amp; test $(wc -l &lt; /data/projects/emmy/eval/phase2/sc3/corpus/real_replay.jsonl) -eq 50</automated>
  </verify>
  <acceptance_criteria>
    - `test -f runs/phase2-sc2/report.json` exits 0
    - `grep -c '"verdict": "pass"' runs/phase2-sc2/report.json` returns 1
    - `grep -cE '"hash_anchored_string_not_found_failures_total":\s*0' runs/phase2-sc2/report.json` returns 1
    - `grep -cE '"baseline_string_not_found_failures_total":\s*[1-9]' runs/phase2-sc2/report.json` returns 1
    - `ls eval/phase2/sc2/fixtures/sc2_task_*.json | wc -l` returns at least 5 (possibly augmented per D-10)
    - `test -f eval/phase2/sc2/fixtures/README.md && grep -c 'synthetic\|lifted' eval/phase2/sc2/fixtures/README.md` returns at least 1
    - `test $(wc -l < eval/phase2/sc3/corpus/synthetic.jsonl) -eq 50`
    - `test $(wc -l < eval/phase2/sc3/corpus/real_replay.jsonl) -eq 50`
    - `test -f eval/phase2/sc3/corpus_fill.ts`
    - `test -f eval/phase2/sc3/README.md && grep -cE '(naturally|backfill)' eval/phase2/sc3/README.md` returns at least 1 (sourcing disclosure)
    - Plan 07 untouched: `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0
  </acceptance_criteria>
  <done>
    SC-2 fixtures (5-7) and runner shipped; SC-2 report shows 0 hash-anchored failures vs ≥1 baseline failure; SC-3 synthetic.jsonl (50) and real_replay.jsonl (50) corpora ready; corpus_fill.ts is available for backfill; README.md discloses sampling provenance.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SC-3 runner (3 runs: reactive + disabled baseline + no_per_tool_sampling) + SC-4 runner (MCP dispatch + 4 poison categories) + SC-5 runner (prompt hash + AGENTS.md + max_input_tokens)</name>
  <read_first>
    - /data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md §D-11..D-14, §D-18, §specifics SC-4/SC-5
    - /data/projects/emmy/CLAUDE.md Pitfall #5 (measure full corpus before/after)
    - /data/projects/emmy/packages/emmy-provider/src/grammar-retry.ts (SC-3 metric source — retry events)
    - /data/projects/emmy/packages/emmy-tools/src/mcp-poison-check.ts (SC-4 poison engine)
    - /data/projects/emmy/packages/emmy-ux/src/prompt-assembly.ts (SC-5 source)
    - /data/projects/emmy/packages/emmy-ux/src/max-model-len.ts (SC-5 regression link)
    - /data/projects/emmy/profiles/qwen3.6-35b-a3b/v2/harness.yaml (Plan 07 filled values)
  </read_first>
  <action>
**Step 1 — SC-3 runner (`eval/phase2/sc3/run_sc3.ts`).**

Accept `--variant reactive | disabled | no_per_tool_sampling`. Variant drives a temporary, scoped mutation of the v2 harness.yaml, RESTORED at the end of the run regardless of outcome.

Variant semantics:
- `reactive` — no mutation; harness.yaml as Plan 07 left it. Output → `runs/phase2-sc3/report.json`.
- `disabled` — temporarily set `tools.grammar.mode: disabled`. Output → `runs/phase2-sc3/baseline.json`. D-14 counterfactual.
- `no_per_tool_sampling` — temporarily REMOVE the `tools.per_tool_sampling` key entirely. Output → `runs/phase2-sc3/no_per_tool_sampling.json`. **W3 fix + Pitfall #5 Before/After discipline — isolates the effect of per-tool sampling on parse rate.**

The runner MUST restore the ORIGINAL v2/harness.yaml bytes after each run (use a `cp profiles/qwen3.6-35b-a3b/v2/harness.yaml <tmp>` before mutation + `cp <tmp> ...` in a finally block). After restore, `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` MUST exit 0 (hash unchanged). If the restore leaves a mismatch, that's a runner bug and the runner MUST exit non-zero with the diagnostic `sc3 runner: harness.yaml restore failed — profile hash mismatch`.

Runner pseudocode:
```
1. Load 100 corpus entries (50 synthetic + 50 real-replay).
2. For each corpus entry: issue a fresh pi-emmy --json turn against emmy-serve and record:
   - Whether tool_calls parsed cleanly on the FIRST try (unconstrained)
   - Whether the provider's retry was triggered (by subscribing to emitEvent records via file tail or by flipping @emmy/telemetry to a JSONL sink for this plan)
   - Final retry outcome (success / exhausted)
3. Compute:
   - synthetic_parse_rate = (50 - synthetic_parse_failures_after_retry) / 50
   - real_replay_parse_rate = (50 - real_parse_failures_after_retry) / 50
   - aggregate_parse_rate = (100 - total_failures_after_retry) / 100
4. Emit output JSON with:
   { sc: "SC-3", variant, verdict (only "reactive" gates), metrics, rows, profile, environment, started_at, ended_at }
5. The "reactive" variant sets verdict="pass" iff synthetic >= 0.98 AND real_replay >= 0.95 AND aggregate >= 0.97.
   The other two variants are informational — no verdict gate; they exist for delta analysis in CLOSEOUT (Plan 09).
```

Run THREE times:
- `bun eval/phase2/sc3/run_sc3.ts --variant reactive --out runs/phase2-sc3/report.json`
- `bun eval/phase2/sc3/run_sc3.ts --variant disabled --out runs/phase2-sc3/baseline.json`
- `bun eval/phase2/sc3/run_sc3.ts --variant no_per_tool_sampling --out runs/phase2-sc3/no_per_tool_sampling.json`

After all three, assert `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0 (bytes restored).

**Step 2 — SC-4 runner (`eval/phase2/sc4/run_sc4.ts`).**

```
1. Spawn an MCP stdio server: `@modelcontextprotocol/server-filesystem` with sandbox root /tmp/emmy-sc4-root/. Verify listTools responds.
2. Run registerMcpServers via a minimal test harness OR stage ./.emmy/mcp_servers.yaml and invoke pi-emmy so the bridge registers. Verify the fs-server tools register with FLAT names (e.g., "read_file" - no collision with native "read").
3. For EACH of 4 Unicode categories, craft a poison fixture and attempt registration:
   (a) Cf — U+200B in name -> PoisonError 0x200B, "Cf (format)"
   (b) Co — U+E000 in description -> PoisonError 0xE000, "Co (private use)"
   (c) Cs — U+D800 in name -> PoisonError 0xD800, "Cs (surrogate)"
   (d) bidi U+202E in description -> PoisonError 0x202E, "bidi U+202A-U+202E"
4. Verify poisoned tool is NOT in the active registry.
5. Cleanup: kill the fs-server subprocess AND `rm -rf /tmp/emmy-sc4-root/`.
6. Emit runs/phase2-sc4/report.json:
   { sc: "SC-4", verdict: "pass" iff all 4 poison categories rejected AND fs-server dispatch works, rows: [per category + per fs-server-tool], metrics: { poison_rejected_count: 4, fs_server_tools_registered: N, collisions_detected: 0 }, profile, environment }
```

For poison fixtures the runner uses a bridge-internal test hook OR stubs `@modelcontextprotocol/sdk`'s `Client.listTools()` for the subset; Plan 06's `mcp-poison-check.test.ts` already proves `assertNoPoison` in isolation; SC-4 proves it's WIRED into the bridge at runtime.

**Step 3 — SC-5 runner (`eval/phase2/sc5/run_sc5.ts`).**

```
1. Stage fixture repo at /tmp/emmy-sc5-fixture/:
   - ./AGENTS.md — known canonical text, exactly 500 bytes
   - ./src/foo.ts — empty stub
2. Run createEmmySession({profile, baseUrl, cwd: /tmp/emmy-sc5-fixture, mode: "print", userPrompt: "hi"}) THREE TIMES. Capture assembledPrompt.sha256 each time.
3. Assert the three sha256s are IDENTICAL (byte-stable per CONTEXT-04 locked order).
4. Assert AGENTS.md layer is present: true with tokens_approx > 0.
5. Assert assembled text CONTAINS the verbatim AGENTS.md content (substring check).
6. Assert the 64-hex sha256 appears in stderr logs (regex check via subprocess capture).
7. Verify harness.yaml.context.max_input_tokens == computeMaxInputTokens(...).max_input_tokens exactly (superset of Plan 07's un-skipped regression).
8. Emit runs/phase2-sc5/report.json:
   { sc: "SC-5", verdict: "pass" iff sha256_stable AND agents_md_included AND max_input_tokens_consistent, rows: [per run], metrics: { sha256_unique_count: 1, agents_md_tokens_approx: N, max_input_tokens_consistent: true }, profile, environment }
```

**Step 4 — Update PROFILE_NOTES.md validation_runs + re-recompute hash.**

Append to `profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md` a `### Phase 2 validation_runs` section listing each SC output artifact with its sha256 (compute via `sha256sum runs/phase2-sc{2,3,4,5}/*.json`):

```markdown
### Phase 2 validation_runs

| Run ID | Artifact | SHA-256 |
|--------|----------|---------|
| phase2-sc2 | runs/phase2-sc2/report.json | sha256:... |
| phase2-sc3-reactive | runs/phase2-sc3/report.json | sha256:... |
| phase2-sc3-disabled | runs/phase2-sc3/baseline.json | sha256:... |
| phase2-sc3-no_per_tool_sampling | runs/phase2-sc3/no_per_tool_sampling.json | sha256:... |
| phase2-sc4 | runs/phase2-sc4/report.json | sha256:... |
| phase2-sc5 | runs/phase2-sc5/report.json | sha256:... |
```

Because PROFILE_NOTES.md changed, re-run `uv run emmy profile hash --write profiles/qwen3.6-35b-a3b/v2/` and assert `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0.

**Step 5 — Commit.** `feat(02-08): SC-2/3/4/5 evidence captured + PROFILE_NOTES validation_runs + v2 hash re-locked`.
  </action>
  <verify>
    <automated>test -f /data/projects/emmy/runs/phase2-sc3/report.json &amp;&amp; test -f /data/projects/emmy/runs/phase2-sc3/baseline.json &amp;&amp; test -f /data/projects/emmy/runs/phase2-sc3/no_per_tool_sampling.json &amp;&amp; test -f /data/projects/emmy/runs/phase2-sc4/report.json &amp;&amp; test -f /data/projects/emmy/runs/phase2-sc5/report.json &amp;&amp; cd /data/projects/emmy &amp;&amp; uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f runs/phase2-sc3/report.json` AND `grep -c '"verdict": "pass"' runs/phase2-sc3/report.json` returns 1
    - `grep -cE '"synthetic_parse_rate":\s*(1\.0|0\.9[89])' runs/phase2-sc3/report.json` returns 1 (>= 0.98)
    - `grep -cE '"real_replay_parse_rate":\s*(1\.0|0\.9[56789])' runs/phase2-sc3/report.json` returns 1 (>= 0.95)
    - `grep -cE '"aggregate_parse_rate":\s*(1\.0|0\.9[789])' runs/phase2-sc3/report.json` returns 1 (>= 0.97)
    - **W3 FIX — third run present:** `test -f runs/phase2-sc3/no_per_tool_sampling.json` exits 0 AND `grep -c '"variant": "no_per_tool_sampling"' runs/phase2-sc3/no_per_tool_sampling.json` returns 1
    - **D-14 baseline:** `test -f runs/phase2-sc3/baseline.json` AND `grep -c '"variant": "disabled"' runs/phase2-sc3/baseline.json` returns 1
    - **SC-3 hygiene: harness.yaml restored:** after all three runs, `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0 AND `grep -cE 'mode:\s+reactive' profiles/qwen3.6-35b-a3b/v2/harness.yaml` returns 1 AND `grep -c 'per_tool_sampling' profiles/qwen3.6-35b-a3b/v2/harness.yaml` returns at least 1
    - `test -f runs/phase2-sc4/report.json` AND `grep -c '"verdict": "pass"' runs/phase2-sc4/report.json` returns 1
    - All 4 poison categories in SC-4 report: each of grep for `Cf`, `Co`, `Cs`, `bidi` returns at least 1 hit in runs/phase2-sc4/report.json
    - `grep -c 'filesystem' runs/phase2-sc4/report.json` returns at least 1 (MCP fs-server dispatch recorded)
    - `test -f runs/phase2-sc5/report.json` AND `grep -c '"verdict": "pass"' runs/phase2-sc5/report.json` returns 1
    - **SC-5 sha256 stability:** `grep -oE '"sha256":\s*"[0-9a-f]{64}"' runs/phase2-sc5/report.json | sort -u | wc -l` returns 1 (all three runs produced identical sha256)
    - `grep -cE '"agents_md_tokens_approx":\s*[1-9]' runs/phase2-sc5/report.json` returns 1
    - `grep -cE '"max_input_tokens_consistent":\s*true' runs/phase2-sc5/report.json` returns 1
    - **PROFILE_NOTES.md updated + hash re-recomputed:** `grep -c 'Phase 2 validation_runs' profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md` returns 1
    - `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0 (v2 still validates after PROFILE_NOTES updates)
    - Phase 1 regression: `cd /data/projects/emmy && uv run pytest tests/unit -q` exits 0
    - TS regression: `cd /data/projects/emmy && bun test` exits 0
  </acceptance_criteria>
  <done>
    SC-3 three-run evidence shipped (reactive, disabled baseline, no_per_tool_sampling counterfactual per W3/Pitfall #5); harness.yaml bytes restored after each mutation (validation confirms); SC-4 evidence covers 4 poison categories + fs-server dispatch; SC-5 evidence confirms sha256 stability + AGENTS.md included + max_input_tokens consistent; PROFILE_NOTES.md updated and profile hash re-recomputed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SC runner <-> emmy-serve | Runners issue hundreds of tool-call turns against live emmy-serve. Session timeouts are bounded by Plan 04's per-turn 120s cap. |
| harness.yaml mutation (SC-3 variant runner) | Runner temporarily edits v2/harness.yaml. Must restore byte-identical + leave v2 validating. |
| SC-4 MCP filesystem sandbox | `@modelcontextprotocol/server-filesystem` runs with sandbox root /tmp/emmy-sc4-root/. Cleanup at end of test. |
| Evidence authenticity | runs/phase2-sc*/report.json carry started_at/ended_at timestamps. No signing in Phase 2; Phase 5 EVAL-03 adds provenance dict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | SC-3 runner leaves v2/harness.yaml mutated on crash | mitigate | `try/finally` around the mutation; restore from tmp-copy; post-run `uv run emmy profile validate` gate. If restore fails, runner exits non-zero with a named diagnostic. |
| T-02-08-02 | Spoofing | "More prompting" / "more sampling" trap (Pitfall #5) | mitigate | W3/Pitfall #5 mitigation IS Task 2's third run (`no_per_tool_sampling`). If it regresses parse rate >1pp, document in CLOSEOUT (Plan 09) and decide: revert or escalate to Phase 5 eval. |
| T-02-08-03 | Tampering | Evidence files committed pre-run (fake pass) | mitigate | Every report.json carries `started_at`/`ended_at` + environment snapshot; commit happens AFTER the runs. Phase 5 can re-execute and compare. |
| T-02-08-04 | Information Disclosure | Fixture prompts from prior repo may include sensitive content | accept | Prior repo is author's own work, Apache-2.0, no secrets; public coding prompts. |
| T-02-08-05 | Denial of Service | SC-3 100-call corpus takes hours against real emmy-serve | accept | Phase 2 close IS a multi-hour milestone. SC-3 is one-shot at phase close; not a per-commit gate. |
| T-02-08-06 | Elevation of Privilege | SC-4 spawns real MCP filesystem server | mitigate | Sandbox root is /tmp/emmy-sc4-root/ (passed as arg); rm -rf cleanup at end. Runner documents this in comments. |
</threat_model>

<verification>
Plan 08 complete when:

1. All 5 SC artifact files exist with pass verdicts (SC-2, SC-3 reactive + baseline + no_per_tool_sampling, SC-4, SC-5)
2. SC-3 real-replay corpus has exactly 50 entries (from B2 capture feed OR backfilled via corpus_fill.ts)
3. harness.yaml bytes restored after SC-3 runs (uv run emmy profile validate exits 0)
4. PROFILE_NOTES.md carries a Phase 2 validation_runs section; v2 profile hash re-recomputed + validate exits 0
5. Phase 1 Python regression green; Phase 2 TS tests still green
6. `packages/*/src/` untouched (no source edits)
</verification>

<success_criteria>
Plan 08 complete when:
- [ ] SC-2 report: 0 hash-anchored failures; at least 1 baseline failure
- [ ] SC-3 reactive report: synthetic >= 98%, real-replay >= 95%, aggregate >= 97%
- [ ] SC-3 disabled baseline captured (D-14)
- [ ] SC-3 no_per_tool_sampling captured (W3 / Pitfall #5 before/after)
- [ ] SC-3 corpus: synthetic.jsonl (50) + real_replay.jsonl (50) present; B2 capture feed consumed or corpus_fill.ts backfilled
- [ ] SC-4 report: 4 poison categories rejected + MCP fs-server dispatches
- [ ] SC-5 report: sha256 stable across 3 runs; AGENTS.md tokens > 0; max_input_tokens_consistent: true
- [ ] PROFILE_NOTES.md validation_runs updated; v2 hash re-recomputed; `uv run emmy profile validate v2` exits 0
- [ ] No source files under `packages/*/src/` modified in this plan (verified via `git diff --stat packages/*/src/` produces no output)
- [ ] All Phase 1 + Phase 2 regression suites green
</success_criteria>

<output>
Create `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-08-SUMMARY.md`:

- SC-2 failure counts (hash-anchored vs baseline); list per-task pass/fail
- SC-3 three-run parse-rate table (synthetic, real-replay, aggregate) for reactive / disabled / no_per_tool_sampling; delta analysis for the W3/Pitfall #5 comparison
- SC-4 poison rejection table (one row per category + codepoint + thrown error class name)
- SC-5 sha256 stability verification + AGENTS.md token count
- Corpus sourcing: how many turns came from B2 capture vs corpus_fill.ts backfill
- v2 profile hash before/after PROFILE_NOTES.md update
- Any deviations (emmy-serve instability, network hiccups during web_fetch fixtures, etc.)
- Next: Plan 09 (SC-1 human-verify + CLOSEOUT).
</output>
