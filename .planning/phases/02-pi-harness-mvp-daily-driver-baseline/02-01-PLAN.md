---
phase: 02-pi-harness-mvp-daily-driver-baseline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - bun.lockb
  - tsconfig.base.json
  - biome.json
  - .gitignore
  - packages/emmy-provider/package.json
  - packages/emmy-provider/tsconfig.json
  - packages/emmy-provider/src/index.ts
  - packages/emmy-tools/package.json
  - packages/emmy-tools/tsconfig.json
  - packages/emmy-tools/src/index.ts
  - packages/emmy-ux/package.json
  - packages/emmy-ux/tsconfig.json
  - packages/emmy-ux/src/index.ts
  - packages/emmy-ux/bin/pi-emmy.ts
  - packages/emmy-telemetry/package.json
  - packages/emmy-telemetry/tsconfig.json
  - packages/emmy-telemetry/src/index.ts
  - profiles/qwen3.6-35b-a3b/v2/profile.yaml
  - profiles/qwen3.6-35b-a3b/v2/serving.yaml
  - profiles/qwen3.6-35b-a3b/v2/harness.yaml
  - profiles/qwen3.6-35b-a3b/v2/prompts/system.md
  - profiles/qwen3.6-35b-a3b/v2/prompts/edit_format.md
  - profiles/qwen3.6-35b-a3b/v2/prompts/tool_descriptions.md
  - profiles/qwen3.6-35b-a3b/v2/tool_schemas/.gitkeep
  - profiles/qwen3.6-35b-a3b/v2/grammars/.gitkeep
  - profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md
  - docs/mcp_servers_template.yaml
  - docs/agents_md_template.md
autonomous: true
requirements:
  - HARNESS-01
  - CONTEXT-01
user_setup:
  - service: bun
    why: "Bun 1.1+ runtime for TS workspace + `pi-emmy` binary (D-02)"
    env_vars: []
    dashboard_config:
      - task: "Install Bun 1.1+ on host: `curl -fsSL https://bun.sh/install | bash`"
        location: "host terminal (DGX Spark)"
must_haves:
  truths:
    - "bun install from clean clone produces a working workspace with all four @emmy/* packages resolvable"
    - "A profile v2 directory exists beside v1; v1 remains byte-identical to Phase 1 closeout"
    - "pi-coding-agent@0.68.0 is pinned exactly (not ^, not ~) in workspace dependencies"
    - "bun link inside packages/emmy-ux puts `pi-emmy` on PATH (shim binary, exits 0 with no-op)"
    - "bun.lockb IS COMMITTED (reproducibility discipline — Pitfall #8 air-gap); .gitignore MUST NOT exclude it"
  artifacts:
    - path: "package.json"
      provides: "Bun workspace root: workspaces=[packages/*]; engines.bun>=1.1"
      contains: "\"workspaces\""
    - path: "bun.lockb"
      provides: "Bun lockfile — committed for reproducibility (Pitfall #8 + Phase 1 REPRO discipline carried forward to TS side)"
      contains: ""
    - path: "packages/emmy-provider/package.json"
      provides: "@emmy/provider manifest with pinned pi dep"
      contains: "\"@mariozechner/pi-coding-agent\": \"0.68.0\""
    - path: "packages/emmy-tools/package.json"
      provides: "@emmy/tools manifest"
      contains: "\"name\": \"@emmy/tools\""
    - path: "packages/emmy-ux/package.json"
      provides: "@emmy/ux manifest with bin: pi-emmy"
      contains: "\"bin\""
    - path: "packages/emmy-ux/bin/pi-emmy.ts"
      provides: "Shebang-led TS entry; Wave-0 prints 'pi-emmy wave-0 shim' and exit 0"
      contains: "#!/usr/bin/env bun"
    - path: "packages/emmy-telemetry/src/index.ts"
      provides: "Stub emitEvent(record) signature — no-op body (Phase 3 fills)"
      contains: "export function emitEvent"
    - path: "profiles/qwen3.6-35b-a3b/v2/profile.yaml"
      provides: "Profile v2 manifest — content hash populated by `uv run emmy profile hash`"
      contains: "version: v2"
    - path: "profiles/qwen3.6-35b-a3b/v2/harness.yaml"
      provides: "v2 harness.yaml — copied from v1 (still has TODO(Phase-2) stubs; Plan 07 fills)"
      contains: "prompts:"
    - path: "docs/mcp_servers_template.yaml"
      provides: "User-level MCP server config example (D-16 layering pattern)"
      contains: "servers:"
    - path: "docs/agents_md_template.md"
      provides: "AGENTS.md starter with build/test/style sections (CONTEXT-01)"
      contains: "# Build"
  key_links:
    - from: "packages/emmy-*/package.json"
      to: "workspace root package.json"
      via: "Bun workspaces resolution"
      pattern: "packages/\\*"
    - from: "profiles/qwen3.6-35b-a3b/v2/"
      to: "profiles/qwen3.6-35b-a3b/v1/"
      via: "sibling immutable directory per Phase 1 D-02 + Phase 2 PATTERNS recommendation"
      pattern: "v[12]"
---

<objective>
Bootstrap the Phase 2 TS/Node workspace. This is strict Wave 0: it creates the package layout, pins `@mariozechner/pi-coding-agent@0.68.0` exactly, stands up `pi-emmy` as a shim binary on PATH, and clones the profile bundle from v1 → v2 so Phase 2 can fill `harness.yaml` without mutating the Phase 1 locked v1 hash.

Purpose: Every downstream plan (02/03/04/06/07/08/09) builds on this skeleton. Wave 1 plans (02/03/06) can run in parallel **only** if this plan is complete and the workspace resolves.

Output:
- Four empty-shell `@emmy/*` packages that compile (via `bun run typecheck`) and link
- Profile v2 bundle as a byte-for-byte clone of v1 (same schema, fresh hash to be recomputed by Plan 07)
- AGENTS.md and mcp_servers.yaml templates in `docs/`
- `pi-emmy` shim binary on PATH after `bun link`
- `bun.lockb` COMMITTED to git (reproducibility discipline — Pitfall #8)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/projects/emmy/.planning/PROJECT.md
@/data/projects/emmy/.planning/STATE.md
@/data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md
@/data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-PATTERNS.md
@/data/projects/emmy/CLAUDE.md
@/data/projects/emmy/profiles/qwen3.6-35b-a3b/v1/harness.yaml
@/data/projects/emmy/profiles/qwen3.6-35b-a3b/v1/serving.yaml
@/data/projects/emmy/profiles/qwen3.6-35b-a3b/v1/profile.yaml
@/data/projects/emmy/profiles/qwen3.6-35b-a3b/v1/PROFILE_NOTES.md
@/data/projects/emmy/profiles/qwen3.6-35b-a3b/v1/prompts/system.md

<interfaces>
<!-- Key contracts downstream plans (02/03/04/06/07/08/09) will consume. Executor should use these directly. -->

Workspace manifest shape (write EXACTLY this structure):

```json
{
  "name": "emmy",
  "private": true,
  "version": "0.2.0",
  "workspaces": ["packages/*"],
  "engines": {
    "bun": ">=1.1"
  },
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "lint": "biome check packages/"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.7.3"
  }
}
```

Per-package manifest shape (each of the 4 packages):

```json
{
  "name": "@emmy/provider",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@mariozechner/pi-coding-agent": "0.68.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
```

`@emmy/ux` ONLY also carries:
```json
"bin": { "pi-emmy": "./bin/pi-emmy.ts" }
```

Stub TS export shape (`packages/emmy-*/src/index.ts` Wave 0):
```typescript
// @emmy/<pkg> — Wave 0 shell. Real exports ship in Plans 02/03/04/06.
export const PACKAGE_VERSION = "0.1.0";
```

Telemetry stub signature (MUST be stable; Phase 3 body-implements):
```typescript
// packages/emmy-telemetry/src/index.ts
export interface TelemetryRecord {
  event: string;
  ts: string;
  profile?: { id: string; version: string; hash: string };
  [k: string]: unknown;
}
export function emitEvent(record: TelemetryRecord): void {
  // Wave 0: no-op. Phase 3 replaces with atomic JSONL append.
  void record;
}
```

`pi-emmy` Wave-0 shim (replace in Plan 04):
```typescript
#!/usr/bin/env bun
// Wave 0 shim — Plan 04 replaces with real runtime wiring.
console.log("pi-emmy wave-0 shim — Plan 04 wires createAgentSessionRuntime");
process.exit(0);
```
</interfaces>

<source_excerpts>
<!-- Phase 1 v1 profile files that must be cloned byte-for-byte into v2 -->
Current v1 harness.yaml has TODO(Phase-2) comments on every line — Plan 07 fills these. Wave 0 just clones.

Phase 1 v1 hash (for the v2 sibling discipline check):
profiles/qwen3.6-35b-a3b/v1/profile.yaml already has `hash: sha256:b91e74730c6460be1454c857dd64459eea3754ef5844de15e7a42e691cb21913` baked in (per Phase 1 closeout).
</source_excerpts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Workspace root + four package shells + telemetry signature</name>
  <read_first>
    - /data/projects/emmy/CLAUDE.md (pinned stack, Bun+Node policy)
    - /data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md §D-01..D-04 (package topology, Bun, pi-emmy bin, install path)
    - /data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-PATTERNS.md §Workspace root + §Package: @emmy/telemetry (stub contract)
    - /data/projects/emmy/.gitignore (must not collide with new bun/node entries; MUST NOT add bun.lockb to the exclude list)
    - Current contents of /data/projects/emmy/ (run `ls`: pyproject.toml + emmy_serve/ + profiles/ + scripts/ + tests/ MUST be preserved; this task adds siblings, does not modify any existing file except .gitignore)
  </read_first>
  <action>
Create the Bun workspace root and four empty-shell TypeScript packages per D-01..D-04.

**Step 1 — Workspace root (write as new files):**

`/data/projects/emmy/package.json`:
```json
{
  "name": "emmy",
  "private": true,
  "version": "0.2.0",
  "workspaces": ["packages/*"],
  "engines": { "bun": ">=1.1" },
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "lint": "biome check packages/"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.7.3"
  }
}
```

`/data/projects/emmy/tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true,
    "types": ["bun-types"],
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  }
}
```

`/data/projects/emmy/biome.json`:
```json
{
  "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
  "organizeImports": { "enabled": true },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "suspicious": { "noExplicitAny": "warn" }
    }
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  }
}
```

**Step 2 — Update `.gitignore`.** Append (do not remove existing lines):
```
# TS/Node
node_modules/
packages/*/dist/
.turbo/
```

**IMPORTANT:** Do NOT add `bun.lockb` to `.gitignore`. Bun convention AND Emmy's reproducibility discipline (CLAUDE.md Pitfall #8 + Phase 1 REPRO-01..04 air-gap carry-over) require the lockfile to be committed. Match Phase 1's `uv.lock` discipline — that file is committed; `bun.lockb` is its TS-side analog. Future plans (eval, phase 7 public artifact) will fail to reproduce numbers if the lockfile is missing.

**Step 3 — Four package shells.** For each of `emmy-provider`, `emmy-tools`, `emmy-ux`, `emmy-telemetry`, create:

`packages/<name>/package.json`:
```json
{
  "name": "@emmy/<short>",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@mariozechner/pi-coding-agent": "0.68.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
```
Where `<short>` = provider | tools | ux | telemetry.

`packages/<name>/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts"]
}
```

`packages/<name>/src/index.ts`:
```typescript
// @emmy/<short> — Wave 0 shell. Real exports ship in Plans 02/03/04/06.
export const PACKAGE_VERSION = "0.1.0";
```

**Step 4 — `@emmy/ux` binary addition.** In `packages/emmy-ux/package.json` ONLY, add:
```json
"bin": { "pi-emmy": "./bin/pi-emmy.ts" }
```
(merge into the object shown above).

Create `packages/emmy-ux/bin/pi-emmy.ts`:
```typescript
#!/usr/bin/env bun
// Wave 0 shim — Plan 04 replaces this entire file with real runtime wiring.
console.log("pi-emmy wave-0 shim — Plan 04 wires createAgentSessionRuntime");
process.exit(0);
```

Update its tsconfig.json include to also cover `bin/`:
```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts", "bin/**/*.ts"]
}
```

**Step 5 — Telemetry stub (signature-only; CONTEXT.md §D-01 says "empty stub scaffold — Phase 3 fills it").** Replace `packages/emmy-telemetry/src/index.ts` contents with:
```typescript
// @emmy/telemetry — Wave 0 signature stub (Phase 3 implements body).
// Stable signature so @emmy/provider, @emmy/tools, @emmy/ux can `import { emitEvent }`
// today without Phase 3 touching their call sites.

export interface TelemetryRecord {
  event: string;
  ts: string;
  profile?: { id: string; version: string; hash: string };
  [k: string]: unknown;
}

export function emitEvent(record: TelemetryRecord): void {
  // Wave 0: no-op. Phase 3 replaces body with atomic JSONL append
  // mirroring emmy_serve/diagnostics/atomic.py:append_jsonl_atomic.
  void record;
}

export const PACKAGE_VERSION = "0.1.0";
```

**Step 6 — Install + verify.** Run (in order, fail-loud):
```bash
cd /data/projects/emmy && bun install
cd /data/projects/emmy && bun run typecheck
cd /data/projects/emmy/packages/emmy-ux && bun link
cd /data/projects/emmy && which pi-emmy
cd /data/projects/emmy && pi-emmy
```

All six commands MUST exit 0. The last command MUST print `pi-emmy wave-0 shim — Plan 04 wires createAgentSessionRuntime`.

After successful `bun install`, `bun.lockb` MUST exist at the repo root. Stage it for commit.

If `bun install` fails due to network (air-gap concern), fall back to `bun install --no-save` with `pi-coding-agent` pre-cached; document in Notes section of SUMMARY.md. If `bun link` fails with "command not found" after success, check that `~/.bun/bin` is on PATH (document in SUMMARY.md).

**Do NOT modify** any existing files under `emmy_serve/`, `scripts/`, `tests/`, `profiles/qwen3.6-35b-a3b/v1/`, `pyproject.toml`, `uv.lock`. Phase 1 artifacts stay byte-identical.
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bun install &amp;&amp; bun run typecheck &amp;&amp; test -f bun.lockb &amp;&amp; test -x packages/emmy-ux/bin/pi-emmy.ts &amp;&amp; bun packages/emmy-ux/bin/pi-emmy.ts | grep -q 'wave-0 shim'</automated>
  </verify>
  <acceptance_criteria>
    - `/data/projects/emmy/package.json` exists and `grep -c '"workspaces"' package.json` returns 1
    - `grep '"workspaces"' /data/projects/emmy/package.json` matches `["packages/*"]`
    - `grep -c '"@mariozechner/pi-coding-agent": "0.68.0"' packages/emmy-provider/package.json packages/emmy-tools/package.json packages/emmy-ux/package.json packages/emmy-telemetry/package.json` returns 4 total (one per package, EXACT version pin, not `^0.68.0`)
    - `head -1 /data/projects/emmy/packages/emmy-ux/bin/pi-emmy.ts` matches `#!/usr/bin/env bun`
    - `grep -c 'export function emitEvent' packages/emmy-telemetry/src/index.ts` returns 1
    - `grep -c '"bin"' packages/emmy-ux/package.json` returns 1 (only @emmy/ux has bin)
    - `grep -c '"bin"' packages/emmy-provider/package.json packages/emmy-tools/package.json packages/emmy-telemetry/package.json` returns 0 (NO bin in other packages)
    - `cd /data/projects/emmy && bun run typecheck` exits 0
    - `cd /data/projects/emmy && bun packages/emmy-ux/bin/pi-emmy.ts` prints line matching `wave-0 shim` and exits 0
    - **`test -f /data/projects/emmy/bun.lockb` exits 0** (lockfile produced by `bun install`)
    - **`grep -c '^bun\.lockb$' /data/projects/emmy/.gitignore` returns 0** (lockfile is NOT ignored — reproducibility discipline; contrast with Phase 1 `uv.lock` which is committed)
    - **`grep -c 'bun\.lockb' /data/projects/emmy/.gitignore` returns 0** (broader pattern check — no bun.lockb reference in gitignore whatsoever)
    - All Phase 1 files byte-identical: `sha256sum profiles/qwen3.6-35b-a3b/v1/profile.yaml` unchanged from Phase 1 closeout (hash was `sha256:b91e747...` — profile.yaml itself does not contain that; re-run `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` MUST exit 0)
    - `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` exits 0
  </acceptance_criteria>
  <done>
    Workspace root + four package shells exist; `bun install` + `bun run typecheck` exit 0; `pi-emmy` shim runs and prints wave-0 message; `bun.lockb` created and NOT gitignored; Phase 1 v1 profile unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Clone profile v1 → v2 sibling + docs templates (AGENTS.md, mcp_servers.yaml)</name>
  <read_first>
    - /data/projects/emmy/profiles/qwen3.6-35b-a3b/v1/ (every file — the clone must be byte-for-byte except profile.yaml.version and hash line)
    - /data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-CONTEXT.md §D-01, §D-02 (profile bundle schema, content-hash discipline — profiles are immutable, any change = new version dir)
    - /data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-PATTERNS.md §Profile-side modifications (v2 as sibling is recommended path to preserve v1-locked Phase 1 smoke test)
    - /data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md §specifics (`docs/mcp_servers_template.yaml` shipped; AGENTS.md starter in `docs/agents_md_template.md`)
    - /data/projects/emmy/.planning/research/FEATURES.md §Context Management (AGENTS.md layering; pi @file; compaction — informs template headers)
    - /data/projects/emmy/CLAUDE.md (this repo's AGENTS-like file — shape donor for the template; the template is SHORTER)
    - /data/projects/emmy/emmy_serve/profile/schema.py (to confirm v2 is a valid directory naming — schema loader finds `v<N>` dirs)
  </read_first>
  <action>
Create `profiles/qwen3.6-35b-a3b/v2/` as a sibling directory cloned from v1 (to be filled by Plan 07), and ship two docs templates.

**Step 1 — Clone v1 → v2 verbatim.** Copy the entire directory tree preserving file bytes:
```bash
cp -r /data/projects/emmy/profiles/qwen3.6-35b-a3b/v1 /data/projects/emmy/profiles/qwen3.6-35b-a3b/v2
```

**Step 2 — Edit ONLY `profiles/qwen3.6-35b-a3b/v2/profile.yaml`:** change `version: v1` → `version: v2`. Leave the existing `hash:` line placeholder as-is for now (it will carry the Phase 1 v1 hash, which is WRONG for v2; Plan 07 recomputes and writes the real v2 hash). Do NOT recompute here.

Add a bold inline comment line at the TOP of `profiles/qwen3.6-35b-a3b/v2/profile.yaml` (preserve existing YAML structure below):
```yaml
# v2 — cloned from v1 2026-04-21 by Phase 2 Plan 01. Hash below is the v1 hash and is KNOWN-STALE.
# Plan 07 fills harness.yaml TODOs then recomputes this hash via `uv run emmy profile hash --write`.
# Do NOT consume v2 in production until Plan 07 lands — v1 remains the Phase 1 certified profile.
```

**Step 3 — Verify clone integrity.** Run:
```bash
diff -r /data/projects/emmy/profiles/qwen3.6-35b-a3b/v1 /data/projects/emmy/profiles/qwen3.6-35b-a3b/v2
```
The ONLY diffs expected are:
- The three added comment lines at the top of `v2/profile.yaml`
- The `version: v1` → `version: v2` line in `v2/profile.yaml`

Every other file (harness.yaml, serving.yaml, prompts/system.md, PROFILE_NOTES.md, .gitkeep files) MUST be byte-identical across v1 and v2.

Also verify v2 validates against the schema (even with stale hash — validate checks schema, `validate --strict` checks hash; use non-strict here):
```bash
cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/
```
MUST exit 0 on the non-strict schema check. (Hash mismatch is expected and is Plan 07's job to fix.)

**Step 4 — `docs/mcp_servers_template.yaml`** (user-level example; D-16 layering spec):
```yaml
# ~/.emmy/mcp_servers.yaml — USER-level MCP server registry (defaults).
# Project-level `./.emmy/mcp_servers.yaml` overrides user on same server key (D-16).
# All servers launch as stdio subprocesses (D-17). All tool names/descriptions pass
# the Unicode blocklist check (D-18) at registration time.

servers:
  # Filesystem access to the user's home, scoped by passing the root as an arg.
  # From https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem
  filesystem:
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - "${HOME}"
    # env: optional map; values interpolate $VAR from the parent process env.
    # env:
    #   SOME_VAR: "$SOME_VAR"

  # Example (commented): Brave Search MCP.
  # Uncomment and provide BRAVE_API_KEY in your shell env.
  # web-search:
  #   command: npx
  #   args:
  #     - -y
  #     - "@modelcontextprotocol/server-brave-search"
  #   env:
  #     BRAVE_API_KEY: "$BRAVE_API_KEY"

# Dispatch rules (emmy-enforced, not user-configurable):
#   1. Flat tool-name registration (D-15). If an MCP-declared tool name collides
#      with a native emmy tool (read, write, edit, bash, grep, find, ls, web_fetch),
#      registration FAILS LOUD. Remediate by adding `alias: <new_name>` under the
#      offending server entry (not implemented in Phase 2 — Phase 2 rejects loudly).
#   2. Names + descriptions are checked against Unicode blocklist (Cf / Co / Cs +
#      bidi-override U+202A..U+202E and U+2066..U+2069). Any hit → registration
#      throws PoisonError with the offending codepoint in the message.
#   3. Subprocess stdio transport only in Phase 2 (D-17). HTTP/SSE deferred to v1.x.
```

**Step 5 — `docs/agents_md_template.md`** (starter stub; CONTEXT-01; shorter than CLAUDE.md):
```markdown
# AGENTS.md

> Drop this at the repo root. Emmy reads it verbatim into every session prompt
> (layered after the profile's `prompts/system.md`, before tool defs). Hash logging
> makes drift observable. Keep it short — every token lives in your KV cache.

## Build / Test Commands

- Build: `<your build command>` (e.g. `bun run build`, `cargo build`, `pytest`)
- Test: `<your test command>`
- Lint: `<your lint command>`
- Single-test run: `<pattern>` (e.g. `bun test --filter <name>`)

## Key Paths

- Source: `src/`
- Tests: `tests/`
- Config: `<path>`

## House Style Rules

- <style point 1> (e.g. "No `any` in TypeScript; prefer `unknown` + narrow")
- <style point 2> (e.g. "Imports sorted by biome organizeImports on save")
- <style point 3>

## Preferred Patterns

- <pattern 1> (e.g. "Fail-loud on config errors; never silently default")
- <pattern 2>

## Things to Avoid

- <anti-pattern 1>
- <anti-pattern 2>

## Model Hints (optional)

- When planning a change, write a short `PLAN.md` before editing.
- When refactoring, make one logical change per commit.
- Read `tests/` before modifying production code.
```

**Step 6 — Commit gate.** Verify that `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` STILL exits 0 (v1 untouched). Verify that `git status` shows only additions under `packages/`, `profiles/qwen3.6-35b-a3b/v2/`, `docs/`, and the new workspace root files (package.json, tsconfig.base.json, biome.json, bun.lockb); no modifications outside those areas.
  </action>
  <verify>
    <automated>cd /data/projects/emmy &amp;&amp; uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/ &amp;&amp; test -f profiles/qwen3.6-35b-a3b/v2/harness.yaml &amp;&amp; test -f docs/mcp_servers_template.yaml &amp;&amp; test -f docs/agents_md_template.md &amp;&amp; grep -q 'version: v2' profiles/qwen3.6-35b-a3b/v2/profile.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Directory `profiles/qwen3.6-35b-a3b/v2/` exists with files: `profile.yaml`, `serving.yaml`, `harness.yaml`, `PROFILE_NOTES.md`, `prompts/system.md`, `tool_schemas/.gitkeep`, `grammars/.gitkeep` (mirrors v1 layout)
    - `grep -c 'version: v2' profiles/qwen3.6-35b-a3b/v2/profile.yaml` returns 1
    - `grep -c 'version: v1' profiles/qwen3.6-35b-a3b/v2/profile.yaml` returns 0 (no stale v1 reference in v2/profile.yaml)
    - `grep -c 'KNOWN-STALE' profiles/qwen3.6-35b-a3b/v2/profile.yaml` returns 1 (the warning comment is present)
    - `diff profiles/qwen3.6-35b-a3b/v1/harness.yaml profiles/qwen3.6-35b-a3b/v2/harness.yaml` produces ZERO output (byte-identical — Plan 07 fills the TODOs)
    - `diff profiles/qwen3.6-35b-a3b/v1/serving.yaml profiles/qwen3.6-35b-a3b/v2/serving.yaml` produces ZERO output
    - `diff profiles/qwen3.6-35b-a3b/v1/prompts/system.md profiles/qwen3.6-35b-a3b/v2/prompts/system.md` produces ZERO output
    - `diff profiles/qwen3.6-35b-a3b/v1/PROFILE_NOTES.md profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md` produces ZERO output
    - `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` exits 0 (v1 still valid per Phase 1 closeout)
    - `test -f docs/mcp_servers_template.yaml && grep -c 'stdio' docs/mcp_servers_template.yaml` returns ≥1 (D-17 transport note present)
    - `grep -c 'U+202A' docs/mcp_servers_template.yaml` returns ≥1 (bidi-override range documented per D-18)
    - `test -f docs/agents_md_template.md && grep -c '## Build' docs/agents_md_template.md` returns 1
    - `grep -c '# AGENTS.md' docs/agents_md_template.md` returns 1
  </acceptance_criteria>
  <done>
    v2 profile is a byte-for-byte clone of v1 (except `version: v2` + warning comment in profile.yaml); v1 validates; docs templates exist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clone-to-host toolchain install | `curl | bash` and `bun install` pull arbitrary code from the internet; this plan assumes the developer is responsible for pre-verifying (user_setup). Not an emmy-runtime threat surface. |
| git working tree ↔ filesystem | Plan 01 writes to `packages/`, `profiles/v2/`, `docs/`. Must not touch Phase 1 v1 bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `profiles/qwen3.6-35b-a3b/v1/` Phase 1 certified bundle | mitigate | Plan performs no writes under `v1/`. Task 2 acceptance criteria explicitly verify `diff v1 v2` shows ZERO diffs on harness.yaml, serving.yaml, prompts/system.md, PROFILE_NOTES.md. `uv run emmy profile validate v1/` gate at end of task. |
| T-02-01-02 | Spoofing | v2 profile carries stale v1 hash until Plan 07 | mitigate | `KNOWN-STALE` comment at top of v2/profile.yaml; Plan 07 acceptance criteria recompute + write the correct v2 hash before phase close. Do not run `pi-emmy --profile v2` in anger until Plan 07 lands (harness never finalizes v2 as active until the hash is computed). |
| T-02-01-03 | Elevation of Privilege | `pi-emmy` added to `$PATH` via `bun link` | accept | Developer-machine-only; `pi-emmy` Wave 0 shim does nothing but print a string. Runtime behavior (including the YOLO bash tool) is Plan 03+06's concern. |
| T-02-01-04 | Denial of Service | `bun install` network-fetching `pi-coding-agent@0.68.0` | mitigate | Exact version pin (`"0.68.0"`, not `^0.68.0` / `~0.68.0`) — no semver drift. Lockfile `bun.lockb` COMMITTED per Pitfall #8 reproducibility discipline; downstream rebuilds resolve to the same tree. If the pi-mono registry is unreachable (air-gap), operator pre-caches per user_setup. |
| T-02-01-05 | Information Disclosure | `docs/mcp_servers_template.yaml` example references `$BRAVE_API_KEY` | accept | Template is commented-out. Documentation explicitly says values interpolate from parent env — no secret committed. Actual `~/.emmy/mcp_servers.yaml` is per-user, out of repo. |
</threat_model>

<verification>
After both tasks complete:

1. `cd /data/projects/emmy && bun install && bun run typecheck` exits 0
2. `cd /data/projects/emmy && bun packages/emmy-ux/bin/pi-emmy.ts` prints `pi-emmy wave-0 shim...` and exits 0
3. `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` exits 0 (Phase 1 v1 unchanged)
4. `diff -r profiles/qwen3.6-35b-a3b/v1 profiles/qwen3.6-35b-a3b/v2 | wc -l` returns ≤ 10 lines (the version + warning comment in profile.yaml only)
5. `cd /data/projects/emmy && uv run pytest tests/unit -q` exits 0 (nothing Phase-1-Python regressed)
6. Workspace structure:
   - `test -d packages/emmy-provider/src && test -d packages/emmy-tools/src && test -d packages/emmy-ux/src && test -d packages/emmy-telemetry/src` all exit 0
   - `test -x packages/emmy-ux/bin/pi-emmy.ts` exits 0
7. All four packages export `PACKAGE_VERSION`:
   - `grep -l 'PACKAGE_VERSION' packages/*/src/index.ts | wc -l` returns 4
8. Lockfile reproducibility: `test -f bun.lockb` exits 0 AND `grep -c 'bun\.lockb' .gitignore` returns 0
</verification>

<success_criteria>
Plan 01 complete when:
- [ ] Bun workspace root exists and resolves (`bun install` exits 0)
- [ ] Four `@emmy/*` packages exist as typed empty shells (`bun run typecheck` exits 0)
- [ ] `pi-coding-agent@0.68.0` is pinned EXACTLY (not ^0.68.0) in all four packages
- [ ] `pi-emmy` shim binary is on PATH after `bun link` and prints wave-0 message
- [ ] Profile v2 bundle exists as byte-for-byte clone of v1 except version line and KNOWN-STALE warning comment
- [ ] Phase 1 v1 validates unchanged
- [ ] `docs/mcp_servers_template.yaml` + `docs/agents_md_template.md` exist with required sections
- [ ] **`bun.lockb` exists at repo root AND is NOT in `.gitignore` (reproducibility discipline; Pitfall #8 + Phase 1 REPRO carry-over)**
- [ ] Zero modifications outside `packages/`, `profiles/qwen3.6-35b-a3b/v2/`, `docs/`, workspace root configs, `bun.lockb`, and `.gitignore`
- [ ] Phase 1 unit tests still green (`uv run pytest tests/unit -q` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-01-SUMMARY.md` covering:

- Workspace layout that was created (confirm paths of all four packages)
- Exact pi-coding-agent version pin used in each package.json
- v2 profile bundle path + confirmation that all files are byte-identical to v1 except profile.yaml
- Any install-time issues (e.g., network, lockfile, bun link PATH) and resolutions
- Confirmation that `bun.lockb` is committed (not gitignored)
- Confirmation that `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` still exits 0
- List of files touched (explicit paths, no globs)
- Next: Plans 02/03/06 run in parallel in Wave 1 against this skeleton; Plan 04 follows in Wave 2.
</output>
</output>
