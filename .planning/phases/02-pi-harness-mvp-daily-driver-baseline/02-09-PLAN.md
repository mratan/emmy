---
phase: 02-pi-harness-mvp-daily-driver-baseline
plan: 09
type: execute
wave: 5
depends_on: [02-08]
files_modified:
  - runs/phase2-sc1/walkthrough.md
  - runs/phase2-sc1/transcript.json
  - .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "SC-1 daily-driver walkthrough resume-signal received (`sc1 green` or `sc1 yellow: <reason>` or `sc1 red: <reason>`)"
    - "All 23 Phase 2 REQ-IDs flipped Pending -> Done in REQUIREMENTS.md traceability"
    - "ROADMAP.md Phase 2 checkbox + all 8 plan checkboxes (02-01..02-04, 02-06..02-09) marked [x]"
    - "STATE.md completed_phases advances from 1 to 2; focus pointer moves to Phase 3"
    - "02-CLOSEOUT.md exists with SC disposition table including SC-1 verdict from runs/phase2-sc1/walkthrough.md"
    - "CLOSEOUT.md carries a Phase-1-schema-patch addendum IFF Plan 07 Step 0 landed that commit (naming the commit SHA)"
    - "v1 AND v2 profiles both validate (v1 untouched across Phase 2; v2 carries Plan 07 hash recompute + Plan 08 PROFILE_NOTES update)"
  artifacts:
    - path: "runs/phase2-sc1/walkthrough.md"
      provides: "Author's daily-driver verdict (green/yellow/red + reasoning); open follow-up tickets if yellow"
    - path: ".planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md"
      provides: "Phase 2 close-out with SC disposition + requirements closed + carry-forward + Phase-1-schema-patch addendum"
      contains: "Success-criterion disposition"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 23 Phase 2 REQ-IDs flipped to Done in the traceability table"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 marked [x] + all 8 plan boxes [x]; plan list mentions 02-06/07/08/09 as the revision-era additions"
    - path: ".planning/STATE.md"
      provides: "completed_phases: 2; focus: Phase 3"
  key_links:
    - from: "runs/phase2-sc1/walkthrough.md"
      to: "02-CLOSEOUT.md"
      via: "Task 2 reads the verdict and records it as the SC-1 row in the disposition table"
      pattern: "sc1 (green|yellow|red)"
    - from: "02-CLOSEOUT.md Phase-1-schema-patch addendum"
      to: "git log (commit `feat(phase-01-schema-patch): ...`)"
      via: "addendum cites the commit SHA from Plan 07 Step 0 so the schema change is discoverable"
      pattern: "phase-01-schema-patch"
---

<objective>
Close Phase 2 via the SC-1 human-verify walkthrough checkpoint and the administrative updates that mark the phase done: CLOSEOUT.md, REQUIREMENTS.md traceability flip, ROADMAP.md phase + plan checkboxes, STATE.md pointer advance.

This plan is **NOT autonomous** — SC-1 requires the author to run pi-emmy against a clean repo, make a multi-file change, and provide a verdict. The CLOSEOUT and doc updates are automatable but gated behind the SC-1 resume signal.

Purpose: SC-1 is the daily-driver quality bar — the whole point of Phase 2. Without the author's verdict, the phase does not close. Plans 01-08 built the machinery + captured the other four SCs' evidence; this plan operates the daily-driver walkthrough and records the outcome.

Output:
- SC-1 walkthrough transcript + verdict document
- 02-CLOSEOUT.md with full SC disposition table (SC-1..SC-5), requirements-closed list, carry-forward, Phase-1-schema-patch addendum (if applicable)
- REQUIREMENTS.md traceability updated (23 REQ-IDs flipped)
- ROADMAP.md Phase 2 section completed (8 plan boxes + the phase-level box)
- STATE.md advanced to Phase 3

This is the final plan in Phase 2. Wave 5; depends on Plan 08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-CLOSEOUT.md
@/data/projects/emmy/.planning/STATE.md
@/data/projects/emmy/.planning/ROADMAP.md
@/data/projects/emmy/.planning/REQUIREMENTS.md
@/data/projects/emmy/.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: SC-1 daily-driver walkthrough — author runs pi-emmy against a clean repo, makes a multi-file change, and confirms the daily-driver bar</name>
  <what-built>
    Plans 01-04 built the harness; Plans 06-08 shipped tools/MCP/verification. This checkpoint is SC-1 verbatim from the ROADMAP:
    "The author can run `pi-emmy` against a clean repo, ask it to make a multi-file change, and the agent completes it using read/write/edit/bash/grep/find/ls + web_fetch - without any cloud call and without leaving the TUI."
  </what-built>
  <how-to-verify>
    1. Confirm emmy-serve is up: `curl -s http://127.0.0.1:8002/v1/models | jq '.data[0].id'` returns `"qwen3.6-35b-a3b"`.
    2. Create a throwaway repo for the walkthrough:
       ```
       cd /tmp && rm -rf emmy-sc1-walkthrough && mkdir emmy-sc1-walkthrough && cd emmy-sc1-walkthrough
       git init && echo "# SC-1 Walkthrough" > README.md && cp /data/projects/emmy/docs/agents_md_template.md AGENTS.md
       # Edit AGENTS.md to describe: "This repo will have src/{foo,bar,baz}.ts; add a function in each."
       git add -A && git commit -m "init"
       ```
    3. Run pi-emmy in TUI mode OR use --print:
       ```
       pi-emmy --print "Add a function log(msg: string) to src/foo.ts, src/bar.ts, and src/baz.ts. Each should print '[<filename>] ' prefix. Create the files if they don't exist. Then run 'bun test' and verify all three files exist."
       ```
    4. Observe:
       - Session startup logs: `pi-emmy SP_OK canary: OK` AND `pi-emmy session ready (prompt.sha256=<hex>, layers=system.md,AGENTS.md,tool_defs,user)` AND `pi-emmy transcript=runs/phase2-sc3-capture/session-<iso>.jsonl`
       - Agent uses write tool to create 3 files
       - Agent uses bash tool to run `bun test`
       - NO network traffic observed (`ss -tnp | grep -v 127.0.0.1 | grep ESTAB` returns nothing during the session)
       - Session completes without the user leaving pi-emmy (no external terminal, no cloud call)
    5. Verify artifacts: `ls /tmp/emmy-sc1-walkthrough/src/` shows foo.ts, bar.ts, baz.ts.
    6. Copy the session transcript to `runs/phase2-sc1/transcript.json` (or transcript.jsonl) for evidence.
    7. Write a 3-bullet "daily-driver verdict" in `runs/phase2-sc1/walkthrough.md`:
       - Did it feel like Claude Code for daily-driver use? (yes / yes-with-caveats / no + one-line reason)
       - Any glaring UX rough edges? (they become Phase 3 tickets, not blockers)
       - Any bugs that warrant a patch commit before Phase 2 close? (fix if small, open follow-up if large)
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `"sc1 green"` - SC-1 passes the daily-driver bar. Phase 2 can close.
    - `"sc1 yellow: <reason>"` - SC-1 passes with UX rough edges (documented as Phase 3 follow-ups). Phase 2 can close; open GH issues for the edges.
    - `"sc1 red: <reason>"` - daily-driver bar not met. Phase 2 cannot close. Open a gap-closure plan via `/gsd-plan-phase 2 --gaps` after documenting the failure in runs/phase2-sc1/walkthrough.md.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CLOSEOUT.md + REQUIREMENTS.md traceability + ROADMAP.md + STATE.md (incl. Phase-1-schema-patch addendum if applicable)</name>
  <read_first>
    - /data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-CLOSEOUT.md (shape donor)
    - /data/projects/emmy/.planning/STATE.md (update current-focus + completed_phases)
    - /data/projects/emmy/.planning/ROADMAP.md §Phase 2 (mark checkboxes for all 8 plans)
    - /data/projects/emmy/.planning/REQUIREMENTS.md §Traceability (flip Phase 2 REQ-IDs Pending -> Done)
    - All per-plan SUMMARY.md files from 02-01 through 02-08 (consume for closeout narrative)
    - runs/phase2-sc{2,3,4,5}/report.json (Plan 08 evidence)
    - runs/phase2-sc1/walkthrough.md (Task 1 output)
    - git log for `feat(phase-01-schema-patch):` (if Plan 07 Step 0 landed that commit — cite SHA in addendum)
  </read_first>
  <action>
**Step 1 — Verify all 23 REQ-IDs covered across the 8 plans' `requirements:` fields.** The 23 Phase 2 requirements are:

HARNESS-01, HARNESS-02, HARNESS-03, HARNESS-04, HARNESS-06, HARNESS-07, HARNESS-10, TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04, TOOLS-05, TOOLS-06, TOOLS-07, TOOLS-08, TOOLS-09, CONTEXT-01, CONTEXT-03, CONTEXT-04, CONTEXT-05, SERVE-05, UX-01, UX-05.

Expected coverage distribution after the revision:
- 02-01: HARNESS-01, CONTEXT-01
- 02-02: HARNESS-01, HARNESS-02, HARNESS-03, SERVE-05
- 02-03: TOOLS-01, TOOLS-03, TOOLS-08
- 02-04: HARNESS-04, HARNESS-06, HARNESS-07, CONTEXT-01, CONTEXT-03, CONTEXT-04, CONTEXT-05, UX-01, UX-05
- 02-06: HARNESS-10, TOOLS-02, TOOLS-04, TOOLS-05, TOOLS-06, TOOLS-07, TOOLS-09, CONTEXT-03
- 02-07: CONTEXT-04, CONTEXT-05, HARNESS-06, HARNESS-07, SERVE-05
- 02-08: HARNESS-04, HARNESS-07, SERVE-05, TOOLS-03, TOOLS-07
- 02-09: (administrative; no new REQ coverage)

Run:
```bash
grep -h '^  - HARNESS\|^  - TOOLS\|^  - CONTEXT\|^  - SERVE\|^  - UX' .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-*-PLAN.md | sort -u
```

Verify the output contains each of the 23 IDs. If any is missing, EDIT the relevant plan's `requirements:` field to include it, then re-commit. This is a covered-by-existing-work correction, not a scope expansion.

**Step 2 — Update REQUIREMENTS.md traceability.** For each of the 23 Phase 2 REQ-IDs, flip the status cell `Pending` -> `Done` in the traceability table.

**Step 3 — Update ROADMAP.md §Phase 2.** Change plan count to 8 and mark each plan done:

Replace the existing `Plans: 5 plans` line with `Plans: 8 plans (structural revision 2026-04-21: split 02-03->03/06 and 02-05->07/08/09)` and the plan list with:
```
Plans:
- [x] 02-01-PLAN.md — Wave 0 scaffold: Bun workspace + 4 @emmy/* packages + profile v2 sibling + docs templates
- [x] 02-02-PLAN.md — @emmy/provider: vLLM HTTP + OpenAI-compat strip + reactive grammar retry (nested grammar shape)
- [x] 02-03-PLAN.md — @emmy/tools (part 1): hash-anchored edit primitives + atomic write + post-hoc diff
- [x] 02-04-PLAN.md — @emmy/ux: pi-emmy CLI + session wiring + SP_OK gate + profile-validate pre-flight + transcript capture
- [x] 02-05-PLAN.md — SUPERSEDED by 02-07/08/09 (structural revision 2026-04-21)
- [x] 02-06-PLAN.md — @emmy/tools (part 2): native tools + MCP bridge + Unicode poison check
- [x] 02-07-PLAN.md — Profile v2 fill: nested tools.grammar + recompute hash + un-skip regression
- [x] 02-08-PLAN.md — Verification runners: SC-2/SC-3 (3 runs)/SC-4/SC-5 evidence
- [x] 02-09-PLAN.md — SC-1 daily-driver walkthrough + CLOSEOUT + traceability
```

Also mark the `[ ] Phase 2:` checkbox in the Phases list as `[x]`.

**Step 4 — Update STATE.md.**
- `milestone_name`: bump (e.g., `"phase-2-daily-driver"`)
- `completed_phases: 2`
- Current focus: Phase 3
- Session Continuity section: note Phase 2 closed on `<date>` with Plans 02-01..02-09 + SC-1 verdict from runs/phase2-sc1/walkthrough.md.

**Step 5 — Write `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md`.** Mirror the shape of 01-CLOSEOUT.md:

```markdown
---
phase: 02-pi-harness-mvp-daily-driver-baseline
closeout_date: 2026-04-XX
status: closed  # or closed-with-documented-deferrals if SC-1 yellow
score: 5/5  # or 4/5 if SC-1 yellow
predecessor_report: (none — phase close, not gap-closure)
---

# Phase 2 Close-Out

**Phase Goal:** (verbatim from ROADMAP.md §Phase 2)

## Current objective reality (verified on-machine, <date>)

- `pi-emmy` on PATH via `bun link`; TUI + --print + --json modes working
- profile v2 hash: sha256:<64-hex> (recomputed in Plan 07; re-recomputed in Plan 08 after PROFILE_NOTES validation_runs update)
- `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0
- Unit suite (TS + Python): green
- SC-1 walkthrough verdict: <sc1 green | sc1 yellow: ... | sc1 red: ...> — see runs/phase2-sc1/walkthrough.md
- SC-2 verdict: pass — runs/phase2-sc2/report.json
- SC-3 verdict: pass — runs/phase2-sc3/report.json (reactive); runs/phase2-sc3/baseline.json (disabled D-14); runs/phase2-sc3/no_per_tool_sampling.json (W3 Pitfall #5)
- SC-4 verdict: pass — runs/phase2-sc4/report.json
- SC-5 verdict: pass — runs/phase2-sc5/report.json

## Success-criterion disposition

| SC | Status | Evidence |
|---|---|---|
| SC-1 daily-driver walkthrough | (from Task 1 verdict) | runs/phase2-sc1/walkthrough.md |
| SC-2 hash-anchored edit regression | pass | runs/phase2-sc2/report.json |
| SC-3 XGrammar parse-rate graduated SLA | pass | runs/phase2-sc3/report.json + baseline.json + no_per_tool_sampling.json |
| SC-4 MCP dispatch + Unicode poison rejection | pass | runs/phase2-sc4/report.json |
| SC-5 prompt-hash + AGENTS.md + honest max_model_len | pass | runs/phase2-sc5/report.json |

## Plans landed (8 plans after 2026-04-21 revision)

| Plan | Title | Key deliverable |
|---|---|---|
| 02-01 | Workspace scaffold | Bun workspaces + 4 @emmy/* packages + profile v2 sibling + docs templates + committed bun.lockb |
| 02-02 | @emmy/provider | pi provider + reactive grammar retry (nested shape) + telemetry events |
| 02-03 | @emmy/tools (part 1) | hash primitives + read-with-hashes + edit-hashline + atomic write + diff |
| 02-04 | @emmy/ux | pi-emmy CLI (real pi 0.68.0 runtime) + SP_OK gate + profile-validate pre-flight + 3-layer prompt + session transcript |
| 02-06 | @emmy/tools (part 2) | 8 native tools + MCP bridge + Unicode poison |
| 02-07 | Profile v2 fill + hash lock | nested tools.grammar + max_model_len + schemas + grammar + prompts + un-skipped regression |
| 02-08 | SC-2/3/4/5 evidence | 100-call corpus (3 runs) + MCP dispatch + 4 poison categories + prompt-hash regression |
| 02-09 | SC-1 walkthrough + CLOSEOUT | daily-driver verdict + traceability flip + ROADMAP/STATE update |

## SC-3 three-run comparison (Pitfall #5 discipline)

Per W3 fix + CLAUDE.md Pitfall #5: any change to per-tool sampling, prompt, or retry policy must be measured on the FULL SC-3 corpus, not a subset. Three runs on the identical 100-call corpus:

| Variant | Synthetic | Real-replay | Aggregate |
|---------|-----------|-------------|-----------|
| reactive + per_tool_sampling (production) | (from report.json) | ... | ... |
| disabled + per_tool_sampling (D-14 no-grammar baseline) | (from baseline.json) | ... | ... |
| reactive + NO per_tool_sampling (W3 counterfactual) | (from no_per_tool_sampling.json) | ... | ... |

**Narrative:** (fill with actual deltas after Plan 08 runs. If per_tool_sampling regressed parse rate by more than 1pp in the counterfactual, document the decision: revert to no per_tool_sampling in v2/harness.yaml OR escalate to Phase 5 eval for deeper study. If per_tool_sampling is neutral or positive, leave v2 as-is.)

## Requirements closed

All 23 Phase 2 REQ-IDs: HARNESS-01/02/03/04/06/07/10, TOOLS-01..09, CONTEXT-01/03/04/05, SERVE-05, UX-01/05.

## Phase-1-schema patch addendum (IF Plan 07 Step 0 landed it)

During Phase 2 planning (specifically the checker revision on 2026-04-21), a discrepancy surfaced between the CONTEXT D-11 nested `tools.grammar.{path, mode}` shape and the Phase-1 pydantic `HarnessConfig.tools.grammar: str | None` model. To preserve CONTEXT compliance without handwaving, Plan 07 Step 0 shipped a dated schema patch:

- Commit: `feat(phase-01-schema-patch): allow nested tools.grammar.{path,mode}; resolves Phase-2 D-11 discovery`
- SHA: (fill from git log after Plan 07 completes)
- Scope: added nested `GrammarConfig` pydantic model under `ToolsConfig.grammar`; removed the bare-string alternative. Phase-1 unit tests remained green post-patch (verified via `uv run pytest tests/unit -q`).
- Why here and not in Phase 1 gap-closure: the Phase 1 schema was correct FOR PHASE 1 (no grammar was shipped yet); Phase 2 is the FIRST consumer, so the discrepancy was latent until Phase 2 planning. Schema-evolution in the phase that first consumes the new shape is cleaner than re-opening Phase 1.

(If Plan 07 determined the patch was NOT needed — e.g., the Phase-1 schema already accepted a union shape — this section reads: "No Phase-1 schema patch was required; the existing `HarnessConfig` already accepted the nested shape.")

## Carry-forward / deferrals

- UX-02 (GPU/KV/spec-accept TUI footer) → Phase 3
- UX-03 (Offline-OK badge) → Phase 3
- HARNESS-05 + CONTEXT-02 (auto-compaction) → Phase 3
- HARNESS-09 (OTel + Langfuse) → Phase 3
- PROFILE-07/08 (Gemma 4 profile + /profile swap) → Phase 4
- EVAL-01..09 → Phase 5
- Plan 02-05 superseded by 02-07/08/09 (file left in place with `superseded_by` frontmatter for tooling compatibility)
- Any "sc1 yellow" UX rough edges (list from runs/phase2-sc1/walkthrough.md)

## Pitfall posture update

| Pitfall | Phase 2 status |
|---|---|
| #3 Grammar fights model | Mitigated — reactive-only path; SC-3 passes; D-14 no-grammar baseline captured. |
| #5 "More prompting" / "more sampling" trap | Mitigated — SC-3 three-run discipline (reactive / disabled / no_per_tool_sampling) is the measurement gate for any future tune. |
| #6 SP delivery silently broken | Mitigated — SP_OK canary fires on every session start via @emmy/ux; pi-emmy also shells `uv run emmy profile validate` as pre-flight. |
| #1 KV theory vs practice | Reinforced — CONTEXT-05 honest max_model_len regression in place; serving.yaml carries verified value. |
| #8 Hidden cloud deps | Reinforced — web_fetch tagged network-required; Phase 3 offline-OK badge closes the loop; bun.lockb COMMITTED (reproducibility). |

## Next action

`/gsd-plan-phase 3` — Observability + Agent-Loop Hardening + Lived-Experience.
```

**Step 6 — Commit.** Two commits:
1. `docs(phase-02): closeout + REQUIREMENTS traceability + ROADMAP + STATE` — documentation-only
2. (Optional tag) `git tag phase-2-daily-driver-baseline` for later reproducibility; cite in CLOSEOUT.md "Tag" section.

**Step 7 — Final verification.** Run the four-way regression:
- `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` exits 0
- `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0
- `cd /data/projects/emmy && bun test` exits 0
- `cd /data/projects/emmy && uv run pytest tests/unit -q` exits 0
  </action>
  <verify>
    <automated>cd /data/projects/emmy &amp;&amp; test -f .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md &amp;&amp; grep -c '## Success-criterion disposition' .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md &amp;&amp; uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/ &amp;&amp; uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/ &amp;&amp; bun test &amp;&amp; uv run pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md` exits 0
    - `grep -c '## Success-criterion disposition' .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md` returns 1
    - `grep -cE 'SC-[1-5]' .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md` returns at least 10 (each SC referenced multiple times)
    - `grep -c 'SC-3 three-run comparison' .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md` returns 1 (W3/Pitfall #5 narrative paragraph present)
    - `grep -cE 'Phase-1-schema patch' .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md` returns at least 1 (addendum section present; either with a commit SHA cite OR the explicit "no patch required" phrasing)
    - All 23 REQ-IDs flipped to Done: executor verifies each row in REQUIREMENTS.md traceability table for Phase 2 has status Done (manual pattern check is sufficient; grep approximation: `grep -cE '^\|.*Done' .planning/REQUIREMENTS.md` returns a value that includes the 23 new Done entries)
    - `grep -c '\[x\] \*\*Phase 2' .planning/ROADMAP.md` returns 1 (phase checkbox)
    - `grep -cE '\[x\] 02-0[1-9]-PLAN\.md' .planning/ROADMAP.md` returns 8 (eight plan checkboxes; 02-05 is also marked [x] with SUPERSEDED note)
    - `grep -c 'structural revision 2026-04-21' .planning/ROADMAP.md` returns 1 (revision note in the Phase 2 plan count line)
    - `grep -c 'completed_phases: 2' .planning/STATE.md` returns 1
    - `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/` exits 0
    - `cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0
    - `cd /data/projects/emmy && bun test` exits 0
    - `cd /data/projects/emmy && uv run pytest tests/unit -q` exits 0
    - `test -f runs/phase2-sc1/walkthrough.md` exits 0 AND `grep -cE '^sc1 (green|yellow|red)' runs/phase2-sc1/walkthrough.md` returns at least 1 (verdict recorded)
    - `git log --oneline | head -5 | grep -c 'docs(phase-02)'` returns at least 1 (closeout commit present)
  </acceptance_criteria>
  <done>
    SC-1 verdict recorded in runs/phase2-sc1/walkthrough.md; CLOSEOUT.md written with SC disposition + 8-plan landing + SC-3 three-run comparison + Phase-1-schema-patch addendum; REQUIREMENTS traceability updated (23 REQ-IDs Done); ROADMAP.md marks Phase 2 complete with all 8 plan boxes + revision note; STATE.md pointer advances to Phase 3; all four regression suites still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SC-1 operator channel | The author's "sc1 green|yellow|red" input is trusted as the daily-driver verdict. Phase 2's bar is author's judgment, not a machine-checkable criterion. |
| CLOSEOUT attestation | 02-CLOSEOUT.md is an assertion that Phase 2 is complete. No signing in Phase 2; Phase 7 REPRO-02 adds reproducer verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Repudiation | Author's "sc1 green" verdict lacks audit trail | accept | Author is the ONLY user; verdict is self-report. runs/phase2-sc1/walkthrough.md captures the verdict + reasoning. Phase 5 multi-sample eval + external judge partially addresses the repudiation gap for benchmarks (not for daily-driver verdicts). |
| T-02-09-02 | Tampering | CLOSEOUT.md edited AFTER phase close to fake pass | accept | Git history is the audit trail. Commit SHAs referenced by CLOSEOUT (Phase-1-schema-patch, per-plan SUMMARY dates) make post-hoc edits auditable. |
| T-02-09-03 | Information Disclosure | SC-1 walkthrough transcript contains author's in-progress code | accept | runs/phase2-sc1/ is local-only; author may or may not commit it. Same discipline as runs/phase2-sc3-capture/ session logs. |
</threat_model>

<verification>
Plan 09 complete when:

1. runs/phase2-sc1/walkthrough.md exists with the author's SC-1 verdict
2. 02-CLOSEOUT.md exists with the full SC disposition table including SC-1
3. All 23 Phase 2 REQ-IDs flipped to Done in REQUIREMENTS.md
4. ROADMAP.md Phase 2 checkbox + 8 plan checkboxes marked [x]; revision note present
5. STATE.md completed_phases: 2
6. v1 AND v2 profiles both validate; bun test + uv run pytest green
7. CLOSEOUT Phase-1-schema-patch addendum present (either citing commit SHA or explicitly documenting "not needed")
</verification>

<success_criteria>
Plan 09 complete when:
- [ ] SC-1 resume-signal received (Task 1)
- [ ] runs/phase2-sc1/walkthrough.md carries the verdict + reasoning
- [ ] 02-CLOSEOUT.md exists with SC-1..SC-5 disposition + 8-plan landing list + SC-3 three-run comparison + Phase-1-schema-patch addendum
- [ ] REQUIREMENTS.md traceability: 23 REQ-IDs Done
- [ ] ROADMAP.md Phase 2 checkbox + 8 plan boxes [x]; revision note mentions split
- [ ] STATE.md advanced to Phase 3
- [ ] All regression suites green (v1 validate, v2 validate, bun test, uv run pytest)
</success_criteria>

<output>
Create `.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-09-SUMMARY.md`:

- SC-1 verdict (green/yellow/red) + one-line reason
- Any Phase 3 follow-up tickets opened (only if yellow)
- Commit SHAs: the closeout commit + the Phase-1-schema-patch commit (if applicable)
- Confirmation that the four-way regression passes
- Next: Phase 2 is closed. `/gsd-plan-phase 3` for Phase 3 observability + agent-loop hardening.
</output>
