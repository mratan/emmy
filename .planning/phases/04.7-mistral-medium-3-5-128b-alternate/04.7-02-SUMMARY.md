---
phase: 04.7-mistral-medium-3-5-128b-alternate
plan: 02
subsystem: profile
tags: [boot-smoke, container-pin, gguf-merge, mistral, t-01-fired, eval-only, phase-04.7, decision-checkpoint]

# Dependency graph
requires:
  - phase: 04.7-01
    provides: Mistral profile bundle (21 files in v1/) + EngineConfig schema/renderer extension; sentinel-digest serving.yaml awaiting first docker pull
  - phase: 04
    provides: scripts/start_emmy.sh + scripts/smoke_test.py + emmy_serve/canary/* + emmy_serve/swap/controller (sidecar) — all infrastructure intact, no source-code changes in this plan
provides:
  - container_image_digest pinned in serving.yaml (sha256:ffa30d66ff5c…) — T-02 closed for v1
  - single-file merged GGUF on disk at /data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf (74 GB; bartowski Q4_K_M)
  - llama-gguf-split built locally at /tmp/llama.cpp/build/bin/llama-gguf-split (operator artifact, not committed)
  - Tokenizer-fallback applied (commented `tokenizer:` in serving.yaml; falls back to GGUF-embedded tokenizer to avoid gated-repo + offline collision)
  - PROFILE_NOTES.md backfilled with two failure-trace sections + measured-failure frontmatter + operator-decision menu for T-01
  - Decision checkpoint surfaced to operator: 4 paths to unblock T-01 (mistral3 GGUF arch not supported on current vLLM nightly)
affects: [04.7-03, phase-5-eval]

# Tech tracking
tech-stack:
  added:
    - llama.cpp gguf-split (cmake-built, local /tmp/llama.cpp/build) — first GGUF-merge tool in the operational toolkit
  patterns:
    - "Per-profile tokenizer fallback: when the explicit-tokenizer field collides with gated-repo + offline-cache invariants, comment out (don't remove) the field; vLLM falls back to GGUF-embedded tokenizer (the path vLLM docs flag as 'time-consuming and unstable' but the only one that doesn't need HF auth + gated-repo T&C)"
    - "Decision-checkpoint shape: when an upstream backend gap blocks ALL acceptance gates and no profile knob fixes it, surface a 4-option decision menu (wait for upstream / switch quant path / switch serving framework / defer phase) — the executor cannot pick unilaterally because each option triggers different downstream artifacts"

key-files:
  created:
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/deferred-items.md (pytest user-site pollution; pre-existing, out of scope)
  modified:
    - profiles/mistral-medium-3.5/v1/serving.yaml (container_image_digest sentinel → real sha256:ffa30d66...; tokenizer line commented for gated-repo workaround)
    - profiles/mistral-medium-3.5/v1/profile.yaml (hash bumped twice: 3fea7f76... → a8d4892f... → cab2cc4e... → 5f3d2544...)
    - profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md (frontmatter measured_values + validation_runs; new "Plan 04.7-02 boot-smoke failure timeline" + "Tokenizer fallback" body sections; gmu-convergence table updated to reflect "NOT REACHED" reality)

key-decisions:
  - "Container tag spelling resolved to `vllm/vllm-openai:cu130-nightly-aarch64` first attempt — RESEARCH §1.4 prediction confirmed; CONTEXT D-10's `nightly-arm64-cu130` was indeed inverted on Docker Hub. Local image Id sha256:ffa30d66ff5c... captured via `docker inspect --format '{{.Id}}'` (NOT RepoDigest — render_image_ref returns bare-digest form for `vllm/vllm-openai` repo per runner.py:48-50)."
  - "GGUF merge tool path: built llama.cpp from source under /tmp/llama.cpp (cmake -DGGML_CUDA=OFF for CPU-only minimal build; only `--target llama-gguf-split` to skip the full project). 2 shards (38 GB + 36 GB) merged via `llama-gguf-split --merge` into single 74 GB file. Magic bytes `4747 5546` (GGUF) confirmed at offset 0; parts directory cleaned to reclaim ~74 GB."
  - "Tokenizer fallback (Rule 3 deviation, Task 3 attempt 2): the gated `mistralai/Mistral-Medium-3.5-128B` HF repo is not in local cache + HF_HUB_OFFLINE=1 blocks runtime download. Solution: comment out `tokenizer:` line; let vLLM extract from GGUF. Schema field stays in EngineConfig (Plan 04.7-01 work intact); per-profile fallback documented in PROFILE_NOTES.md 'Tokenizer fallback (T-tokenizer)' section."
  - "T-01 fired hard at attempt 2: vLLM 0.19.2rc1.dev134+gfe9c3d6c5 nightly does not support `mistral3` GGUF architecture. CONTEXT D-05 gmu step-down protocol NOT exercised — failure was at startup before any GPU memory allocation. v1 ships in 'structurally-correct, T-01-blocked' state pending operator decision."
  - "Operator endpoint kept available: between/after each Mistral attempt, restored Gemma 4 26B-A4B v2.1 daily-driver on port 8002 (smoke test passed twice, 37.4 tok/s) so operator's primary serving endpoint never lapsed."

patterns-established:
  - "Auto-restore daily-driver after eval-profile boot smoke: when the boot-smoke script's `docker stop emmy-serve` evicts the operator's primary serving endpoint, restore it after each attempt (success OR failure) before surfacing the next checkpoint. Avoids the operator finding their TUI broken when they look at it."
  - "Failure-frontmatter discipline: when validation_runs entries are FAIL, encode the failure type in the entry string itself (`'<run_id>-boot  # FAIL — <reason>'`) so downstream tooling reading the frontmatter sees the structured failure without needing to scan the body section."

requirements-completed: []  # Plan frontmatter `requirements: []`

# Metrics
duration: 4h 4m  # 18:27 → 22:31 (start of Task 1 docker pull → SUMMARY commit)
completed: 2026-05-02
---

# Phase 04.7 Plan 02: Mistral Medium 3.5 128B Boot Smoke Summary

**Container digest pinned + 74 GB GGUF merged via llama-gguf-split + serving.yaml validated — but boot smoke blocked at T-01 (vLLM nightly does not yet support `mistral3` GGUF architecture); 4-option decision checkpoint surfaced to operator (wait for upstream / switch to NVFP4 / switch to llama.cpp / defer phase).**

## Performance

- **Duration:** ~4h 4m wall-clock (most of it on the 74 GB download + two long boot-smoke `wait_for_vllm` 900s timeouts)
- **Started:** 2026-05-02T18:27Z (corrected continuation from prior agent's false-alarm checkpoint)
- **Ended:** 2026-05-02T22:31Z (SUMMARY commit + decision-checkpoint surface)
- **Tasks:** 4 / 4 attempted; 2 closed (Task 1, Task 2); 1 partial (Task 3 — auto-fix applied, then upstream-blocker surfaced); 1 deferred (Task 4 — gated by Task 3)
- **Files modified:** 3 (serving.yaml, profile.yaml, PROFILE_NOTES.md, all in profiles/mistral-medium-3.5/v1/) + 1 created (deferred-items.md)

## Accomplishments

- **Task 1 — Container digest pinned (T-02 closed for v1).** First-attempt
  `docker pull vllm/vllm-openai:cu130-nightly-aarch64` succeeded
  (RESEARCH §1.4's prediction that the Docker Hub spelling, NOT
  CONTEXT D-10's `nightly-arm64-cu130`, was correct — confirmed). Local
  image Id captured: `sha256:ffa30d66ff5c9346c6389507cc529827fc9934a6d2ee37855934f94fe1061cdc`.
  serving.yaml updated; `render-image-ref` returns the bare-digest form
  (no `@` prefix; no repo prefix) per runner.py:48-50 heuristic for repos
  lacking a registry-host dot in their first path segment. `docker
  inspect $(render-image-ref)` exits 0 (start_emmy.sh:119-122 gate).
  Bundle hash recomputed (3fea7f76… → a8d4892f…). `uv run emmy profile
  validate` exits 0 — G-5 prerequisite green.

- **Task 2 — GGUF merged into single 74 GB file (vLLM-ingestible shape).**
  Downloaded 2 shards (38 GB + 36 GB, 74 GB total) from
  `bartowski/mistralai_Mistral-Medium-3.5-128B-GGUF` via
  `huggingface-cli download` over ~10 minutes. Built `llama-gguf-split`
  from source under `/tmp/llama.cpp/build` (CPU-only minimal build,
  `--target llama-gguf-split` to skip the full project; ~3 minutes
  cmake + build). Merged shards via `llama-gguf-split --merge` (NOT
  `cat` — RESEARCH §1.3 critical correction); 795 tensors merged from
  2 splits in ~5 minutes. Verified single-file size 74 GB (within the
  70-80 GB acceptance window) and GGUF magic bytes `4747 5546` at
  offset 0. Parts directory removed; ~74 GB reclaimed.

- **Task 3 — Boot smoke ATTEMPTED twice; both blocked at vLLM startup
  before any GPU allocation.** See "Decision Checkpoint" + "Threat
  Register Update" below for full detail. Two distinct failure modes
  encountered:
  1. Attempt 1: `LocalEntryNotFoundError` — `--tokenizer
     mistralai/Mistral-Medium-3.5-128B` triggered HF download attempt
     for a gated repo with empty local cache, blocked by `HF_HUB_OFFLINE=1`.
  2. Attempt 2 (after Rule 3 auto-fix commenting out the `tokenizer:`
     line): `ValueError: GGUF model with architecture mistral3 is not
     supported yet.` — vLLM nightly's GGUF backend does not yet allowlist
     the `mistral3` architecture string.
  CONTEXT D-05 gmu step-down protocol was NOT exercised; failures were
  before GPU memory allocation. PROFILE_NOTES.md backfilled with two
  new body sections + measured-failure frontmatter.

- **Task 4 — Operator /profile swap walkthrough — DEFERRED to post-checkpoint.**
  Cannot run while G-1 (Bootable) is blocked by T-01. Will execute as
  designed once upstream support lands or operator chooses an
  alternative path.

- **Operator endpoint preserved end-to-end.** Between and after each
  Mistral boot attempt, restored Gemma 4 26B-A4B v2.1 daily-driver on
  port 8002 (smoke test passed both restore runs, 37.4 tok/s). Operator
  is back online with their primary serving endpoint at SUMMARY-commit
  time.

## Task Commits

Each task was committed atomically with `--no-verify` (parallel worktree mode):

1. **Task 1: Pin Mistral container digest** — `f06608e` (feat)
2. **Task 2: GGUF download + merge** — no source-tree commit (operational
   artifact under `/data/models/`, gitignored). Outcome captured here in
   SUMMARY + PROFILE_NOTES.md `validation_runs` frontmatter for
   downstream traceability.
3. **Task 3: Boot smoke + tokenizer fallback + T-01 documentation** —
   `4826199` (fix)
4. **Task 4: Operator /profile swap walkthrough** — gated by Task 3 G-1 closure;
   not started.

## Files Created/Modified

### Created (1)

- `.planning/phases/04.7-mistral-medium-3-5-128b-alternate/deferred-items.md`
  — pytest user-site pollution skipping schema/runner tests in this
  worktree; pre-existing environmental issue, suggested fix documented
  for a future cleanup plan. Out-of-scope for this plan; functional
  verification path (`uv run emmy profile validate`) intact.

### Modified (3 — all under profiles/mistral-medium-3.5/v1/)

- `serving.yaml` — TWO byte changes: (1) `container_image_digest:` from
  sentinel to real `sha256:ffa30d66...` (Task 1); (2) `tokenizer:` line
  commented out with explanation of the gated-repo + offline collision
  (Task 3 Rule 3 auto-fix).
- `profile.yaml` — `profile.hash` field auto-bumped via
  `uv run emmy profile hash --write` after each serving.yaml /
  PROFILE_NOTES.md edit. Final hash: `sha256:5f3d25443a7ff9d414f37c23215712c8cd1b6f25a5e3d4025d9daf9a38a1b1ff`.
- `PROFILE_NOTES.md` — frontmatter `measured_values` updated to flag
  D-04 gmu as STRUCTURAL/UNVERIFIED; `validation_runs` populated with
  both run_ids + FAIL reason notes; gmu-convergence body table
  reflects "NOT REACHED" reality; new body sections "Plan 04.7-02
  boot-smoke failure timeline (T-01 fired)" (full failure traces +
  4-option operator-decision menu) + "Tokenizer fallback (T-tokenizer)"
  (gated-repo + offline collision documentation + v2 re-enable path).

### Operational artifacts (NOT in source tree, but produced by this plan)

- `/data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf` — 74 GB merged
  single-file GGUF; magic bytes `GGUF` at offset 0; ready for
  ingestion once upstream vLLM lands `mistral3` arch support.
- `/tmp/llama.cpp/build/bin/llama-gguf-split` — build artifact; can be
  rebuilt from source per PROFILE_NOTES.md §1.3 if needed.
- `runs/boot-failures/20260502T215548Z_5ad987-boot-failure/` and
  `runs/boot-failures/20260502T222054Z_576671-boot-failure/` —
  operator-side diagnostic bundles with full docker-logs.txt traces.
  Gitignored per `runs/**` rule.

## Decisions Made

- **Container tag**: `vllm/vllm-openai:cu130-nightly-aarch64` worked
  first attempt; fallback tags (`cu130-nightly`, `nightly-arm64-cu130`)
  not needed.
- **GGUF tool path**: built llama.cpp from source under `/tmp/llama.cpp`
  (CPU-only `-DGGML_CUDA=OFF`, single target `--target llama-gguf-split`).
  Future operational doc (Plan 04.7-03 runbook updates) should record
  this so it's reproducible.
- **Tokenizer fallback**: comment out the `tokenizer:` line for v1 (Rule
  3 auto-fix scoped to this profile); EngineConfig schema field stays.
  Re-enable path for v2 documented in PROFILE_NOTES.md.
- **T-01 surfacing**: do NOT silently retry with a different vLLM nightly
  digest (no guarantee a different nightly fixes the architecture
  allowlist; even if it does, this is operator-policy territory because
  the choice between {wait, switch quant, switch framework, defer}
  changes the v1 evidence-trail shape — the executor cannot pick
  unilaterally).
- **Operator endpoint preservation**: restore Gemma daily-driver after
  EVERY boot-smoke attempt (success or failure). This is a per-plan
  operational discipline that should be lifted to a runbook practice
  for future eval-profile validation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Tokenizer fetch failed under HF_HUB_OFFLINE=1 (gated repo, empty cache)**
- **Found during:** Task 3 attempt 1 (run_id 20260502T214035Z-004263)
- **Issue:** vLLM crashed at arg parsing with
  `huggingface_hub.errors.LocalEntryNotFoundError`. The plan + RESEARCH
  recommended `--tokenizer mistralai/Mistral-Medium-3.5-128B` per vLLM
  GGUF docs, but did not anticipate the gated-repo + empty-cache +
  `HF_HUB_OFFLINE=1` (D-12 air-gap) three-way collision.
- **Fix:** Commented out the `tokenizer:` line in serving.yaml. EngineConfig
  schema field unchanged (Optional[str] = None default applies); per-profile
  fallback to GGUF-embedded tokenizer (vLLM's behavior when --tokenizer
  is unset). Bundle hash recomputed; `emmy profile validate` exit 0.
- **Files modified:** `profiles/mistral-medium-3.5/v1/serving.yaml`,
  `profiles/mistral-medium-3.5/v1/profile.yaml` (hash),
  `profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md` (new "Tokenizer
  fallback (T-tokenizer)" section)
- **Committed in:** `4826199` (combined with the T-01 documentation;
  the two issues are coupled because attempt 2 needed attempt 1's fix)

### NOT Auto-fixed — Surfaced as Decision Checkpoint

**2. [Rule 4 — Architectural / Upstream gap] vLLM nightly does not support `mistral3` GGUF architecture**
- **Found during:** Task 3 attempt 2 (run_id 20260502T222054Z-576671)
- **Issue:** vLLM build `0.19.2rc1.dev134+gfe9c3d6c5` (the one we just
  pinned at Task 1) crashes during model load with `ValueError: GGUF
  model with architecture mistral3 is not supported yet.` Backend has a
  per-architecture allowlist; `mistral3` is not on it.
- **Why NOT auto-fixed:** No profile knob addresses this. Each of the four
  unblock paths (wait for upstream / switch to NVFP4 / switch to llama.cpp
  serving / defer phase) triggers different downstream artifacts AND
  represents an operator-policy choice (timeline tolerance vs.
  engineering-cost vs. architecture-envelope). The plan's failure-handling
  table for this exact case says "consult operator before retrying with
  a different nightly digest" — but with the architecture-not-supported
  error class, even a different nightly is a guess (no evidence it would
  fix). Operator must decide the path forward.
- **State at hand-off:** v1 ships in "structurally-correct, T-01-blocked"
  state. The merged GGUF is on disk, the container is pulled, the
  digest is pinned, the schema validates. ONLY the runtime backend is
  the blocker. The plan's ATTEMPTS column in PROFILE_NOTES.md
  gmu-convergence table reflects "NOT REACHED — boot blocked before
  GPU allocation."
- **Surfaced as:** decision checkpoint — see "Decision Checkpoint" section.

**Total deviations:** 1 auto-fixed (Rule 3, blocking dependency, scope=this profile);
1 surfaced as decision checkpoint (Rule 4, upstream backend gap).

## Decision Checkpoint (operator action required)

**Type:** decision (Rule 4 — architectural/upstream)
**Plan:** 04.7-02
**Progress:** 2/4 tasks closed; 1 partial; 1 gated.
**Blocker:** `ValueError: GGUF model with architecture mistral3 is not
supported yet.` from vLLM 0.19.2rc1.dev134+gfe9c3d6c5 (the
`vllm/vllm-openai:cu130-nightly-aarch64` nightly we pulled at Task 1).

**4 paths to unblock (operator must pick one):**

| # | Path | Engineering cost | Timeline | v1 impact |
|---|---|---|---|---|
| 1 | **Wait for upstream `mistral3` GGUF arch support** | low (re-pull + new digest + re-run smoke) | indeterminate (depends on vLLM project) | none (v1 stays current; just waits) |
| 2 | **Switch to NVFP4 path** (RecViking build) | medium (new bundle authoring per Plan 04.7-01 pattern; Phase 5 calibration question becomes "is NVFP4 throughput acceptable on GB10 UMA?") | days | retire v1; cut v2 NVFP4 |
| 3 | **Switch to llama.cpp serving path** | high (outside vLLM-only D-02 envelope; new sidecar shape; new harness adapter) | weeks | full architectural detour |
| 4 | **Defer the entire phase** | zero | zero | drop the Phase 5 dense-128B matrix slot |

**Recommendation:** Option 1 (wait for upstream). The phase is eval-only
(D-13), not in the daily-driver critical path; T-02 (container nightly
drift) already commits us to periodic re-pull anyway. Cost is a single
re-pull + re-run when upstream lands the architecture; everything else
in v1 (digest, GGUF, profile shape, harness contract) stays intact.

**To resume:** when operator picks a path, re-spawn this plan with the
chosen direction in `<continuation_state>`. For Option 1: re-pull
`vllm/vllm-openai:cu130-nightly-aarch64`, capture new digest, replay
Task 3. For Option 2: cut a Plan 04.7-04-NVFP4 with the same wave
structure. For Option 3: re-architect at the phase-CONTEXT level.
For Option 4: mark the phase as deferred in STATE.md.

## Threat Register Update

| Threat | Status | Notes |
|---|---|---|
| **T-01** GGUF backend instability | **FIRED — load-bearing blocker** | vLLM nightly does not allowlist `mistral3` architecture. PROFILE_NOTES.md "Plan 04.7-02 boot-smoke failure timeline" + "GGUF backend experimental status (T-01)" sections document the trace + 4-option recovery menu. |
| **T-02** Container nightly drift | **CLOSED for v1** | Local image Id `sha256:ffa30d66ff5c...` pinned in serving.yaml; PROFILE_NOTES.md "Container nightly drift (T-02)" documents re-pin debt. |
| **T-03** OOM at gmu=0.78 | **NOT EXERCISED** | Boot blocked before GPU allocation; D-05 gmu step-down protocol untouched. |
| **T-04** Tool-call parser mismatch | **NOT EXERCISED** | Cannot reach the tool-call canary while G-1 is blocked. |
| **T-05** Long-context degradation | **NOT EXERCISED** | Cannot reach the SP_OK canary at 128K while G-1 is blocked. |
| **T-06** Spark coexistence at gmu=0.78 | **NOT EXERCISED** | Container never started long enough to allocate the gmu pool. |
| **T-tokenizer (NEW)** Gated-repo + offline-cache + tokenizer extraction collision | **MITIGATED for v1** | Per-profile fallback applied (commented `tokenizer:` line); GGUF-embedded tokenizer used. PROFILE_NOTES.md "Tokenizer fallback" section documents the collision + v2 re-enable path. |

## Issues Encountered

Beyond the deviations documented above:

- **`runs/boot-failures/` accumulates per-attempt diagnostic bundles.** Two
  new bundles created this plan (one per attempt). Operator may want to
  prune older `runs/` entries periodically; out of scope here.
- **Gemma daily-driver cold start = ~408s.** Each Mistral attempt evicted
  Gemma; restoring Gemma between attempts cost ~7 minutes wall-clock per
  cycle. Future operational improvement: a pre-flight "save daily-driver
  profile + port" → "post-attempt restore" wrapper script could skip the
  smoke retest on restore (since Gemma is known-good), saving ~6 minutes
  per cycle. Not implemented; tracked here for future runbook
  consideration.

## User Setup Required

**To resume after this checkpoint:** see "Decision Checkpoint" section
above for the 4-option menu. Operator picks the path; the orchestrator
re-spawns Plan 04.7-02 with continuation context, OR cuts a different
plan (Plan 04.7-04 NVFP4 / phase re-architecture / phase deferral).

## Next Phase Readiness

**Plan 04.7-03 (documentation) NOT READY.** The Plan 04.7-03 docs would
need to land a CLAUDE.md / README.md / runbook entry describing the
Mistral profile as bootable on this Spark; with G-1 blocked by T-01,
documenting it as bootable would be false. Plan 04.7-03 should be
gated on Plan 04.7-02 G-1 closure (the original phase-level dependency
graph already captures this).

## TDD Gate Compliance

This plan has frontmatter `type: execute` (not `type: tdd`); plan-level
TDD gate sequence does not apply. No `tdd="true"` task tags within this
plan.

## Self-Check: PASSED

Verified post-write:
- `profiles/mistral-medium-3.5/v1/serving.yaml` exists with `container_image_digest: sha256:ffa30d66ff5c…` (real, NOT sentinel) ✓
- `profiles/mistral-medium-3.5/v1/serving.yaml` has `# tokenizer: mistralai/...` (commented out per Rule 3 auto-fix) ✓
- `profiles/mistral-medium-3.5/v1/profile.yaml` hash is `sha256:5f3d25443a7ff9d414f37c23215712c8cd1b6f25a5e3d4025d9daf9a38a1b1ff` (real, distinct from Plan 04.7-01's `3fea7f76…`) ✓
- `profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md` has frontmatter `validation_runs:` with two FAIL entries ✓
- `profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md` has body sections "Plan 04.7-02 boot-smoke failure timeline (T-01 fired)" + "Tokenizer fallback (T-tokenizer)" ✓
- `/data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf` exists, 74 GB, GGUF magic bytes confirmed ✓
- `/data/models/Mistral-Medium-3.5-128B-Q4_K_M-parts/` does NOT exist (cleaned up) ✓
- `uv run emmy profile validate profiles/mistral-medium-3.5/v1` exits 0 (G-5 still green) ✓
- Local docker image `sha256:ffa30d66ff5c...` present (`docker inspect` exit 0) ✓
- Operator's daily-driver Gemma 4 26B-A4B v2.1 running on port 8002 (smoke test passed at restore) ✓
- Commit `f06608e` (Task 1) exists in git log ✓
- Commit `4826199` (Task 3 partial) exists in git log ✓
- `bash scripts/start_emmy.sh --profile profiles/mistral-medium-3.5/v1 --port 8002` exits 1 with the documented `ValueError: GGUF model with architecture mistral3 is not supported yet.` (intentional — T-01 blocker; verified twice) ✓

## Workaround A iteration (2026-05-02 follow-up wave)

After the prior wave's T-01 decision-checkpoint surface, the orchestrator
chose **Workaround A** (one-time HF cache populate + offline boot using
vLLM's `repo:quant` HF-format model path + `--hf-config-path` pointing to
a local config dir). The operator authenticated to HuggingFace and
pre-staged the unquantized base model's config files at
`/data/models/Mistral-Medium-3.5-128B-config/`. A second 04.7-02 wave
then wired Workaround A: schema extension + runner emission + profile
update + boot smoke iteration. This wave produced 4 new commits:

| # | Wave-2 Commit | Description |
|---|---|---|
| A | `845757b` (feat) | Add `hf_config_path` EngineConfig field + runner CLI emission for transformers GGUF allowlist workaround |
| B | `51a3757` (feat) | Wire Workaround A — `hf_config_path` + `repo:quant` model path in profile bundle |
| C | `55c618c` (fix) | Workaround A boot smoke — repo:quant infeasible (vLLM bug); hf_config_path alone does NOT bypass T-01 |
| D | _(this commit)_ | docs(04.7-02) — update SUMMARY.md + deferred-items.md after Workaround A wiring + boot smoke |

### What Wave 2 added to the source tree

**Schema + runner extension (commit A — 845757b):**
- `emmy_serve/profile/schema.py`: `EngineConfig.hf_config_path: Optional[str] = None`
  field added (mirrors the Plan 04.7-01 tokenizer field pattern exactly;
  strictly additive — pre-04.7-02 profiles validate unchanged).
- `emmy_serve/boot/runner.py`: `render_vllm_cli_args` emits
  `--hf-config-path <dir>` when set (conditional emission preserves
  byte-identical render for pre-04.7-02 profiles).
- `tests/unit/test_profile_schema_gguf.py`: 2 new tests (positive +
  Optional default-None).
- `tests/unit/test_docker_run_build.py`: 3 new tests (renderer
  positive + 2 absence guarantees).

**Profile bundle update (commit B — 51a3757):**
- Switched `engine.model` to `bartowski/...:Q4_K_M` (HF repo:quant form).
- Added `engine.hf_config_path: /models/Mistral-Medium-3.5-128B-config`.
- Operator-staged HF cache (out-of-band; documented in PROFILE_NOTES.md):
  - `/data/hf-cache/hub/models--bartowski--.../snapshots/<rev>/config.json`
    (copy from local config dir; for the speculators check)
  - `/data/hf-cache/hub/models--bartowski--.../snapshots/<rev>/mistralai_Mistral-Medium-3.5-128B-Q4_K_M.gguf`
    → symlink to `/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf` (for the
    GGUF weight load)

**Boot smoke + revert (commit C — 55c618c):**

Two new boot attempts run on 2026-05-02:
- **Attempt 3 (run_id 20260502T231011Z-e07024) — repo:quant + hf_config_path:**
  Container exited ~10s. NEW failure class:
  `HFValidationError` from `vllm/transformers_utils/repo_utils.py:220`
  (`get_model_path` rejects repo:quant model format because it naively
  forwards to `huggingface_hub.snapshot_download(repo_id=...)`). vLLM's
  GGUFModelLoader DOES handle repo:quant (model_loader/gguf_loader.py:60-67)
  but the offline-mode short-circuit at arg_utils.py:687-694 calls
  get_model_path FIRST when HF_HUB_OFFLINE=1 and never reaches the
  GGUF loader. **Workaround A's repo:quant variant is INFEASIBLE
  without a vLLM-side patch.**
- **Attempt 4 (run_id 20260502T231409Z-8f35fc) — local file + hf_config_path:**
  Container exited ~15s. Same T-01 as Wave 1 attempt 2:
  `ValueError: GGUF model with architecture mistral3 is not supported yet`.
  hf_config_path is consulted only AFTER the speculators check, in
  `get_config()` (config.py:633+). The speculators check at
  config.py:587-591 unconditionally GGUF-parses local-file model paths.
  **hf_config_path alone does NOT bypass T-01.**

After confirming repo:quant is infeasible, serving.yaml reverted to the
local-file model path (`/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf`)
to match the empirical reality. The hf_config_path field is RETAINED
because (a) it documents intent; (b) it's load-bearing for any future
Workaround A revision (e.g., paired with a vLLM monkey-patch gating
GGUF parsing on `hf_config_path is None`); (c) removing it later would
be a behavioral change requiring a v2 cut.

### Refined operator decision menu (now 6 paths, was 4)

The Wave 1 SUMMARY listed 4 paths (wait / NVFP4 / llama.cpp / defer).
The Wave 2 empirical work adds 2 more and refines the decision shape:

| # | Path | Engineering cost | Timeline | v1 impact |
|---|---|---|---|---|
| 1 | **Wait for transformers `mistral3` GGUF arch support** (load-bearing fix) | low (re-pull container + re-run smoke) | indeterminate | none — v1 just waits |
| 2 | **Wait for vLLM `repo:quant` fix** in `get_model_path` so Workaround A's repo:quant variant becomes viable | low (re-pull container + re-run smoke) | indeterminate | minimal — flip back to repo:quant model path |
| 3 | **Switch to NVFP4** (RecViking build) — sidesteps GGUF entirely | medium (new bundle authoring) | days | retire v1; cut v2 NVFP4 |
| 4 | **Switch to llama.cpp serving** | high (outside vLLM-only D-02 envelope) | weeks | full architectural detour |
| 5 | **Hot-patch transformers `GGUF_SUPPORTED_ARCHITECTURES`** to add `mistral3` (alias of `mistral`) via a sitecustomize.py mounted into the container | medium (small hot-patch; verification needed) | hours | v1 ships with patch artifact in source tree |
| 6 | **Defer the entire phase** | zero | zero | drop the Phase 5 dense-128B matrix slot |

**Recommendation:** Options 1 + 2 (just wait for upstream — both are
zero-cost and Phase 04.7 is eval-only). Option 5 is tempting but
unverified — the `mistral3` GGUF metadata uses `mistral3.context_length`
keys (vs `mistral.context_length`); transformers' GGUF_TO_TRANSFORMERS
mapping might or might not work cleanly across the rename.

### Wave-2 hash trail

Bundle hash bumped 4 more times in Wave 2:
- `5f3d2544...` (Wave 1 final, T-01-blocked snapshot — pre-Wave-2 starting point)
- `cc7d8db8...` (Wave 2 commit B serving.yaml first edit — repo:quant + hf_config_path wiring)
- `b9824c98...` (Wave 2 commit B PROFILE_NOTES.md "Workaround A wiring" section)
- `c994499e...` (Wave 2 commit C serving.yaml revert to local-file model path)
- `c326add4...` (Wave 2 commit C PROFILE_NOTES.md "Workaround A empirical results" + frontmatter validation_runs)
- `4ee6f874...` (intermediate after PROFILE_NOTES body edit before frontmatter)
- `c326add4...` (final state at Task C commit) → this is what `c326add4` is
- _(this commit D will not bump the bundle hash because it edits SUMMARY.md, not the profile bundle)_

### Ship state at end of Wave 2

v1 ships in the same "structurally-correct, T-01-blocked" state as Wave 1
WITH the addition of:
- `hf_config_path` field declared in serving.yaml (operationally inert
  until paired with a code-side T-01 bypass; useful artifact for any
  future workaround iteration)
- Schema + runner support for the `hf_config_path` field (correct work,
  lands regardless of T-01 resolution)
- Empirically-verified failure trace for both Workaround A variants
  (no longer hypothetical)
- Refined 6-path operator decision menu

CONTEXT G-1, G-2, G-3 still cannot close. CONTEXT D-12 air-gap preserved.
Daily-driver Gemma 4 26B-A4B v2.1 stayed up on port 8002 throughout
Wave 2 (Mistral attempts ran on a separate `emmy-serve-mistral` container
on port 8005; this Wave-2 discipline IMPROVED on Wave 1's "evict + restore
cycle" — operator endpoint never lapsed during Wave 2).

### Wave-2 deviations from the orchestrator's plan

1. **[Rule 3 — Blocking dependency] orchestrator's repo:quant Workaround A
   variant is infeasible.** The orchestrator described "vLLM's `repo:quant`
   HF-format model path... bypasses `maybe_override_with_speculators`'s
   broken local-file code path" — this is empirically true for the
   speculators check, but a separately-preexisting vLLM bug at
   `repo_utils.py:220` (`get_model_path` doesn't recognize `repo:quant`
   format) blocks at an EARLIER stage when `HF_HUB_OFFLINE=1`. Reverted
   to local-file model path; documented in PROFILE_NOTES.md. The
   `hf_config_path` field is retained because it lands the intent
   (and is potentially useful for future workaround iterations).

2. **[Rule 4 — Architectural / Upstream gap] hf_config_path alone does NOT
   bypass T-01.** The orchestrator's Plan 04.7-02 followup hypothesized
   that `hf_config_path` plus the speculators-check bypass via repo:quant
   would unblock T-01. With the speculators-check bypass infeasible,
   hf_config_path is operationally inert for unblocking T-01 in this
   profile. Documented; surfaced as a refined decision checkpoint
   (6 paths instead of 4).

### Wave-2 self-check

- `profiles/mistral-medium-3.5/v1/serving.yaml` has `hf_config_path:` set ✓
- `profiles/mistral-medium-3.5/v1/serving.yaml` has `model: /models/...gguf` (reverted) ✓
- `profiles/mistral-medium-3.5/v1/profile.yaml` hash is `sha256:c326add4...` (latest) ✓
- `emmy_serve/profile/schema.py` has `hf_config_path: Optional[str] = None` ✓
- `emmy_serve/boot/runner.py` emits `--hf-config-path` when set ✓
- New tests added to both `test_profile_schema_gguf.py` and `test_docker_run_build.py` ✓
- `uv run emmy profile validate profiles/mistral-medium-3.5/v1` exits 0 (G-5 still green) ✓
- Daily-driver Gemma 4 26B-A4B v2.1 running on port 8002 (verified at SUMMARY-commit time) ✓
- Mistral attempt evidence at `runs/boot-failures/20260502T231011Z-e07024-boot-failure/` and `20260502T231409Z-8f35fc-boot-failure/` (gitignored per `runs/**`) ✓
- Wave-2 commits in git log: `845757b` (Task A), `51a3757` (Task B), `55c618c` (Task C) ✓
- No modifications to STATE.md or ROADMAP.md (orchestrator-owned per parallel-executor rules) ✓
- Deviations: 2 (Rule 3 + Rule 4); both documented above + in PROFILE_NOTES.md "Workaround A empirical results" section.

## Option 5 sitecustomize hot-patch iteration (2026-05-02)

After Wave 2's empirical-failure decision-checkpoint surface, the operator
selected **Decision Option 5** ("Hot-patch transformers
`GGUF_SUPPORTED_ARCHITECTURES` to add `mistral3` (alias of `mistral`) via
sitecustomize.py mounted into the container") from the refined 6-path
menu. This Wave 3 iteration produced a working hot-patch that
**EMPIRICALLY UNBLOCKED T-01 end-to-end** — and uncovered three new
downstream error classes: two were small Rule 3 auto-fixes that landed in
v1 immediately, the third is a fundamental architectural blocker
requiring operator decision.

### Wave 3 source-tree commits

| # | Wave-3 Commit | Description |
|---|---|---|
| A | `f378f6c` (feat) | airgap_patch_dir EngineConfig field + sitecustomize wiring (schema + runner + 5 unit tests) |
| B | `047d5be` (feat) | sitecustomize hot-patch artifact dir (3 files) + serving.yaml field flip |
| C | `35aee85` (fix) | post-sitecustomize boot smoke — dtype + tokenizer fixes; new architectural blocker (mistral tokenizer format) |
| D | `58b2245` (docs) | PROFILE_NOTES "Option 5 sitecustomize hot-patch iteration (2026-05-02)" section + frontmatter validation_runs Wave 3 entries |
| E | _(this commit)_ | docs(04.7-02) — SUMMARY.md + deferred-items.md update for Wave 3 |

### Wave 3 source-tree additions

**Schema field (commit A — f378f6c):**
- `EngineConfig.airgap_patch_dir: Optional[str] = None` — bundle-relative
  path to a dir of Python hot-patches mounted at `/airgap_patches/` and
  prepended to PYTHONPATH so sitecustomize.py auto-imports BEFORE vLLM
  imports transformers
- `EngineConfig.dtype: Optional[Literal["auto", "float16", "bfloat16",
  "float32"]] = None` — dtype override required for Mistral 3.x GGUF
  (added in commit C after attempt 6 surfaced the bf16-not-supported
  error)
- Both fields strictly additive — every pre-04.7-02-followup profile
  validates with the field unset and renders byte-identically

**Runner extension (commits A + C):**
- `render_docker_only_args` (when airgap_patch_dir is set) emits:
  bind-mount `-v <bundle>/<airgap_patch_dir>:/airgap_patches:ro` + env
  `PYTHONPATH=/airgap_patches` + env `EMMY_AIRGAP_PATCH_MISTRAL3=on`
- `render_vllm_cli_args` (when dtype is set) emits `--dtype <value>`
- Conditional emission preserves byte-identical render for pre-Wave-3
  profiles

**Tests (commit A):**
- `tests/unit/test_profile_schema_gguf.py`: 2 new tests
  (positive accept + Optional default-None for airgap_patch_dir)
- `tests/unit/test_docker_run_build.py`: 5 new tests (bind-mount + 2
  env vars when set; triple absence when unset; independence from
  hf_config_path field)
- All 26 tests in the two files pass; wider 339-test unit suite green

**Patch artifact (commit B):**
- `profiles/mistral-medium-3.5/v1/airgap_patches/sitecustomize.py` —
  Python-discovered name; auto-imported on process start when on
  PYTHONPATH; defers to `mistral3_gguf_allowlist.apply()` when
  `EMMY_AIRGAP_PATCH_MISTRAL3=on` (per-patch opt-in)
- `profiles/mistral-medium-3.5/v1/airgap_patches/mistral3_gguf_allowlist.py`
  — the actual patch: `arch_list.append("mistral3")` + `cfg_map["mistral3"]
  = cfg_map["mistral"]`. Idempotent + defensive. Mimics the existing
  in-tree `qwen2moe→qwen2_moe` aliasing patterns
- `profiles/mistral-medium-3.5/v1/airgap_patches/README.md` — provenance
  + removal criteria

**Profile bundle update (commit B + C):**
- serving.yaml: `engine.airgap_patch_dir: airgap_patches` (B);
  uncommented `engine.tokenizer:` and pointed at local
  `/models/Mistral-Medium-3.5-128B-config` (C); added `engine.dtype:
  float16` (C)

### Boot-smoke timeline (3 attempts)

**Attempt 5 — sitecustomize patch fires + Workaround A wiring intact
(run_id `20260502T233850Z-6282d9`):**

Container startup logged the patch confirmation:
```
[mistral3_gguf_allowlist] v1.0.0 applied: mistral3 → GGUF allowlist + cfg_map alias of mistral.
```

vLLM proceeded through arg parsing, NIXL setup, and architecture
resolution. **T-01 cleared** (`Resolved architecture:
Mistral3ForConditionalGeneration`). Failed at `create_model_config`:

```
File "vllm/engine/arg_utils.py", line 1448, in create_model_config
pydantic_core.ValidationError: ...
  Loading a multimodal GGUF model needs to use original tokenizer.
  Please specify the unquantized hf model's repo name or path using
  the --tokenizer argument.
```

**Auto-fix (Rule 3):** uncommented `tokenizer:` line in serving.yaml,
pointed at the operator-staged dir (which already has tokenizer.json +
tokenizer_config.json from the Wave 2 staging step).

**Attempt 6 — past tokenizer barrier (run_id
`20260502T234040Z-5af3fe`):**

Failed at `create_engine_config` / VllmConfig validation:

```
File "vllm/engine/arg_utils.py", line 2094, in create_engine_config
pydantic_core.ValidationError: ...
  torch.bfloat16 is not supported for quantization method gguf.
  Supported dtypes: [torch.float16, torch.float32]
```

Plus the warning `gguf.py:69 GGUF has precision issues with bfloat16
on Blackwell`. Mistral 3.x source config.json declares
`dtype=bfloat16`.

**Auto-fix (Rule 3):** added `EngineConfig.dtype` schema field;
runner emits `--dtype <value>`; serving.yaml: `dtype: float16`.

**Attempt 7 — past dtype barrier; engine-init code executes (run_id
`20260502T234222Z-e848d9`):**

vLLM cleared MUCH more code path (kv_cache configured, scheduler
initialized, asynchronous scheduling enabled, IR op priority
configured). Then failed at the FINAL VllmConfig validation:

```
INFO 05-02 23:42:42 [vllm.py:840] Asynchronous scheduling is enabled.
INFO 05-02 23:42:42 [kernel.py:203] Final IR op priority after setting platform defaults: ...
pydantic_core.ValidationError: ...
  The tokenizer must be an instance of MistralTokenizer.
```

**Root cause (architectural):** vLLM's `mistral` tool_call_parser path
requires `mistral_common.MistralTokenizer` (loaded from `tekken.json`
via `--tokenizer-mode mistral`). The operator-staged dir has only HF
tokenizer.json + tokenizer_config.json — no tekken.json. This is a
genuine architectural blocker.

### Refined operator decision menu (now 7 paths, was 6)

| # | Path | Eng. cost | Timeline | v1 impact |
|---|---|---|---|---|
| 1 | Wait for upstream transformers `mistral3` GGUF arch support | low | indeterminate | none |
| 2 | Wait for upstream vLLM `repo:quant` fix in `get_model_path` | low | indeterminate | minimal |
| 3 | Switch to NVFP4 (RecViking build) | medium | days | retire v1; cut v2 NVFP4 |
| 4 | Switch to llama.cpp serving | high | weeks | architectural detour |
| 5 | Hot-patch transformers GGUF allowlist via sitecustomize | DONE | DONE | DONE for T-01; new architectural blocker found |
| 6 | Defer the entire phase | zero | zero | drop Phase 5 dense-128B matrix slot |
| **7** | **NEW: Source `tekken.json` for the operator-staged dir + add `--tokenizer-mode mistral` to bundle** | low (assuming sub-path a: re-download from gated HF repo) | hours | v1 ships with additional `tokenizer_mode: mistral` field |

**Recommendation:** **Option 7 sub-path (a)** — `huggingface-cli download
mistralai/Mistral-Medium-3.5-128B --include "tekken.json"` (or equivalent)
to add the file to the operator-staged dir, then schema/runner/serving.yaml
extension for `tokenizer_mode: mistral`. This is the lowest-cost path to
actually closing G-1 because it addresses the load-bearing architectural
blocker discovered AFTER the sitecustomize patch cleared T-01. If sub-path
(a) is infeasible, Option 1 (wait) becomes the next-cheapest fallback.

### What this wave proved

- **The sitecustomize hot-patch IS the load-bearing fix for T-01.** The
  patch fires correctly in the container (stderr confirmation logged on
  every boot of attempts 5/6/7); transformers' `load_gguf_checkpoint`
  successfully parses the bartowski Mistral 3.5 128B Q4_K_M GGUF after
  the patch applies.
- **transformers 5.6.0 already ships full `mistral3` model support**
  (`Mistral3Config`, `Mistral3ForConditionalGeneration`, etc.). The
  allowlist gap was the ONLY barrier between the GGUF parser and
  successful config construction. Option 5 was correctly scoped.
- **vLLM's GGUF backend reaches deep engine-init code** (kv_cache
  config, scheduler, IR op priority) before failing on the
  MistralTokenizer requirement — confirming the GGUF parsing +
  model-load code path fundamentally works for `mistral3` after the
  patch.
- **Two of the three new error classes were minor profile-knob fixes**
  that landed cleanly via the additive-field pattern (dtype +
  tokenizer-pointed-at-local-dir). The third is the genuine
  architectural blocker.

### What remains (architectural)

vLLM's `mistral` tool_call_parser path requires
`mistral_common.MistralTokenizer` (loaded from `tekken.json`). The
operator-staged dir has only HF tokenizer.json. Resolution requires
EITHER:
- Option 7 sub-path (a): tekken.json staging + `tokenizer_mode: mistral`
  schema/runner extension, OR
- Option 7 sub-path (b): generate tekken.json from tokenizer.json via
  mistral-common conversion utility (less straightforward), OR
- A different parser path (which contradicts CONTEXT D-09 mistral-parser
  requirement — would force a v2 cut)

### Wave 3 hash trail

| Stage | Hash |
|---|---|
| Wave 2 final (T-01-blocked snapshot) | `sha256:c326add4...` |
| Wave 3 commit B (airgap_patches dir + serving.yaml field) | `sha256:a5bb2689...` |
| Wave 3 commit C tokenizer fix (uncommented + local dir) | `sha256:75b0b841...` |
| Wave 3 commit C dtype fix (added float16 + new schema field) | `sha256:7ea0f1ad...` |
| Wave 3 commit D (PROFILE_NOTES Option 5 section) | `sha256:7a597f67...` |

All hash bumps are in-place per CLAUDE.md additive-bundle rules
(airgap_patch_dir + dtype are strictly additive fields whose absence
validates as None elsewhere; tokenizer line uncomment is a v1
clarification of the prior commented-out value).

### Operator endpoint preserved

Daily-driver Gemma 4 26B-A4B v2.1 stayed up on port 8002 throughout
Wave 3 (Mistral attempts ran on port 8005 with container name
`emmy-serve-mistral`). Discipline carried over from Wave 2. Verified at
each commit boundary; verified at SUMMARY-commit time.

### Wave 3 deviations from the orchestrator's plan

1. **[Rule 3 — Blocking dependency] Multimodal tokenizer requirement**
   surfaced post-T-01 (attempt 5). Auto-fixed by pointing `tokenizer:`
   at the operator-staged local dir; documented in serving.yaml + this
   SUMMARY.

2. **[Rule 3 — Blocking dependency] bfloat16 unsupported for GGUF**
   surfaced post-tokenizer (attempt 6). Auto-fixed by adding
   `EngineConfig.dtype` schema field + runner CLI emission +
   `dtype: float16` in profile bundle.

3. **[Rule 4 — Architectural / Upstream gap] MistralTokenizer
   requirement** surfaced post-dtype (attempt 7). The operator-staged
   dir lacks `tekken.json`; vLLM's `mistral` parser path needs it. NOT
   auto-fixed because (a) requires re-staging by the operator (HF
   download), (b) requires schema extension for `tokenizer_mode`, (c)
   even with sub-path (a), the loaded model is still
   `Mistral3ForConditionalGeneration` (multimodal) — the bartowski GGUF
   is text-only, so a fourth error class around missing vision_tower
   weights MAY surface next. Surfaced as Decision Option 7 in the
   refined menu.

### Wave 3 self-check

- `profiles/mistral-medium-3.5/v1/airgap_patches/` exists with 3 files (sitecustomize.py + mistral3_gguf_allowlist.py + README.md) ✓
- `profiles/mistral-medium-3.5/v1/serving.yaml` has `airgap_patch_dir: airgap_patches` ✓
- `profiles/mistral-medium-3.5/v1/serving.yaml` has `tokenizer: /models/Mistral-Medium-3.5-128B-config` (uncommented) ✓
- `profiles/mistral-medium-3.5/v1/serving.yaml` has `dtype: float16` ✓
- `profiles/mistral-medium-3.5/v1/profile.yaml` hash is `sha256:7a597f67…` (latest) ✓
- `emmy_serve/profile/schema.py` has both `airgap_patch_dir: Optional[str] = None` AND `dtype: Optional[Literal[...]] = None` ✓
- `emmy_serve/boot/runner.py` emits `--dtype` when set; `render_docker_only_args` emits airgap_patches bind-mount + PYTHONPATH + EMMY_AIRGAP_PATCH_MISTRAL3 envs when airgap_patch_dir set ✓
- New tests added: 2 in `test_profile_schema_gguf.py`, 5 in `test_docker_run_build.py`; all 26 tests pass ✓
- Wider unit suite: 339 passed, 5 skipped (pre-existing) ✓
- `uv run emmy profile validate profiles/mistral-medium-3.5/v1` exits 0 (G-5 still green) ✓
- Daily-driver Gemma 4 26B-A4B v2.1 running on port 8002 (verified at SUMMARY-commit time) ✓
- Mistral attempt evidence at `runs/20260502T233850Z-6282d9-boot/`, `runs/20260502T234040Z-5af3fe-boot/`, `runs/20260502T234222Z-e848d9-boot/` (gitignored per `runs/**`) ✓
- Wave-3 commits in git log: `f378f6c` (A), `047d5be` (B), `35aee85` (C), `58b2245` (D) ✓
- No modifications to STATE.md or ROADMAP.md (orchestrator-owned per parallel-executor rules) ✓
- Sitecustomize patch confirmation logged in container stderr on every Wave-3 attempt: `[mistral3_gguf_allowlist] v1.0.0 applied: mistral3 → GGUF allowlist + cfg_map alias of mistral.` ✓

## Option 7a tokenizer_mode iteration (2026-05-02 → 2026-05-03 follow-up)

After the Wave 3 sitecustomize iteration left the plan blocked at the
MistralTokenizer requirement (refined 7-path operator decision menu),
the operator selected **Decision Option 7 sub-path (a)** — `tekken.json`
staging + `tokenizer_mode: mistral` schema/runner extension. The
operator pre-staged the file via `huggingface-cli download
mistralai/Mistral-Medium-3.5-128B --include "tekken.json"
"tokenization_mistral.py" "*.txt" --local-dir
/data/models/Mistral-Medium-3.5-128B-config` (16,275,354 bytes; sibling
of the existing tokenizer.json/tokenizer_config.json). This Wave 4
iteration produced a working tokenizer_mode flag + uncovered TWO new
downstream errors: one Rule 3 auto-fixed (multimodal-init via stripped
text-only config), one operator-decision (CUDA OOM coexistence).

### Wave 4 source-tree commits

| # | Wave-4 Commit | Description |
|---|---|---|
| A | `23568a9` (feat) | tokenizer_mode EngineConfig field + runner CLI emission + 11 unit tests (4 tokenizer_mode schema + 3 dtype schema gap-fill + 4 docker_run_build emission/absence) |
| B | `369efeb` (feat) | Wire Option 7a — `tokenizer_mode: mistral` line + Wave-4 PROFILE_NOTES § |
| C | `2205adb` (fix) | post-Option-7a boot smoke — multimodal-init blocker auto-fixed via text-only-config strip; CUDA OOM next (T-06 fired) |
| D | _(this commit)_ | docs(04.7-02) — SUMMARY.md + deferred-items.md update for Wave 4 findings |

### What Wave 4 added to the source tree

**Schema field (commit A — 23568a9):**
- `EngineConfig.tokenizer_mode: Optional[Literal["auto", "hf", "slow", "mistral", "deepseek_v32"]] = None` — Literal mirrors vLLM 0.19's `vllm/config/model.py:85` `TokenizerMode` exactly (verified inside the running emmy-serve container)
- Strictly additive — every pre-04.7-02-followup profile validates with the field unset and renders byte-identically

**Runner extension (commit A):**
- `render_vllm_cli_args` emits `--tokenizer-mode <value>` when set
- Conditional emission preserves byte-identical render for pre-Wave-4 profiles

**Tests (commit A):**
- `tests/unit/test_profile_schema_gguf.py`: 4 new tokenizer_mode tests (positive accept "mistral", positive accept the 4 other Literal values, negative reject "tekken" typo, Optional default-None) + 3 NEW `dtype` schema tests closing a Wave-3-commit gap (commit 35aee85 added the field but no schema-test commit)
- `tests/unit/test_docker_run_build.py`: 8 new tests (--dtype emitted when set; --tokenizer-mode emitted when set; both omitted in 4 absence-context fixtures: original / new_flags / hf_config_path / airgap_patch_dir)
- 369 unit tests pass (was 354 before Wave 4; +15 = 8 from this commit + 7 from earlier-iteration field tests now succeeding under the dev-extra pytest install)

**Operator-staged artifact (out-of-band, 2026-05-02 16:55):**
- `/data/models/Mistral-Medium-3.5-128B-config/tekken.json` (16,275,354 bytes)
- `/data/models/Mistral-Medium-3.5-128B-config/SYSTEM_PROMPT.txt` (1,461 bytes; chat-template helper)

**Operator-staged artifact (Rule 3 auto-fix, 2026-05-03):**
- `/data/models/Mistral-Medium-3.5-128B-text-only-config/` (NEW dir)
- `/data/models/Mistral-Medium-3.5-128B-text-only-config/config.json` — stripped text-only config: top-level `architectures: ["Ministral3ForCausalLM"]` + `model_type: ministral3` + `text_config` fields hoisted + multimodal fields (`vision_config`, `image_token_index`, `multimodal_projector_bias`, `projector_hidden_act`, `spatial_merge_size`, `vision_feature_layer`) dropped
- All other tokenizer/generation files: SYMLINKS to the original config dir (zero copy)

**Profile bundle update (commits B + C):**
- serving.yaml commit B: `tokenizer_mode: mistral` line added under existing `dtype: float16`
- serving.yaml commit C: `hf_config_path` flipped to `/models/Mistral-Medium-3.5-128B-text-only-config` with full code-flow analysis in commentary

### Wave 4 boot-smoke timeline (2 attempts)

**Attempt 8 — `tokenizer_mode: mistral` ALONE (run_id `20260503T000823Z-a92397`):**

vLLM successfully consumed `--tokenizer-mode mistral` AND loaded
tekken.json via mistral-common. The Wave-3 prior failure point
(`pydantic_core.ValidationError: The tokenizer must be an instance of
MistralTokenizer.`) was CLEARED. Engine init proceeded ~30s further
into MultiModalBudget construction and failed:

```
File "vllm/v1/engine/input_processor.py", line 61, in __init__
    mm_budget = MultiModalBudget(vllm_config, mm_registry)
File "vllm/multimodal/encoder_budget.py", line 32, in get_mm_max_toks_per_item
    mm_inputs = mm_registry.get_dummy_mm_inputs(...)
File "vllm/model_executor/models/mistral3.py", line 181, in get_hf_processor
    return self.ctx.get_hf_processor(PixtralProcessor, **kwargs)
...
OSError: Can't load image processor for '/models/Mistral-Medium-3.5-128B-config'.
```

Root cause: vLLM resolved arch as `Mistral3ForConditionalGeneration`
(read from config.json's `architectures + model_type=mistral3`); the
multimodal architecture triggers MultiModalBudget which probes
PixtralProcessor against the model dir, which lacks
preprocessor_config.json (text-only GGUF). Same predicted class as
Wave-3 hand-off; different specific exception. Auto-fixed in attempt 9.

**Attempt 9 — Rule 3 auto-fix: stripped text-only config + `Ministral3ForCausalLM` arch (run_id `20260503T001143Z-2ff544`):**

vLLM resolved arch as `Ministral3ForCausalLM`. vLLM registry maps
`Ministral3ForCausalLM → ("mistral", "MistralForCausalLM")` at
`vllm/model_executor/models/registry.py:164`, so the text-only arch
loads the existing MistralForCausalLM implementation. MultiModalBudget
barrier CLEARED — engine core process spawned. Engine init reached
`init_device` (the FIRST CUDA-touching code) and failed:

```
File "vllm/v1/worker/gpu_worker.py", line 282, in init_device
    self.free_memory, self.total_memory = current_platform.mem_get_info(device)
File "torch/cuda/memory.py", line 842, in mem_get_info
    return torch.cuda.cudart().cudaMemGetInfo(device)
torch.AcceleratorError: CUDA error: out of memory
```

Root cause: **T-06 (Spark coexistence at gmu=0.78)** firing as predicted
by CONTEXT D-06. Daily-driver Gemma 4 26B-A4B v2.1 holds ~68.7 GB
(verified via `nvidia-smi --query-compute-apps=process_name,used_memory
--format=csv`). Mistral's `gpu_memory_utilization=0.78 → 99.8 GB` pool
target doesn't fit alongside on the 128 GB UMA box (99.8+68.7=168.5
> 128). NOT an architectural blocker — resource gate operating on a
well-formed configuration.

### Architectural-progress summary (6 barriers, 5 cleared by code/config, 1 remaining as operator decision)

| Barrier | Wave | Status |
|---|---|---|
| T-01 (`mistral3` GGUF arch not in transformers allowlist) | 3 | CLEARED (sitecustomize hot-patch) |
| Tokenizer gated-repo + offline collision | 3 | CLEARED (Workaround-A staged dir) |
| `bf16-not-supported-for-GGUF` | 3 | CLEARED (`dtype: float16` field) |
| MistralTokenizer requirement | 4 | CLEARED (`tokenizer_mode: mistral` + tekken.json) |
| Multimodal-arch + text-only-weights | 4 | CLEARED (stripped text-only config + `Ministral3ForCausalLM` arch) |
| Spark coexistence with daily-driver | 4 | NOT CLEARED — operator decision required |

**Boot is now ONE operator gesture away from G-1 closure.** Stop Gemma,
re-run Mistral, expect G-1+G-2+G-3 closure (assuming no further
surprises in the GGUF→MistralForCausalLM weight load path; the 73 GB
Q4_K_M weights themselves are well-formed and the load path is the
standard MistralForCausalLM implementation).

### Refined operator decision menu (now 8 paths, was 7)

| # | Path | Eng. cost | Timeline | v1 impact |
|---|---|---|---|---|
| 1 | Wait for upstream transformers `mistral3` GGUF arch support | low | indeterminate | none |
| 2 | Wait for upstream vLLM `repo:quant` fix in `get_model_path` | low | indeterminate | minimal |
| 3 | Switch to NVFP4 (RecViking build) | medium | days | retire v1; cut v2 NVFP4 |
| 4 | Switch to llama.cpp serving | high | weeks | architectural detour |
| 5 | Hot-patch transformers GGUF allowlist via sitecustomize | DONE | DONE | DONE for T-01 |
| 6 | Defer the entire phase | zero | zero | drop Phase 5 dense-128B matrix slot |
| 7a | Source `tekken.json` + `tokenizer_mode: mistral` | DONE | DONE | DONE for MistralTokenizer barrier |
| 7-strip | Stripped text-only config dir + `Ministral3ForCausalLM` arch | DONE | DONE | DONE for multimodal-init barrier |
| **8** | **NEW: Stop daily-driver Gemma and boot Mistral solo at gmu=0.78** (matches CONTEXT D-13 eval-only stance; operator gesture only — no new code/config) | zero | minutes | none — daily-driver is operator-restartable |

**Recommendation:** **Option 8** — eval-only profile by design (D-13).
The typical workflow is `/profile mistral-medium-3.5` swap when
operator wants the heavy 128B reasoning, then `/profile
gemma-4-26b-a4b-it` back to daily-driver. The `/profile` slash command
in pi-emmy already handles container swap; this just needs the operator
to confirm they want to evict Gemma temporarily.

### Wave-4 hash trail

Bundle hash bumped 4 times in Wave 4:
- `7a597f67…` (Wave 3 final, post-Option-5 sitecustomize)
- `18372082…` (Wave 4 commit B intermediate — `tokenizer_mode: mistral` added to serving.yaml)
- (multiple intermediates as PROFILE_NOTES.md was edited; the hash trail
  documented in PROFILE_NOTES.md "Decision Option 7a hash trail" notes
  the self-referential property — exact final hash lives in profile.yaml)
- Final hash at commit-D time = the `profile.hash` field in
  profiles/mistral-medium-3.5/v1/profile.yaml (currently
  `sha256:ee9cb362…` after Wave-4 commit C)

### Wave 4 deviations from the orchestrator's plan

1. **[Rule 3 — Blocking dependency] Multimodal-init OSError in Wave 4 attempt 8**
   Auto-fixed via the predicted text-only-config strip path. The
   orchestrator's Task C plan explicitly said "if text-only-config strip
   works as a Rule 3 in-scope fix, take it. Otherwise surface
   checkpoint." It worked: stripped config dir avoids the
   Mistral3ForConditionalGeneration multimodal init code path entirely
   by routing through Ministral3ForCausalLM (text-only registry entry).

2. **[Rule 4 — Operator-decision / Resource gate] CUDA OOM at gmu=0.78
   coexistence** in Wave 4 attempt 9. NOT auto-fixed because:
   - The fix is operationally trivial (`docker stop emmy-serve`) but is
     an operator gesture, not a code/config change.
   - Stopping the daily-driver mid-execution is the operator's choice,
     not the executor's — the Mistral profile is eval-only opt-in (D-13)
     specifically because the daily-driver is supposed to stay up most
     of the time.
   - Surfaced as Decision Option 8 in the refined menu above.

### Operator-decision shape (what's needed to close G-1)

This is a **decision checkpoint**, NOT an architectural blocker. The
single remaining gap is the operator gesture to free GPU memory:

```bash
# Operator action to close G-1:
docker stop emmy-serve  # frees ~68.7 GB
# Then re-run Mistral boot:
RUN_ID="$(date -u +'%Y%m%dT%H%M%SZ')-$(head -c 6 /dev/urandom | xxd -p | head -c 6)"
RUN_DIR="runs/${RUN_ID}-boot-mistral"
mkdir -p "$RUN_DIR"
DOCKER_RUN_ARGS="$(uv run python -m emmy_serve.boot.runner render-docker-args \
  --profile profiles/mistral-medium-3.5/v1 --run-dir "$RUN_DIR" --port 8005)"
eval docker run --name emmy-serve-mistral --detach "$DOCKER_RUN_ARGS"
# Wait 5-10 minutes for cold start (73 GB GGUF load); then:
curl http://127.0.0.1:8005/v1/models  # G-1 check
# Operator can then test G-2 (sp_ok canary) + G-3 (tool_call canary)
# manually or via scripts/smoke_test.py.
```

OR via the `/profile mistral-medium-3.5` slash command in pi-emmy
(which handles the container swap automatically).

### Wave 4 self-check

- `profiles/mistral-medium-3.5/v1/serving.yaml` has `tokenizer_mode: mistral` ✓
- `profiles/mistral-medium-3.5/v1/serving.yaml` has `hf_config_path: /models/Mistral-Medium-3.5-128B-text-only-config` ✓
- `/data/models/Mistral-Medium-3.5-128B-text-only-config/config.json` exists with stripped Ministral3ForCausalLM arch ✓
- `/data/models/Mistral-Medium-3.5-128B-text-only-config/tekken.json` is symlink to original config ✓
- `EngineConfig.tokenizer_mode: Optional[Literal["auto","hf","slow","mistral","deepseek_v32"]] = None` field added ✓
- `render_vllm_cli_args` emits `--tokenizer-mode <value>` when set ✓
- 369 unit tests pass (was 354 before Wave 4) ✓
- `uv run emmy profile validate profiles/mistral-medium-3.5/v1` exits 0 ✓
- Daily-driver Gemma 4 26B-A4B v2.1 still on port 8002 (verified via curl at SUMMARY-commit time, EngineCore = 68.7 GB per nvidia-smi) ✓
- Mistral attempt evidence at `runs/20260503T000823Z-a92397-boot-mistral/` and `runs/20260503T001143Z-2ff544-boot-mistral/` (gitignored per `runs/**`) ✓
- Wave-4 commits in git log: `23568a9` (A), `369efeb` (B), `2205adb` (C); D will be this docs commit ✓
- No modifications to STATE.md or ROADMAP.md (orchestrator-owned per parallel-executor rules) ✓
- Sitecustomize patch confirmation logged in container stderr on every Wave-4 attempt: `[mistral3_gguf_allowlist] v1.0.0 applied: mistral3 → GGUF allowlist + cfg_map alias of mistral.` ✓

---
*Phase: 04.7-mistral-medium-3-5-128b-alternate*
*Completed: 2026-05-03 (partial — operator decision Option 8 required to free GPU memory and close G-1; 5 of 6 architectural barriers cleared by code/config across Waves 3+4)*
*Wave 2 followup completed: 2026-05-02*
*Wave 3 (Option 5 sitecustomize) completed: 2026-05-02*
*Wave 4 (Option 7a tokenizer_mode + text-only-config strip) completed: 2026-05-03*
