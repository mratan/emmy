---
phase: 04.7
slug: mistral-medium-3-5-128b-alternate
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-01
---

# Phase 04.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

CONTEXT.md acceptance gates G-1..G-7 are the validation contract. Per RESEARCH §8, the existing 4-step canary chain (`scripts/smoke_test.py:44-187` → `wait_for_vllm` + `run_sp_ok` + `run_tool_call` + `run_generate`) covers G-1, G-2, G-3 with NO new code. Plan 04.7-01 needs Wave 0 unit tests for the schema+renderer changes. Plan 04.7-02 is a single integration smoke run. Plan 04.7-03 is doc-only.

D-05 (skip formal KV-bisection — substitute single-run smoke + 0.05 step-down) and D-06 (skip 2× 2h thermal replay) are EXPLICITLY NOT planned. PROFILE_NOTES.md `## D-05 substituted protocol` and `## D-06 substituted protocol` sections record the substitution honestly.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Python)** | pytest 7.x |
| **Framework (TypeScript)** | bun test (in `packages/emmy-ux`) |
| **Config file (Python)** | `pyproject.toml` |
| **Config file (TypeScript)** | `packages/emmy-ux/package.json` test script |
| **Quick run command (Python)** | `cd /data/projects/emmy && uv run pytest tests/unit/ -x --no-cov` |
| **Quick run command (TypeScript)** | `cd /data/projects/emmy/packages/emmy-ux && bun test` |
| **Full suite command** | `cd /data/projects/emmy && uv run pytest && cd packages/emmy-ux && bun test` |
| **Boot-smoke command** | `bash scripts/start_emmy.sh --profile profiles/mistral-medium-3.5/v1 --port 8002` |
| **Estimated runtime (quick)** | ~30 seconds (pytest unit) |
| **Estimated runtime (full)** | ~2-3 minutes (full pytest + bun test) |
| **Estimated runtime (boot-smoke)** | ~5-10 minutes (cold pull + first-time GGUF load + 4-step canary chain) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/unit/ -x --no-cov`
- **After Plan 04.7-01 completion:** Full Python suite + bun test (regression sanity for schema/renderer changes that touch all profiles)
- **After Plan 04.7-02 Task 3 completion:** Boot-smoke command must exit 0; canary.jsonl PASS rows for `run_sp_ok` + `run_tool_call`
- **After Plan 04.7-03 completion:** Full suite green (touches no source; quick sanity)
- **Before `/gsd-verify-work`:** Full suite + boot smoke + manual /profile swap walkthrough operator-confirmed
- **Max feedback latency:** 30 seconds for unit tests; ~10 minutes for boot smoke

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Acceptance Gate | Threat Ref | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------------|------------|-----------|-------------------|-------------|--------|
| 04.7-01-T1 | 01 | 0 | G-5 prerequisite (schema valid) | T-01 | unit | `cd /data/projects/emmy && uv run pytest tests/unit/test_profile_schema_gguf.py tests/unit/test_docker_run_build.py -x --no-cov` | ❌ W0 (creates `test_profile_schema_gguf.py`; extends `test_docker_run_build.py`) | ⬜ pending |
| 04.7-01-T2 | 01 | 1 | G-5 partial (validate fails only on digest sentinel) | T-01 | integration | `cd /data/projects/emmy && uv run emmy profile validate profiles/mistral-medium-3.5/v1 2>&1 \| grep -c REPLACE_AT_FIRST_PULL` returns `1`; `cd /data/projects/emmy && uv run pytest tests/unit/test_profile_bundle_mistral.py -x --no-cov` | ❌ W0 (creates `test_profile_bundle_mistral.py`) | ⬜ pending |
| 04.7-02-T1 | 02 | 1 | G-5 (validate fully green) | T-02 | integration | `cd /data/projects/emmy && uv run emmy profile validate profiles/mistral-medium-3.5/v1` exits 0 | ✅ (existing `emmy profile validate` CLI) | ⬜ pending |
| 04.7-02-T2 | 02 | 1 | infra (GGUF on disk) | — | shell | `head -c 4 /data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf \| xxd \| grep -q GGUF` AND `[ "$(du -BG /data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf \| awk '{print $1}' \| tr -d G)" -ge 70 ]` | ✅ (shell builtins) | ⬜ pending |
| 04.7-02-T3 | 02 | 2 | G-1 + G-2 + G-3 | T-01, T-03, T-04, T-05 | integration | `bash scripts/start_emmy.sh --profile profiles/mistral-medium-3.5/v1 --port 8002` exits 0; `runs/<RUN_ID>-boot/canary.jsonl` has `pass` rows for `run_sp_ok` and `run_tool_call` | ✅ (existing `scripts/smoke_test.py:44-187` 4-step canary chain) | ⬜ pending |
| 04.7-02-T4 | 02 | 3 | G-4 | T-06 | manual (operator checkpoint) | TUI walkthrough: `/profile mistral-medium-3.5` then `/profile gemma-4-26b-a4b-it`; verify `curl -s 127.0.0.1:8002/v1/models \| jq -r '.data[].id'` after each swap | n/a (manual gate) | ⬜ pending |
| 04.7-03-T1 | 03 | 1 | G-6 (CLAUDE.md updated) | — | grep | `grep -c "Phase 04.7 heavyweight alternate" /data/projects/emmy/CLAUDE.md` returns `1`; `grep "Updated 2026-05-01 — Phase 04.7" CLAUDE.md` matches | ✅ (grep) | ⬜ pending |
| 04.7-03-T2 | 03 | 1 | G-6 (README + MATRIX + runbook updated) | — | grep | `grep -c "profiles/mistral-medium-3.5/" README.md` ≥ 1; `grep -c "mistral-medium-3.5" eval/MATRIX.md` ≥ 2; `grep -c "Mistral 3.5 128B GGUF (multi-part)" docs/runbook.md` returns `1`; AND no remaining placeholder strings | ✅ (grep) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_profile_schema_gguf.py` — NEW; covers schema-extension behaviors (`quantization=gguf` accepted; bad value rejected; `tokenizer` Optional default None)
- [ ] `tests/unit/test_docker_run_build.py` — EXTEND with `_VALID_SERVING_YAML_WITH_NEW_FLAGS` fixture variant + 4 new test functions for `--reasoning-parser`, `--max-num-seqs`, `--tokenizer` emit + omit-when-unset
- [ ] `tests/unit/test_profile_bundle_mistral.py` — NEW; round-trips the on-disk bundle through `loader.load_serving`; asserts the schema-extended fields (quantization=gguf, tokenizer, max_num_seqs=1); asserts community_sources ≥ 4; asserts DEFAULT_VARIANT == "v1\n"
- [ ] No new framework install needed
- [ ] Existing canary chain (`scripts/smoke_test.py` + `emmy_serve/canary/{sp_ok,tool_call}.py`) covers G-1, G-2, G-3 with NO new code

---

## Manual-Only Verifications

| Behavior | Acceptance Gate | Why Manual | Test Instructions |
|----------|-----------------|------------|-------------------|
| Atomic 4-phase /profile swap end-to-end (Gemma → Mistral → Gemma) | G-4 | The Phase 4 D-23 swap runs through pi-emmy TUI + Phase 04.2 sidecar; confirming the footer + /v1/models response after each swap is operator-eyeballed. The sidecar's `STOPPING → LOADING(pct) → WARMUP → READY` progress lines render in the TUI footer. | See Plan 04.7-02 Task 4 `<how-to-verify>` block (7-step procedure: start Gemma → confirm sidecar → launch pi-emmy → /profile mistral → SP_OK probe → /profile gemma → confirm sidecar status). Resume signal: `g4 phase4.7 green`. |
| /profile swap operator-monitoring for T-06 (Spark coexistence at gmu=0.78) | T-06 mitigation | The "~28 GB system headroom + bio-pipeline coexistence" check is observation-shaped, not assertion-shaped — operator watches `nvidia-smi` + `free -m` during the swap walkthrough for sidecar OOM cascade per Phase 04.2 OOM postmortem. | Implicit acceptance signal during Plan 04.7-02 Task 4. If sidecar OOMs cascade into bio-pipeline territory, drop `engine.max_model_len` to 96K (pool ~88 GB → ~40 GB system headroom) per CONTEXT T-06. |

---

## Skipped Validations (CONTEXT-locked, with substitutes)

| Skipped | Locked by | Substitute |
|---------|-----------|------------|
| Formal KV-bisection (`scripts/find_kv_budget.py`) | D-05 | Single boot smoke at gmu=0.78 (Plan 04.7-02 Task 3 first attempt); 0.05 step-down (0.78 → 0.73 → 0.68) on OOM; final attempt drops max_model_len 131072 → 65536. PROFILE_NOTES.md `## D-05 substituted protocol` records the converged value + audit trail. |
| 2× 2h thermal replay protocol | D-06 | None. CLAUDE.md Pitfall #4 updated to "characterized on Spark across 4 prior profiles, no throttle observed; gate retained for daily-driver candidates, skipped for non-daily-driver alternates." PROFILE_NOTES.md `## D-06 substituted protocol` records the substitution. |
| Throughput floor enforcement | D-13 | None. Throughput is informational only. `decode_throughput_p50_smoke_tokps` recorded in PROFILE_NOTES.md frontmatter for Phase 5 baseline; not gated. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (Plans 04.7-01 + 04.7-03 fully automated; Plan 04.7-02 has 3 automated tasks + 1 manual /profile-swap operator checkpoint per the standard pi-emmy TUI walkthrough discipline used in Phase 1 SC-1, Phase 4 SC-3, Phase 04.2 SC-1/2/3)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (Plan 04.7-01 Task 1 + Task 2 both automated; Plan 04.7-02 Task 1 + Task 2 + Task 3 automated; Task 4 is the only manual checkpoint)
- [x] Wave 0 covers all MISSING references: `test_profile_schema_gguf.py` (new), `test_docker_run_build.py` (extended), `test_profile_bundle_mistral.py` (new)
- [x] No watch-mode flags
- [x] Feedback latency < 30s for unit tests; ~10 min acceptable for boot-smoke (cold pull + GGUF first load)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned (pending Plan 04.7-01 Wave 0 to satisfy `wave_0_complete: true`)
