# Phase 04.7: Mistral Medium 3.5 128B alternate — Pattern Map

**Mapped:** 2026-05-01
**Files analyzed:** 23 (17 net-new profile-bundle files, 2 modified Python files, 4 modified docs)
**Analogs found:** 23 / 23 (every new file maps to an in-repo analog except the gguf-merge operational step, which is correctly captured as "no analog" — net-new operational doc only)

---

## File Classification

| New / Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `profiles/mistral-medium-3.5/v1/serving.yaml` | config | request-response (vLLM CLI render) | `profiles/gemma-4-31b-it/v1.2/serving.yaml` | exact (dense, upstream `vllm/vllm-openai:*-arm64-cu130`, runtime quant, has `reasoning_parser`, no fastsafetensors) |
| `profiles/mistral-medium-3.5/v1/harness.yaml` | config | request-response | `profiles/gemma-4-31b-it/v1.2/harness.yaml` | exact (compaction + memory + subagents blocks already shipped) |
| `profiles/mistral-medium-3.5/v1/profile.yaml` | config / manifest | identity / hash | `profiles/gemma-4-31b-it/v1.2/profile.yaml` | exact (single-version case, no sibling-of-siblings) |
| `profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md` | docs (provenance) | n/a | `profiles/qwen3.6-27b/v1.1/PROFILE_NOTES.md` | role-match (clone-provenance section); D-05/D-06 honesty section is **net-new shape** |
| `profiles/mistral-medium-3.5/v1/prompts/system.md` | prompt | n/a | `profiles/gemma-4-31b-it/v1.2/prompts/system.md` | exact (SP_OK canary, byte-identical) |
| `profiles/mistral-medium-3.5/v1/prompts/edit_format.md` | prompt | n/a | `profiles/gemma-4-31b-it/v1.2/prompts/edit_format.md` | exact (Hashline, model-agnostic) |
| `profiles/mistral-medium-3.5/v1/prompts/tool_descriptions.md` | prompt | n/a | `profiles/gemma-4-31b-it/v1.2/prompts/tool_descriptions.md` | exact (9 native tools) |
| `profiles/mistral-medium-3.5/v1/prompts/compact.md` | prompt | n/a | `profiles/gemma-4-31b-it/v1.2/prompts/compact.md` | exact |
| `profiles/mistral-medium-3.5/v1/tool_schemas/{bash,edit,find,grep,ls,read,web_fetch,web_search,write}.schema.json` (9 files) | schema | n/a | `profiles/gemma-4-31b-it/v1.2/tool_schemas/*.schema.json` | exact (JSON-Schema, model-agnostic) |
| `profiles/mistral-medium-3.5/v1/grammars/tool_call.lark` | grammar | request-response (reactive backstop) | `profiles/gemma-4-31b-it/v1.2/grammars/tool_call.lark` | partial — Mistral's `[TOOL_CALLS]<json-array>` envelope differs from Gemma's `<\|tool_call>...<tool_call\|>`. Acceptable v1 path: byte-clone the Gemma lark; harness uses parser output (already OpenAI shape) so the lark only fires on reactive retry. If T-04 fires, swap to a Mistral-shape lark in v2. |
| `profiles/mistral-medium-3.5/v1/subagents/research/AGENTS.md` | prompt (persona) | n/a | `profiles/gemma-4-31b-it/v1.2/subagents/research/AGENTS.md` | exact (byte-clone) |
| `profiles/mistral-medium-3.5/v1/subagents/code-reviewer/AGENTS.md` | prompt (persona) | n/a | `profiles/gemma-4-31b-it/v1.2/subagents/code-reviewer/AGENTS.md` | exact |
| `profiles/mistral-medium-3.5/v1/subagents/bash-runner/AGENTS.md` | prompt (persona) | n/a | `profiles/gemma-4-31b-it/v1.2/subagents/bash-runner/AGENTS.md` | exact |
| `profiles/mistral-medium-3.5/DEFAULT_VARIANT` | config | n/a | `profiles/gemma-4-31b-it/DEFAULT_VARIANT` | exact (single line `v1\n`) |
| `emmy_serve/profile/schema.py` (MODIFIED — `EngineConfig`) | model (pydantic) | validate | `EngineConfig.kv_cache_dtype` Literal pattern (same file, line 68); `EngineConfig.reasoning_parser` Optional pattern (line 91) | exact (Literal extension + Optional[str] field follow established patterns in the same class) |
| `emmy_serve/boot/runner.py` (MODIFIED — `render_vllm_cli_args`) | service | transform (config → CLI argv) | `render_vllm_cli_args` lines 92–98 (same function) | exact (same conditional-emit pattern; just append three more `if e.<field>: cli += [...]` clauses) |
| `tests/unit/test_profile_schema_gguf.py` (NEW) | test | validate | `tests/unit/test_profile_schema_gemma4.py` | exact (Pydantic schema-extension test, isolated) |
| `tests/unit/test_docker_run_build.py` (MODIFIED) | test | validate | same file, lines 89–96 (existing assertion pattern) | exact |
| `tests/unit/test_profile_bundle_mistral.py` (NEW) | test | validate | (closest = `tests/unit/test_schema.py`) | role-match |
| `CLAUDE.md` (MODIFIED) | docs | n/a | self (Phase 4.1 dense siblings bullet, line 35) | exact (insert sibling row in same Pinned Tech Stack section) |
| `README.md` (MODIFIED) | docs | n/a | README.md profile-ladder section | exact |
| `eval/MATRIX.md` (MODIFIED) | docs | n/a | self (lines 8–16 matrix table) | exact (append row) |
| `docs/runbook.md` (MODIFIED) | docs | n/a | self (lines 808–820 Available profiles) + (lines 823–833 Weight downloads) | exact (append-only edits + new sub-section for GGUF merge) |

**No analog found** — net-new operational shapes the planner must author from RESEARCH §1.3 and §5 with no existing in-repo template:

| Item | Why no analog | RESEARCH section |
|---|---|---|
| `gguf-split --merge` invocation in `docs/runbook.md` | First GGUF profile in the repo; existing weights are HF safetensors-shaped | RESEARCH §1.3 |
| PROFILE_NOTES.md "D-05/D-06 substituted protocol" section (gmu convergence table without a `find_kv_budget.py` run) | First profile to ship without a KV-bisection audit | RESEARCH §2.4 + §5.2 |

---

## Pattern Assignments

### `profiles/mistral-medium-3.5/v1/serving.yaml` (config, request-response)

**Analog:** `profiles/gemma-4-31b-it/v1.2/serving.yaml`

**Why this analog:** dense profile, runtime-quantized BF16 → FP8, upstream `vllm/vllm-openai:*-arm64-cu130` container, ships `reasoning_parser` alongside `tool_call_parser`, uses `safetensors`/`auto` load (NOT fastsafetensors), `attention_backend` deliberately unset, has the `container_entrypoint_override: ""` discipline that upstream `vllm-openai` images require.

**Engine identity pattern** (analog lines 23–30):

```yaml
engine:
  # --- immutable identity ---
  model: /models/gemma-4-31B-it                    # container-internal path (matches -v mount)
  model_hf_id: google/gemma-4-31B-it               # HF ID
  served_model_name: gemma-4-31b-it                # name vLLM exposes on /v1/models
  container_image: vllm/vllm-openai:gemma4-0409-arm64-cu130
  container_image_digest: sha256:db59febc6c476c9c93701cc9f54917533cee774d4e4bb27eff6fafeae37509f8
  container_entrypoint_override: ""                # CRITICAL — clears upstream ENTRYPOINT=[vllm serve] collision
```

**Mistral retarget (delta the planner must instruct):**

| Field | Gemma 31B v1.2 value | Mistral v1 value | Source |
|---|---|---|---|
| `model` | `/models/gemma-4-31B-it` | `/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf` | RESEARCH §1.3 (single merged GGUF file path) |
| `model_hf_id` | `google/gemma-4-31B-it` | `mistralai/Mistral-Medium-3.5-128B` | base model, NOT bartowski's GGUF repo (tokenizer comes from base — RESEARCH §1.1) |
| `served_model_name` | `gemma-4-31b-it` | `mistral-medium-3.5` | CONTEXT D-13 (`/profile mistral-medium-3.5`) |
| `container_image` | `vllm/vllm-openai:gemma4-0409-arm64-cu130` | `vllm/vllm-openai:cu130-nightly-aarch64` | CONTEXT D-10 + RESEARCH §1.4 (CONTEXT spelling appears inverted; resolve at first `docker pull`) |
| `container_image_digest` | `sha256:db59febc6c47…` | `sha256:REPLACE_AT_FIRST_PULL` initially → real digest captured at boot smoke per Plan 04.7-02 | RESEARCH §1.4; schema rejects sentinel at validate, so digest must be pasted before `emmy profile validate` |
| `container_entrypoint_override` | `""` | `""` (CARRY OVER — upstream `vllm-openai` image still bakes `ENTRYPOINT=[vllm serve]`) | analog line 30 comment |

**Context + memory pattern** (analog lines 32–39):

```yaml
  # --- context + memory ---
  max_model_len: 262144                            # Gemma 256K
  gpu_memory_utilization: 0.55
  kv_cache_dtype: fp8
  enable_prefix_caching: true
  enable_chunked_prefill: true
  max_num_batched_tokens: 8192
  max_num_seqs: 4
```

**Mistral retarget:**

| Field | Gemma 31B v1.2 | Mistral v1 | Source |
|---|---|---|---|
| `max_model_len` | `262144` | `131072` | CONTEXT D-03 (256K deferred to v2 per OOS list) |
| `gpu_memory_utilization` | `0.55` | `0.78` | CONTEXT D-04 (structural — weights+KV math forces > 0.55) |
| `max_num_seqs` | `4` | `1` | CONTEXT D-07 (single-stream; pool too tight for batching) |

**Tool-call + reasoning parser pattern** (analog lines 47–50):

```yaml
  # --- tool-call parser ---
  tool_call_parser: gemma4
  reasoning_parser: gemma4                          # vLLM auto-detects from tokenizer_config.json today; renderer extension makes the implicit explicit (RESEARCH §1.2)
  enable_auto_tool_choice: true
```

**Mistral retarget:**

```yaml
  tool_call_parser: mistral                          # CONTEXT D-09; Mistral HF card recommends
  reasoning_parser: mistral                          # CONTEXT D-09 + RESEARCH §3.3
  enable_auto_tool_choice: true                      # CONTEXT D-09
```

**Quantization + loader + tokenizer pattern (NEW — schema gap to close, see RESEARCH §1.2):**

```yaml
  # --- loader ---
  load_format: auto                                  # GGUF backend ignores fastsafetensors; "auto" is valid Literal value
  # --- quantization ---
  quantization: gguf                                 # NEW — extends EngineConfig.quantization Literal (Plan 04.7-01 schema work)
  # --- tokenizer (NEW field on EngineConfig) ---
  tokenizer: mistralai/Mistral-Medium-3.5-128B       # vLLM GGUF docs strongly recommend base-model tokenizer
```

**Sampling defaults pattern** (analog lines 67–76):

```yaml
sampling_defaults:
  temperature: 1.0                                  # Google Gemma 4 card default
  top_p: 0.95
  top_k: 64
  repetition_penalty: 1.0
  max_tokens: 8192
  stop: []
```

Mistral retarget per RESEARCH §2.5 (assumption flagged; planner pin choice + cite in PROFILE_NOTES.md): `temperature: 0.15, top_p: 0.95, top_k: 40, repetition_penalty: 1.0, max_tokens: 8192, stop: []`.

**Quirks pattern** (analog lines 83–86):

```yaml
quirks:
  strip_thinking_tags: true                         # Gemma channel-bleed backstop
  promote_reasoning_to_content: false
  buffer_tool_streams: false
```

Mistral retarget — `strip_thinking_tags: false` (no Gemma channel-bleed analog known for Mistral). Other two byte-clone. PROFILE_NOTES.md records "if T-04 fires, flip per §3.2 fallback playbook."

**Env block** (analog lines 88–95): byte-clone, but flip `VLLM_LOAD_FORMAT: safetensors` → `VLLM_LOAD_FORMAT: auto` to match `engine.load_format`. The schema-enforced air-gap pair (`VLLM_NO_USAGE_STATS=1`, `HF_HUB_OFFLINE=1`) is mandatory (`schema.py` lines 163–173, `_airgap_policy`).

---

### `profiles/mistral-medium-3.5/v1/harness.yaml` (config, request-response)

**Analog:** `profiles/gemma-4-31b-it/v1.2/harness.yaml`

**Why this analog:** carries the full block set the schema accepts today (`prompts`, `context.compaction`, `context.memory`, `tools.web_fetch`, `tools.web_search`, `agent_loop`, `subagents`); Mistral inherits all of these unchanged.

**Prompts pattern** (analog lines 10–15):

```yaml
prompts:
  system: prompts/system.md                        # SP_OK canary
  edit_format: prompts/edit_format.md              # Hashline
  tool_descriptions: prompts/tool_descriptions.md  # 9 native tools
  use_system_role: true                             # verified by SP_OK canary at boot
  prepend_system_text: ""
```

Mistral retarget: byte-clone (Mistral honors system role; SP_OK canary applies model-agnostically).

**Context pattern** (analog lines 17–36):

```yaml
context:
  max_input_tokens: 114688                          # = max_model_len(131072) - output_reserve(16384)
  include_repo_map: false
  repo_map_max_tokens: 0
  default_pruning: head_tail
  compaction:
    soft_threshold_pct: 0.75
    preserve_recent_turns: 5
    summarization_prompt_path: prompts/compact.md
    preserve_tool_results: error_only
  memory:
    enabled: true
    project_root: ".emmy/notes"
    global_root: "~/.emmy/memory"
    read_at_session_start: true
    max_file_bytes: 65536
    max_total_bytes: 10485760
    blocked_extensions: [".env", ".key", ".pem"]
```

Mistral retarget: `max_input_tokens` formula yields `131072 - 16384 = 114688` — **same as Gemma 31B v1.2** despite Gemma having a 256K `max_model_len`, because v1.2 left `max_input_tokens` at the v1.1 `131072 - 16384` value. Planner: confirm whether Mistral's 128K bumps `max_input_tokens` to match Gemma 31B v1.2's literal value (`114688`) or recomputes from CONTEXT's 128K (`131072 - 16384 = 114688` — identical). All other fields byte-clone.

**Tools pattern** (analog lines 38–70):

```yaml
tools:
  format: openai                                    # parser converts to OpenAI shape
  schemas: tool_schemas/
  grammar:
    path: grammars/tool_call.lark
    mode: reactive                                   # parse unconstrained first; grammar on retry only
  per_tool_sampling:
    edit: { temperature: 0.0 }
    bash: { temperature: 0.0 }
    read: { temperature: 0.0 }
  web_fetch:
    allowlist:
      - docs.python.org
      - developer.mozilla.org
      - docs.vllm.ai
      - huggingface.co
      - ai.google.dev
      - docs.langfuse.com
    search_bypass_ttl_ms: 300000
  web_search:
    enabled: true
    base_url: http://127.0.0.1:8888
    max_results_default: 10
    rate_limit_per_turn: 10
    timeout_ms: 10000
```

Mistral retarget: byte-clone everything. No `tools.ask_claude` block (CONTEXT D-12: Mistral is NOT an `ask_claude.enabled` flip target; the absent block validates as None per `ToolsConfig.ask_claude: Optional[AskClaudeConfig] = None`). Optional cosmetic: drop `ai.google.dev` from `web_fetch.allowlist` since it's Gemma-specific (analog line 56) and add `docs.mistral.ai` if planner deems useful — but byte-clone is acceptable and lower-risk.

**Subagents pattern** (analog lines 82–108): byte-clone. Per RESEARCH §9 question 7, ship subagents for harness-side parity even though Mistral is eval-only.

**Agent loop + advanced settings whitelist** (analog lines 72–80): byte-clone.

---

### `profiles/mistral-medium-3.5/v1/profile.yaml` (config / manifest)

**Analog:** `profiles/gemma-4-31b-it/v1.2/profile.yaml`

**Profile manifest pattern** (analog lines 1–14):

```yaml
profile:
  id: gemma-4-31b-it
  version: v1.2
  family: gemma-4
  base_model: google/gemma-4-31B-it
  description: "Gemma 4 31B dense profile — v1.2 long-context bump (256K; gmu=0.55…)"
  created: '2026-04-25'
  hash: sha256:78a0c9070911fc3bf9b7954893bba2581e5be073f535b064451fcffaf5530670
  hash_algorithm: sha256
  hash_manifest_version: 1
  tags:
  - coding
  - dgx-spark
  - fp8
  - gemma-4
  - phase-4.1
  - …
```

**Mistral retarget:**

| Field | Mistral value | Source |
|---|---|---|
| `id` | `mistral-medium-3.5` | CONTEXT D-13 (matches `/profile` route) |
| `version` | `v1` | CONTEXT D-11 (no sibling-of-siblings) |
| `family` | `mistral` | net-new family |
| `base_model` | `mistralai/Mistral-Medium-3.5-128B` | RESEARCH §2.2 |
| `description` | "Mistral Medium 3.5 128B — Phase 04.7 alternate. Dense 128B via bartowski Q4_K_M GGUF (single-file merged, ~73 GB). Eval-only opt-in (`/profile mistral-medium-3.5`). NOT a daily-driver, NOT a routes.yaml participant, NOT an ask_claude target. Boot-only validation per CONTEXT D-05/D-06 (no KV-bisection, no thermal replay)." | CONTEXT decisions |
| `created` | `'2026-05-01'` | CONTEXT |
| `hash` | `sha256:PENDING` initially → recomputed via `uv run emmy profile hash <bundle> --write` | RESEARCH §2.3 |
| `tags` | `[coding, dgx-spark, gguf, mistral, phase-04.7, dense, alternate, eval-only, q4-k-m]` | RESEARCH §2.2 |
| `community_sources` | (see RESEARCH §11 sources list) — minimum: bartowski GGUF card, mistralai HF card, vLLM GGUF docs, vLLM tool_calling docs | RESEARCH §11 |

---

### `profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md` (docs, provenance)

**Analog:** `profiles/qwen3.6-27b/v1.1/PROFILE_NOTES.md` (clean shape, RAM-headroom retune narrative analogous to a "structural-fit" narrative)

**Frontmatter pattern** (analog lines 1–15):

```yaml
---
profile_id: qwen3.6-27b
profile_version: v1.1
created: 2026-04-25
hardware_id: dgx-spark-01
measured_values:
  gpu_memory_utilization: 0.55
  gpu_clock_p5_hour2_mhz: 2476
  gpu_clock_p50_hour2_mhz: 2476
  decode_throughput_p50_hour2_tokps: 7.6
  decode_throughput_p1_hour2_tokps: 6.5
  cold_start_seconds: <inherited-from-v1>
  warm_throughput_tokps: <inherited-from-v1>
validation_runs: []
---
```

**Mistral retarget:**

```yaml
---
profile_id: mistral-medium-3.5
profile_version: v1
created: 2026-05-01
hardware_id: dgx-spark-01
measured_values:
  gpu_memory_utilization: 0.78  # CONTEXT D-04 — operational ceiling, NOT bisected (D-05 skip)
  decode_throughput_p50_smoke_tokps: <FILL_AT_BOOT>  # informational, NOT a gate (D-13)
  cold_start_seconds: <FILL_AT_BOOT>
  # NO thermal fields — D-06 skips 2× 2h thermal replay; CLAUDE.md Pitfall #4 retained for daily-drivers only.
  # NO KV-bisection fields — D-05 skips formal protocol; gmu=0.78 is structural fit, not measured ceiling.
validation_runs:
  - <RUN_ID>-boot-smoke  # filled at Plan 04.7-02
---
```

**Body sections (in order — analog has them in this exact sequence)**:

1. Top-line summary (analog lines 17–26) — Mistral version: purpose, eval-only posture, 128B-class quality data point.
2. **Phase 04.7 clone provenance table** (analog "## Phase 4.1 clone provenance" lines 69–96 model the shape):
   - Clone source: `profiles/gemma-4-31b-it/v1.2/` (source hash `sha256:78a0c9070911fc3bf9b7954893bba2581e5be073f535b064451fcffaf5530670`)
   - Retargeted-fields tables for `serving.yaml` and `profile.yaml`
   - Byte-identical-from-analog list (harness.yaml + prompts/ + tool_schemas/ + grammars/ + subagents/)
3. Provenance of defaults (analog "## Provenance of defaults" lines 112–139 model the shape) — every non-trivial knob with HF/blog source + retrieval date. New rows: `quantization: gguf` (vLLM Quantization docs), `tokenizer` (vLLM GGUF docs), `tool_call_parser: mistral` (Mistral HF card), `reasoning_parser: mistral` (Mistral HF card), sampling defaults (RESEARCH §2.5 assumption — flag explicitly).
4. **`## D-05 substituted protocol — no KV-bisection`** (NET-NEW — no analog has this): explicit acknowledgment of skip; cite CONTEXT D-05 + RESEARCH §5; include the gmu-convergence placeholder table from RESEARCH §5.2.
5. **`## D-06 substituted protocol — no thermal replay`** (NET-NEW): explicit acknowledgment; cite CONTEXT D-06 + the 8h prior data thesis verbatim; cross-reference CLAUDE.md Pitfall #4 status update.
6. **`## Container nightly drift (T-02)`** section: re-pin debt; Pitfall #2 acknowledgment.
7. **`## GGUF backend experimental status (T-01)`** section: link vLLM GGUF docs "highly experimental" wording + #39583 deprecation RFC.
8. **`## Tool-call parser fallback (T-04)`** section: the §3.2 RESEARCH playbook (explicit `--chat-template` if leaking; quirk handler as last resort — but byte-cloned grammar should be enough).
9. Open issues / future v2 candidates (256K context, NVFP4 alt, ask_mistral tool, fastsafetensors-derived image — same shape as analog "## Deferred / future" lines 254–260).
10. References block.

---

### `profiles/mistral-medium-3.5/v1/prompts/{system,edit_format,tool_descriptions,compact}.md`, `tool_schemas/*.json`, `grammars/tool_call.lark`, `subagents/{research,code-reviewer,bash-runner}/AGENTS.md`

**Analog:** `profiles/gemma-4-31b-it/v1.2/prompts/`, `tool_schemas/`, `grammars/`, `subagents/` (file-for-file)

**Pattern:** byte-clone. The SP_OK canary system prompt (analog `system.md` is one line: `"When the user says 'ping' you must reply with the exact literal text [SP_OK] and nothing else."`) is model-agnostic; the Hashline `edit_format.md` is model-agnostic; the 9 tool schemas are JSON-Schema (model-agnostic); the lark grammar is reactive backstop only (RESEARCH §1.1 + §3.1 confirm Mistral parser converts to OpenAI shape, so the grammar rarely fires).

**Per Pitfall #1 (CLAUDE.md):** do NOT add Mistral-specific examples in v1; clone byte-identical. Phase 5 calibration determines whether v2 needs Mistral tweaks.

---

### `profiles/mistral-medium-3.5/DEFAULT_VARIANT`

**Analog:** `profiles/gemma-4-31b-it/DEFAULT_VARIANT` (contents: `v1.2\n`)

**Pattern:** single-line file containing the variant identifier.

**Mistral content:** `v1\n` (single line, trailing newline, no trailing space — verify with `xxd`).

---

### `emmy_serve/profile/schema.py` (MODIFIED — `EngineConfig`)

**Analog within same file:**
- `EngineConfig.kv_cache_dtype: Literal["auto", "fp8", "fp16", "bf16"] = "fp8"` (line 68) — pattern for **adding a value to an existing Literal**
- `EngineConfig.reasoning_parser: Optional[str] = None` (line 91) — pattern for **adding a new Optional[str] field**
- `EngineConfig.max_num_seqs: Optional[int] = Field(default=None, gt=0)` (line 77) — pattern for **adding a new Optional numeric field with constraint**

**Existing pattern to extend** (current `quantization` line 83):

```python
    # --- quantization ---
    quantization: Literal["fp8", "bf16", "auto"] = "fp8"
```

**Required Mistral delta (Plan 04.7-01):**

```python
    # --- quantization ---
    # 04.7 — "gguf" added for Mistral Medium 3.5 128B Q4_K_M (CONTEXT D-02).
    # vLLM experimental GGUF backend; long-context flagged WIP by Mistral.
    quantization: Literal["fp8", "bf16", "auto", "gguf"] = "fp8"
```

**New field — `tokenizer`** (place near `model_hf_id`, lines 50–51, in the immutable-identity block — RESEARCH §1.2):

```python
    # 04.7 — explicit tokenizer override. Used by GGUF profiles where the
    # bundled tokenizer is "time-consuming and unstable" to extract per
    # vLLM GGUF docs; the recommended value is the base-model HF id (e.g.
    # "mistralai/Mistral-Medium-3.5-128B"). Default None = vLLM uses the
    # value of `model:` (HF model_id or local path).
    tokenizer: Optional[str] = None
```

**Why no validators are needed:** existing `_digest_shape` and `_airgap_policy` validators stay unchanged. The Literal extension is byte-additive (no existing profile loads as `"gguf"`); `tokenizer: Optional[str] = None` is byte-additive (existing profiles load as `None`).

---

### `emmy_serve/boot/runner.py` (MODIFIED — `render_vllm_cli_args`)

**Analog within same function** (lines 92–98):

```python
    if e.tool_call_parser:
        cli += ["--tool-call-parser", e.tool_call_parser]
    if e.enable_auto_tool_choice:
        cli.append("--enable-auto-tool-choice")
    if e.attention_backend:
        cli += ["--attention-backend", e.attention_backend]
    return cli
```

**Pattern:** conditional emission — `if e.<field>: cli += [<flag>, <value>]` (or `cli.append(<flag>)` for boolean toggles).

**Required Mistral delta (Plan 04.7-01) — append three new conditional blocks before `return cli`**:

```python
    # 04.7 — orphaned-flag fix: schema fields exist but were never emitted.
    # Forces explicit pass for Mistral (whose parser MUST be set explicitly)
    # AND surfaces Gemma's reasoning_parser/max_num_seqs through the CLI
    # (previously vLLM auto-detected from tokenizer_config.json — see
    # 04.7-RESEARCH.md §1.2).
    if e.reasoning_parser:
        cli += ["--reasoning-parser", e.reasoning_parser]
    if e.max_num_seqs is not None:
        cli += ["--max-num-seqs", str(e.max_num_seqs)]
    if e.tokenizer:
        cli += ["--tokenizer", e.tokenizer]
```

**Test coverage that must extend** (`tests/unit/test_docker_run_build.py` lines 89–96 establish the assertion pattern):

```python
def test_render_includes_prefix_caching_flag(rendered_args):
    """SERVE-07: --enable-prefix-caching must appear in the rendered CLI."""
    assert "--enable-prefix-caching" in rendered_args
```

Add three new assertions following the same pattern: `--reasoning-parser`, `--max-num-seqs`, `--tokenizer` each appear when the matching serving.yaml field is set (use a fixture variant of `_VALID_SERVING_YAML` that sets all three — pattern at lines 20–62).

---

### `tests/unit/test_profile_schema_gguf.py` (NEW)

**Analog:** `tests/unit/test_profile_schema_gemma4.py` (focused single-feature schema test, isolated)

**Pattern:** Pydantic-construction test that builds a minimal-valid `ServingConfig` with the new fields set, asserts no exception; then constructs a deliberately invalid variant (`quantization: "ggml"` — plausible typo) and asserts a `ValidationError`. Uses the same fixture style as `test_docker_run_build.py:_VALID_SERVING_YAML` (lines 20–62).

---

### `tests/unit/test_profile_bundle_mistral.py` (NEW)

**Analog:** (closest existing pattern is the round-trip approach in `test_schema.py`)

**Pattern:** load `profiles/mistral-medium-3.5/v1/` via `load_profile()` from `emmy_serve.profile.loader`, assert no exception. Optionally re-hash and assert equality with `profile.yaml.profile.hash`. Skip if profile bundle has not been authored yet (pytest skip marker keyed on bundle existence) — this is the "guard rail" for the eval-only profile being present in tree.

---

### Documentation Modifications

#### `CLAUDE.md`

**Analog lines 35** (Phase 4.1 dense siblings bullet — modeled by RESEARCH §7.1) — append a new bullet between line 35 and line 36 (KV ceiling row). Append-style edit, no replacement of existing text.

**Pattern (insertion, not modification):**

```markdown
- **Phase 04.7 heavyweight alternate (eval-only, NOT daily-driver):** Mistral Medium 3.5 128B v1 via bartowski Q4_K_M GGUF (~73 GB on disk; merged single-file). Container `vllm/vllm-openai:cu130-nightly-aarch64` (digest pinned at first boot). 128K context, gmu=0.78, max_num_seqs=1, kv_cache_dtype=fp8. Boots via `--quantization gguf` experimental backend (T-01 acknowledged). NOT a routes.yaml participant; NOT an `ask_claude.enabled` target. ~3 tok/s decode (bandwidth-bound 73 GB on GB10 LPDDR5X) — gate is bootability + first chat completion + tool call (NOT throughput, NOT thermal per CONTEXT D-05/D-06). Phase 5 dense-vs-MoE matrix participant.
```

**Pitfall #4 update** (line 71 — modeled by RESEARCH §7.1):

```markdown
... thermal-replay protocol — never measured on this hardware. **2026-05-01 update:** characterized on Spark across 4 prior profiles (8 h cumulative thermal-replay data, zero throttle observed); gate retained for daily-driver candidates, skipped for non-daily-driver alternates per Phase 04.7 D-06 precedent.
```

**Footer update** (line 135) — append dated note as the analog footer pattern.

#### `README.md`

**Analog:** README.md profile-ladder section. Append new subsection `### profiles/mistral-medium-3.5/` after the Gemma dense table (RESEARCH §7.2 specifies line 148).

#### `eval/MATRIX.md`

**Analog:** the matrix table at lines 8–16. Append one row following the existing column shape (RESEARCH §7.3). Add a new axis 4 to the Phase 5 axes (lines 32–38) for "Heavyweight quality vs throughput tradeoff."

#### `docs/runbook.md`

**Analog:** the "Available profiles (Phase 4.1)" table at line 808 + "Weight downloads" section at line 823. Append a row to the table for `mistral-medium-3.5@v1`; add a new subsection "Mistral 3.5 128B GGUF (multi-part)" with the §1.3 commands (`huggingface-cli download` → `gguf-split --merge` → cleanup). The "Swapping profiles" section at line 741 needs no body change — Mistral works through the same atomic 4-phase D-23 path.

---

## Shared Patterns

### Profile bundle clone-and-retarget

**Source:** `profiles/qwen3.6-27b/v1.1/PROFILE_NOTES.md` lines 69–110 (canonical "Phase 4.1 clone provenance" pattern); reused by `profiles/gemma-4-31b-it/v1.2/PROFILE_NOTES.md` lines 71–113.

**Apply to:** PROFILE_NOTES.md authorship for Mistral.

**Pattern:**
1. Name the clone source bundle with its full path AND source hash.
2. Two retargeted-fields tables: one for `serving.yaml`, one for `profile.yaml`.
3. Explicit "byte-identical from <source>" list of files (`harness.yaml + prompts/* + tool_schemas/* + grammars/* + subagents/*`).
4. Rationale paragraph for the byte-clone (per RESEARCH §6: Mistral parser shape diffs from Gemma's, but harness consumes parsed OpenAI-shape, so harness layer is model-agnostic — same justification Phase 4.1 used for Qwen 35B → 27B and Gemma 26B → 31B).

### Air-gap policy enforcement

**Source:** `emmy_serve/profile/schema.py:163–173` — `EnvVars._airgap_policy` model_validator.

**Apply to:** `serving.yaml` `env:` block on every shipped profile, including Mistral.

**Pattern (already enforced — Mistral inherits this for free):**

```python
@model_validator(mode="after")
def _airgap_policy(self) -> "EnvVars":
    if self.VLLM_NO_USAGE_STATS != "1":
        raise ValueError('env.VLLM_NO_USAGE_STATS must equal "1" (SERVE-09, D-12 layer c)')
    if self.HF_HUB_OFFLINE != "1":
        raise ValueError('env.HF_HUB_OFFLINE must equal "1" (REPRO-04, D-12 layer d)')
    return self
```

Mistral profile must set both to `"1"` in `serving.yaml` env block — the schema layer rejects otherwise.

### Container-image-digest sentinel discipline

**Source:** `emmy_serve/profile/schema.py:101–116` — `_digest_shape` field_validator.

**Apply to:** `engine.container_image_digest` on Mistral profile.

**Pattern:** Initial authoring uses `sha256:REPLACE_AT_FIRST_PULL` placeholder; schema rejects this sentinel at `emmy profile validate` time, forcing the operator to capture and paste the real digest from `docker inspect --format '{{.Id}}'` before `profile validate` can pass (G-5 acceptance gate in CONTEXT).

For Mistral specifically, RESEARCH §1.4 details the resolution order at first pull (`cu130-nightly-aarch64` then `cu130-nightly` then CONTEXT D-10 spelling).

### Boot smoke 4-step canary chain

**Source:** `scripts/smoke_test.py:44–187` — `wait_for_vllm` → `run_sp_ok` → `run_tool_call` → `run_generate`.

**Apply to:** Mistral G-1 + G-2 + G-3 acceptance gates collectively.

**Pattern:** invoke `bash scripts/start_emmy.sh --profile profiles/mistral-medium-3.5/v1`; the script chains all 4 canary calls and writes `canary.jsonl`. Exit 0 == G-1 + G-2 + G-3 all green. **No new code path needed.** The tool-call canary at `emmy_serve/canary/tool_call.py:33–64` is parser-format-agnostic (asserts `tool_calls[0].function.name == "read_file"` after vLLM has already converted the Mistral `[TOOL_CALLS]` envelope to OpenAI shape) — confirmed at lines 51–63.

### Schema-extension test isolation

**Source:** `tests/unit/test_profile_schema_gemma4.py` (single-feature schema test pattern), `tests/unit/test_docker_run_build.py:20–62` (fixture-seeded `_VALID_SERVING_YAML`).

**Apply to:** Plan 04.7-01 schema/renderer test additions.

**Pattern:** seed a tmp_path bundle with a fixture-shaped `_VALID_SERVING_YAML` that sets the new fields (`quantization: gguf`, `tokenizer: ...`, `reasoning_parser: ...`, `max_num_seqs: ...`); assert the schema validates AND the renderer emits the matching CLI flags. Avoids pulling in real on-disk profiles (which carry sentinel digests that schema rejects).

---

## Order-of-Operations Constraint (planner-relevant)

Per RESEARCH §9 question 8: schema + renderer changes (Plan 04.7-01 Wave 0) must land BEFORE the profile bundle (Plan 04.7-01 Wave 1). Reason:

- Without the `quantization: gguf` Literal extension, `emmy profile validate` rejects the Mistral bundle on first parse (G-5 fails).
- Without the renderer extension, `--reasoning-parser`, `--max-num-seqs`, `--tokenizer` are dropped on the floor at boot — Mistral fails to start (G-1 fails).

The planner therefore wave-orders Plan 04.7-01 as:

1. **Wave 0:** schema field extension (`quantization` Literal + `tokenizer` field) + renderer extension (3 new conditional emits) + tests for both (`test_profile_schema_gguf.py` + extend `test_docker_run_build.py`).
2. **Wave 1:** profile-bundle authorship (clone-and-retarget all 17 files).
3. **Wave 2:** `uv run emmy profile validate profiles/mistral-medium-3.5/v1` + `uv run emmy profile hash profiles/mistral-medium-3.5/v1 --write`.

Plan 04.7-02 (boot smoke) and Plan 04.7-03 (documentation) follow.

---

## Metadata

**Analog search scope:**
- `/data/projects/emmy/profiles/{gemma-4-31b-it,gemma-4-26b-a4b-it,qwen3.6-27b}/**` (full read on v1.1 / v1.2 / v2.1 bundles)
- `/data/projects/emmy/emmy_serve/profile/schema.py` (full read)
- `/data/projects/emmy/emmy_serve/boot/runner.py` (full read)
- `/data/projects/emmy/emmy_serve/canary/tool_call.py` (full read — confirmed format-agnosticism)
- `/data/projects/emmy/scripts/smoke_test.py` (full read)
- `/data/projects/emmy/tests/unit/test_docker_run_build.py` (lines 1–150 read)
- `/data/projects/emmy/docs/`, `/data/projects/emmy/eval/` (directory listings)

**Files scanned:** ~32 (profile bundles, Python source, test files, doc directories).

**Pattern extraction date:** 2026-05-01.

**Confidence:** HIGH on profile-bundle clone, schema/renderer extensions, and documentation insertion targets. MEDIUM on the grammars/tool_call.lark byte-clone-vs-Mistral-shape decision (planner falls back to RESEARCH §3.2 fallback playbook only if T-04 fires at boot smoke).
