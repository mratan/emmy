---
phase: 04.7
plan: 03
type: execute
wave: 3
depends_on: [04.7-02]
files_modified:
  - CLAUDE.md
  - README.md
  - eval/MATRIX.md
  - docs/runbook.md
autonomous: true
requirements: []
threats: []
tags: [docs, phase-04.7, claude-md, readme, eval-matrix, runbook]

must_haves:
  truths:
    - "CLAUDE.md Pinned Tech Stack section has a bullet documenting the Mistral 128B alternate (eval-only)"
    - "CLAUDE.md Pitfall #4 has the 2026-05-01 thermal-characterization update note (4 prior profiles, zero throttle, gate retained for daily-drivers only)"
    - "README.md profile-ladder section has a `### profiles/mistral-medium-3.5/` subsection with one-row v1 table"
    - "eval/MATRIX.md active-participants table has a row for Mistral 3.5 dense-128B (eval-only)"
    - "eval/MATRIX.md profile-swap-commands has the `/profile mistral-medium-3.5` line"
    - "docs/runbook.md Available-profiles table has a row for `mistral-medium-3.5@v1` and a NEW Weight-downloads subsection for the GGUF multi-part merge"
    - "All documentation values quote the converged gmu/digest/hash from Plan 04.7-02 (no placeholders left)"
    - "No documentation surface silently changes any profile default (Pitfall #1 trap)"
  artifacts:
    - path: CLAUDE.md
      provides: "Pinned Tech Stack row + Pitfall #4 status update + footer dated note"
      contains: "Mistral Medium 3.5 128B"
    - path: README.md
      provides: "Profile-ladder subsection for mistral-medium-3.5"
      contains: "profiles/mistral-medium-3.5/"
    - path: eval/MATRIX.md
      provides: "Phase 5 dense-128B participant row + new axis 4"
      contains: "mistral-medium-3.5"
    - path: docs/runbook.md
      provides: "Available-profiles row + GGUF Weight-downloads subsection"
      contains: "Mistral 3.5 128B GGUF (multi-part)"
  key_links:
    - from: CLAUDE.md (Pinned Tech Stack new bullet)
      to: profiles/mistral-medium-3.5/v1/serving.yaml
      via: "Pinned-row references the profile bundle"
      pattern: "mistral-medium-3.5"
    - from: docs/runbook.md (Weight-downloads subsection)
      to: scripts/start_emmy.sh + the host /data/models bind-mount
      via: "operator-runnable command sequence to merge the GGUF"
      pattern: "gguf-split --merge"
---

<objective>
Land the documentation surfaces for the Mistral profile across the four target files. This plan is doc-only; no Python source, no profile-bundle bytes change. Every value in every doc surface MUST be quoted from the Plan 04.7-02 SUMMARY (the actual converged gmu, the actual captured digest, the actual recomputed bundle hash) — no placeholders, no aspirational numbers.

Per CLAUDE.md Pitfall #1: documentation MUST NOT silently change any profile default. The four files ADD information about an existing profile bundle; they do not modify the bundle's behavior.

Sequence:
1. CLAUDE.md — insert Pinned Tech Stack bullet for Phase 04.7 alternate (after the dense-siblings bullet); update Pitfall #4 with the "characterized on Spark across 4 profiles, no throttle observed; gate retained for daily-driver candidates" note; append dated footer note.
2. README.md — insert `### profiles/mistral-medium-3.5/` subsection in the profile-ladder section, after the Gemma 31B dense block.
3. eval/MATRIX.md — append a row to the active-participants matrix table; add a new Phase 5 axis 4 (heavyweight quality vs throughput tradeoff); add `/profile mistral-medium-3.5` to the swap-commands cheat sheet; add Mistral PROFILE_NOTES.md to References.
4. docs/runbook.md — append a row to the Available-profiles table (rename heading to "Phase 4.1 + 04.7"); add a NEW "Mistral 3.5 128B GGUF (multi-part)" subsection under Weight-downloads with the gguf-split merge command sequence; append note to Container-per-family list.

Output: 4 modified docs files; G-6 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-CONTEXT.md
@.planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-RESEARCH.md
@.planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-01-SUMMARY.md
@.planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-02-SUMMARY.md
@CLAUDE.md
@README.md
@eval/MATRIX.md
@docs/runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md — Pinned Tech Stack bullet + Pitfall #4 update + dated footer</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full read; Pinned Tech Stack section starts at line 29; Phase 4.1 dense-siblings bullet at line 35; KV-ceiling bullet at line 36; Pitfall #4 at line 71; footer dated note at line 135)
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-RESEARCH.md §7.1 (CLAUDE.md edit map with line numbers)
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-CONTEXT.md D-04 + D-05 + D-06 + D-13 (the values that the new bullet quotes)
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-02-SUMMARY.md (the converged gmu, the actual container digest, the bundle hash — these go into the bullet verbatim)
    - profiles/mistral-medium-3.5/v1/serving.yaml (the actual `container_image:` tag the executor wrote; the actual gmu and max_model_len after Plan 04.7-02 step-down if any)
    - profiles/mistral-medium-3.5/v1/profile.yaml (the actual `profile.hash` value for citation)
  </read_first>
  <action>
    **A. Insert Pinned Tech Stack bullet (after line 35, before line 36 — between "Phase 4.1 dense siblings" bullet and "Hardware-level KV ceiling" bullet).**

    Use Edit tool with the surrounding existing bullet text as the unique anchor. Pattern:

    OLD ANCHOR (the line ending in "Note: Qwen 27B dense lives on as the only Qwen profile in the active stack..."):
    Find the exact end of the Phase 4.1 dense-siblings bullet (line 35). Use the Edit tool to insert the new bullet immediately after that bullet's terminating newline.

    NEW BULLET (insert as a new bullet starting with `- **Phase 04.7 heavyweight alternate (eval-only, NOT daily-driver):**`). Quote the actual values from Plan 04.7-02 (replace `<X.XX>`, `<digest-prefix>`, `<hash-prefix>` placeholders with real values from the bundle):

    ```markdown
    - **Phase 04.7 heavyweight alternate (eval-only, NOT daily-driver):** `mistralai/Mistral-Medium-3.5-128B` v1 via bartowski Q4_K_M GGUF (~73 GB on disk; merged single-file via `gguf-split --merge`). Container `vllm/vllm-openai:<actual-tag-from-Plan-04.7-02-Task-1>` (digest `sha256:<digest-prefix>...` pinned at first boot per CONTEXT D-10). 128K context (D-03; 256K deferred to v2 per OOS list), gmu=<X.XX> (D-04; converged via D-05 substituted protocol — single-run smoke + step-down rather than formal KV-bisection), max_num_seqs=1 (D-07), kv_cache_dtype=fp8 (D-08, mandatory at this size), tool_call_parser=mistral + reasoning_parser=mistral + enable_auto_tool_choice=true (D-09; vLLM nightly emits `--reasoning-parser` since Plan 04.7-01 renderer fix). Boots via `--quantization gguf` experimental backend (T-01 acknowledged; Mistral flagged long-context as WIP). NOT a routes.yaml participant; NOT an `ask_claude.enabled` flip target (D-12). Estimated decode ~3 tok/s (bandwidth-bound 73 GB on GB10 LPDDR5X) — gate is **bootability + first chat completion + tool call** (D-13), NOT throughput, NOT thermal (D-06 skip per "8 h prior data, zero throttle" thesis). Phase 5 dense-vs-MoE matrix participant; bundle hash `sha256:<hash-prefix>...`.
    ```

    **B. Append note to "Hardware-level KV ceiling" bullet at line 36.**

    Use Edit. Find the OLD line ending (the closing period after "...sibling versions that re-target operator comfort, not no-preempt ceiling).") and replace with:

    ```
    ...sibling versions that re-target operator comfort, not no-preempt ceiling). **Note (Phase 04.7):** Mistral 128B Q4_K_M's gmu=<X.XX> is structurally required (weights+KV math: 73 GB weights + 22 GB FP8 KV at 128K + ~5 GB scratch must fit within `gmu × 128 GB` pool), not bisected — D-05 skip for non-daily-driver alternates.
    ```

    Substitute the actual converged gmu from the shipped bundle.

    **C. Update Pitfall #4 (line 71).**

    Use Edit. Find the OLD line:
    ```
    4. **DGX Spark thermal throttle** — short benchmarks look fine; 2-hour sessions throttle 2.8→~2 GHz. Run a 2-hour sustained-load thermal validation per profile.
    ```

    Replace with (per RESEARCH §7.1 + CONTEXT D-06):
    ```
    4. **DGX Spark thermal throttle** — short benchmarks look fine; 2-hour sessions throttle 2.8→~2 GHz. Run a 2-hour sustained-load thermal validation per profile. **2026-05-01 update (Phase 04.7 D-06):** characterized on Spark across 4 prior profiles (8 h cumulative thermal-replay data: Qwen 35B-A3B v3, Qwen 27B v1, Gemma 26B-A4B v2, Gemma 31B v1 — all preemptions=0, oom=0, no clock dip below 2.4 GHz). Original "2.8→2 GHz throttle" claim originates from `.planning/research/PITFALLS.md:815` citing an NVIDIA Developer Forum report — never measured on this hardware. Gate retained for daily-driver candidates; skipped for non-daily-driver alternates (Phase 04.7 Mistral 128B Q4_K_M is the precedent — eval-only, occasional-use, lighter thermal envelope at ~3 tok/s).
    ```

    **D. Append dated footer note (after line 135 — the existing 2026-04-30 Phase 04.6 footer).**

    Use Edit. Find the OLD anchor (the existing 2026-04-30 footer line) and append a new footer note after it. The 2026-04-28 daily-driver-switch footer at line 136 also stays. Insert the new footer between or after them, preserving chronological order:

    ```markdown
    *Updated 2026-05-01 — Phase 04.7 Mistral Medium 3.5 128B alternate documented: heavyweight eval-only profile via bartowski Q4_K_M GGUF; vLLM nightly `--quantization gguf` experimental backend; eval-only opt-in `/profile mistral-medium-3.5`; CONTEXT D-05/D-06 skip KV-bisect + thermal-replay per "8 h prior data, zero throttle" thesis. Schema/renderer extension landed in Plan 04.7-01: `EngineConfig.quantization` Literal grew "gguf"; `EngineConfig.tokenizer` field added; `render_vllm_cli_args` now emits `--reasoning-parser` / `--max-num-seqs` / `--tokenizer` (the previously-orphaned flags benefit all profiles).*
    ```

    **E. Constraint — NO behavioral changes to documented defaults of OTHER profiles.**

    The Pitfall #4 update touches the gate scope (daily-drivers only). It does NOT change any profile's default values. Per CLAUDE.md Pitfall #1 ("more prompting" trap analog for docs): if executing this task tempts you to "while we're here, update profile X's row," STOP — that is out of scope and creates silent drift.
  </action>
  <verify>
    <automated>cd /data/projects/emmy &amp;&amp; grep -c "Phase 04.7 heavyweight alternate" CLAUDE.md &amp;&amp; grep -c "2026-05-01 update (Phase 04.7 D-06)" CLAUDE.md &amp;&amp; grep -c "Phase 04.7 Mistral Medium 3.5 128B alternate documented" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 04.7 heavyweight alternate" /data/projects/emmy/CLAUDE.md` returns `1`
    - `grep "Mistral Medium 3.5 128B" /data/projects/emmy/CLAUDE.md | head -1` returns the new bullet (visible in the Pinned Tech Stack section)
    - `grep "2026-05-01 update (Phase 04.7 D-06)" /data/projects/emmy/CLAUDE.md` returns one line containing the Pitfall #4 thermal-characterization note
    - `grep "8 h cumulative thermal-replay data" /data/projects/emmy/CLAUDE.md` returns one match (precise wording check)
    - `grep -c "gate retained for daily-driver candidates" /data/projects/emmy/CLAUDE.md` returns at least `1` (in the new Pitfall #4 wording)
    - `grep "Updated 2026-05-01 — Phase 04.7" /data/projects/emmy/CLAUDE.md` returns the new footer
    - `grep "Hardware-level KV ceiling" /data/projects/emmy/CLAUDE.md` returns one line; the line that follows the bullet contains "Mistral 128B Q4_K_M's gmu=<actual-value>" (verifiable via `grep -A 0 "Hardware-level KV ceiling" CLAUDE.md`)
    - `grep -c "gemma-4-26b-a4b-it" /data/projects/emmy/CLAUDE.md` is unchanged from pre-edit count (no accidental edits to other profile rows — verify with `git diff CLAUDE.md` showing only additions in the new locations)
    - `grep -c "v2.1" /data/projects/emmy/CLAUDE.md` is unchanged from pre-edit count (Gemma daily-driver row untouched)
  </acceptance_criteria>
  <done>
    CLAUDE.md has the new Phase 04.7 bullet, the updated Pitfall #4, the appended footer; no other profile rows or defaults changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md profile-ladder + eval/MATRIX.md + docs/runbook.md</name>
  <files>README.md, eval/MATRIX.md, docs/runbook.md</files>
  <read_first>
    - README.md (full read of profile-ladder section starting near line 111; the `### profiles/qwen3.6-27b/` subsection at ~131 + `### profiles/gemma-4-31b-it/` at ~138 are the analog subsections to clone shape from; line 146 has the "Phase 4.1 dense profiles are additive..." paragraph which the new subsection slots in after)
    - eval/MATRIX.md (full read; matrix table at lines 8-16; Notes at 22; Phase 5 axes at 32; Profile swap commands at 42; References at 55)
    - docs/runbook.md (focused read; line 741 Swapping profiles section header — body needs no change; line 808 Available profiles table — append row + rename heading; line 821 Container-per-family — append note; line 823 Weight downloads — add NEW subsection)
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-RESEARCH.md §7.2 + §7.3 + §7.4 (exact line numbers + insertion shapes for each file)
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-RESEARCH.md §1.3 (the `huggingface-cli download` + `gguf-split --merge` command sequence — verbatim into runbook Weight-downloads subsection)
    - .planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-02-SUMMARY.md (the actual converged gmu, actual container digest prefix, actual bundle hash, the actual run_id of the boot smoke — these populate the eval/MATRIX.md row + runbook table)
  </read_first>
  <action>
    **A. README.md — insert `### profiles/mistral-medium-3.5/` subsection after the Gemma dense table.**

    Find the line "**Phase 4.1 dense profiles are additive + opt-in.**" (line ~146 per RESEARCH §7.2). The new subsection slots BEFORE that paragraph or AFTER its closing paragraph (executor's choice; cleaner is BEFORE it, so the additive-paragraph references all four eval profiles). The minor header before line 146 ("To swap to a dense profile inside a running pi-emmy TUI..." at line 148) MUST remain untouched.

    Insert the new subsection (between Gemma 31B v1.2 row's closing prose and the "additive" paragraph):

    ```markdown
    ### `profiles/mistral-medium-3.5/` (Phase 04.7 heavyweight alternate — eval-only, opt-in)

    | Version | Status | Bootable | Notes |
    |---|---|---|---|
    | **v1** | **2026-05-01 — DEFAULT_VARIANT (only version)** | **yes** | Heavyweight 128B-class data point for the Phase 5 dense-vs-MoE matrix. Source: `bartowski/mistralai_Mistral-Medium-3.5-128B-GGUF` Q4_K_M (multi-part, merged single-file ~73 GB). Engine: vLLM nightly `vllm/vllm-openai:<actual-tag>` (digest `sha256:<digest-prefix>...`) with `--quantization gguf` experimental backend. 128K context (CONTEXT D-03; 256K deferred), gmu=<X.XX> (CONTEXT D-04 — structurally required to fit weights+KV; departs from sibling 0.55 RAM-headroom convention), kv_cache_dtype=fp8, max_num_seqs=1, tool_call_parser=mistral + reasoning_parser=mistral. NOT a daily-driver, NOT a routes.yaml participant, NOT an ask_claude.enabled target (D-12). Estimated decode ~3 tok/s (bandwidth-bound). Boot-only validation per CONTEXT D-05/D-06 — no formal KV-bisection, no 2× 2h thermal replay; the "8 h prior data, zero throttle" thesis covers the lighter-load eval-only posture. `sha256:<hash-prefix>...` Phase 5 evaluates this against the 3 active profiles in the dense-vs-MoE matrix.
    ```

    Substitute the placeholder values (`<actual-tag>`, `<digest-prefix>`, `<hash-prefix>`, `<X.XX>`) from Plan 04.7-02 SUMMARY + the shipped serving.yaml.

    Also: the introductory paragraph at line ~146 ("Phase 4.1 dense profiles are additive + opt-in...") may benefit from a parenthetical `(plus the Phase 04.7 Mistral 128B alternate, eval-only — see below)` per RESEARCH §7.2 line 454. Append that parenthetical at the END of the existing first sentence in that paragraph (no other text changes).

    Also: the `/profile qwen3.6-27b` examples on line 148 stay untouched. Optionally append "or `/profile mistral-medium-3.5`" at the end of that sentence.

    **B. eval/MATRIX.md — append matrix row + new axis 4 + swap-command line + reference.**

    1. Append a row to the table at lines 9-16. After the last row (line 16, the Gemma 31B `v1` audit row), insert:

    ```markdown
    | Mistral 3.5 | **Dense (128B alternate, eval-only)** | `mistral-medium-3.5` | **`v1`** (only version) | 128B (dense) | GGUF Q4_K_M (bartowski) | upstream `vllm/vllm-openai:<actual-tag>` | **<X.XX>** | **131072 (128K)** | `sha256:<hash-prefix>...` | **2026-05-01 INSERTED — Phase 04.7 closeout** | <smoke-tokps-or-"~3 est"> | n/a (D-06 skip) | dense-128B (opt-in) |
    ```

    2. After the matrix's `## Phase 5 axes` section (line 32+), find the existing axes (1, 2, 3) and add:

    ```markdown
    4. **Heavyweight quality vs throughput tradeoff** — does the 128B-class GGUF profile clear correctness gates (Mistral baseline: SWE-bench Verified 77.6%, τ³-Telecom 91.4% per HF card) at ~3 tok/s decode, and does that quality justify the throughput cost for occasional escalations? Phase 04.7 ships the profile bootable; Phase 5 measures.
    ```

    3. Find the `## Profile swap commands` section (line 42+). After the existing Phase 4.1 dense entries (around line 48), append:

    ```markdown
    # Phase 04.7 heavyweight alternate (eval-only, opt-in):
    /profile mistral-medium-3.5    # Mistral 128B GGUF v1 (128K, gmu=<X.XX>, ~3 tok/s) — Phase 04.7 alternate, eval-only
    ```

    Insert this inside the existing fenced code block (do NOT open a new fence).

    4. Find the `## References` section (line 55+) and append:

    ```markdown
    - `profiles/mistral-medium-3.5/v1/PROFILE_NOTES.md` — Mistral 128B GGUF Phase 04.7 provenance + D-05/D-06 honesty section (no formal KV-bisection, no thermal replay; gmu structurally required at <X.XX>)
    ```

    **C. docs/runbook.md — append Available-profiles row + rename heading + add Weight-downloads subsection + Container-per-family note.**

    1. Find the heading at line 808 — `### Available profiles (Phase 4.1)`. Rename to `### Available profiles (Phase 4.1 + 04.7)`.

    2. Append a row to that table for `mistral-medium-3.5@v1`. Use the exact column shape of the existing rows (read lines 808-820 to see the column set; common columns: profile id, HF source, weights path, container, cold boot, gmu, notes). Quote the actual values from Plan 04.7-02 — `bartowski/mistralai_Mistral-Medium-3.5-128B-GGUF`, `/data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf` (host) / `/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf` (container), `vllm/vllm-openai:<tag>`, "~5-10 min cold boot estimate (first-run; nightly container, not benchmarked)", `<X.XX>` gmu, "Eval-only; GGUF experimental backend; multi-part split must be merged via `gguf-split --merge` (see Weight downloads subsection below)".

    3. Find line ~821 (Container-per-family list per RESEARCH §7.4). Append:

    ```
    Mistral profiles boot on `vllm/vllm-openai:<actual-tag>` — third family-specific container (Qwen → NGC fastsafetensors; Gemma → upstream Day-1; Mistral → upstream nightly aarch64).
    ```

    4. Find the "Weight downloads" subsection at line 823. After the existing weight-download blocks (per-existing-profile), insert a NEW subsection titled `#### Mistral 3.5 128B GGUF (multi-part)`. Body is verbatim from RESEARCH §1.3 lines 118-138, with one annotation: replace the `# Step 2 — install gguf-split` block with the resolution-order check (`which llama-gguf-split` first; build only if missing). Concrete shape:

    ```bash
    # 1. Download all parts (HF CLI handles glob)
    huggingface-cli download bartowski/mistralai_Mistral-Medium-3.5-128B-GGUF \
      --include "mistralai_Mistral-Medium-3.5-128B-Q4_K_M/*" \
      --local-dir /data/models/Mistral-Medium-3.5-128B-Q4_K_M-parts

    # 2. Locate or build llama.cpp's gguf-split tool
    GGUF_SPLIT="$(which llama-gguf-split 2>/dev/null)"
    if [ -z "$GGUF_SPLIT" ]; then
      git clone https://github.com/ggml-org/llama.cpp /tmp/llama.cpp
      cd /tmp/llama.cpp && cmake -B build && cmake --build build --config Release -j
      GGUF_SPLIT=/tmp/llama.cpp/build/bin/llama-gguf-split
    fi

    # 3. Merge — point at FIRST shard; tool auto-discovers shards 2..N
    FIRST=$(ls /data/models/Mistral-Medium-3.5-128B-Q4_K_M-parts/mistralai_Mistral-Medium-3.5-128B-Q4_K_M/*-00001-of-*.gguf | head -1)
    "$GGUF_SPLIT" --merge "$FIRST" /data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf

    # 4. Verify single-file size (~73 GB) and GGUF magic header
    ls -lh /data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf
    head -c 4 /data/models/Mistral-Medium-3.5-128B-Q4_K_M.gguf | xxd | head -1
    # → "00000000: 4747 5546                                GGUF"

    # 5. Reclaim disk
    rm -rf /data/models/Mistral-Medium-3.5-128B-Q4_K_M-parts
    ```

    Add a one-paragraph header to that subsection explaining: "GGUF requires single-file input for vLLM (per https://docs.vllm.ai/en/stable/features/quantization/gguf/). Plain `cat` does NOT produce a valid merged GGUF — shard headers carry per-shard metadata that `gguf-split --merge` reconstitutes."

    5. The "Swapping profiles" section at line 741 needs NO body changes (per RESEARCH §7.4 line 472 — the atomic 4-phase D-23 path is unchanged for Mistral). The new Available-profiles table row above is sufficient.
  </action>
  <verify>
    <automated>cd /data/projects/emmy &amp;&amp; grep -c "profiles/mistral-medium-3.5/" README.md &amp;&amp; grep -c "mistral-medium-3.5" eval/MATRIX.md &amp;&amp; grep -c "Mistral 3.5 128B GGUF (multi-part)" docs/runbook.md &amp;&amp; grep -c "Available profiles (Phase 4.1 + 04.7)" docs/runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### \`profiles/mistral-medium-3.5/\`" /data/projects/emmy/README.md` returns `1` (new subsection header)
    - `grep "Phase 04.7 heavyweight alternate — eval-only" /data/projects/emmy/README.md` returns one line in the new subsection
    - `grep -c "mistral-medium-3.5" /data/projects/emmy/eval/MATRIX.md` returns at least `2` (table row + swap-commands line + references — ≥3 occurrences in practice)
    - `grep "Heavyweight quality vs throughput tradeoff" /data/projects/emmy/eval/MATRIX.md` returns one line in the Phase 5 axes section
    - `grep "/profile mistral-medium-3.5" /data/projects/emmy/eval/MATRIX.md` returns one line inside the swap-commands code block
    - `grep "Available profiles (Phase 4.1 + 04.7)" /data/projects/emmy/docs/runbook.md` returns one line (heading renamed)
    - `grep -c "mistral-medium-3.5@v1" /data/projects/emmy/docs/runbook.md` returns at least `1` (Available-profiles table row)
    - `grep -c "Mistral 3.5 128B GGUF (multi-part)" /data/projects/emmy/docs/runbook.md` returns `1` (new Weight-downloads subsection heading)
    - `grep -c "gguf-split --merge" /data/projects/emmy/docs/runbook.md` returns at least `1` (the merge command in the new subsection)
    - `grep -c "third family-specific container" /data/projects/emmy/docs/runbook.md` returns `1` (the Container-per-family note append)
    - `grep -c "huggingface-cli download bartowski" /data/projects/emmy/docs/runbook.md` returns at least `1` (the download command in the new subsection)
    - No placeholder strings remain: `grep -c "<X.XX>\|<digest-prefix>\|<hash-prefix>\|<actual-tag>\|<smoke-tokps" /data/projects/emmy/CLAUDE.md /data/projects/emmy/README.md /data/projects/emmy/eval/MATRIX.md /data/projects/emmy/docs/runbook.md` returns `0` (every placeholder substituted with real values from Plan 04.7-02 SUMMARY)
    - `git diff README.md eval/MATRIX.md docs/runbook.md | grep -E '^-[^-]' | grep -v "^---"` returns empty (additive-only edits — no existing rows or text removed) OR contains only the heading rename `Available profiles (Phase 4.1)` -> `Available profiles (Phase 4.1 + 04.7)` and the explicit-anchor edit on README.md's "additive" paragraph parenthetical
  </acceptance_criteria>
  <done>
    README.md has the new Mistral subsection; eval/MATRIX.md has the matrix row + new axis 4 + swap-commands line + reference; docs/runbook.md has the renamed Available-profiles heading + new row + new Weight-downloads subsection + Container-per-family note. All placeholder values resolved to real numbers from Plan 04.7-02 SUMMARY. G-6 (documentation in tree) green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files → operator's mental model | Doc surfaces are read by humans, not parsed by code paths. Misinformation in docs causes operator confusion, not runtime failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| 04.7-03-S1 | I (Info disclosure — wrong defaults) | Documentation surfaces silently changing the documented gmu/max_model_len/digest values away from what the bundle actually carries | mitigate | Acceptance criteria in Task 1 + Task 2 require every value to be quoted from the shipped bundle (`grep` checks for placeholder strings). The "no placeholder remaining" check is a hard gate. |
| 04.7-03-S2 | T (Tampering — Pitfall #1 trap) | Doc updates accidentally change another profile's documented behavior (e.g. trimming Pitfall #4 in a way that weakens the gate for daily-drivers) | mitigate | Task 1 explicit constraint: scope is ADDING information about Mistral + UPDATING Pitfall #4 status (gate scope, not gate strength). Acceptance check: `grep -c "v2.1" CLAUDE.md` and `grep -c "gemma-4-26b-a4b-it" CLAUDE.md` are unchanged from pre-edit count. Use `git diff CLAUDE.md` for pre-merge review. |
| 04.7-03-S3 | I (Info disclosure — operator deception) | Pitfall #4 update could be read as "thermal validation is no longer needed" | mitigate | The new Pitfall #4 wording is explicit: "gate retained for daily-driver candidates; skipped for non-daily-driver alternates" — the gate is unchanged for new daily-drivers, only narrowed for explicit eval-only profiles. |

**No high-severity threats.** This plan touches no runtime code, no profile bytes, no schema. Pitfall #1 (the prompting trap) has a doc-analog: "do not change profile defaults silently while editing docs." Acceptance criteria backstop the constraint with grep-based diff sanity checks.
</threat_model>

<verification>
**Plan 04.7-03 closes when:**

- [ ] CLAUDE.md has the Phase 04.7 Pinned Tech Stack bullet, the updated Pitfall #4, and the new dated footer
- [ ] CLAUDE.md Hardware-level KV ceiling bullet has the Mistral note appended
- [ ] README.md has the `### profiles/mistral-medium-3.5/` subsection populated with real values
- [ ] eval/MATRIX.md has the new matrix row (8th row total), Phase 5 axis 4, swap-commands line, and reference
- [ ] docs/runbook.md has the renamed Available-profiles heading, the new row, the new Weight-downloads subsection with the `gguf-split --merge` command sequence, and the Container-per-family note append
- [ ] Every quoted value (gmu, digest, hash, container tag) matches the actual values in `profiles/mistral-medium-3.5/v1/serving.yaml` and `profiles/mistral-medium-3.5/v1/profile.yaml` (no placeholder strings remain)
- [ ] `git diff CLAUDE.md README.md eval/MATRIX.md docs/runbook.md` shows additive-only changes (plus the Available-profiles heading rename and the README "additive" parenthetical)
- [ ] `cd /data/projects/emmy && uv run pytest tests/unit/ -x --no-cov` and `cd /data/projects/emmy/packages/emmy-ux && bun test` continue to pass — G-7 stays green (this plan touches no source so no regression risk)

**Acceptance gate ties (G-1..G-7 from CONTEXT § Acceptance Gates):**
- G-6 (documentation in tree — CLAUDE.md, README.md, eval/MATRIX.md, runbook all updated and committed) — closed by this plan.
- G-7 (no regressions) — preserved (doc-only).
- G-1..G-5 — closed by Plans 04.7-01 + 04.7-02.

**Phase 04.7 closeout follows:** All 7 acceptance gates green; STATE.md "Phase 04.7 CLOSED" entry; `/gsd-verify-work` runs the full pytest + bun test suite + a sanity `bash scripts/start_emmy.sh --profile profiles/mistral-medium-3.5/v1 --port 8002` exit-0 check.
</verification>

<success_criteria>
1. **CLAUDE.md updated.** New Pinned Tech Stack bullet for Phase 04.7 alternate; Pitfall #4 has the 4-profiles-zero-throttle update; KV-ceiling bullet has the Mistral note appended; new dated 2026-05-01 footer.
2. **README.md updated.** `### profiles/mistral-medium-3.5/` subsection slotted into the profile-ladder section, after Gemma 31B v1.2.
3. **eval/MATRIX.md updated.** Matrix table has the new Mistral row; Phase 5 axes has new axis 4; swap-commands has the `/profile mistral-medium-3.5` line; references include PROFILE_NOTES.md.
4. **docs/runbook.md updated.** Available-profiles heading renamed; new row for `mistral-medium-3.5@v1`; new Weight-downloads subsection with the `gguf-split --merge` command sequence; Container-per-family note appended.
5. **All values real, not placeholder.** Every gmu, digest, hash, tag, run_id quoted in any of the 4 docs files matches the shipped Mistral bundle bytes (no `<X.XX>` / `<digest-prefix>` / `<hash-prefix>` / `<actual-tag>` strings remain).
6. **No silent profile-default changes (Pitfall #1).** `git diff` of the 4 docs files contains only additive edits + the explicit Available-profiles heading rename + the README "additive" paragraph parenthetical. No other profile's documented gmu / max_model_len / digest / hash changed.
7. **No regressions.** Full pytest + bun test suite continues to pass (this plan touches no source code).
</success_criteria>

<output>
After completion, create `.planning/phases/04.7-mistral-medium-3-5-128b-alternate/04.7-03-SUMMARY.md` per the standard SUMMARY template, including:

- Files modified: 4 (CLAUDE.md, README.md, eval/MATRIX.md, docs/runbook.md).
- The specific values that filled placeholders (gmu, digest, hash, container tag) — for closeout traceability.
- Whether the README.md "additive" paragraph parenthetical was added.
- Whether the README.md `/profile` examples line was extended with the Mistral example.
- Test-suite stats at close (full pytest + bun test green).
- Phase 04.7 closeout status: all 7 acceptance gates green (G-1..G-7).
- Open follow-ups for Phase 5 eval matrix run (the only downstream consumer).
</output>
