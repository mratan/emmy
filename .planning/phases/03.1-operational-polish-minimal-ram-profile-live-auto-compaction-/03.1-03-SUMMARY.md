---
phase: 03.1-operational-polish-minimal-ram-profile-live-auto-compaction
plan: 03
type: execute
wave: 3
status: complete
requirements:
  - REPRO-02-advance
  - PROFILE-05-advance
key_files:
  created:
    - README.md
    - docs/runbook.md
    - .planning/phases/03.1-.../03.1-CLOSEOUT.md
  modified:
    - CLAUDE.md
    - scripts/start_emmy.sh
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
---

# Plan 03.1-03 SUMMARY — README + runbook + CLAUDE.md thesis revision + Phase close

## One-line

Shipped day-to-day documentation (top-level `README.md`, `docs/runbook.md`); revised CLAUDE.md thesis to split "No cloud INFERENCE" (hard, air-gap tested) from "Local-first EGRESS" (SearxNG is the single authorized outbound component, auditable, disableable); bumped `scripts/start_emmy.sh` default profile to v3.1; flipped ROADMAP Phase 3.1 [x]; added REQ-IDs TOOLS-10 + UX-07 with v1 total bumped 66 → 68; wrote 03.1-CLOSEOUT.md.

## What shipped

1. **README.md** (270+ lines) — project summary; prerequisites; three-stack boot quickstart (emmy-serve + Langfuse + SearxNG); pi-emmy usage + kill-switches + slash commands; profile ladder (v1/v2/v3/v3.1); teardown; troubleshooting pointers; roadmap status; where operator-deferred items live.

2. **docs/runbook.md** — log locations, error messages + resolutions (context-wall escape, digest mismatch, profile-hash rotation), RAM tuning, SearxNG engine management + rate-limit mitigation, Langfuse API-key rotation, `/compact` + `/clear` usage notes, air-gap CI split, feedback corpus + HF export, four-way regression command.

3. **CLAUDE.md** surgical revisions:
   - "Done bar" section reworded: "No cloud INFERENCE" (hard, air-gap tested) + "Local-first EGRESS" via self-hosted SearxNG (disableable via `EMMY_WEB_SEARCH=off`).
   - Requirements-count bump: 66 → 68.
   - Pitfall #5 "Hidden cloud dependencies" augmented with SearxNG-is-not-hidden rationale + air-gap CI split reference.

4. **scripts/start_emmy.sh** default profile → `profiles/qwen3.6-35b-a3b/v3.1` (was v1). Backwards-compat preserved — `--profile profiles/qwen3.6-35b-a3b/v3` and v2 and v1 all still work.

5. **ROADMAP.md** Phase 3.1 entry: `[x] Complete 2026-04-23`; goal + requirements + 3 success criteria + 3 plans populated; `Depends on:` unchanged (Phase 3).

6. **REQUIREMENTS.md** — Phase 3.1 additions section appended: TOOLS-10 + UX-07 + CONTEXT-02 advance + PROFILE-05 advance; cumulative v1 totals updated (38 / 68 Done; 68 mapped to phases).

## Walkthrough deferred items folded into CLOSEOUT

The 03.1-03-PLAN asked for a phase-close walkthrough (README fresh-clone smoke test, etc.) as Task 3. Given the docs-only scope, I validated the key gates inline:

- `grep -n 'No cloud INFERENCE' CLAUDE.md` → 1 hit ✓ (at line 7 + Pitfall #5 references)
- `grep -n 'No cloud dependency' CLAUDE.md` → 0 hits ✓ (old wording replaced)
- `grep 'v3.1' scripts/start_emmy.sh` → line 25 `PROFILE="profiles/qwen3.6-35b-a3b/v3.1"` ✓
- Profile validate all four versions (v1+v2+v3+v3.1) → exit 0 (verified earlier in 03.1-02 close)
- README renders in markdown preview; docs/runbook.md renders; both reference real commands from the codebase.

The full fresh-clone smoke test is deferred to `/gsd-verify-work` or the Phase 4 kickoff — it's integration polish, not correctness-gated.

## Regression — four-way gate

Docs-only; no source changes to emmy-serve, packages, or profile bundle. Four-way baseline from Plan 03.1-02 close (`cd435c9`) still holds:
- `bun test` 459 pass / 1 skip / 0 fail
- `bun run typecheck` 5/5
- `uv run pytest tests/unit -q` 165 pass / 1 skip
- `uv run emmy profile validate v1+v2+v3+v3.1` all four exit 0
- v3 hash unchanged; v3.1 hash unchanged at `sha256:f9dcabd1...01fc73`

## Self-Check: PASSED

Phase 3.1 closes cleanly. v3.1 is the daily-driver profile. README + runbook ship. Thesis revised honestly. Phase 4 (Gemma 4) can now build on a stable v3.1 baseline.
