---
phase: 04.5
plan: 07
subsystem: subagent-closeout
tags: [closeout, V8, e2e, runbook, operator-gated, pitfall-18, pitfall-22, W1]
requires: [04.5-01, 04.5-02, 04.5-03, 04.5-04, 04.5-05, 04.5-06]
provides:
  - "session.ts Agent tool wiring (createSubAgentTool injected into customTools when subagents enabled)"
  - "v8-real-deal-e2e.ts operator-runnable E2E + W1 4-level trace assertion"
  - "docs/runbook.md Sub-agents section (Pitfall #18 + #22 callouts, persona registry, observability diagram)"
affects: [packages/emmy-ux/src/session.ts, packages/emmy-ux/src/pi-emmy-extension.ts, docs/runbook.md]
tech-stack:
  added: []
  patterns: ["lateBound _sessionRef closure for parentInputTokens accessor", "in-memory span exporter for operator-replayable trace evidence", "placeholder runs/ files overwritten by operator V8 run"]
key-files:
  created:
    - packages/emmy-ux/scripts/v8-real-deal-e2e.ts
    - packages/emmy-ux/tests/session.subagent-wiring.test.ts
    - .planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-trace-tree.txt
    - .planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-sidecar-sample.jsonl
  modified:
    - packages/emmy-ux/src/session.ts
    - packages/emmy-ux/src/pi-emmy-extension.ts
    - docs/runbook.md
key-decisions:
  - "Agent tool wired INSIDE buildRealPiRuntime (not outside) so it gets parentServices reference; piFactory stub path bypasses (intentional — stub tests don't need Agent)"
  - "Placeholder runs/ evidence files committed; operator overwrites via SC walkthrough"
  - "SubagentBlock TUI wiring deferred to phase 04.5-followup (TODO marker grep-discoverable in pi-emmy-extension.ts) — pi 0.68 lacks tool_render hook"
  - "Profile hash bumps DEFERRED — adding new files within v3.1/v1.1/v2 dirs does change content hash, but bumping all 4 profiles is closeout-followup work, not Phase 04.5 acceptance"
requirements-completed: []
duration: ~50 min (autonomous portions; Task 3 operator-gated)
completed: 2026-04-26 (Tasks 1-2; Task 3 pending operator)
---

# Phase 04.5 Plan 07: V8 closeout Summary (Tasks 1-2 autonomous; Task 3 operator-gated)

`createEmmySession`'s `buildRealPiRuntime` now wires the `Agent` tool into
`customTools` when the active profile has `subagents.enabled: true`. The V8
runnable harness exists; runbook documents the entire sub-agent surface
including Pitfall #22 (prefix-cache informational only) and the W1 LOCKED
4-level trace tree. Task 3 — the live-Qwen SC walkthrough — is captured as
operator-gated work with placeholder evidence files committed.

**Files:** 4 created (1 src + 1 test + 2 runs/ placeholders), 3 modified
(session.ts, pi-emmy-extension.ts, docs/runbook.md)
**Tests:** 5 new wiring contract tests
**Total Phase 04.5 tests:** 103 pass / 0 fail / 323 expect calls
**Typecheck:** clean (both emmy-tools and emmy-ux)

## Phase 04.5 closeout — V1-V7 verdict

| ID | Spec | Test file | Status |
|----|------|-----------|--------|
| V1 | Pattern A (lean) spawn+execute < 100ms | `dispatch.test.ts` | ✓ pass |
| V2 | Pattern B (persona) spawn+execute with agentsFilesOverride | `dispatch.test.ts` | ✓ pass |
| V2-cwd | B2 fix: child operates on parentCwd, NOT personaDir | `dispatch.test.ts` | ✓ pass |
| V3 | toolAllowlist enforcement (pi-rejection-at-registration) | `allowlist.test.ts` | ✓ pass |
| V4 | OTel parent → tool → subagent 4-level chain (W1) | `otel-propagation.test.ts` | ✓ pass |
| V5 | Concurrent children no cross-talk | `governor.test.ts` | ✓ pass |
| V5-over-cap | I3: 3rd concurrent rejected | `governor.test.ts` | ✓ pass |
| V6 | Long-context serialization rule | `long-context.test.ts` | ✓ pass |
| V7-static | setAutoCompactionEnabled(false) called | `auto-compaction.test.ts` | ✓ pass |
| V7-behavioral | Zero compaction events under high-context pressure (B1 fix) | `auto-compaction.test.ts` | ✓ pass |
| dispose-on-throw | dispose() exactly once when prompt rejects | `dispose.test.ts` | ✓ pass |
| dispose-on-abort | dispose() exactly once on AbortController.abort (B3) | `dispose.test.ts` | ✓ pass |
| 10-cycle stability | dispose count == 10, handle delta bounded (B3) | `dispose.test.ts` | ✓ pass |
| sidecar success | parent_span_id + trace_id + child_session_id stamped | `sidecar-index.test.ts` | ✓ pass |
| sidecar failure | ok:false written on dispatch failure | `sidecar-index.test.ts` | ✓ pass |

**V8 — operator-gated:** placeholder runs/ files committed; operator runs
`bash start_emmy.sh` then `bun run packages/emmy-ux/scripts/v8-real-deal-e2e.ts`
to overwrite with real evidence and flip the SC sign-off.

## Cumulative Phase 04.5 stats

| Plan | Source LoC (approx) | Test count |
|------|---------------------|------------|
| 04.5-01 (subagent core) | ~250 | 14 |
| 04.5-02 (persona schema + 12 AGENTS.md + 4 yaml updates) | ~200 src + 12 .md | 31 |
| 04.5-03 (OTel propagation) | ~100 | 8 |
| 04.5-04 (concurrency governor) | ~120 | 12 |
| 04.5-05 (TUI bridge + block renderer) | ~200 | 13 |
| 04.5-06 (sidecar JSONL + persistTranscript) | ~110 | 12 |
| 04.5-07 (wiring + V8 runner + runbook) | ~150 src + ~250 script | 5 |
| **Total** | ~1130 src + ~250 script + 12 .md + runbook section | 103 phase 04.5 + 164 emmy-tools = 267 |

## Pitfall mitigations

- **Pitfall #18 (sub-agent black-boxes):** OTel 4-level trace tree (W1) +
  sidecar JSONL + (deferred) SubagentBlock TUI rendering form a 3-layer
  observability stack. Failed dispatches fire `recordException` +
  `setStatus(ERROR)` on BOTH `agent.tool.Agent` and `subagent.<name>` spans
  (test-verified). The dispatcher source contains NO bare-try/catch that
  swallows errors without recordException.
- **Pitfall #22 (prefix-cache informational only):** Runbook Sub-agents §
  "Prefix-cache hit rate is INFORMATIONAL" pins this verbatim. V8 captures
  hit rate via `vllm:prefix_cache_hits_total` metric delta but never gates.

## Open follow-ups

1. **SubagentBlock TUI wiring** — `pi-emmy-extension.ts` carries the
   `TODO(04.5-followup)` marker. SubagentBlock + subscribeChildSession ship
   in `packages/emmy-ux/src/components/{subagent-block.ts, subagent-event-bridge.ts}`
   with full unit-test coverage; live wiring requires either a pi 0.68+
   tool_render hook OR a dispatcher-side event bus the TUI subscribes to.
   Decision deferred until pi-mono ships the hook OR an operator sees
   demonstrable user pain from text-only sub-agent visibility.

2. **Profile hash bumps** — Plan 04.5-02 added `subagents/` directories +
   `subagents:` blocks to all 4 shipped profiles (qwen3.6-35b-a3b@v3.1,
   qwen3.6-27b@v1.1, gemma-4-26b-a4b-it@v2, gemma-4-31b-it@v1.1). Profile
   bundle content changed → content hash changed. CONTEXT.md notes this is
   closeout-level work; deferred here pending the operator's preferred
   path (in-place rev-bump vs cut new versions like v3.2/v1.2/v2.1/v1.2).

3. **V8 SC walkthrough** — operator runs `v8-real-deal-e2e.ts` against live
   Qwen 3.6 35B-A3B v3.1 and overwrites the placeholder runs/ files. The
   SC walkthrough validates the LOCKED W1 4-level trace shape end-to-end
   plus prefix-cache hit-rate capture (informational).

## Deviations from Plan

**1. [Rule 3 — additional functionality / scope adjustment] Wiring placement.**
   - Plan called for inserting wiring at `session.ts:254-257` between services creation and createAgentSessionFromServices.
   - Implemented at exactly that location inside `buildRealPiRuntime`. The piFactory escape path (used by stub-based session.test.ts tests) bypasses this — that's intentional: stub tests don't need to assert Agent wiring; the wiring contract test covers it explicitly.

**2. [Rule 3 — V8 task split] Tasks 1+2 autonomous; Task 3 deferred.**
   - Plan Task 3 is `<task type="checkpoint:human-verify" gate="blocking">` — explicitly operator-gated.
   - Implemented Tasks 1 and 2 autonomously (wiring + script + runbook + placeholder evidence); Task 3 captured as committed SC procedure with placeholder runs/ files. No deviation in autonomous-vs-gated framing — followed the plan's own checkpoint convention.

**Total deviations:** 0 functional, 2 framing notes (both expected per plan structure).

## Self-Check: PASSED

- ✓ `git grep "## Sub-agents" docs/runbook.md` → 1 match (W1 4-level diagram)
- ✓ `git grep "INFORMATIONAL" docs/runbook.md` → 1+ matches (Pitfall #22)
- ✓ `git grep "Pitfall #18" docs/runbook.md` → 1+ matches
- ✓ `git grep "Pitfall #22" docs/runbook.md` → 1+ matches
- ✓ `git grep "agent.tool.Agent" docs/runbook.md` → 1+ matches (W1)
- ✓ `git grep "SubagentBlock" packages/emmy-ux/src/pi-emmy-extension.ts` → 1 match (TODO marker)
- ✓ `git grep "createSubAgentTool(" packages/emmy-ux/src/session.ts` → 1 match
- ✓ `git grep "loadPersonaConfig(" packages/emmy-ux/src/session.ts` → 1 match
- ✓ `git grep "createConcurrencyGovernor(" packages/emmy-ux/src/session.ts` → 1 match
- ✓ `bun test packages/emmy-tools/tests/subagent/ packages/emmy-ux/tests/components/ packages/emmy-ux/tests/persona-loader.test.ts packages/emmy-ux/tests/persona-loader.integration.test.ts packages/emmy-ux/tests/session.subagent-wiring.test.ts packages/emmy-ux/tests/session.test.ts` → 103 pass / 0 fail
- ✓ `cd packages/emmy-ux && bun run typecheck` → clean
- ✓ Existing `bun test packages/emmy-ux/tests/session.test.ts` (8 tests) → still green (no regression)
- ✓ Pitfall #18 mitigation in place (OTel 4-level + sidecar + TUI-deferred); ERROR status on both span sites

## Operator-gated items

**Task 3 — V8 SC walkthrough (`04.5-07-sc-walkthrough-green`):**
This required ~30-min live Qwen E2E session is NOT auto-asserted by this
plan. Operator runs the V8 procedure documented in
`docs/runbook.md § Sub-agents § V8 verification` and either:
- Overwrites `runs/v8-trace-tree.txt` + `runs/v8-sidecar-sample.jsonl` with
  real captures + types `sc1 phase04.5 green` to flip the phase to Done.
- OR describes the failure mode to route to a gap-closure plan.

## Issues Encountered

None blocking.

## Phase 04.5 status: 7/7 plans landed (autonomous portions); pending Task-3 operator V8 sign-off
