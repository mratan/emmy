---
phase: 04.5
plan: 02
type: execute
wave: 1
depends_on: []
last_revised: 2026-04-26
files_modified:
  - packages/emmy-ux/src/persona-loader.ts
  - packages/emmy-ux/tests/persona-loader.test.ts
  - profiles/qwen3.6-35b-a3b/v3.1/harness.yaml
  - profiles/qwen3.6-35b-a3b/v3.1/subagents/research/AGENTS.md
  - profiles/qwen3.6-35b-a3b/v3.1/subagents/code-reviewer/AGENTS.md
  - profiles/qwen3.6-35b-a3b/v3.1/subagents/bash-runner/AGENTS.md
  - profiles/qwen3.6-27b/v1.1/harness.yaml
  - profiles/qwen3.6-27b/v1.1/subagents/research/AGENTS.md
  - profiles/qwen3.6-27b/v1.1/subagents/code-reviewer/AGENTS.md
  - profiles/qwen3.6-27b/v1.1/subagents/bash-runner/AGENTS.md
  - profiles/gemma-4-26b-a4b-it/v2/harness.yaml
  - profiles/gemma-4-26b-a4b-it/v2/subagents/research/AGENTS.md
  - profiles/gemma-4-26b-a4b-it/v2/subagents/code-reviewer/AGENTS.md
  - profiles/gemma-4-26b-a4b-it/v2/subagents/bash-runner/AGENTS.md
  - profiles/gemma-4-31b-it/v1.1/harness.yaml
  - profiles/gemma-4-31b-it/v1.1/subagents/research/AGENTS.md
  - profiles/gemma-4-31b-it/v1.1/subagents/code-reviewer/AGENTS.md
  - profiles/gemma-4-31b-it/v1.1/subagents/bash-runner/AGENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 shipped profiles' harness.yaml include a top-level `subagents:` block with `enabled`, `max_concurrent`, `long_context_serialize_threshold_tokens`, `default_memory_scope`, and `personas:` keys"
    - "Every profile ships with the 3 built-in personas (research, code_reviewer, bash_runner) defined under `subagents.personas`"
    - "Every persona_dir on disk contains an AGENTS.md file targeting 100-300 TOKENS (W2 fix: gated as `wc -w < 230` so that ≈300 tokens at 1 token ≈ 0.75 words for English)"
    - "loadPersonaConfig(profilePath) parses a profile's harness.yaml, validates the subagents block, resolves persona_dir to absolute paths, blocks symlink/path-traversal escape, AND pre-loads each persona's AGENTS.md content into the returned spec's `agentsContent` field (B2 fix — dispatcher consumes this without re-reading)"
    - "Path-traversal protection: a personaDir resolving outside the profile bundle root is rejected with a clear error"
    - "loadPersonaConfig returns Record<string, SubAgentSpec> ready to pass to createSubAgentTool, with `personaDir` AND `agentsContent` populated for `pattern: persona` entries"
  artifacts:
    - path: "packages/emmy-ux/src/persona-loader.ts"
      provides: "loadPersonaConfig + validation + path resolution + traversal block + AGENTS.md content pre-loading (B2)"
      exports: ["loadPersonaConfig", "PersonaLoadError"]
    - path: "profiles/qwen3.6-35b-a3b/v3.1/harness.yaml"
      provides: "subagents block with 3 personas"
      contains: "subagents:"
    - path: "profiles/qwen3.6-35b-a3b/v3.1/subagents/research/AGENTS.md"
      provides: "research persona system prompt"
    - path: "profiles/qwen3.6-27b/v1.1/harness.yaml"
      contains: "subagents:"
    - path: "profiles/gemma-4-26b-a4b-it/v2/harness.yaml"
      contains: "subagents:"
    - path: "profiles/gemma-4-31b-it/v1.1/harness.yaml"
      contains: "subagents:"
  key_links:
    - from: "packages/emmy-ux/src/persona-loader.ts"
      to: "packages/emmy-tools/src/subagent/types.ts (SubAgentSpec)"
      via: "type import — output shape MUST match SubAgentSpec from Plan 04.5-01 (including agentsContent field)"
      pattern: "import.*SubAgentSpec"
    - from: "harness.yaml subagents.personas.<persona_slug>"
      to: "subagents/<persona_slug>/AGENTS.md on disk"
      via: "persona_dir field resolves to <profilePath>/subagents/<kebab-case-persona_slug>/"
      pattern: "persona_dir:.*subagents/"
    - from: "loadPersonaConfig"
      to: "dispatcher.ts (Plan 04.5-01 Task 2)"
      via: "agentsContent field on SubAgentSpec is read by dispatcher's Pattern B block — avoids re-read at dispatch time"
      pattern: "agentsContent"
---

<objective>
Define the `subagents:` block schema for `harness.yaml`, ship it filled in across all 4 shipped profiles (qwen3.6-35b-a3b@v3.1, qwen3.6-27b@v1.1, gemma-4-26b-a4b-it@v2, gemma-4-31b-it@v1.1), and write the loader that turns the YAML block into the `Record<string, SubAgentSpec>` consumed by `createSubAgentTool` (Plan 04.5-01). Each profile gets 3 built-in personas (research, code_reviewer, bash_runner) — that's 4 profiles × 3 personas = 12 `AGENTS.md` files plus 4 harness.yaml updates.

The loader pre-loads each Pattern B persona's AGENTS.md content into the returned spec's `agentsContent` field (B2 fix — the dispatcher's Pattern B block consumes this directly via `resourceLoaderOptions.agentsFilesOverride` without re-reading on each dispatch). The kebab-case-on-disk vs snake_case-in-yaml convention is preserved: yaml keys are `research` / `code_reviewer` / `bash_runner` (snake), on-disk dirs are `subagents/research/`, `subagents/code-reviewer/`, `subagents/bash-runner/` (kebab) — `persona_dir` carries the kebab name as written in yaml.

Purpose: Personas are profile-owned per the keystone abstraction (CLAUDE.md). Plan 04.5-01's tool factory takes a `personas` record as input — this plan creates the canonical source of those records. Path-traversal protection at load time mitigates the symlink-escape threat class (same pattern as the memory tool work in Phase 04.4). Pre-loading `agentsContent` decouples persona-prompt-source from tool-cwd in the dispatcher (B2 contract).

Output: 4 harness.yaml updates (additive — preserves byte-for-byte everything else); 12 AGENTS.md files (each &lt;230 words ≈ 300 tokens — W2 fix); 1 new `persona-loader.ts` module + tests in `@emmy/ux`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-CONTEXT.md
@.planning/pre-phase/04.5-subagents/INTEGRATION-SKETCH.md
@CLAUDE.md
@profiles/qwen3.6-35b-a3b/v3.1/harness.yaml
@packages/emmy-ux/src/profile-loader.ts
@packages/emmy-tools/src/subagent/types.ts

<interfaces>
<!-- Output shape MUST match SubAgentSpec defined in Plan 04.5-01 (including the agentsContent field added in revision). -->
<!-- The yaml `pattern` value (string) maps to SubAgentSpec.pattern. -->
<!-- The yaml `persona_dir` (relative to profile dir) is resolved to ABSOLUTE in the loader. -->
<!-- For pattern: "persona", AGENTS.md content is READ AT LOAD TIME and stamped onto SubAgentSpec.agentsContent (B2 fix). -->

LOCKED YAML schema (CONTEXT.md §decisions, copied verbatim):
```yaml
subagents:
  enabled: true
  max_concurrent: 2
  long_context_serialize_threshold_tokens: 40000
  default_memory_scope: "project"        # project | global | none
  personas:
    research:                            # snake_case YAML key (consumed by parent's tool param Union)
      description: "Investigate a specific question without polluting parent's context."
      pattern: "persona"                 # vs "lean"
      persona_dir: "subagents/research"  # kebab-case on disk; relative to profile dir
      tool_allowlist: ["read", "grep", "find", "ls"]
      model_override: null
      max_turns: 10
      persist_transcript: false
    code_reviewer:                       # snake_case YAML key
      description: "Review a diff or file for bugs and style."
      pattern: "persona"
      persona_dir: "subagents/code-reviewer"  # kebab-case dir
      tool_allowlist: ["read", "grep"]
      max_turns: 5
      persist_transcript: false
    bash_runner:                         # snake_case YAML key
      description: "Execute a bash task and return the output."
      pattern: "lean"                    # uses parent's services
      tool_allowlist: ["bash", "read"]
      max_turns: 3
      persist_transcript: false
```

LOCKED return type (matches `SubAgentSpec` from Plan 04.5-01, including the `agentsContent` field added during revision):
```typescript
import type { SubAgentSpec } from "@emmy/tools";  // re-exported in 04.5-01

export function loadPersonaConfig(profilePath: string): Promise<Record<string, SubAgentSpec>>;
// For pattern: "persona" entries, the returned spec MUST have BOTH
//   personaDir: <absolute path under profilePath>
//   agentsContent: <fs.readFileSync(personaDir + "/AGENTS.md", "utf8")>
// For pattern: "lean" entries, BOTH fields are undefined.

export class PersonaLoadError extends Error {
  readonly code: "no_subagents_block" | "invalid_persona" | "path_traversal" | "missing_agents_md" | "missing_persona_dir";
  readonly persona?: string;
}
```

**Persona slug convention (LOCKED — also called out in jsonl.ts Plan 04.5-06 per checker I2):**
- YAML keys (consumed by tool param Union, parent model sees these): snake_case (`research`, `code_reviewer`, `bash_runner`)
- On-disk dirs (the actual `persona_dir` field value): kebab-case (`subagents/research`, `subagents/code-reviewer`, `subagents/bash-runner`)
- `SubAgentSpec.name` = the YAML key (snake_case), preserved through to dispatcher and OTel span
</interfaces>

<source_excerpts>
Existing harness.yaml shape (qwen3.6-35b-a3b/v3.1) — `subagents:` is APPENDED at the END of the file, NOT modifying any existing key. The file currently ends with `advanced_settings_whitelist:` block at line 71.
</source_excerpts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: persona-loader module + path-traversal protection + validation + agentsContent pre-loading (B2)</name>
  <files>packages/emmy-ux/src/persona-loader.ts, packages/emmy-ux/tests/persona-loader.test.ts</files>
  <read_first>
    - packages/emmy-ux/src/persona-loader.ts (the file being created)
    - packages/emmy-ux/tests/persona-loader.test.ts (the file being created)
    - packages/emmy-tools/src/subagent/types.ts (SubAgentSpec — return type contract; created in Plan 04.5-01, includes agentsContent field per revision)
    - packages/emmy-ux/src/profile-loader.ts (sibling pattern — yaml load + validate)
    - .planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-CONTEXT.md (LOCKED YAML schema + revised Pattern B AGENTS.md injection)
    - profiles/qwen3.6-35b-a3b/v3.1/harness.yaml (existing structure context)
  </read_first>
  <behavior>
    - Test 1: loadPersonaConfig(absoluteProfilePath) returns Record<string, SubAgentSpec> with 3 personas (research, code_reviewer, bash_runner) when harness.yaml has the canonical block.
    - Test 2: Each returned spec has `personaDir` resolved to ABSOLUTE path (under the profile dir) for `pattern: "persona"` personas; `personaDir` is undefined for `pattern: "lean"` personas.
    - Test 2b (B2 fix — agentsContent pre-loading): for each `pattern: "persona"` entry, the returned spec has `agentsContent: string` populated with the byte-for-byte contents of `<personaDir>/AGENTS.md`. For `pattern: "lean"` entries, `agentsContent` is undefined.
    - Test 3: When a persona's `persona_dir` resolves OUTSIDE the profile dir (e.g. `../../etc/passwd` or absolute `/etc/passwd` or symlink escape via `path.resolve` then `path.relative`), throws PersonaLoadError with code `"path_traversal"` and the offending persona name.
    - Test 4: When a `persona` pattern entry's `persona_dir` does not exist on disk, throws PersonaLoadError code `"missing_persona_dir"` with the persona name.
    - Test 5: When the resolved personaDir lacks `AGENTS.md`, throws PersonaLoadError code `"missing_agents_md"`.
    - Test 6: When `subagents:` block is absent or `subagents.enabled: false`, returns `{}` (empty record — no error). This lets profiles ship without sub-agents enabled.
    - Test 7: `pattern` field must be `"lean"` or `"persona"`; any other value throws `"invalid_persona"`.
    - Test 8: `tool_allowlist` MUST be a non-empty array of strings; empty array OR non-array throws `"invalid_persona"`.
  </behavior>
  <action>
    1. Create `packages/emmy-ux/src/persona-loader.ts`:
       - Import `js-yaml` (already a workspace dep), `node:fs`, `node:path`.
       - Re-import `SubAgentSpec` type from `@emmy/tools` (the type lives there per Plan 04.5-01; revised type includes `agentsContent?: string`).
       - Export class `PersonaLoadError extends Error` with `code` and optional `persona` properties.
       - Export `async function loadPersonaConfig(profilePath: string): Promise<Record<string, SubAgentSpec>>`. Body:
         a. Read `<profilePath>/harness.yaml`; if missing, throw with `code: "no_subagents_block"` and helpful message naming the path.
         b. yaml.load + extract `subagents` key. If absent OR `subagents.enabled !== true`, return `{}`.
         c. For each entry `[personaSlug, raw]` in `subagents.personas` (where `personaSlug` is the snake_case YAML key like `research` or `code_reviewer`):
            - Validate `description: string`, `pattern: "lean"|"persona"`, `tool_allowlist: string[]` (length > 0), `max_turns: number`. Reject other values with `code: "invalid_persona"` + persona name.
            - For `pattern: "persona"`:
              - Require `persona_dir: string`. The yaml carries the kebab-case dir name (e.g. `subagents/code-reviewer`).
              - Resolve via `path.resolve(profilePath, personaDir)`. Compute `path.relative(profilePath, resolved)`. If it starts with `..` OR is absolute → throw `code: "path_traversal"`.
              - Use `fs.realpathSync(resolved)` to dereference symlinks; re-check the relative-to-profile invariant on the realpath.
              - Verify `fs.statSync(resolved).isDirectory()` → else `missing_persona_dir`.
              - Verify `fs.existsSync(path.join(resolved, "AGENTS.md"))` → else `missing_agents_md`.
              - **B2 fix:** read AGENTS.md content: `const agentsContent = fs.readFileSync(path.join(resolved, "AGENTS.md"), "utf8");` and stamp it on the returned spec.
            - For `pattern: "lean"`: skip dir checks; `personaDir` and `agentsContent` are undefined.
            - Build the SubAgentSpec:
              ```typescript
              const spec: SubAgentSpec = {
                name: personaSlug,                                  // snake_case YAML key
                description: raw.description,
                pattern: raw.pattern,
                personaDir: raw.pattern === "persona" ? resolved : undefined,
                agentsContent: raw.pattern === "persona" ? agentsContent : undefined,  // B2
                toolAllowlist: raw.tool_allowlist,
                modelOverride: raw.model_override ?? undefined,
                maxTurns: raw.max_turns,
                persistTranscript: raw.persist_transcript ?? false,
              };
              ```
         d. Return `Record<string, SubAgentSpec>` keyed by `personaSlug`.

    2. Create `packages/emmy-ux/tests/persona-loader.test.ts` covering all behaviors. Use `mkdtempSync` to build per-test fake profile dirs with synthetic harness.yaml + subagents/<name>/AGENTS.md skeletons. Path-traversal test: write a yaml with `persona_dir: "../escape"` and an actual `../escape/AGENTS.md` outside the profile dir, then assert the loader rejects it. **Test 2b (B2):** write an AGENTS.md containing the literal `RESEARCH_PROMPT_MARKER` and assert the returned spec's `agentsContent` field contains that exact substring.
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bun test packages/emmy-ux/tests/persona-loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/emmy-ux/src/persona-loader.ts` exists AND contains exact strings `export async function loadPersonaConfig` AND `export class PersonaLoadError` AND `code: "path_traversal"` AND `fs.realpathSync`
    - File contains the literal string `path.relative(profilePath` (anchors the traversal check to the profile dir)
    - File contains literal `agentsContent` AND reads `path.join(resolved, "AGENTS.md")` via `fs.readFileSync` (B2 fix — pre-loads content)
    - File `packages/emmy-ux/tests/persona-loader.test.ts` exists AND contains test labels including `"path_traversal"`, `"missing_agents_md"`, `"no_subagents_block"`, `"missing_persona_dir"`, AND a test asserting `agentsContent` is populated with the AGENTS.md byte content (B2)
    - `bun test packages/emmy-ux/tests/persona-loader.test.ts` exits 0 with at least 9 tests passing (was 8 pre-revision; B2 +1)
  </acceptance_criteria>
  <done>Loader handles all 9 validation cases including B2 agentsContent pre-loading; symlink-realpath traversal escape is blocked; types align with revised SubAgentSpec from Plan 04.5-01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append subagents block + write 12 AGENTS.md files across all 4 profiles (W2: tightened to ≈300 tokens)</name>
  <files>profiles/qwen3.6-35b-a3b/v3.1/harness.yaml, profiles/qwen3.6-35b-a3b/v3.1/subagents/research/AGENTS.md, profiles/qwen3.6-35b-a3b/v3.1/subagents/code-reviewer/AGENTS.md, profiles/qwen3.6-35b-a3b/v3.1/subagents/bash-runner/AGENTS.md, profiles/qwen3.6-27b/v1.1/harness.yaml, profiles/qwen3.6-27b/v1.1/subagents/research/AGENTS.md, profiles/qwen3.6-27b/v1.1/subagents/code-reviewer/AGENTS.md, profiles/qwen3.6-27b/v1.1/subagents/bash-runner/AGENTS.md, profiles/gemma-4-26b-a4b-it/v2/harness.yaml, profiles/gemma-4-26b-a4b-it/v2/subagents/research/AGENTS.md, profiles/gemma-4-26b-a4b-it/v2/subagents/code-reviewer/AGENTS.md, profiles/gemma-4-26b-a4b-it/v2/subagents/bash-runner/AGENTS.md, profiles/gemma-4-31b-it/v1.1/harness.yaml, profiles/gemma-4-31b-it/v1.1/subagents/research/AGENTS.md, profiles/gemma-4-31b-it/v1.1/subagents/code-reviewer/AGENTS.md, profiles/gemma-4-31b-it/v1.1/subagents/bash-runner/AGENTS.md</files>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v3.1/harness.yaml (existing — APPEND only)
    - profiles/qwen3.6-27b/v1.1/harness.yaml
    - profiles/gemma-4-26b-a4b-it/v2/harness.yaml
    - profiles/gemma-4-31b-it/v1.1/harness.yaml
    - .planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-CONTEXT.md (§decisions — LOCKED yaml schema; AGENTS.md "100-300 tokens" bound)
    - CLAUDE.md (pi-minimalism — AGENTS.md must be ~100-300 tokens, NOT bloated)
  </read_first>
  <action>
    1. **For each of the 4 profile dirs** append the LOCKED yaml block at the END of `harness.yaml` (after existing content; insert a blank separator line first). Block content (CONTEXT.md verbatim):

    ```yaml

    subagents:
      enabled: true
      max_concurrent: 2                              # hardware-imposed cap
      long_context_serialize_threshold_tokens: 40000 # parent input > 40K → serialize
      default_memory_scope: "project"                # project | global | none
      personas:
        research:
          description: "Investigate a specific question without polluting parent's context."
          pattern: "persona"
          persona_dir: "subagents/research"
          tool_allowlist: ["read", "grep", "find", "ls"]
          model_override: null
          max_turns: 10
          persist_transcript: false
        code_reviewer:
          description: "Review a diff or file for bugs and style."
          pattern: "persona"
          persona_dir: "subagents/code-reviewer"
          tool_allowlist: ["read", "grep"]
          max_turns: 5
          persist_transcript: false
        bash_runner:
          description: "Execute a bash task and return the output."
          pattern: "lean"
          tool_allowlist: ["bash", "read"]
          max_turns: 3
          persist_transcript: false
    ```

    2. **For each of the 4 profiles** create the 3 persona dirs (`subagents/research/`, `subagents/code-reviewer/`, `subagents/bash-runner/`) and write a tight `AGENTS.md` in each.

       **W2 fix — token budget:** CONTEXT.md says "100-300 tokens." At 1 token ≈ 0.75 words for English, 300 tokens ≈ 225 words. Each AGENTS.md MUST satisfy `wc -w &lt; 230` (gives a small headroom margin) AND `wc -w &gt;= 50` (substantive). The pre-revision plan gated at &lt;300 words ≈ 400 tokens, which exceeded the spec — this revision corrects that.

       Use the SAME content across all 4 profiles for v1 — model-specific tuning is a v2 concern. Drafts (each &lt; 230 words):

       - `subagents/research/AGENTS.md` (target ~140 words):
         ```markdown
         You are a research sub-agent.

         Your job: answer ONE specific question by reading the codebase. Return ONLY a short text summary to the parent — your tool calls are not visible upstream.

         Tools: `read`, `grep`, `find`, `ls`. No edits, writes, or bash. If a question requires those, return that finding instead of attempting it.

         How to work:
         1. Decompose the question into 2-3 grep/find probes.
         2. Read the most relevant 3-5 files (or fewer).
         3. Synthesize a 4-8 sentence answer with file:line citations.
         4. Stop. Do NOT speculate beyond what you read.

         If the question is ambiguous, return your best interpretation and flag the ambiguity. Do not ask clarifying questions — the parent cannot reply mid-dispatch.

         Output format: plain prose, ≤ 200 words. Cite specific file:line locations.
         ```

       - `subagents/code-reviewer/AGENTS.md` (target ~140 words):
         ```markdown
         You are a code-review sub-agent.

         Your job: review the diff or file the parent identifies. Return ONLY a short critique — your tool calls are not visible upstream.

         Tools: `read`, `grep`. No edit/write/bash.

         How to work:
         1. Read the target file(s) fully.
         2. Grep for related callers/usages if needed for context.
         3. Score along: correctness, error handling, naming, test coverage, security smells.
         4. Return findings as a numbered list with file:line and a one-sentence rationale per item.

         Style: terse, specific, evidence-based. No vague advice ("consider refactoring"). Either flag a concrete issue with location, or skip it.

         Output cap: ≤ 200 words. Top 3-5 findings only — surface the highest-leverage ones.
         ```

       - `subagents/bash-runner/AGENTS.md` (target ~100 words):
         ```markdown
         You are a bash-runner sub-agent.

         Your job: execute ONE specific bash command (or short pipeline) the parent requested, and return the output.

         Tools: `bash`, `read`. No edits.

         How to work:
         1. Run the command exactly as specified.
         2. If it fails, capture stderr verbatim.
         3. Return: command run, exit code, stdout (truncated to 4 KB), stderr if non-empty.

         Do NOT modify the command. If it looks dangerous (e.g. `rm -rf /`, fork bomb), refuse and say why.

         Output: plain text, ≤ 200 words. No interpretation — just the result.
         ```

    3. The 12 AGENTS.md files are byte-identical across the 4 profiles in v1 (no model-specific tuning). The persona_dir convention uses kebab-case on disk (`code-reviewer`, `bash-runner`) while the harness.yaml key uses snake_case (`code_reviewer`, `bash_runner`) — this is the LOCKED convention from CONTEXT.md §decisions. The persona-loader maps the key but resolves the dir as written in `persona_dir`.

    4. **DO NOT** modify profile.yaml or recompute profile hash in this plan. Profile hash bumps are CLOSEOUT-level work (Plan 04.5-07 wraps that). The profile-immutability contract (CLAUDE.md keystone abstraction) treats this as adding new files within v3.1/v1.1/v2 dirs — those bundles will need a hash recomputation, but that's the closeout's job, not Wave 1's.
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bash -c 'set -e; for p in profiles/qwen3.6-35b-a3b/v3.1 profiles/qwen3.6-27b/v1.1 profiles/gemma-4-26b-a4b-it/v2 profiles/gemma-4-31b-it/v1.1; do test -f "$p/harness.yaml"; grep -q "^subagents:" "$p/harness.yaml" || { echo "FAIL $p missing subagents block"; exit 1; }; for persona in research code-reviewer bash-runner; do test -f "$p/subagents/$persona/AGENTS.md" || { echo "FAIL $p/subagents/$persona/AGENTS.md missing"; exit 1; }; w=$(wc -w < "$p/subagents/$persona/AGENTS.md"); [ "$w" -lt 230 ] || { echo "FAIL $p/subagents/$persona/AGENTS.md word count $w >= 230 (W2 token budget)"; exit 1; }; [ "$w" -ge 50 ] || { echo "FAIL $p/subagents/$persona/AGENTS.md word count $w < 50 (substantive)"; exit 1; }; done; done; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - All 4 `harness.yaml` files contain the literal string `subagents:` at the start of a line
    - All 4 `harness.yaml` files contain `personas:` AND `research:` AND `code_reviewer:` AND `bash_runner:`
    - All 4 profiles have `subagents/research/AGENTS.md` AND `subagents/code-reviewer/AGENTS.md` AND `subagents/bash-runner/AGENTS.md` on disk
    - Each AGENTS.md file: `wc -w` shows &lt; 230 words (W2 fix — ≈300 tokens at 1 token ≈ 0.75 words)
    - Each AGENTS.md file: `wc -w` shows &gt;= 50 words (substantive)
    - Existing `harness.yaml` content is byte-identical above the new `subagents:` block (verify by `git diff` showing only ADDITIONS at end)
  </acceptance_criteria>
  <done>4 harness.yaml updated (additive only); 12 AGENTS.md committed with W2-tightened word budget (&lt;230 words ≈ ≤300 tokens); loader test (Task 1) green when pointed at any of the 4 profiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — loadPersonaConfig against all 4 shipped profiles</name>
  <files>packages/emmy-ux/tests/persona-loader.integration.test.ts</files>
  <read_first>
    - packages/emmy-ux/tests/persona-loader.integration.test.ts (the file being created)
    - packages/emmy-ux/src/persona-loader.ts (Task 1 output)
    - profiles/qwen3.6-35b-a3b/v3.1/harness.yaml (Task 2 output)
    - profiles/qwen3.6-27b/v1.1/harness.yaml (Task 2 output)
    - profiles/gemma-4-26b-a4b-it/v2/harness.yaml (Task 2 output)
    - profiles/gemma-4-31b-it/v1.1/harness.yaml (Task 2 output)
  </read_first>
  <behavior>
    - For each of the 4 shipped profiles: loadPersonaConfig returns exactly 3 persona keys: research, code_reviewer, bash_runner.
    - research has `pattern === "persona"` AND `personaDir` is an absolute path that exists on disk AND contains AGENTS.md AND `agentsContent` is the file's byte content (B2).
    - code_reviewer has `pattern === "persona"` AND personaDir + agentsContent populated similarly (B2).
    - bash_runner has `pattern === "lean"` AND personaDir is undefined AND agentsContent is undefined.
    - Each persona's toolAllowlist matches the LOCKED values: research=["read","grep","find","ls"], code_reviewer=["read","grep"], bash_runner=["bash","read"].
  </behavior>
  <action>
    Create `packages/emmy-ux/tests/persona-loader.integration.test.ts`. For each of the 4 profile paths (parameterize a `describe.each` over the array), assert all 5 invariants in the behavior spec — INCLUDING the B2 `agentsContent` invariant. Use `path.resolve(import.meta.dir, "../../..", "profiles/<name>/<ver>")` to anchor paths. This test runs against the REAL committed profile bundles — it's the first sentinel that catches typos in the yaml across all 4 profiles in one shot.
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bun test packages/emmy-ux/tests/persona-loader.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/emmy-ux/tests/persona-loader.integration.test.ts` exists
    - Test exercises all 4 profile paths: contains literal `qwen3.6-35b-a3b` AND `qwen3.6-27b` AND `gemma-4-26b-a4b-it` AND `gemma-4-31b-it`
    - Test asserts `agentsContent` is populated for `research` and `code_reviewer` and undefined for `bash_runner` (B2 fix)
    - `bun test packages/emmy-ux/tests/persona-loader.integration.test.ts` exits 0; combined assertion count &gt;= 4 profiles × 5 invariants = 20
  </acceptance_criteria>
  <done>All 4 profile bundles parse cleanly; persona keys + patterns + allowlists exactly match LOCKED schema across the matrix; agentsContent pre-loaded for Pattern B personas (B2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile yaml → loader | Profile is operator-trusted (committed to git) but path-traversal in `persona_dir` is the same threat class as memory-tool path-traversal. |
| persona AGENTS.md → child systemPrompt | Profile-controlled — appears in child's system prompt verbatim. Operator-controlled content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-02-01 | Tampering | malicious `persona_dir` value escapes profile dir (`../`, absolute path, symlink) | mitigate | `path.relative(profilePath, fs.realpathSync(resolved))` must not start with `..` or be absolute. Test 3 in Task 1 covers this. |
| T-04.5-02-02 | Information Disclosure | persona AGENTS.md leaks into child's system prompt and child summarizes it back to parent context | accept | Operator owns the profile content. AGENTS.md text is intended to be consumed by the model. |
| T-04.5-02-03 | DoS | enormous tool_allowlist or pathological yaml structure | accept | Profile is operator-controlled and version-pinned. Schema validation rejects non-array tool_allowlist. |
| T-04.5-02-04 | Tampering | yaml entry with unknown `pattern` value bypasses dispatcher branch | mitigate | Loader validates `pattern in {"lean","persona"}` and rejects others with `invalid_persona`. |
| T-04.5-02-05 | Information Disclosure | agentsContent pre-loaded at config time captures the AGENTS.md content into in-memory state for the lifetime of the harness | accept | Profile is operator-controlled; in-memory retention is the same risk class as the profile bundle on disk. |
</threat_model>

<verification>
Run `bun test packages/emmy-ux/tests/persona-loader.test.ts packages/emmy-ux/tests/persona-loader.integration.test.ts` — must exit 0.
Spot-check `git diff profiles/*/v*/harness.yaml` shows ONLY appended `subagents:` blocks (no deletions, no modifications above).
Each AGENTS.md is &lt; 230 words (W2 token budget gate).
</verification>

<success_criteria>
1. `loadPersonaConfig` exported from `@emmy/ux` with full validation + path-traversal block + B2 agentsContent pre-loading.
2. All 4 shipped profiles' harness.yaml have the canonical `subagents:` block appended.
3. 12 AGENTS.md files exist (3 personas × 4 profiles), each &lt; 230 words ≈ ≤300 tokens (W2 fix).
4. Integration test loads all 4 profiles cleanly and validates the LOCKED toolAllowlist values match AND agentsContent is populated for Pattern B personas.
5. Existing harness.yaml content above the new block is byte-identical (no accidental whitespace/indentation tweaks).
</success_criteria>

<output>
After completion, create `.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-02-SUMMARY.md` documenting:
- 4 profiles × 3 personas = 12 AGENTS.md files written; word counts per file (W2 fix — all &lt;230)
- Test count + pass status across persona-loader.test.ts + .integration.test.ts (target &gt;= 9 + integration)
- Confirmation that `agentsContent` is pre-loaded for Pattern B personas (B2 contract)
- Confirmation that profile hash bump is DEFERRED to Plan 04.5-07 closeout (not done here)
</output>
