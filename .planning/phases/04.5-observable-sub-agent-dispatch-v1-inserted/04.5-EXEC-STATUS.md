# Phase 04.5 — Execution Status

**Date:** 2026-04-26T19:00:00Z
**Branch:** gsd/04.5-subagents
**Worktree:** /data/projects/emmy-04.5
**Final commit:** 97964c93f0ffda7ef3a3817c8d8960ccf042d7d9
**Verification:** PARTIAL (autonomous portions PASSED; V8 SC walkthrough operator-gated)

## Plans landed

- 04.5-01: 6cbab4c (+ 10f8abb feat types/factory + d3642d4 feat dispatcher/V3/B3): SubAgent core — types, runOneTurn helper, Agent tool factory, Pattern A/B dispatcher, V3 allowlist, B3 dispose-on-abort. 14 tests.
- 04.5-02: 0673682 (+ 20ba108 loader + e237481 4 profiles × 12 AGENTS.md): persona-loader with B2 agentsContent pre-load, path-traversal guard, subagents block in all 4 shipped profiles. 31 tests.
- 04.5-03: bb7bc1c (+ 481d3ca otel.ts + dispatcher/index wiring): OTel parent-child propagation — withSubagentSpan + withAgentToolSpan helpers, W1 4-level trace tree (parent_session → agent.tool.Agent → subagent.\<name> → child). 8 tests.
- 04.5-04: 4bcd6ec (+ ccfde89 governor + dispatcher wiring): concurrency governor — semaphore + I3 over-cap rejection + V6 long-context serialization + V7 BEHAVIORAL B1 fix. OTel ALS-loss-across-await workaround. 12 tests.
- 04.5-05: 76abb6e (+ e4f9a39 bridge + block renderer): subagent event bridge + pure-text block renderer (Rule 3 deviation: pure-TS instead of TSX/Ink — @emmy/ux has zero React footprint). 13 tests.
- 04.5-06: 335efb7 (+ b5fba6d jsonl + dispatcher wiring): per-child JSONL routing + sidecar forensics index. I2 fix: kebab-vs-snake persona naming convention documented. 12 tests.
- 04.5-07: 97964c9 (+ 6433fc7 wiring + V8 runner + runbook): session.ts wires Agent tool into customTools; v8-real-deal-e2e.ts script; docs/runbook.md Sub-agents section with W1 4-level diagram + Pitfall #18 + #22 callouts. 5 wiring contract tests. **Task 3 (V8 SC walkthrough) is OPERATOR-GATED.**

## Verification summary

**Phase 04.5 tests (run package-isolated — bun:test mock.module leaks across packages when both are loaded):**
- `bun test packages/emmy-tools/` → 196 pass / 0 fail / 499 expect calls
- `bun test packages/emmy-ux/` → 414 pass / 0 fail / 1054 expect calls
- Combined unique Phase 04.5 test count: 103 (subagent + persona-loader + persona-loader.integration + components + session.subagent-wiring)

**V1-V7 acceptance criteria (LOCKED in CONTEXT.md):**
- V1 (Pattern A spawn+execute < 100ms) — PASS
- V2 (Pattern B spawn+execute via agentsFilesOverride) — PASS
- V3 (toolAllowlist enforcement, pi-rejection-at-registration) — PASS
- V4 (OTel parent → tool → subagent W1 4-level chain) — PASS
- V5 (concurrent children no cross-talk; over-cap rejection per I3) — PASS
- V6 (long-context serialization rule) — PASS
- V7 BEHAVIORAL (auto-compaction OFF; zero compaction events) — PASS (B1 fix landed)

**Pitfall mitigations:**
- Pitfall #18 (sub-agent black-boxes): OTel 4-level trace tree + sidecar JSONL + (deferred TUI) — recordException + setStatus(ERROR) on both span sites verified
- Pitfall #22 (prefix-cache informational): runbook callout in place, V8 captures hit rate but never gates

**Typecheck:** clean across both packages

## Blockers / Notes

None blocking. Two structural decisions documented:

1. **Plan 04.5-05 deviation (Rule 3):** Replaced TSX/Ink component with pure-TS line-array renderer. `@emmy/ux` has zero React/Ink footprint; adding 3 deps + JSX TS config for code that's explicitly NOT wired to live TUI exceeded plan scope. Same data contract; Plan 04.5-07's wiring step decides the JSX-or-plain-text path when the pi-emmy-extension hook surface is known.

2. **Phase 04.5 / pi-emmy-extension.ts SubagentBlock wiring deferred to phase 04.5-followup:** pi 0.68 lacks a tool_render hook. TODO marker grep-discoverable in pi-emmy-extension.ts. OTel trace tree is the authoritative observability surface in v1; SubagentBlock TUI rendering is the secondary user-visible surface.

3. **bun:test mock.module package isolation quirk:** `governor.test.ts` mocks `@emmy/telemetry`'s `emitEvent`; when run in the same bun process as emmy-ux tests, the mock leaks across the package boundary. Tests pass when run within their own package (`bun test packages/emmy-tools/` and `bun test packages/emmy-ux/` both clean). Not a Phase 04.5 regression — same pattern existed in earlier phases' module mocks. Documented as a follow-up if cross-package CI runs become required.

4. **Profile hash bumps DEFERRED:** Plan 04.5-02 added 12 AGENTS.md files + 4 harness.yaml subagents blocks across the 4 shipped profiles (qwen3.6-35b-a3b@v3.1, qwen3.6-27b@v1.1, gemma-4-26b-a4b-it@v2, gemma-4-31b-it@v1.1). Bundle content changed → content hash changed. CONTEXT.md notes hash bumps are closeout-level work; the plan author and I both deferred to operator preference (in-place rev-bump vs cut new versions like v3.2/v1.2/v2.1/v1.2). NOT blocking V8 — runbook contract is purely additive.

## Operator-gated items still open

- **Plan 04.5-07 Task 3 — V8 SC walkthrough (`04.5-07-sc-walkthrough-green`):**
  Requires live Qwen 3.6 35B-A3B v3.1 served by `bash start_emmy.sh`. Operator runs:
  ```
  bash start_emmy.sh
  bun run packages/emmy-ux/scripts/v8-real-deal-e2e.ts
  ```
  Expected: real evidence overwrites placeholder `runs/v8-trace-tree.txt` and `runs/v8-sidecar-sample.jsonl`. Script's exit code gates W1 4-level shape verification. Operator types `sc1 phase04.5 green` to flip the phase to Done† pending the runs/ commit.

- **(Optional) Profile hash recomputation** for the 4 shipped profile bundles after Plan 04.5-02's additive changes. Not blocking V8 because the contract is additive (no existing content modified).

- **(Optional) SubagentBlock TUI live wiring** when pi-mono ships a tool_render hook OR an operator sees demonstrable user pain from text-only sub-agent visibility. Pure-TS renderer + bridge already ship and are unit-test green; only the pi-extension wiring is missing.
