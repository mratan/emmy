---
phase: 04.5
plan: 02
subsystem: persona-config
tags: [subagents, profiles, harness-yaml, persona-loader, B2]
requires: []
provides:
  - "@emmy/ux loadPersonaConfig + PersonaLoadError"
  - "subagents: block in all 4 shipped profile harness.yaml files"
  - "12 AGENTS.md files (3 personas × 4 profiles)"
affects: []
tech-stack:
  added: []
  patterns: ["realpathSync symlink-escape guard", "agentsContent pre-load", "snake_case-yaml/kebab-case-disk convention"]
key-files:
  created:
    - packages/emmy-ux/src/persona-loader.ts
    - packages/emmy-ux/tests/persona-loader.test.ts
    - packages/emmy-ux/tests/persona-loader.integration.test.ts
    - profiles/qwen3.6-35b-a3b/v3.1/subagents/research/AGENTS.md
    - profiles/qwen3.6-35b-a3b/v3.1/subagents/code-reviewer/AGENTS.md
    - profiles/qwen3.6-35b-a3b/v3.1/subagents/bash-runner/AGENTS.md
    - profiles/qwen3.6-27b/v1.1/subagents/research/AGENTS.md
    - profiles/qwen3.6-27b/v1.1/subagents/code-reviewer/AGENTS.md
    - profiles/qwen3.6-27b/v1.1/subagents/bash-runner/AGENTS.md
    - profiles/gemma-4-26b-a4b-it/v2/subagents/research/AGENTS.md
    - profiles/gemma-4-26b-a4b-it/v2/subagents/code-reviewer/AGENTS.md
    - profiles/gemma-4-26b-a4b-it/v2/subagents/bash-runner/AGENTS.md
    - profiles/gemma-4-31b-it/v1.1/subagents/research/AGENTS.md
    - profiles/gemma-4-31b-it/v1.1/subagents/code-reviewer/AGENTS.md
    - profiles/gemma-4-31b-it/v1.1/subagents/bash-runner/AGENTS.md
  modified:
    - packages/emmy-ux/src/index.ts
    - profiles/qwen3.6-35b-a3b/v3.1/harness.yaml
    - profiles/qwen3.6-27b/v1.1/harness.yaml
    - profiles/gemma-4-26b-a4b-it/v2/harness.yaml
    - profiles/gemma-4-31b-it/v1.1/harness.yaml
key-decisions:
  - "AGENTS.md content byte-identical across profiles in v1 (model-specific tuning deferred to v2)"
  - "Persona slug convention LOCKED: snake_case YAML keys, kebab-case on-disk dirs"
  - "agentsContent pre-loaded at config-load time (B2 fix — dispatcher consumes without re-read)"
  - "Profile hash bump DEFERRED to Plan 04.5-07 closeout"
requirements-completed: []
duration: ~25 min
completed: 2026-04-26
---

# Phase 04.5 Plan 02: Persona schema + 4 profiles + 12 AGENTS.md Summary

`loadPersonaConfig(profilePath)` reads `harness.yaml`, validates the `subagents:` block,
resolves `persona_dir` to absolute paths under the profile root (with symlink-realpath
traversal guard), and pre-loads `AGENTS.md` content into `SubAgentSpec.agentsContent`
(B2 fix). All 4 shipped profiles now carry the canonical block with 3 built-in personas.

**Files:** 16 created (1 src + 2 tests + 12 AGENTS.md + 1 barrel update) + 4 harness.yaml modified
**Tests:** 31 new (11 unit + 20 integration over all 4 profiles) — all green under `bun test`
**Word counts:** AGENTS.md research=127, code-reviewer=110, bash-runner=89 (W2 budget &lt;230)
**Typecheck:** clean

## What was built

### Source

- `persona-loader.ts` — `loadPersonaConfig(profilePath: string): Promise<Record<string, SubAgentSpec>>`
  - Returns `{}` when block missing or `enabled !== true`.
  - Validates `description: string`, `pattern: lean|persona`, `tool_allowlist: string[]` (non-empty), `max_turns: number`.
  - Pattern B: resolves `persona_dir` relative to profile root; rejects `..` traversal AND symlink escape via `realpathSync`. Verifies dir exists + AGENTS.md exists.
  - **B2 fix:** `agentsContent: readFileSync(personaDir + "/AGENTS.md", "utf8")` stamped into spec.
  - Pattern A: `personaDir` and `agentsContent` are `undefined`.
- `PersonaLoadError` class with discriminated `code` field: `no_subagents_block | invalid_persona | path_traversal | missing_agents_md | missing_persona_dir`.

### Profile bundle changes (additive only — no existing content modified)

All 4 profiles get:
- `harness.yaml` appended with the LOCKED `subagents:` block (CONTEXT.md verbatim).
- `subagents/research/AGENTS.md` (127 words) — investigative research persona.
- `subagents/code-reviewer/AGENTS.md` (110 words) — code review persona.
- `subagents/bash-runner/AGENTS.md` (89 words) — bash execution persona.

### Tests

| File | Tests | Coverage |
|------|-------|----------|
| `persona-loader.test.ts` | 11 | happy path + B2 agentsContent + 7 error paths (path_traversal × 2, missing_persona_dir, missing_agents_md, invalid pattern, empty allowlist, no/disabled block) |
| `persona-loader.integration.test.ts` | 20 | parametrized over 4 profiles × 5 invariants — keys, Pattern B research+B2, Pattern B code_reviewer+B2, Pattern A bash_runner, name-preserves-yaml-key |

## Deviations from Plan

**1. [Rule 1 — bug] Bogus mkdir() chained with .catch() in path_traversal test.**
   - Found during: Task 1 test 3 first run.
   - Issue: `mkdirSync(...).catch?.(...)` — `mkdirSync` is sync and doesn't return a Promise.
   - Fix: removed the dead chain (the test path_traversal logic doesn't need the escape file to exist on disk; the loader rejects the path BEFORE trying to read AGENTS.md when it detects the relative-path escape).
   - Verification: test 3 + 3b both pass.

**Total deviations:** 1 auto-fixed.

## Self-Check: PASSED

- ✓ All 4 harness.yaml grep `^subagents:` exactly once
- ✓ All 12 AGENTS.md exist with `wc -w` between 50 and 230 words
- ✓ `bun test packages/emmy-ux/tests/persona-loader.test.ts` → 11 pass
- ✓ `bun test packages/emmy-ux/tests/persona-loader.integration.test.ts` → 20 pass
- ✓ `cd packages/emmy-ux && bun run typecheck` → clean
- ✓ `git diff` against pre-plan baseline shows existing harness.yaml content above the appended block is byte-identical

## Issues Encountered

None.

## Ready for Plan 04.5-03 (OTel parent-child propagation) and Plan 04.5-05 (TUI nesting) — both can now consume the persona registry from this loader.
