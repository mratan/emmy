---
phase: 04.5
plan: 01
subsystem: subagent-dispatch
tags: [subagents, pi-mono, agent-tool, defineTool, agentsFilesOverride]
requires: [pi-coding-agent@0.68.0, pi-ai@0.68.0]
provides:
  - "@emmy/tools createSubAgentTool factory + Agent tool definition"
  - "@emmy/tools dispatchSubAgent (Pattern A lean + Pattern B persona)"
  - "@emmy/tools SubAgentSpec + CreateSubAgentToolOpts types"
affects: [packages/emmy-tools/src/index.ts]
tech-stack:
  added: ["@mariozechner/pi-ai (runtime dep, was devDependency)"]
  patterns: ["TypeBox Type.Union(Literal) dynamic schema", "agentsFilesOverride append-base injection", "test-only session factory seam"]
key-files:
  created:
    - packages/emmy-tools/src/subagent/types.ts
    - packages/emmy-tools/src/subagent/run-one-turn.ts
    - packages/emmy-tools/src/subagent/dispatcher.ts
    - packages/emmy-tools/src/subagent/index.ts
    - packages/emmy-tools/tests/subagent/dispatch.test.ts
    - packages/emmy-tools/tests/subagent/allowlist.test.ts
    - packages/emmy-tools/tests/subagent/dispose.test.ts
  modified:
    - packages/emmy-tools/src/index.ts
    - packages/emmy-tools/package.json
    - bun.lock
key-decisions:
  - "Pattern B uses cwd=parentCwd + agentsFilesOverride injection (B2 fix LOCKED)"
  - "Tool description dynamically built from persona descriptions (W4 fix)"
  - "AbortSignal forwarded via child.abort() with dispose-once guarantee (B3 fix)"
  - "Test-only session factory seam (__setSessionFactoryForTests) for dispose-counting"
requirements-completed: []
duration: ~50 min
completed: 2026-04-26
---

# Phase 04.5 Plan 01: SubAgentTool core Summary

`createSubAgentTool` factory + Pattern A (lean) and Pattern B (persona) dispatch via pi-mono's
`createAgentSessionFromServices`, with `cwd=parentCwd` + `agentsFilesOverride` for Pattern B
(decoupling persona prompt source from tool cwd — the B2 fix LOCKED in CONTEXT.md §decisions).

**Files:** 7 created (4 src + 3 tests), 3 modified  
**Tests:** 14 new (5 factory + 4 dispatch behaviors + 2 allowlist + 4 dispose) — all green under `bun test`  
**Total emmy-tools test count:** 164 pass / 0 fail / 398 expect calls  
**Typecheck:** clean

## What was built

### Source

- `subagent/types.ts` — `SubAgentSpec` (name, description, pattern, personaDir, agentsContent,
  toolAllowlist, modelOverride, maxTurns, persistTranscript) + `CreateSubAgentToolOpts`
  (parentServices, parentCwd, personas record, modelResolver).
- `subagent/run-one-turn.ts` — `runOneTurnReturningText(child, prompt)` subscribes to
  `agent_end`, walks messages from end, captures last assistant text, unsubscribes in `finally`.
- `subagent/dispatcher.ts` — `dispatchSubAgent(opts, persona, params)`:
  - Pattern A: `services = opts.parentServices` (reference equality, no copy).
  - Pattern B: `createAgentSessionServices({cwd: opts.parentCwd, authStorage: opts.parentServices.authStorage, resourceLoaderOptions: { agentsFilesOverride: append [persona AGENTS.md] }})`.
  - `child.setAutoCompactionEnabled(false)` BEFORE `prompt()` (H7 isolation).
  - `params.signal` → `child.abort()` forwarding; `dispose()` in `finally` exactly once.
  - `__setSessionFactoryForTests` seam (production never sets it).
- `subagent/index.ts` — `createSubAgentTool(opts)` returns a `defineTool` shape:
  - Name/label `Agent` (LOCKED Claude Code naming).
  - **Dynamic description** built at factory-call time from each persona's `description` field
    (W4 fix — parent's model sees a per-persona bulleted list).
  - TypeBox parameters: `subagent_type` Union of Literals built from `Object.keys(personas)`,
    required `description` + `prompt`, optional `model`.
  - Unknown `subagent_type` returns structured `{content, details: {ok: false, reason: "unknown-subagent-type"}}` (NOT throws).
  - Unknown errors surfaced as `{content: "[Agent] dispatch failed: ...", details: {ok: false}}`.

### Tests

| File | Tests | Coverage |
|------|-------|----------|
| `dispatch.test.ts` | 8 | factory shape (name, description, params, error path, W4) + V1/V2/V2-decoupling |
| `allowlist.test.ts` | 2 | V3 LOCKED — `child.getActiveToolNames()` narrows; dispatch completes |
| `dispose.test.ts` | 4 | clean-completion, throw-path, abort-mid-prompt, 10-cycle stability (B3) |

**Abort test mechanism:** the faux response factory listens to `options.signal` (provided by
the SDK during prompt()) and rejects with an `AbortError` when aborted. This proves the abort
chain works end-to-end while the dispose-counter seam confirms exactly-once semantics.

## Pattern B alignment (B2 fix verified)

Confirmed `cwd: opts.parentCwd` + `agentsFilesOverride` injection in `dispatcher.ts:74-83`.
The original CONTEXT.md sketch had `cwd: persona.personaDir` which would have broken
`read`/`grep`/`find`/`ls` on the user's project — caught in checker B2, fixed before merge.

## Deviations from Plan

**1. [Rule 3 — additional functionality] Test-only session factory seam.**
   - Found during: Task 2 dispose tests.
   - Issue: bun:test ESM does not allow patching `pi-coding-agent` exports at runtime
     (`require()` returns frozen Module Namespace Object).
   - Fix: added `__setSessionFactoryForTests(factory | undefined)` exported from
     `dispatcher.ts`. Production code paths NEVER set it; tests install dispose-counting
     wrappers around `createAgentSessionFromServices` and uninstall in `finally`.
   - Files modified: `packages/emmy-tools/src/subagent/dispatcher.ts` (test seam).
   - Verification: `bun test packages/emmy-tools/tests/subagent/` exits 0 with all 14 tests passing.

**2. [Rule 1 — bug] Faux provider state has no `api` field.**
   - Found during: Task 2 dispose test setup helper (`reg.state.api` undefined → auth lookup fails).
   - Issue: `FauxProviderRegistration.state` only has `callCount`, not `api`.
   - Fix: closure-capture the `apiId` string in the `setupParent` helper and use it for
     `setRuntimeApiKey(apiId, ...)` directly.
   - Verification: dispose tests now pass.

**Total deviations:** 2 auto-fixed (1 Rule 3 — test infrastructure, 1 Rule 1 — bug).
**Impact:** zero runtime behavior change; both are test-fixture cleanups.

## Self-Check: PASSED

- ✓ `git grep "name: \"Agent\"" packages/emmy-tools/src/subagent/index.ts` → 1 match
- ✓ `git grep "agentsFilesOverride" packages/emmy-tools/src/subagent/dispatcher.ts` → 1 match
- ✓ `git grep "cwd: opts.parentCwd" packages/emmy-tools/src/subagent/dispatcher.ts` → 1 match
- ✓ `git grep "cwd: persona.personaDir"` → 0 matches (rejected pre-revision pattern absent)
- ✓ `bun test packages/emmy-tools/tests/subagent/` → 14 pass / 0 fail
- ✓ `bun test packages/emmy-tools/` → 164 pass / 0 fail
- ✓ `cd packages/emmy-tools && bun run typecheck` → clean (exit 0)

## Issues Encountered

None.

## Ready for Plan 04.5-02 (persona schema + harness.yaml + 12 AGENTS.md across 4 profiles)
