---
phase: 04.5
plan: 07
type: execute
wave: 3
depends_on: [04.5-01, 04.5-02, 04.5-03, 04.5-04, 04.5-05, 04.5-06]
last_revised: 2026-04-26
files_modified:
  - packages/emmy-ux/src/session.ts
  - packages/emmy-ux/src/pi-emmy-extension.ts
  - packages/emmy-ux/scripts/v8-real-deal-e2e.ts
  - packages/emmy-ux/tests/session.subagent-wiring.test.ts
  - docs/runbook.md
  - .planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-trace-tree.txt
  - .planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-sidecar-sample.jsonl
autonomous: false
requirements: []

must_haves:
  truths:
    - "session.ts wires the Agent tool into pi's customTools by calling createSubAgentTool with parentServices + parentCwd + personas (loaded via loadPersonaConfig from the active profile) + parentSessionId + parentSessionDir + a parentInputTokens accessor"
    - "pi-emmy-extension.ts wires SubagentBlock as a custom renderer for tool_call_start events whose toolName === 'Agent' (or an equivalent surfacing path if pi 0.68 lacks the hook)"
    - "V8 — live Qwen 3.6 35B-A3B v3.1 E2E: parent fires Agent({subagent_type: 'research', prompt: 'find usages of customTools'}); child does grep+read; returns coherent summary; OTel trace tree shows the LOCKED 4-level shape (parent_session → agent.tool.Agent → subagent.<name> → child_invoke_agent → child_chat_completion) — visible in Langfuse OR captured via in-memory exporter to a file in runs/. The W1 explicit `agent.tool.Agent` span (Plan 04.5-03) is the level-2 span that materializes this 4-level tree."
    - "docs/runbook.md gains a `Sub-agents` section: how /Agent is dispatched (model-driven via Agent tool), persona registry location, sidecar JSONL layout, OTel trace tree shape, prefix-cache hit-rate is INFORMATIONAL not a pass criterion (Pitfall #22 explicit callout)"
    - "Pitfall #22 acknowledgment: prefix-cache hit rate measurement is captured but NOT used as a gate; runbook says so verbatim"
  artifacts:
    - path: "packages/emmy-ux/scripts/v8-real-deal-e2e.ts"
      provides: "the V8 run harness — boots a real session against vLLM, fires Agent tool, captures trace tree + sidecar"
    - path: "docs/runbook.md"
      provides: "Sub-agents operator section"
      contains: "## Sub-agents"
    - path: ".planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-trace-tree.txt"
      provides: "V8 evidence — captured trace tree (operator-checkpointed)"
    - path: ".planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-sidecar-sample.jsonl"
      provides: "V8 evidence — captured sidecar JSONL line(s)"
    - path: "packages/emmy-ux/tests/session.subagent-wiring.test.ts"
      provides: "Plan 04.5-01..06 wiring contract regression — emmy-ux session.ts passes the right opts to createSubAgentTool"
  key_links:
    - from: "packages/emmy-ux/src/session.ts"
      to: "createSubAgentTool from @emmy/tools + loadPersonaConfig from @emmy/ux"
      via: "added to customTools array passed to createAgentSessionFromServices"
      pattern: "createSubAgentTool|loadPersonaConfig"
    - from: "packages/emmy-ux/src/pi-emmy-extension.ts"
      to: "SubagentBlock + subscribeChildSession from @emmy/ux/components"
      via: "wires render hook OR records the wiring TODO if pi 0.68 lacks the hook surface"
      pattern: "SubagentBlock"
---

<objective>
Wire all 6 prior plans together into the running pi-emmy harness, run V8 (the smoke-test forcing-function) against live Qwen 3.6 35B-A3B v3.1, capture trace-tree + sidecar evidence, and write the operator runbook section. This is the close-out plan — non-autonomous because it requires a live vLLM (operator-gated SC walkthrough per Phase 1/Phase 4 closeout precedent).

Purpose: Plans 04.5-01..06 deliver the parts; this plan delivers the working whole. Without wiring at session.ts level, the Agent tool isn't reachable from a real pi-emmy session. Without V8 evidence, V1-V7 are scaffolding without proof. The runbook section is the operator-readable contract for what sub-agent dispatch looks like in production.

Output: 2 wiring edits (session.ts + pi-emmy-extension.ts); 1 wiring test; 1 runnable v8 script; runbook section; 2 evidence files in `.planning/phases/.../runs/`.

**Pitfall #22 explicit callout (LOCKED):** V8's prefix-cache hit-rate measurement is INFORMATIONAL, not a hard pass criterion. Mamba-hybrid Qwen 3.6 35B-A3B has prefix caching marked "experimental" in vLLM and shows 0% hit rate in spike H9. Runbook MUST state this verbatim.

**W1 follow-through:** V8's trace-tree assertion is the LOCKED 4-level shape, made possible by Plan 04.5-03's explicit `agent.tool.Agent` span. The pre-revision plan's V8 acceptance was loose ("4-level visible") — this revision pins the chain explicitly: each adjacent pair (parent_session.spanId → agent.tool.Agent.parentSpanId; agent.tool.Agent.spanId → subagent.<name>.parentSpanId; subagent.<name>.spanId → child_invoke_agent.parentSpanId where child_invoke_agent is auto-instrumented by @emmy/telemetry's HTTP instrumentation on the wire to vLLM).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-CONTEXT.md
@.planning/pre-phase/04.5-subagents/INTEGRATION-SKETCH.md
@.planning/pre-phase/04.5-subagents/SPIKE-RESULTS.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@docs/runbook.md
@packages/emmy-ux/src/session.ts
@packages/emmy-ux/src/pi-emmy-extension.ts
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-01-SUMMARY.md
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-02-SUMMARY.md
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-03-SUMMARY.md
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-04-SUMMARY.md
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-05-SUMMARY.md
@.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire createSubAgentTool into emmy-ux session.ts customTools; wiring contract test</name>
  <files>packages/emmy-ux/src/session.ts, packages/emmy-ux/tests/session.subagent-wiring.test.ts</files>
  <read_first>
    - packages/emmy-ux/src/session.ts (lines 220-360 — existing createAgentSessionFromServices construction; add Agent tool to customTools array)
    - packages/emmy-ux/tests/session.subagent-wiring.test.ts (the file being created)
    - packages/emmy-ux/src/persona-loader.ts (Plan 04.5-02 — loadPersonaConfig)
    - packages/emmy-tools/src/subagent/index.ts (Plan 04.5-01 — createSubAgentTool)
    - packages/emmy-tools/src/subagent/governor.ts (Plan 04.5-04 — createConcurrencyGovernor)
    - packages/emmy-tools/src/subagent/types.ts (CreateSubAgentToolOpts shape)
  </read_first>
  <behavior>
    - Test 1: When session.ts builds the runtime for a profile that has `subagents.enabled: true`, the customTools array passed to createAgentSessionFromServices contains a tool with name `"Agent"`. Mock loadPersonaConfig + createSubAgentTool to assert the call shape, OR build a real fake profile dir and verify the tool comes through.
    - Test 2: createSubAgentTool is called with `parentServices === services` (the same services object passed to createAgentSessionFromServices), `parentCwd === cwd`, `personas` matching the profile's loadPersonaConfig output, `parentSessionId` matching the session's ID, `parentSessionDir` set (when pi exposes it; otherwise undefined — accept), `parentInputTokens` is a function (not undefined), `governor` is a real ConcurrencyGovernor instance.
    - Test 3: When the active profile lacks a `subagents:` block (or `enabled: false`), createSubAgentTool is NOT called and `Agent` is NOT in customTools.
    - Test 4: modelResolver passed to createSubAgentTool returns the parent's emmyModel for any id input (single-model in v1; cross-model deferred per CONTEXT.md §deferred).
  </behavior>
  <action>
    1. In `packages/emmy-ux/src/session.ts` between lines 254 (after `services = await createAgentSessionServices(...)`) and 257 (the createAgentSessionFromServices call):
       a. Import: `import { createSubAgentTool, createConcurrencyGovernor } from "@emmy/tools";` and `import { loadPersonaConfig } from "./persona-loader";` (top of file, alongside existing imports).
       b. Resolve the profile's bundle path. The existing `profile` argument has `profile.path` or equivalent — verify by reading the ProfileSnapshot type (packages/emmy-ux/src/profile-loader.ts). If the path isn't directly exposed, derive it from `profile.id + profile.version` against the `profiles/` root.
       c. Build `const personas = await loadPersonaConfig(profileBundlePath);`. Wrap in try/catch — if loadPersonaConfig throws or returns `{}`, skip Agent tool wiring (no customTool added).
       d. If `Object.keys(personas).length > 0`: build `const governor = createConcurrencyGovernor({ maxConcurrent: 2, longContextSerializeThresholdTokens: 40000, rejectOverCap: true })` (LOCKED defaults — Plan 04.5-04 I3 fix).
       e. Build the tool spec:
          ```typescript
          const agentTool = createSubAgentTool({
            parentServices: services,
            parentCwd: cwd,
            personas,
            modelResolver: (_id) => emmyModel,                  // v1 single-model
            parentSessionId: sessionId,
            parentSessionDir: undefined,                        // pi 0.68 doesn't expose; sidecar will no-op until SDK surfaces it
            parentInputTokens: () => {
              // best-effort token-count accessor; pi exposes session.messages — count tokens or characters as proxy.
              // For v1: return a coarse char-count / 3 estimate so V6 (long-context) still triggers above ~120K chars.
              try {
                const msgs = (session as any).messages ?? [];
                const chars = JSON.stringify(msgs).length;
                return Math.floor(chars / 3);
              } catch { return 0; }
            },
            governor,
          });
          ```
          NOTE: `session` is created AFTER customTools is assembled. We cannot read `session.messages` at customTools-assembly time. Two options:
          - **Option A (preferred):** make `parentInputTokens` a closure over a lateBound `sessionRef` that is filled in AFTER session is created. Use a `let _sessionRef: any = null;` pattern and `parentInputTokens: () => _sessionRef ? estimateTokens(_sessionRef) : 0;`. Then `_sessionRef = session;` after the createAgentSessionFromServices call.
          - **Option B:** move the customTools assembly to happen AFTER session creation by reordering. This requires understanding pi's API more carefully — customTools is passed AT session creation time. Option A is cleaner.
          Use Option A.
       f. Push agentTool onto `customTools` (the array passed at line 261 — adapt to the existing customTools handling — see line 261's `customTools as unknown as Parameters<...>` cast).

    2. Create `packages/emmy-ux/tests/session.subagent-wiring.test.ts`. Use the same testing approach as `session.test.ts` (look at `packages/emmy-ux/tests/session.test.ts` and `packages/emmy-ux/tests/session.integration.test.ts` for the existing pattern). Mock or spy on `createSubAgentTool` to capture its call args, then assert the 4 behaviors. The test runs against a synthetic fake profile dir built via mkdtempSync containing the LOCKED subagents block.

    3. **DO NOT** modify the existing customTools assembly in any way that would break the Plan 02-06 + Plan 03-01 customTools wire-through tests. The Agent tool is APPENDED to the existing array; nothing else changes.
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bun test packages/emmy-ux/tests/session.subagent-wiring.test.ts packages/emmy-ux/tests/session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/emmy-ux/src/session.ts` contains exact strings `createSubAgentTool(` AND `loadPersonaConfig(` AND `createConcurrencyGovernor(`
    - File contains `parentSessionId: sessionId` AND `parentInputTokens` AND `governor`
    - File `packages/emmy-ux/tests/session.subagent-wiring.test.ts` exists with at least 4 tests covering the behaviors above
    - `bun test packages/emmy-ux/tests/session.subagent-wiring.test.ts` exits 0
    - `bun test packages/emmy-ux/tests/session.test.ts` STILL exits 0 (no regression in the existing wiring tests)
    - The customTools array order: existing tools FIRST, Agent tool appended LAST (so customTools indexing in any other test isn't disturbed)
  </acceptance_criteria>
  <done>session.ts wires Agent into customTools when profile has subagents enabled; existing tests still green; Agent tool reachable from a real pi-emmy session.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: pi-emmy-extension.ts SubagentBlock wiring (best-effort) + V8 runner script + runbook section</name>
  <files>packages/emmy-ux/src/pi-emmy-extension.ts, packages/emmy-ux/scripts/v8-real-deal-e2e.ts, docs/runbook.md</files>
  <read_first>
    - packages/emmy-ux/src/pi-emmy-extension.ts (existing extension factory; check if pi 0.68 exposes a `tool_render` or similar hook for nested rendering)
    - packages/emmy-ux/src/components/subagent-block.tsx (Plan 04.5-05 output)
    - packages/emmy-ux/src/components/subagent-event-bridge.ts (Plan 04.5-05 output)
    - packages/emmy-ux/scripts/v8-real-deal-e2e.ts (the file being created)
    - docs/runbook.md (existing — append section, NOT modify above)
    - .planning/research/PITFALLS.md (Pitfall #18, Pitfall #22)
    - packages/emmy-ux/scripts/spikes/04.5-subagents/h9-real-qwen.ts (V8 reference — wire-level vLLM probe)
  </read_first>
  <action>
    1. **TUI wiring (best-effort — depends on pi 0.68 surface):**
       a. Read `packages/emmy-ux/src/pi-emmy-extension.ts` and the pi 0.68 ExtensionAPI d.ts (`node_modules/.bun/@mariozechner+pi-coding-agent@0.68.0+.../node_modules/@mariozechner/pi-coding-agent/dist/...`).
       b. If pi 0.68 exposes `pi.registerToolRenderer({toolName: "Agent", render: (snapshot) => SubagentBlock})` or `pi.subscribeToolEvents`, wire it: when a parent tool_call_start fires for `toolName === "Agent"`, spawn a `subscribeChildSession`-style observer (BUT — child session here is NOT a pi-managed session in the parent's TUI; it's spawned inside the dispatcher. The wiring needs to bridge the dispatcher's child subscription to the TUI render hook).
       c. If pi 0.68 does NOT expose a suitable hook (likely — the spec H9 caveat noted pi has no off-the-shelf collapsible-nested-turn widget), then:
          - Add a comment in pi-emmy-extension.ts marking the deferred wiring: `// TODO(04.5-followup): pi 0.68 lacks a tool_render hook for nested transcripts. SubagentBlock + subscribeChildSession ship in @emmy/ux/components ready for use; live wiring requires either a pi 0.68+ extension hook or a dispatcher-side event bus that the TUI subscribes to. Tracked in .planning/STATE.md as a phase 04.5 followup.`
          - Do NOT block V8 on this — V8 evidence focuses on OTel trace tree (the authoritative observability surface per Pitfall #18 mitigation), with TUI nesting as the secondary user-visible surface.
       d. Either way, the file MUST contain the literal string `SubagentBlock` (either as a real wiring or a TODO reference) so future grep audits find the wiring site.

    2. **Create `packages/emmy-ux/scripts/v8-real-deal-e2e.ts`:**
       The script does the following end-to-end against a live emmy-serve + qwen3.6-35b-a3b@v3.1:
       - Spin up `@emmy/telemetry` OTel SDK with InMemorySpanExporter (so the script captures every span and writes them to `.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-trace-tree.txt` as a hierarchical text dump on completion).
       - Build a real pi-emmy session (mirroring `buildRealPiRuntime` in session.ts, but as a script-mode wrapper) pointed at qwen3.6-35b-a3b@v3.1.
       - Set `parentSessionDir` to a temp dir we control so the sidecar JSONL lands somewhere we can read.
       - Fire ONE prompt to the parent: `Use the Agent tool with subagent_type="research" and prompt="find usages of customTools in the @emmy/tools package; return a 4-sentence summary citing file:line".` — let the model decide to invoke Agent.
       - On completion: dump the trace-tree text + the sidecar JSONL to the runs/ dir. Print the prefix-cache hit-rate observed (informational only — Pitfall #22).
       - **Exit 0 if (W1 LOCKED 4-level shape):** the captured spans contain
         (a) a parent span (the pi-emmy session's root span — name varies by emmy-telemetry config; locate by absence of a parent_span_id),
         (b) a span named `agent.tool.Agent` whose parent_span_id equals (a)'s span_id,
         (c) a span named `subagent.research` whose parent_span_id equals (b)'s span_id,
         (d) at least one child-side span (the `@opentelemetry/instrumentation-http` auto-instrumented HTTP request to vLLM; name typically `POST /v1/chat/completions`) whose parent_span_id equals (c)'s span_id;
         AND all four spans share the same `trace_id`;
         AND the sidecar JSONL has at least one entry with `parent_span_id` populated;
         AND the parent's final message contains either "customTools" or "registerNativeTools" or "@emmy/tools".
       - Exit 1 if any of those fail.
       - The script is OPERATOR-RUN — it requires `emmy-serve` started via `start_emmy.sh` first. The plan does not run this script automatically; instead, the operator runs it post-implementation and pastes the resulting `runs/v8-trace-tree.txt` + `runs/v8-sidecar-sample.jsonl` into the phase dir.

    3. **Append the Sub-agents section to `docs/runbook.md`** (insert BEFORE the "Common error messages" section if convenient, otherwise append at end). Section content (use exact heading levels and markdown):

    ```markdown
    ## Sub-agents

    Emmy ships a Claude Code-style `Agent` tool that lets the parent model dispatch focused sub-tasks to a child session. The child runs to completion and returns ONLY a text summary — its intermediate tool calls do NOT pollute the parent's context. Every dispatch is observable.

    ### How dispatch works

    The parent's model invokes the `Agent` tool via tool-call:
    ```json
    {"name": "Agent", "arguments": {"subagent_type": "research", "description": "find usages of customTools", "prompt": "Search the @emmy/tools package..."}}
    ```

    The harness:
    1. Looks up the persona (`research` / `code_reviewer` / `bash_runner`) in the active profile's `harness.yaml subagents.personas` block.
    2. Spawns a child `AgentSession` per the persona's `pattern` (`lean` = shared services; `persona` = per-persona services + parent's cwd + persona AGENTS.md injected via resourceLoaderOptions.agentsFilesOverride).
    3. Disables auto-compaction on the child.
    4. Runs ONE turn against the child, captures the final assistant text, disposes the child.
    5. Returns the text to the parent's model as a tool result.

    ### Persona registry

    Personas live in each profile's bundle:
    ```
    profiles/<name>/<version>/
      harness.yaml                        # subagents: { enabled, max_concurrent, ..., personas: { ... } }
      subagents/<persona>/
        AGENTS.md                         # persona's system prompt (pi-minimalism: ~100-300 tokens, &lt; 230 words)
    ```

    The 3 built-in personas are shipped with all 4 of Emmy's first-class profiles. Persona files are profile-scoped — model-shaped logic stays out of code.

    ### Concurrency cap + long-context rule

    - Default `max_concurrent: 2` (vLLM hardware ceiling on the GB10 / 128 GB UMA box; default `rejectOverCap: true` — over-cap dispatches are REJECTED with `agent.dispatch.rejected` telemetry event).
    - When parent input tokens exceed `long_context_serialize_threshold_tokens` (default 40000), dispatches SERIALIZE — effective cap = 1 — to prevent KV-cache contention with the parent.
    - Telemetry events fire on all branches: `agent.dispatch.queued` (cap reached, waited), `agent.dispatch.serialized` (long-context branch), and `agent.dispatch.rejected` (over-cap, hard reject).

    ### Observability (Pitfall #18)

    Every dispatch emits the LOCKED 4-level OTel trace tree, parented to the parent's session span:
    ```
    parent_session_span
      └── agent.tool.Agent                         (gen_ai.tool.name=Agent, gen_ai.tool.persona=research)
            └── subagent.research                  (gen_ai.agent.name=research, gen_ai.conversation.id=<parent_session_id>)
                  ├── child_invoke_agent
                  │     ├── child_chat_completion  (POST /v1/chat/completions; auto-instrumented HTTP)
                  │     ├── child_tool_call.grep
                  │     └── child_tool_call.read
                  └── (agent_end)
    ```

    The explicit `agent.tool.Agent` span is the level-2 link (Plan 04.5-03 W1 fix) — without it the trace tree would skip the per-tool-call layer.

    Inspect via Langfuse (http://localhost:3000 → Traces) or the JSONL stream at `runs/<iso>-<sha>/events.jsonl`.

    ### Forensics — sidecar index

    Every dispatch appends one line to `<parentSessionDir>/session-<parentSessionId>.subagents.jsonl` with `parent_span_id`, `child_session_id`, `persona`, `pattern`, `started_at`, `ended_at`, `ok`, `trace_id`. This is the offline replay/audit record — use `jq` to filter by persona or trace_id.

    When a persona has `persist_transcript: true`, the child's full JSONL also lands at `<parentCwd>/.emmy/subagents/<persona>-<timestamp>-<id>/<sessionId>.jsonl`.

    ### Prefix-cache hit rate is INFORMATIONAL (Pitfall #22)

    The H9 spike on Qwen 3.6 35B-A3B v3.1 observed 0% prefix-cache hit rate during real wire-level probes. vLLM marks prefix caching "experimental" for Mamba-hybrid models in this version. The original "Pattern A = free prefix-cache hits" rationale has been WITHDRAWN — Pattern A's benefit is now solely the ~5 ms instantiation cost (vs Pattern B's ~50 ms).

    **Do NOT gate Agent dispatch on prefix-cache hit rate.** It is captured for inspection but never blocks. When vLLM upstream graduates Mamba prefix caching out of "experimental," re-measure per profile and tighten the gate then.

    ### Disabling sub-agents

    Set `subagents.enabled: false` in any profile's `harness.yaml` to remove the `Agent` tool from that profile's customTools registry. The 12 AGENTS.md files remain on disk but are never read.

    ### V8 verification

    The end-to-end smoke is `packages/emmy-ux/scripts/v8-real-deal-e2e.ts`. Operator runs:
    ```bash
    bash start_emmy.sh                  # boot vLLM + qwen3.6-35b-a3b@v3.1
    bun run packages/emmy-ux/scripts/v8-real-deal-e2e.ts
    ```

    On success, evidence lands in `.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-trace-tree.txt` + `v8-sidecar-sample.jsonl`. These are committed as the phase-close artifact.
    ```

    4. The runbook diff MUST be additive — no existing sections modified.
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bash -c 'set -e; grep -q "## Sub-agents" docs/runbook.md && grep -q "INFORMATIONAL" docs/runbook.md && grep -q "Pitfall #22" docs/runbook.md && grep -q "Pitfall #18" docs/runbook.md && grep -q "agent.tool.Agent" docs/runbook.md && grep -q "SubagentBlock" packages/emmy-ux/src/pi-emmy-extension.ts && test -f packages/emmy-ux/scripts/v8-real-deal-e2e.ts && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/runbook.md` contains exact heading `## Sub-agents`
    - File `docs/runbook.md` contains the LOCKED Pitfall #22 callout: literal string `INFORMATIONAL` AND `Pitfall #22` AND `Do NOT gate Agent dispatch on prefix-cache hit rate.`
    - File `docs/runbook.md` contains literal `Pitfall #18` (the observability rationale)
    - File `docs/runbook.md` contains literal `agent.tool.Agent` (W1 — the level-2 span name in the LOCKED 4-level tree diagram)
    - File `docs/runbook.md` contains the persona registry layout block
    - File `packages/emmy-ux/src/pi-emmy-extension.ts` contains the literal string `SubagentBlock` (either real wiring or TODO reference)
    - File `packages/emmy-ux/scripts/v8-real-deal-e2e.ts` exists AND contains literal `subagent_type` AND `customTools` AND `find usages of customTools`
    - Existing runbook content above the new section is byte-identical (verify by `git diff docs/runbook.md` showing only ADDITIONS)
  </acceptance_criteria>
  <done>Runbook Sub-agents section landed with W1 4-level diagram; v8 script exists and is runnable; pi-emmy-extension references SubagentBlock; ready for operator V8 run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: V8 operator-gated SC walkthrough — live Qwen E2E + 4-level trace-tree + sidecar evidence (W1)</name>
  <what-built>
    All Wave 1 + Wave 2 plans (04.5-01 through 04.5-06) ship the parts. Task 1 of this plan wires them into session.ts. Task 2 ships the runnable v8 script + runbook section. This task is the OPERATOR-GATED smoke that confirms the whole stack works against live Qwen 3.6 35B-A3B v3.1 and lands committed evidence — including the LOCKED 4-level trace tree (W1 — agent.tool.Agent → subagent.<name> chain).
  </what-built>
  <how-to-verify>
    1. Start the serving stack: `bash start_emmy.sh` (default profile = qwen3.6-35b-a3b@v3.1). Wait for the SP_OK boot smoke to pass.
    2. Run the V8 script: `bun run packages/emmy-ux/scripts/v8-real-deal-e2e.ts`. Expected wall: ~30-90 s (includes 1 parent turn + 1 child run with grep+read).
    3. The script writes evidence to `.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/runs/v8-trace-tree.txt` (hierarchical span dump) and `runs/v8-sidecar-sample.jsonl` (sidecar entry sample). Inspect both.
    4. Open Langfuse (http://localhost:3000 → Traces) and confirm: ONE trace shows the LOCKED 4-level shape `parent_session → agent.tool.Agent → subagent.research → child_invoke_agent → child_chat_completion`. The W1 explicit `agent.tool.Agent` span MUST be visible as a separate node in the tree (not a missing/skipped link).
    5. Capture the prefix-cache hit-rate from `curl http://127.0.0.1:8002/metrics | grep prefix_cache_hits_total` BEFORE and AFTER the V8 run. Difference = hits during V8. Record the number; expected to be 0 on Qwen 3.6 35B-A3B (Pitfall #22 — informational only).
    6. Verify the parent's final assistant message references `customTools` or `registerNativeTools` or `@emmy/tools` (loose semantic check that the sub-agent did real work).
    7. Commit the two evidence files (`v8-trace-tree.txt` + `v8-sidecar-sample.jsonl`) under the phase's `runs/` dir.
    8. Update `.planning/STATE.md` Phase 04.5 line: flip from `[ ]` to `[x]` and record the SC verdict.

    Pass criteria:
    - V1-V7 tests all green from prior plans (`bun test packages/emmy-tools/tests/subagent/ packages/emmy-ux/tests/persona-loader.test.ts packages/emmy-ux/tests/persona-loader.integration.test.ts packages/emmy-ux/tests/components/ packages/emmy-ux/tests/session.subagent-wiring.test.ts`).
    - V8 trace tree shows the **LOCKED 4-level nesting (W1)**: parent_session_span → agent.tool.Agent → subagent.research → child_invoke_agent → child_chat_completion, with shared trace_id and adjacent-pair parent_span_id linkage at every layer.
    - V8 sidecar entry has `parent_span_id` + `trace_id` + `child_session_id` + `persona: "research"` + `ok: true`.
    - Parent's final message is semantically coherent.
    - Prefix-cache hit-rate captured (any value — INFORMATIONAL per Pitfall #22).

    Fail criteria:
    - Trace tree missing the `agent.tool.Agent` span OR the `subagent.research` span (W1 — both must be present and chain-linked).
    - trace_id not shared across the chain.
    - Sidecar JSONL not written.
    - Parent's final message is empty / refuses / loops.
  </how-to-verify>
  <resume-signal>Type `sc1 phase04.5 green` to flip Phase 04.5 to Done† pending the runs/ commit, OR describe the failure mode to route the work to a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vLLM → harness | Same as every other phase — vLLM is loopback-bound; SP_OK canary still fires. |
| sub-agent's child request → vLLM | Inherits parent's authentication; OTel traceparent on the wire enables per-child trace linkage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-07-01 | Information Disclosure | V8 evidence files commit prompts/outputs that contain code snippets from the live repo | accept | The eval prompt is bounded and operator-controlled ("find usages of customTools"); evidence files are the same retention class as existing `runs/<phase>/` artifacts. |
| T-04.5-07-02 | Repudiation | V8 fails silently with a partial trace tree | mitigate | Script's exit code gates evidence commit; partial traces dumped to runs/ for offline diagnosis. |
| T-04.5-07-03 | DoS | runaway sub-agent dispatch saturates the engine | mitigate | Concurrency governor (Plan 04.5-04) caps at 2 + rejects 3rd; long-context branch caps at 1. |
| T-04.5-07-04 | Tampering | malicious profile yaml during V8 run swaps in a hostile persona | accept | Profile bundle is operator-controlled and committed. V8 runs against the committed v3.1 bundle. |
</threat_model>

<verification>
Run `bun test packages/emmy-tools/tests/subagent/ packages/emmy-ux/tests/components/ packages/emmy-ux/tests/persona-loader.test.ts packages/emmy-ux/tests/persona-loader.integration.test.ts packages/emmy-ux/tests/session.subagent-wiring.test.ts` — must exit 0 with combined &gt;= 32 tests (was &gt;=30 pre-revision; B1 +1, B3 +2, I3 +2, W1 +2, W4 +1; some additions are in existing files).
Run `bun run typecheck` — must exit 0.
Operator runs `bun run packages/emmy-ux/scripts/v8-real-deal-e2e.ts` post-implementation; commits the runs/ artifacts.
Verify `git grep "INFORMATIONAL" docs/runbook.md` matches the Pitfall #22 callout.
Verify `git grep "Pitfall #18" docs/runbook.md` matches the observability rationale.
Verify `git grep "agent.tool.Agent" docs/runbook.md` matches the W1 LOCKED 4-level diagram.
</verification>

<success_criteria>
1. session.ts wires Agent tool into customTools when subagents enabled.
2. Wiring contract test green (4 behaviors).
3. v8-real-deal-e2e.ts script exists and is runnable.
4. Runbook Sub-agents section landed with LOCKED Pitfall #22 callout AND the W1 4-level trace-tree diagram.
5. V1-V7 (combined from Waves 1+2 + this plan's Task 1) all green.
6. V8 evidence files committed (operator-gated SC walkthrough — passes when `sc1 phase04.5 green` resume signal fires); LOCKED 4-level trace-tree shape verified end-to-end (W1).
7. No regression in existing emmy-ux session tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04.5-observable-sub-agent-dispatch-v1-inserted/04.5-07-SUMMARY.md` documenting:
- Phase 04.5 closeout summary: V1-V7 test count breakdown, V8 verdict (with operator resume signal text)
- Profile hash recomputation status: each of the 4 shipped profiles' bundles gained `subagents/` files from Plan 04.5-02 — record whether hashes were re-computed (CONTEXT.md notes this is closeout-level; either do it here or defer with explicit reason).
- Cumulative diff: Wave 1 plans + Wave 2 plans + this plan = total LoC + test count
- Pitfall #18 mitigation: confirm OTel trace tree achieves the LOCKED 4-level nesting end-to-end (W1)
- Pitfall #22 acknowledgment: prefix-cache hit-rate observed in V8 (any value), runbook callout in place
- Open follow-ups (e.g. SubagentBlock TUI wiring if pi 0.68 lacked the hook)
- Update ROADMAP.md Phase 04.5 line: 7/7 plans complete; flip to `[x]` on operator SC sign-off
</output>
