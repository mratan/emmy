---
phase: 04.2
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - docs/runbook.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TOOLS-10, UX-04, UX-07]
requirements_addressed: [TOOLS-10, UX-04, UX-07]
threats_addressed: []
tags: [closeout, sc-walkthrough, operator-gated, roadmap, state]

must_haves:
  truths:
    - "docs/runbook.md gains a Phase-04.2 SC walkthroughs section with verbatim operator-runbook scripts for SC-1, SC-2, SC-3 (matching D-08 LOCKED)"
    - "ROADMAP.md Phase 04.2 entry has a real Goal line (not 'Urgent work - to be planned'), real Requirements (TOOLS-10, UX-04, UX-07), and lists 6 plans with their objectives"
    - "STATE.md Project Memory has a Phase 04.2 closure entry summarizing what shipped + which tests are green + which SCs are operator-gated"
    - "REQUIREMENTS.md TOOLS-10 + UX-07 + UX-04 status updated (Done after SC-1 green; Done† while operator-gated SCs pending)"
    - "Operator can copy-paste SC walkthrough scripts from runbook and produce SC-1/SC-2/SC-3 phase4.2 green/red verdicts that the executor can use to flip the closeout"
  artifacts:
    - path: "docs/runbook.md"
      provides: "New ## Phase 04.2 SC walkthroughs section with 3 operator-runbook scripts (SC-1: Mac one-shot; SC-2: /start /stop /status round-trip; SC-3: /profile swap from Mac); each script includes setup, expected output, resume signal phrase, and rollback steps"
      contains: "SC-1 phase4.2"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 04.2 entry replaces 'Urgent work - to be planned' Goal with the real CONTEXT.md goal; Requirements: TOOLS-10, UX-04, UX-07; Plans: 6 listed with objectives + status checkboxes"
      contains: "TOOLS-10, UX-04, UX-07"
    - path: ".planning/STATE.md"
      provides: "Project Memory entry dated 2026-04-25 onwards summarizing Phase 04.2 work; Performance Metrics row for each Phase 04.2 plan; Current Position updated"
      contains: "Phase 04.2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TOOLS-10, UX-04, UX-07 status flipped per SC outcomes (Done if all 3 SCs green; Done† if pending operator gates)"
      contains: "TOOLS-10"
  key_links:
    - from: "docs/runbook.md"
      to: ".planning/phases/04.2-remote-client-mode-parity/04.2-CONTEXT.md D-08 LOCKED"
      via: "SC-1/SC-2/SC-3 walkthrough scripts derived from D-08 acceptance contract"
      pattern: "sc1 phase4.2 green\\|sc2 phase4.2 green\\|sc3 phase4.2 green"
    - from: ".planning/ROADMAP.md Phase 04.2"
      to: "TOOLS-10 + UX-04 + UX-07 requirements"
      via: "Requirements field listing"
      pattern: "TOOLS-10\\|UX-04\\|UX-07"
    - from: ".planning/STATE.md Project Memory"
      to: "Phase 04.2 plan summaries"
      via: "narrative summary referencing 04.2-01-PLAN through 04.2-05-PLAN"
      pattern: "04.2-0[1-5]"
---

<objective>
Land Phase 04.2 as a closed phase from a planning-system perspective: operator-gated SC walkthroughs documented; ROADMAP's "TBD" placeholders replaced with the real phase scope; STATE + REQUIREMENTS updated.

Purpose: Plans 01-05 ship code; this plan ships paperwork. Without this plan, the next person opening ROADMAP.md sees "Urgent work - to be planned" for Phase 04.2 even though the work is done. The 3 operator-gated SC walkthroughs are the green-light gates that flip Done† → Done in REQUIREMENTS.md.

Output: 1 modified runbook (SC walkthrough scripts) + 1 modified ROADMAP (Phase 04.2 entry filled in) + 1 modified STATE (Project Memory + Performance Metrics) + 1 modified REQUIREMENTS (TOOLS-10/UX-04/UX-07 status). After this plan, Phase 04.2 is closed pending operator confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-CONTEXT.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-VALIDATION.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-01-PLAN.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-02-PLAN.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-03-PLAN.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-04-PLAN.md
@.planning/phases/04.2-remote-client-mode-parity/04.2-05-PLAN.md
@.planning/phases/04-gemma-4-profile-profile-system-maturity/04-CLOSEOUT.md
@docs/runbook.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator-gated SC-1 phase4.2 walkthrough — Mac client one-shot through Tailscale</name>
  <what-built>
Plans 01-05 ship the FastAPI sidecar (Plan 01) + systemd lifecycle (Plan 02) + TS dual-path dispatcher (Plan 03) + slash commands + footer remote branch (Plan 04) + install-client.sh + web-search.ts EMMY_SEARXNG_URL override (Plan 05). All unit + integration tests green:
- 5 sidecar unit-test files + 1 sidecar integration test (Plan 01-02)
- 1 HTTP-mode runner test + 1 spawn-argv snapshot test (Plan 03)
- 1 sidecar-status-client test + 1 metrics-poller remote test + 1 slash-commands.start-stop-status test (Plan 04)
- 1 web-search-env-override test + 1 air-gap-local-mode smoke (Plan 05)
- 1 systemd-unit smoke + 1 air-gap-phase3 dry-run smoke (Plans 02 + 05)

Per D-08 LOCKED, the 3 operator-gated SC walkthroughs are now the only remaining gates between this phase and closure.

**SC-1 phase4.2:** Mac client one-shot through Tailscale. Operator runs `emmy --print "Reply with: SP_OK_PHASE4_2_GREEN"` from a Mac/laptop with the install-client.sh wrapper installed. The request must traverse: Mac wrapper → Tailscale Serve (vLLM:8002) → Spark vLLM container → response back through TS → Mac TUI. Web_search must work via EMMY_SEARXNG_URL → Tailscale Serve (SearxNG:8888) → Spark SearxNG container if exercised.
  </what-built>
  <how-to-verify>
**Pre-flight (operator does these first):**

1. Confirm sidecar unit installed + running on Spark:
   ```bash
   ssh $EMMY_SPARK_HOST 'systemctl --user status emmy-sidecar'
   # Expected: Active: active (running)
   ```

2. Confirm 3 Tailscale Serve routes on Spark:
   ```bash
   ssh $EMMY_SPARK_HOST 'tailscale serve status'
   # Expected: routes for :8002 (vLLM), :8003 (sidecar), :8888 (SearxNG)
   ```

3. Confirm Mac wrapper installed:
   ```bash
   cat ~/.local/bin/emmy | grep -E 'EMMY_REMOTE_CLIENT|EMMY_SERVE_URL|EMMY_SEARXNG_URL'
   # Expected: 3 lines matching all three env vars
   ```

4. Confirm vLLM is up on Spark (otherwise /start it first):
   ```bash
   ssh $EMMY_SPARK_HOST 'curl -sf http://127.0.0.1:8002/v1/models | head -c 100'
   # Expected: JSON with current model
   ```

**SC-1 walkthrough (run from Mac terminal):**

```bash
# Step 1 — sanity check sidecar reachability over Tailscale:
curl -sf "https://$EMMY_SPARK_HOST:8003/healthz"
# Expected: {"ok":true,"version":"0.4.2"}

# Step 2 — sanity check sidecar /status:
curl -sf "https://$EMMY_SPARK_HOST:8003/status" | python3 -m json.tool
# Expected: JSON with state="ready", vllm_up=true, profile_id+profile_variant populated

# Step 3 — one-shot inference through emmy wrapper:
emmy --print "Reply with: SP_OK_PHASE4_2_GREEN"
# Expected: Output contains "SP_OK_PHASE4_2_GREEN" (Pitfall #6 SP_OK canary alive on remote path)
# This proves the full Mac → TS → Spark vLLM round-trip works.

# Step 4 — web_search smoke (only if SearxNG is up on Spark):
emmy --print "Use web_search to find: bun 1.3.13 release date"
# Expected: agent uses web_search tool, gets results from Spark-hosted SearxNG via Tailscale
# Proves EMMY_SEARXNG_URL override path works.
```

**Resume signal:** Type `sc1 phase4.2 green` if all 4 steps pass. If a step fails:
- Step 1 fails → sidecar systemd unit problem; `journalctl --user -u emmy-sidecar -f` on Spark
- Step 2 fails → Pydantic schema mismatch or state machine race; check sidecar logs
- Step 3 fails → SP_OK delivery broken on remote path (Pitfall #6 regression); diagnose with TUI footer trace
- Step 4 fails → EMMY_SEARXNG_URL not picked up OR SearxNG TS Serve route missing; confirm via `curl -sf "https://$EMMY_SPARK_HOST:8888/search?q=test&format=json"` from Mac

Type `sc1 phase4.2 red: <one-line reason>` if any step fails irrecoverably.
  </how-to-verify>
  <resume-signal>Type `sc1 phase4.2 green` (success) or `sc1 phase4.2 red: <reason>` (failure)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-gated SC-2 phase4.2 walkthrough — /start /stop /status round-trip with graceful drain</name>
  <what-built>
Plans 01 (controller.py /start /stop /status endpoints with D-01 graceful drain), 04 (slash commands), 05 (install-client.sh wrapper exporting EMMY_SERVE_URL).

**SC-2 phase4.2:** /start /stop /status round-trip from Mac. Operator runs all three slash commands from the pi-emmy TUI; the graceful-drain semantics of /stop are validated against an in-flight long generation. Demonstrates D-01 LOCKED + D-02 LOCKED + D-03 LOCKED operating together.
  </what-built>
  <how-to-verify>
**Pre-flight:** SC-1 green (or at least Step 1-3 of SC-1 green). Sidecar healthy.

**SC-2 walkthrough (run from Mac terminal in interactive pi-emmy TUI):**

```bash
emmy   # opens interactive pi-emmy TUI
```

In the TUI:

```
# Step 1 — /status from cold:
/status
# Expected (footer notify): "sidecar: state=ready vllm=qwen3.6-35b-a3b@v3.1-default kv=N% temp=N°C"
# (or state=stopped if Spark vLLM was already stopped)

# Step 2 — /stop while idle (no in-flight requests):
/stop
# Confirm dialog appears; choose Yes
# Expected (footer setStatus): "emmy.swap: draining" then "emmy.swap: <empty>" then notify "emmy-serve stopped"
# Expected (Spark side): vLLM container stops gracefully within ~2 seconds (no in-flight requests)

# Step 3 — /status after /stop:
/status
# Expected: "sidecar: state=stopped vllm_up=false ..." (the underlying httpx probe to vLLM fails, vllm_up flips false)

# Step 4 — /start to bring back vLLM:
/start qwen3.6-35b-a3b@v3.1-default
# Expected (footer setStatus): "emmy.swap: stopping vLLM" → "loading weights 0%" → "loading weights 50%" → "warmup" → "ready"
# Then notify: "started qwen3.6-35b-a3b@v3.1-default"

# Step 5 — long-generation drain test (the D-01 30s drain semantic):
# In a SECOND Mac terminal, kick off a long generation:
#   emmy --print "Write a 4000-word essay about the history of the Fortran programming language"
# Within 5 seconds (while it's still generating), in the FIRST terminal's TUI:
/stop
# Confirm dialog → Yes
# Expected (footer): "emmy.swap: draining {in_flight: 1}" — visible within 1 second
# Expected (second terminal): generation either completes (if it finishes in <30s) OR truncates with SIGTERM (if >30s)
# Expected (footer after drain): "emmy.swap: <empty>" then notify "emmy-serve stopped"
# CRITICAL: vLLM did NOT crash mid-stream uncleanly; the drain SSE event fired; SIGTERM is the deadline cleanup.

# Step 6 — restart for downstream tests:
/start qwen3.6-35b-a3b@v3.1-default
# Restore the running state.
```

**Resume signal:** Type `sc2 phase4.2 green` if all 6 steps pass. Specifically verify:
- /stop drain UI shows "draining" with in_flight count
- /stop on long-gen does NOT corrupt the second terminal's session beyond truncation
- /start restores fully; subsequent /status shows state=ready

If any step fails: `sc2 phase4.2 red: <reason>`. Common failure modes:
- Drain SSE event never fires → metrics-poller's vllm:num_requests_running parsing broken; check controller.py /stop event_generator
- vLLM doesn't actually stop after SIGKILL deadline → controller.py _vllm_pid not tracked correctly; the systemd unit doesn't own vLLM directly so this needs investigation
- /start cold-start argv missing --from on a from-stopped state → orchestrator subprocess fails; check Plan 01 Task 3 acceptance criteria for "test_cold_start_argv"
  </how-to-verify>
  <resume-signal>Type `sc2 phase4.2 green` (success) or `sc2 phase4.2 red: <reason>` (failure)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-gated SC-3 phase4.2 walkthrough — /profile swap from Mac (C-06 SSE wire format end-to-end)</name>
  <what-built>
Plans 01 (controller.py /profile/swap endpoint + SSE wrapping of orchestrator stdout), 03 (HTTP-mode dispatcher + SSE consumer), 04 (slash command wiring already extant for /profile via Phase 4 plus dual-path dispatcher branch).

**SC-3 phase4.2:** /profile swap initiated from Mac, completes end-to-end with the four-phase progress sequence visible in the TUI footer. Validates the C-06 LOCKED Phase-4 D-02 progress contract is unchanged when wrapped in SSE over Tailscale.
  </what-built>
  <how-to-verify>
**Pre-flight:** SC-1 + SC-2 both green. vLLM running on Spark (state=ready). At least 2 profiles registered on Spark (e.g. qwen3.6-35b-a3b@v3.1-default and gemma-4-26b-a4b-it@v2-default).

**SC-3 walkthrough (run from Mac terminal in interactive pi-emmy TUI):**

```bash
emmy   # opens interactive pi-emmy TUI
```

In the TUI:

```
# Step 1 — confirm starting profile:
/status
# Expected: "sidecar: state=ready vllm=qwen3.6-35b-a3b@v3.1-default ..."

# Step 2 — /profile swap to a different model:
/profile gemma-4-26b-a4b-it@v2-default
# Confirm dialog appears; choose Yes
# Expected (footer setStatus, in this exact order — the C-06 LOCKED contract):
#   "emmy.swap: stopping vLLM"
#   "emmy.swap: loading weights 0%"
#   "emmy.swap: loading weights 50%"
#   "emmy.swap: warmup"
#   "emmy.swap: ready"
# Final notify: "swapped to gemma-4-26b-a4b-it@v2-default"
# Total wall time: ~3-8 minutes (weight load dominates)

# Step 3 — verify swap completed correctly:
/status
# Expected: "sidecar: state=ready vllm=gemma-4-26b-a4b-it@v2-default ..."

# Step 4 — confirm inference works on the new model:
emmy --print "Reply with: SP_OK_GEMMA4_VIA_TAILSCALE_GREEN"
# (Open new terminal — the TUI is still in the swap session)
# Expected: response contains "SP_OK_GEMMA4_VIA_TAILSCALE_GREEN"
# Proves the new model is loaded and serving inference.

# Step 5 — swap back to verify symmetry:
/profile qwen3.6-35b-a3b@v3.1-default
# Confirm; observe same 4-phase progress; expect ~3-5 min wall time.

# Step 6 — D-02 idempotent same-variant short-circuit test:
/start qwen3.6-35b-a3b@v3.1-default
# Confirm dialog (if /start has confirm)
# Expected: nearly instant return — single "ready" phase fires; notify "started ..."
# This proves the D-02 idempotent-same-variant code path on the sidecar fires correctly.
```

**Resume signal:** Type `sc3 phase4.2 green` if all 6 steps pass. Specifically verify:
- The 4-phase sequence renders verbatim (no extra phases, no shape drift) in the footer (C-06 LOCKED proven over the wire)
- A 10+ minute idle SSE stream survives without disconnect (RESEARCH Risk A1 — sse-starlette ping=15 keepalive validated on real tailnet)
- /profile back-and-forth swaps both succeed
- D-02 idempotent same-variant short-circuit returns within ~1 second (no orchestrator subprocess fired)

If any step fails: `sc3 phase4.2 red: <reason>`. Common failure modes:
- Phase shape drift in footer (e.g. "stopping vllm" lowercase instead of "stopping vLLM") → SSE re-framing in controller.py corrupted the JSON; check Plan 01 Task 3 test_sse_frames_match_phase4_d02
- SSE drops mid-load → Tailscale Serve idle timeout exceeded the 15s ping; lower ping in controller.py and re-test (RESEARCH §2 fallback)
- /profile fails with "swap pre-flight failed" → variant string parsing differs from local mode; verify routes-loader.ts:86 and Plan 01 controller.py StartRequest variant resolution
  </how-to-verify>
  <resume-signal>Type `sc3 phase4.2 green` (success) or `sc3 phase4.2 red: <reason>` (failure)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md + STATE.md + REQUIREMENTS.md + add SC walkthroughs to runbook</name>
  <files>docs/runbook.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (analog: lines 224-233 the existing Phase 04.2 stub; lines 192-222 Phase 4 entry as the formatting template for fully-populated Phase entries)
    - .planning/STATE.md (analog: existing Project Memory entries + Performance Metrics table; mirror Phase 4 + Phase 04.1 entry styles)
    - .planning/REQUIREMENTS.md (analog: existing TOOLS-10 / UX-07 / UX-04 status entries — find them via grep; flip per SC outcomes)
    - .planning/phases/04-gemma-4-profile-profile-system-maturity/04-CLOSEOUT.md (analog: closeout structure + SC verdict tracking + Done† vs Done semantics)
    - docs/runbook.md (analog: existing structure with `## Heading` sections; the SC walkthrough scripts from Tasks 1-3 above; bake them into runbook for reproducibility)
  </read_first>
  <action>
**EXPLICIT PRE-CONDITION (per WARNING #8 fix):** This task runs AFTER Tasks 1, 2, AND 3 complete their operator-gated checkpoints. ALL THREE resume signals (`sc1 phase4.2 green`, `sc2 phase4.2 green`, `sc3 phase4.2 green`) MUST be received before this task executes. The order matters: SC-1 proves the network path; SC-2 proves lifecycle; SC-3 proves the C-06 wire format end-to-end.

**If ANY SC reports red** (`sc1 phase4.2 red: ...`, `sc2 phase4.2 red: ...`, OR `sc3 phase4.2 red: ...`):
- DO NOT flip REQUIREMENTS.md TOOLS-10/UX-04/UX-07 to Done — keep them at Done† (operator-gated pending).
- DO NOT mark ROADMAP.md Phase 04.2 row as "Closed YYYY-MM-DD" — keep status as "Closure pending; SC-N red — see X-CLOSEOUT.md or follow-up plan".
- DO write a brief failure note in STATE.md Project Memory ("SC-N phase4.2 red: <reason>; follow-up plan needed").
- DO write a `04.2-06-SUMMARY.md` documenting which SCs are green vs red.
- DO consider running `/gsd-plan-phase 04.2 --gaps` to plan the gap-closure work.

This task lands AFTER Tasks 1-3 above and consumes their resume-signal outcomes (sc1/sc2/sc3 phase4.2 green or red).

**Step 1 — docs/runbook.md: add Phase 04.2 SC walkthroughs section:**

Append a new top-level section after the ## Remote-client posture section (which Plan 02 added):

```markdown
## Phase 04.2 SC walkthroughs

> Reproducible operator scripts for the 3 SC walkthroughs (D-08 LOCKED).

### SC-1 phase4.2: Mac client one-shot through Tailscale

**Purpose:** Validate Mac → Tailscale → Spark vLLM round-trip + EMMY_SEARXNG_URL override.

**Pre-flight:**
- Sidecar running on Spark (`systemctl --user status emmy-sidecar`)
- 3 tailscale serve routes configured (`tailscale serve status` shows :8002, :8003, :8888)
- Mac wrapper installed (`cat ~/.local/bin/emmy | grep EMMY_REMOTE_CLIENT`)
- vLLM up on Spark (`curl -sf http://127.0.0.1:8002/v1/models` from Spark)

**Walkthrough:**
[copy verbatim from Task 1 SC-1 walkthrough section above]

**Resume signal:** `sc1 phase4.2 green`

### SC-2 phase4.2: /start /stop /status round-trip with graceful drain

**Purpose:** Validate D-01 LOCKED graceful drain + D-02 LOCKED start variants + D-03 LOCKED status poll-only.

**Pre-flight:** SC-1 green.

**Walkthrough:**
[copy verbatim from Task 2 SC-2 walkthrough section above]

**Resume signal:** `sc2 phase4.2 green`

### SC-3 phase4.2: /profile swap from Mac (C-06 SSE wire format end-to-end)

**Purpose:** Validate C-06 LOCKED Phase-4 D-02 progress contract preserved over SSE+Tailscale; A1 risk (SSE idle timeout) absent in real-world ≥10-min weight loads.

**Pre-flight:** SC-1 + SC-2 green; ≥2 profiles registered on Spark.

**Walkthrough:**
[copy verbatim from Task 3 SC-3 walkthrough section above]

**Resume signal:** `sc3 phase4.2 green`
```

**Step 2 — .planning/ROADMAP.md: replace Phase 04.2 stub:**

Read ROADMAP.md. Locate lines 224-233 (the existing Phase 04.2 entry). Replace per Phase 4 entry's formatting template (lines 192-222):

```markdown
### Phase 04.2: Remote-client mode parity (INSERTED)

**Goal:** Make `/profile`, `/start`, `/stop`, `/status`, and `web_search` work from a Mac/laptop client over Tailscale by adding a Spark-side always-on FastAPI sidecar (`emmy_serve.swap.controller`) and a Mac-side dual-path dispatcher in `profile-swap-runner.ts`. Local mode (no `EMMY_REMOTE_CLIENT`) MUST be byte-stable.

**Depends on:** Phase 4 (needs the swap orchestrator + /profile slash command + JSON-per-line progress contract)

**Requirements**: TOOLS-10 (web_search remote-client compatibility), UX-04 (model-swap progress UX over SSE), UX-07 (3-state offline badge logic preserved in remote mode)

**Success Criteria** (operator-gated SC walkthroughs per D-08 LOCKED):
  1. **SC-1 phase4.2** — Mac client one-shot through Tailscale: `emmy --print "..."` from a Mac with the install-client.sh wrapper succeeds; web_search via EMMY_SEARXNG_URL works; SP_OK canary alive on remote path (Pitfall #6 preserved).
  2. **SC-2 phase4.2** — /start /stop /status round-trip with graceful drain: D-01 LOCKED 30s drain + SIGTERM + 5s SIGKILL semantics work; in-flight long generation either completes or truncates cleanly without corrupting the session.
  3. **SC-3 phase4.2** — /profile swap from Mac: 4-phase progress sequence (`stopping vLLM` → `loading weights` 0/50/90 → `warmup` → `ready`) renders verbatim in footer (C-06 LOCKED Phase-4 D-02 contract preserved over SSE); D-02 idempotent same-variant short-circuit fires within ~1s; SSE survives ≥10-min idle (RESEARCH Risk A1 negative).

**Plans:** 6 plans

Plans:
- [ ] 04.2-01-PLAN.md — Sidecar core: state machine + orchestrator_runner + sidecar_metrics + FastAPI controller.py + 5 unit-test files
- [ ] 04.2-02-PLAN.md — Sidecar lifecycle: systemd user unit + start_emmy.sh --install-sidecar-unit + integration test (no real vLLM) + runbook Remote-client posture section
- [ ] 04.2-03-PLAN.md — TS dual-path dispatcher: profile-swap-runner-http.ts + profile-swap-runner.ts dispatcher branch + spawn-argv snapshot test (D-04 BYTE-STABLE canary)
- [ ] 04.2-04-PLAN.md — TS slash commands + footer remote branch: /start /stop /status + sidecar-status-client + metrics-poller D-09 branch + pi-emmy-extension wiring
- [ ] 04.2-05-PLAN.md — Install + posture: install-client.sh wrapper env vars + tailscale serve reminders + web-search.ts EMMY_SEARXNG_URL override + README + air-gap regression smoke (D-33 LOCKED canary)
- [ ] 04.2-06-PLAN.md — Operator-gated SC walkthroughs + ROADMAP/STATE/REQUIREMENTS backfill + closeout (this plan)
```

**Step 3 — .planning/STATE.md: update Project Memory + Performance Metrics + Current Position:**

Read STATE.md. Update three areas:

**Project Memory:** Add a new dated entry near the top of the Project Memory section:

```
- 2026-04-25 (Phase 04.2 — remote-client mode parity SHIPPED): 6 plans landed across 3 waves. Sidecar (`emmy_serve.swap.controller`) is now the always-on Spark-side FastAPI control plane bound to 0.0.0.0:8003; systemd user unit `emmy-sidecar.service` enables it at boot. TS-side dual-path dispatcher (`profile-swap-runner.ts`) routes through HTTP+SSE when `EMMY_REMOTE_CLIENT=1`; spawn-argv snapshot test pins the local-mode argv at byte equivalence (D-04 BYTE-STABLE canary). New slash commands `/start`, `/stop`, `/status` operate from the Mac TUI. `web-search.ts` honors `EMMY_SEARXNG_URL` (D-33 LOCKED loopback default preserved when unset; air-gap regression smoke green). `install-client.sh` writes EMMY_REMOTE_CLIENT/EMMY_SERVE_URL/EMMY_SEARXNG_URL into the wrapper at install time. Operator-gated SC-1/SC-2/SC-3 phase4.2 walkthroughs documented in `docs/runbook.md`. REQ-IDs touched: TOOLS-10, UX-04, UX-07. Local mode is byte-stable; air-gap CI continues to gate STRICT egress.
```

**Performance Metrics: Per-plan execution log:** Append rows for each Phase 04.2 plan once executed (the executor will fill in actual durations during /gsd-execute-phase; for this planning task, write placeholder entries):

```
| Phase 04.2 P01 | TBD | 3 tasks | ~10 files |
| Phase 04.2 P02 | TBD | 2 tasks | 5 files |
| Phase 04.2 P03 | TBD | 2 tasks | 5 files |
| Phase 04.2 P04 | TBD | 2 tasks | 7 files |
| Phase 04.2 P05 | TBD | 3 tasks | 5 files |
| Phase 04.2 P06 | TBD | 4 tasks (3 SC walkthroughs + ROADMAP/STATE/REQ update) | 4 files |
```

**Current Position:** Update to reflect Phase 04.2 closure-pending status:

Change the Phase line:
```
Phase: --phase (04.2) — CLOSURE PENDING (awaiting SC-1/SC-2/SC-3 phase4.2 operator gates)
```

Also update the **Phase 04.2** progress field (add a new line for it after the Phase 4 line):

```
**Phase 04.2:** Remote-client mode parity — Plans 01-05 LANDED 2026-04-25 (sidecar + dual-path dispatcher + slash commands + install-client.sh + EMMY_SEARXNG_URL + air-gap regression green); SC-1/SC-2/SC-3 phase4.2 operator-gated; flips to Done† pending green; promote to Done after all 3 SCs green.
```

**Step 4 — .planning/REQUIREMENTS.md: flip TOOLS-10 / UX-04 / UX-07 status:**

Read REQUIREMENTS.md. Find the entries for TOOLS-10, UX-04, UX-07. Phase 04.2 ships behavior for all three:

- **TOOLS-10 (web_search):** Was Done (Phase 03.1). Phase 04.2 EXTENDS it for remote-client mode via EMMY_SEARXNG_URL. Update the status note to: "Done (Phase 03.1) + extended for remote-client mode (Phase 04.2: EMMY_SEARXNG_URL override; D-33 LOCKED loopback default preserved in local mode)."
- **UX-07 (3-state offline badge):** Was Done (Phase 03.1). Phase 04.2 PRESERVES badge logic UNCHANGED; metrics-poller's remote branch feeds the existing state machine. Update status note: "Done (Phase 03.1); preserved through Phase 04.2 remote-mode (badge logic UNCHANGED; metrics-poller D-09 remote branch feeds the same state machine)."
- **UX-04 (model-swap progress UX):** Was Done (Phase 4). Phase 04.2 EXTENDS it to operate over SSE+Tailscale via the dual-path dispatcher. Update status note: "Done (Phase 4) + extended for remote-client mode (Phase 04.2: SSE wraps Phase-4 D-02 LOCKED contract verbatim per C-06; same 4-phase footer progress in both local and remote modes)."

If REQUIREMENTS.md uses Done†/Done semantics for operator-gated states, the status flips to Done† immediately after this plan lands and to Done after all 3 SCs are green.

**Step 5 — Update ROADMAP.md Progress Table:**

If ROADMAP.md has a Progress Table (lines ~324-334), add a row for Phase 04.2:

```
| 4.2. Remote-client mode parity | 6/6 | Closure pending (awaiting SC-1/SC-2/SC-3 phase4.2 operator gates) | TBD |
```

After all 3 SCs are green and this plan completes execution, the executor should:
- Flip the status to "Closed" with the green-gate date
- Update REQUIREMENTS.md TOOLS-10/UX-04/UX-07 to fully Done (not Done†)
- Add the v1 requirement counter to STATE.md Performance Metrics
  </action>
  <acceptance_criteria>
    - `grep -c "## Phase 04.2 SC walkthroughs" docs/runbook.md` returns 1
    - `grep -c "SC-1 phase4.2: Mac client one-shot through Tailscale" docs/runbook.md` returns 1
    - `grep -c "SC-2 phase4.2: /start /stop /status round-trip" docs/runbook.md` returns 1
    - `grep -c "SC-3 phase4.2: /profile swap from Mac" docs/runbook.md` returns 1
    - `grep -c "sc1 phase4.2 green\\|sc2 phase4.2 green\\|sc3 phase4.2 green" docs/runbook.md` returns ≥3
    - `grep -c "Urgent work - to be planned" .planning/ROADMAP.md` returns 0 (Phase 04.2 stub replaced)
    - `grep -c "TOOLS-10 (web_search remote-client" .planning/ROADMAP.md` returns 1 OR `grep -c "TOOLS-10" .planning/ROADMAP.md` shows ≥1 in the Phase 04.2 entry context
    - `grep -c "04.2-01-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "04.2-06-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "Phase 04.2 — remote-client mode parity SHIPPED" .planning/STATE.md` returns 1
    - `grep -c "Phase 04.2 P01" .planning/STATE.md` returns 1
    - `grep -c "Phase 04.2 P06" .planning/STATE.md` returns 1
    - REQUIREMENTS.md TOOLS-10 status note mentions "Phase 04.2" + "EMMY_SEARXNG_URL"
    - REQUIREMENTS.md UX-04 status note mentions "Phase 04.2" + "SSE" or "C-06"
    - REQUIREMENTS.md UX-07 status note mentions "Phase 04.2" + "preserved" or "UNCHANGED"
    - Per WARNING #11 fix — no real tailnet hostnames committed in docs/runbook.md SC walkthroughs:
      `! grep -E '\\.[a-z]+-[a-z]+\\.ts\\.net' docs/runbook.md` exits 0 (no `[adjective]-[noun].ts.net` Tailscale-tailnet patterns)
      `! grep -E '\\b[a-z][a-z0-9-]+\\.[a-z][a-z0-9-]+\\.ts\\.net\\b' docs/runbook.md | grep -v 'spark\\.example\\.ts\\.net'` returns nothing (only `spark.example.ts.net`-style placeholders permitted)
      Acceptable placeholder forms: `spark.example.ts.net`, `${EMMY_SPARK_HOST}`, `<spark>.<tailnet>.ts.net`. Forbid real tailnet hostnames (which Tailscale typically renders as `<word1>-<word2>.<tailnet>.ts.net`)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## Phase 04.2 SC walkthroughs" docs/runbook.md && grep -q "Remote-client mode parity" .planning/ROADMAP.md && grep -q "04.2-01-PLAN.md" .planning/ROADMAP.md && grep -q "Phase 04.2 — remote-client mode parity SHIPPED" .planning/STATE.md && grep -qE "Phase 04.2.*EMMY_SEARXNG_URL|EMMY_SEARXNG_URL.*Phase 04.2" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - docs/runbook.md has 3 reproducible SC walkthrough scripts with green resume signals
    - .planning/ROADMAP.md Phase 04.2 entry has real Goal + Requirements + 6 plan listing + Progress-Table row
    - .planning/STATE.md has Project Memory entry + 6 Per-plan execution log rows + Current Position update
    - .planning/REQUIREMENTS.md TOOLS-10 / UX-04 / UX-07 status notes updated with Phase 04.2 references
    - All grep-verifiable strings present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(This plan modifies only documentation + planning artifacts — no new attack surface. The 3 operator-gated checkpoints are human-in-the-loop verification, not security boundaries per se.)

| Boundary | Description |
|----------|-------------|
| `Operator interpreting SC walkthrough scripts` → `production Spark` | Human-in-the-loop; runbook scripts are explicit step-by-step; resume-signal phrases prevent ambiguity |
| `Plan 06 task 4 edits to STATE/ROADMAP/REQUIREMENTS` → `git history` | All changes committed; reviewable; no secrets touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-Closeout1 | Repudiation | Operator marks SC green when it actually red | accept | Trust-on-operator (single-user project); the resume-signal phrase is recorded in commit history; if regressions surface later, the plan SUMMARY + STATE entry is revisable per the Phase 4 precedent (operator-gated deferrals can be re-opened). |
| T-04.2-Closeout2 | Information Disclosure | runbook SC walkthroughs document EMMY_SPARK_HOST values + variant names | accept | Same posture as the install-client.sh / runbook discussions in Plans 02 + 05: hostnames are not secrets in this trust model. The runbook examples use placeholder values (`spark.example.ts.net`) rather than real tailnet hostnames. |
| T-04.2-Closeout3 | Tampering | A future PR removes Phase 04.2 entry from ROADMAP/STATE | accept | Git history protects against silent regressions; PR reviewer (the project owner) catches before merge. |

(No code changes in this plan = no STRIDE-rated code-level threats. The plan deliberately has zero `threats_addressed` in the frontmatter — all S-X threats from this phase are addressed by Plans 01-05.)
</threat_model>

<verification>
**Per-task verification:**
- Tasks 1, 2, 3: operator-gated checkpoints — pass criteria are operator confirmation via resume-signal phrases
- Task 4: automated grep against the modified docs (see acceptance_criteria)

**Plan-level verification:**

1. All Plans 01-05 SUMMARY files exist (per their `<output>` blocks):
   - `.planning/phases/04.2-remote-client-mode-parity/04.2-01-SUMMARY.md`
   - `.planning/phases/04.2-remote-client-mode-parity/04.2-02-SUMMARY.md`
   - `.planning/phases/04.2-remote-client-mode-parity/04.2-03-SUMMARY.md`
   - `.planning/phases/04.2-remote-client-mode-parity/04.2-04-SUMMARY.md`
   - `.planning/phases/04.2-remote-client-mode-parity/04.2-05-SUMMARY.md`

2. Full unit test suite still green: `uv run pytest tests/unit -q && cd packages/emmy-ux && bun test && cd ../emmy-tools && bun test`

3. Smoke tests still green:
   - `bash tests/smoke/verify_systemd_unit.sh` exits 0
   - `bash tests/smoke/verify_airgap_local_mode.sh` exits 0

4. ROADMAP.md no longer has the "Urgent work - to be planned" placeholder for Phase 04.2

5. `.planning/STATE.md` Current Position field reflects Phase 04.2 closure-pending status

**Post-operator-confirmation actions** (after all 3 SCs green):
- Flip Phase 04.2 status to "Closed YYYY-MM-DD" in ROADMAP.md Progress Table
- Promote TOOLS-10/UX-04/UX-07 from Done† to Done in REQUIREMENTS.md
- Optionally write a `04.2-CLOSEOUT.md` per Phase 4 precedent (small file with the green-verdict commit hashes + a 1-paragraph narrative)
</verification>

<success_criteria>
- [ ] Tasks 1, 2, 3 (operator-gated SC walkthroughs) executed; resume-signal phrases recorded
- [ ] Task 4 lands documentation updates: docs/runbook.md gets ## Phase 04.2 SC walkthroughs section; .planning/ROADMAP.md Phase 04.2 entry filled in (Goal, Requirements, 6-plan listing, Progress-Table row); .planning/STATE.md Project Memory + Performance Metrics + Current Position updated; .planning/REQUIREMENTS.md TOOLS-10/UX-04/UX-07 status notes updated
- [ ] No regressions: all unit/integration/smoke tests still green
- [ ] D-04 BYTE-STABLE: no TS source modifications in this plan
- [ ] D-33 LOCKED: no web-search.ts source modifications in this plan
- [ ] After all 3 SCs green: REQUIREMENTS.md flipped to Done (not Done†); ROADMAP.md Phase 04.2 row marked Closed YYYY-MM-DD
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-remote-client-mode-parity/04.2-06-SUMMARY.md` documenting:
- 3 operator-gated SC outcomes (sc1/sc2/sc3 phase4.2 green/red verdicts)
- ROADMAP.md Phase 04.2 entry now reflects 6-plan structure with TOOLS-10 + UX-04 + UX-07 requirements
- STATE.md Project Memory entry + Performance Metrics rows for all 6 Phase 04.2 plans
- REQUIREMENTS.md TOOLS-10/UX-04/UX-07 status flipped (Done† immediate; Done after operator green)
- docs/runbook.md ## Phase 04.2 SC walkthroughs section is reproducible (any future operator can re-run the 3 walkthroughs verbatim)
- Phase 04.2 closure status: pending all 3 SC greens, then promote to Closed
- If a SC fails: this plan returns ## CHECKPOINT REACHED with a follow-up plan (gap-closure) recommendation rather than ## PLANNING COMPLETE
</output>
