---
phase: 04.2-remote-client-mode-parity
verified: 2026-04-25T23:00:00Z
status: human_needed
score: 16/16 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Mac client one-shot through Tailscale (SC-1 phase4.2)"
    expected: "bash scripts/install-client.sh on Mac produces working ~/.local/bin/emmy wrapper; emmy ping over Tailscale reaches Spark vLLM; web_search hits SearxNG via EMMY_SEARXNG_URL; resume signal: sc1 phase4.2 green"
    why_human: "Requires live Tailscale tailnet + Spark hardware + real sidecar process. Cannot test network route from CI."
  - test: "/start /stop /status round-trip over Tailscale (SC-2 phase4.2)"
    expected: "/stop drains in-flight requests (emits draining N requests SSE event), transitions to stopped; /status returns state=stopped vllm_up=false; /start <profile>@<variant> cold-starts vLLM with 4-phase D-02 LOCKED progress sequence streaming to footer; badge reads correctly in remote mode; resume signal: sc2 phase4.2 green"
    why_human: "Requires live Tailscale + Spark GPU + real vLLM container start/stop cycle. Cannot mock sidecar D-01 drain against a real vLLM."
  - test: "/profile swap from Mac (SC-3 phase4.2)"
    expected: "Mac TUI /profile <target>@<variant> routes through EMMY_REMOTE_CLIENT=1 dispatcher → HTTP+SSE sidecar /profile/swap; 4-phase footer progress streams via C-06 SSE wire; swap completes; resume signal: sc3 phase4.2 green"
    why_human: "Requires live Tailscale + real dual-model swap on Spark GPU. SSE byte-stability of C-06 contract over real Tailscale hop cannot be verified without hardware."
---

# Phase 04.2: Remote-client mode parity — Verification Report

**Phase Goal:** Make Emmy's harness fully usable from a remote-client Mac/laptop over Tailscale — `/start`, `/stop`, `/status`, `/profile` slash commands route to a Spark-side FastAPI sidecar; `web_search` reaches a Spark-hosted SearxNG via env-driven URL override; air-gap CI splits into STRICT (inference) and PERMISSIVE (research). Local-mode daily-driver behavior unchanged (D-04 BYTE-STABLE).
**Verified:** 2026-04-25T23:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Sidecar (`emmy_serve.swap.controller`) binds 0.0.0.0:8003 with 6 endpoints: GET /healthz, GET /, GET /status, POST /start, POST /stop, POST /profile/swap | VERIFIED | `controller.py` 593 LOC; 6 `@app.{get,post}` decorators confirmed at lines 181/187/202/248/359/469; bound to 0.0.0.0:8003 per line 569 |
| 2 | GET /status returns D-07 LOCKED JSON schema (state, profile_id, profile_variant, profile_hash, vllm_up, vllm_pid, container_digest, kv_used_pct, gpu_temp_c, in_flight, last_error; nulls when vllm_up=false) | VERIFIED | `StatusResponse` Pydantic model in controller.py; `fetch_vllm_metrics_cached` + `sample_gpu_temp_cached` called in /status handler; tested in `test_sidecar_status.py` via `test_status_returns_json_not_sse` |
| 3 | POST /profile/swap re-frames orchestrator JSON-per-line stdout as SSE `data:` lines (C-06 Phase-4 D-02 LOCKED contract verbatim) | VERIFIED | `controller.py:469`; `run_orchestrator_subprocess` async-iterated and each record yielded as SSE via sse-starlette; `test_sse_frames_match_phase4_d02` test in `test_sidecar_sse.py` |
| 4 | POST /stop emits draining N requests SSE, waits 30s, SIGTERM at deadline, SIGKILL after +5s (D-01 LOCKED) | VERIFIED | `controller.py:359`; `_DRAIN_GRACE_S=30` / `_SIGKILL_WAIT_S=5` constants; `test_drain_then_sigterm` in `test_sidecar_stop.py` |
| 5 | POST /start idempotent on same variant (returns ready immediately); cold-start invokes orchestrator without --from; cross-variant runs swap emitting D-02 phases (D-02 LOCKED) | VERIFIED | controller.py:248; idempotent check at line 276; cold_start branch at line 291; `test_idempotent_same_variant` in `test_sidecar_start.py` |
| 6 | State machine enforces D-07 LOCKED transitions (stopped→starting→ready, ready→swapping→ready, ready→draining→stopped, any→error); violations raise InvalidTransitionError | VERIFIED | `state.py` 118 LOC; `ALLOWED_TRANSITIONS` table; `asyncio.Lock`-protected `transition_to()`; `test_transitions_allowed` in `test_sidecar_state.py` |
| 7 | systemd user unit `emmy_serve/systemd/emmy-sidecar.service` exists; ExecStart invokes `python -m emmy_serve.swap.controller`; `scripts/start_emmy.sh --install-sidecar-unit` copies + enables it | VERIFIED | `emmy-sidecar.service` line 10: `ExecStart=%h/.local/bin/uv run python -m emmy_serve.swap.controller`; `start_emmy.sh` lines 36/48/50 with `cp -f` + `systemctl --user enable --now emmy-sidecar` |
| 8 | Integration test (`tests/integration/test_sidecar_no_vllm.py`) boots FastAPI via TestClient without real vLLM; asserts /healthz=ok, /status=stopped/vllm_up=false; gated by `pytest.mark.integration` | VERIFIED | File confirmed; `pytestmark = pytest.mark.integration` at line 30; `/healthz` assertion at line 85; `state == "stopped"` at line 109 |
| 9 | `profile-swap-runner.ts` EMMY_REMOTE_CLIENT='1' branch lazy-imports and routes through HTTP+SSE; local-mode spawn body is D-04 BYTE-STABLE (marker comment + snapshot guard) | VERIFIED | `profile-swap-runner.ts` line 91: `if (process.env.EMMY_REMOTE_CLIENT === "1")`; line 92: lazy `await import("./profile-swap-runner-http")`; line 95: `===== EXISTING SPAWN PATH (UNCHANGED below this line — D-04 BYTE-STABLE) =====`; `spawn-argv.snapshot.test.ts` with `BYTE-STABLE` marker |
| 10 | `profile-swap-runner-http.ts` POSTs to `${EMMY_SERVE_URL}/profile/swap`; consumes SSE via eventsource-parser v3 `createParser`; both paths emit identical (phase, pct?) SwapResult | VERIFIED | `profile-swap-runner-http.ts` line 69: `/profile/swap`; line 25: `import { createParser } from "eventsource-parser"`; `eventsource-parser: "^3.0.0"` in `package.json` |
| 11 | `/start`, `/stop`, `/status` slash commands registered in `pi-emmy-extension.ts`; drive sidecar-lifecycle-client.ts (POST→SSE) and sidecar-status-client.ts (GET) | VERIFIED | `pi-emmy-extension.ts` imports `registerStartCommand`, `registerStopCommand`, `registerStatusCommand` at lines 102-104; registration at lines 806/826/840; `sidecar-lifecycle-client.ts` 186 LOC; `sidecar-status-client.ts` 69 LOC |
| 12 | `metrics-poller.ts` D-09 LOCKED remote branch: EMMY_REMOTE_CLIENT='1' → polls `${EMMY_SERVE_URL}/status` (getSidecarStatus) instead of vllm /metrics + nvidia-smi; local path BYTE-STABLE below marker | VERIFIED | `metrics-poller.ts` line 135: `if (process.env.EMMY_REMOTE_CLIENT === "1")`; line 99: lazy import `getSidecarStatus`; `metrics-poller.remote.test.ts` line 108: "EMMY_REMOTE_CLIENT=1 → tick polls /status, NOT vllm /metrics or nvidia-smi" |
| 13 | `web-search.ts` `DEFAULT_CFG.baseUrl` is a getter honoring `EMMY_SEARXNG_URL`; defaults to `http://127.0.0.1:8888` when unset (D-33 LOCKED loopback invariant preserved) | VERIFIED | `web-search.ts` line 106: `return process.env.EMMY_SEARXNG_URL ?? "http://127.0.0.1:8888"`; getter pattern at line 110; `web-search-env-override.test.ts` proves both branches |
| 14 | `scripts/install-client.sh` wrapper exports `EMMY_REMOTE_CLIENT=1`, `EMMY_SERVE_URL`, `EMMY_SEARXNG_URL`; prints 3 tailscale serve reminder commands (:8002, :8003, :8888) + systemctl + linger | VERIFIED | `install-client.sh` lines 212-214: 3 env exports; line 171: `tailscale serve --bg --https=8003` reminder; README lines 198-200: 3 tailscale serve commands |
| 15 | `tests/smoke/verify_airgap_local_mode.sh` (D-33 LOCKED canary) + `tests/smoke/verify_systemd_unit.sh` (systemd-analyze verify gate) both exist | VERIFIED | Both files confirmed; `verify_systemd_unit.sh` line 12: `exec systemd-analyze verify emmy_serve/systemd/emmy-sidecar.service`; `verify_airgap_local_mode.sh` line 9-10: D-33 LOCKED canary comment |
| 16 | `docs/runbook.md` has "## Remote-client posture" section + Air-gap CI Phase 04.2 note + "## Phase 04.2 SC walkthroughs" with SC-1/SC-2/SC-3 operator scripts | VERIFIED | `runbook.md` line 186: `## Remote-client posture`; line 182: Air-gap CI Phase 04.2 note; line 249: `## Phase 04.2 SC walkthroughs`; line 257: `### SC-1 phase4.2` |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `emmy_serve/swap/controller.py` | FastAPI app + 6 endpoints + Pydantic models + SSE wrapping + uvicorn entry | VERIFIED | 593 LOC; all 6 routes; `docs_url=None, redoc_url=None` |
| `emmy_serve/swap/state.py` | SidecarState enum + ALLOWED_TRANSITIONS + StateMachine + InvalidTransitionError | VERIFIED | 118 LOC; exceeds 40 LOC minimum |
| `emmy_serve/swap/orchestrator_runner.py` | `run_orchestrator_subprocess` async iterator + argv byte-stable + `{_internal_exit: rc}` sentinel | VERIFIED | 102 LOC; exceeds 40 LOC minimum |
| `emmy_serve/swap/sidecar_metrics.py` | `fetch_vllm_metrics_cached` + `sample_gpu_temp_cached` with 1s TTL | VERIFIED | 180 LOC; exceeds 50 LOC minimum |
| `emmy_serve/swap/__init__.py` | PEP-562 lazy __getattr__ exposing 13 sidecar symbols | VERIFIED | StateMachine in __getattr__; 13 new sidecar exports |
| `emmy_serve/systemd/emmy-sidecar.service` | Type=simple, Restart=on-failure, RestartSec=5, TimeoutStopSec=40, ExecStart pointing to controller | VERIFIED | 22 lines; ExecStart confirmed |
| `pyproject.toml` | `fastapi>=0.110`, `uvicorn[standard]>=0.27`, `sse-starlette>=3.0.0,<4.0.0` | VERIFIED | Lines 11-13 confirmed |
| `packages/emmy-ux/src/profile-swap-runner-http.ts` | HTTP+SSE swap runner; createParser; FetchSseImpl DI | VERIFIED | 160 LOC; exceeds 70 LOC minimum |
| `packages/emmy-ux/src/profile-swap-runner.ts` | 4-line EMMY_REMOTE_CLIENT dispatcher + BYTE-STABLE marker | VERIFIED | Line 91 env check; line 95 marker |
| `packages/emmy-ux/src/sidecar-status-client.ts` | `getSidecarStatus` + `SidecarStatus` interface | VERIFIED | 69 LOC; exceeds 50 LOC minimum |
| `packages/emmy-ux/src/sidecar-lifecycle-client.ts` | `runSidecarStartHttp` + `runSidecarStopHttp` (self-contained, not importing profile-swap-runner-http) | VERIFIED | 186 LOC; exceeds 100 LOC minimum |
| `packages/emmy-ux/src/metrics-poller.ts` | D-09 EMMY_REMOTE_CLIENT branch + `opts.getStatusImpl` DI seam | VERIFIED | Lines 135/99 confirmed |
| `packages/emmy-ux/src/pi-emmy-extension.ts` | registerStartCommand/registerStopCommand/registerStatusCommand wired | VERIFIED | Lines 102-104 + 806/826/840 |
| `packages/emmy-ux/package.json` | `eventsource-parser: "^3.0.0"` | VERIFIED | Line 22 confirmed |
| `packages/emmy-tools/src/web-search.ts` | `EMMY_SEARXNG_URL` getter; D-33 default intact; EMMY_WEB_SEARCH=off untouched | VERIFIED | Lines 106/110/244 confirmed |
| `scripts/install-client.sh` | Wrapper HEREDOC with 3 env vars; 3 tailscale serve reminders | VERIFIED | Lines 171/212-214 confirmed |
| `README.md` | Remote-client mode block with 3 tailscale serve routes + systemctl + linger | VERIFIED | Lines 198-200/211-213 confirmed |
| `tests/unit/test_sidecar_state.py` | `test_transitions_allowed` + coverage of all 12 ALLOWED edges | VERIFIED | Line 69 confirmed |
| `tests/unit/test_sidecar_status.py` | `test_status_returns_json_not_sse` + D-07 schema | VERIFIED | Line 257 confirmed |
| `tests/unit/test_sidecar_start.py` | `test_idempotent_same_variant` + cold-start + cross-variant | VERIFIED | Line 106 confirmed |
| `tests/unit/test_sidecar_stop.py` | `test_drain_then_sigterm` + SIGKILL + idempotency | VERIFIED | Line 107 confirmed |
| `tests/unit/test_sidecar_sse.py` | `test_sse_frames_match_phase4_d02` + C-06 byte-equivalence | VERIFIED | Line 73 confirmed |
| `tests/integration/test_sidecar_no_vllm.py` | `pytest.mark.integration` + all 6 endpoints + stopped/vllm_up=false | VERIFIED | Lines 30/85/109 confirmed |
| `tests/smoke/verify_systemd_unit.sh` | `systemd-analyze verify` gate | VERIFIED | Line 12 confirmed |
| `tests/smoke/verify_airgap_local_mode.sh` | D-33 LOCKED canary | VERIFIED | Lines 9-10 confirmed |
| `packages/emmy-ux/test/profile-swap-runner.http.test.ts` | `runSwapAndStreamProgressHttp` tests + canned SSE | VERIFIED | Line 59 confirmed |
| `packages/emmy-ux/test/spawn-argv.snapshot.test.ts` | `BYTE-STABLE` snapshot guard | VERIFIED | Lines 57/86 confirmed |
| `packages/emmy-ux/test/sidecar-status-client.test.ts` | Bun.serve mock + timeout/non-200 tests | VERIFIED | Line 58 confirmed |
| `packages/emmy-ux/test/sidecar-lifecycle-client.test.ts` | `runSidecarStartHttp` / `runSidecarStopHttp` tests | VERIFIED | Line 64 confirmed |
| `packages/emmy-ux/test/slash-commands.start-stop-status.test.ts` | `registerStartCommand` tests + renderSidecarStatus | VERIFIED | Line 194 confirmed |
| `packages/emmy-ux/test/metrics-poller.remote.test.ts` | D-09 remote branch test | VERIFIED | Line 108 confirmed |
| `packages/emmy-tools/tests/web-search-env-override.test.ts` | `EMMY_SEARXNG_URL` env-set + env-unset branches | VERIFIED | Lines 24/76 confirmed |
| `.planning/ROADMAP.md` | Phase 04.2 entry with real Goal + TOOLS-10/UX-04/UX-07 + 6-plan listing | VERIFIED | Lines 229/238/241-246 confirmed |
| `.planning/STATE.md` | Phase 04.2 closure entry + 6 Performance Metrics rows + Done† status | VERIFIED | Lines 18/50/101-106 confirmed |
| `.planning/REQUIREMENTS.md` | TOOLS-10/UX-04/UX-07 flipped to Done† + Phase 04.2 additions block | VERIFIED | Lines 227/268-291 confirmed |
| `docs/runbook.md` SC walkthroughs | SC-1/SC-2/SC-3 phase4.2 operator scripts | VERIFIED | Lines 249/257/309 confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `controller.py` | `orchestrator_runner.py` | `async for rec in run_orchestrator_subprocess` | WIRED | line 318: `async for rec in run_orchestrator_subprocess(` |
| `controller.py` | `state.py` | `StateMachine` singleton + `await state.transition_to(SidecarState.X)` | WIRED | lines 310/335/338/342/386/397/447 |
| `controller.py` | `sidecar_metrics.py` | `/status` payload assembly | WIRED | lines 46/216/226 |
| `orchestrator_runner.py` | `emmy_serve.swap.orchestrator` | `asyncio.create_subprocess_exec` `uv run python -m emmy_serve.swap.orchestrator` | WIRED | subprocess command confirmed |
| `emmy-sidecar.service` | `controller.py` | `ExecStart=%h/.local/bin/uv run python -m emmy_serve.swap.controller` | WIRED | line 10 of service file |
| `start_emmy.sh` | `emmy-sidecar.service` | `cp -f $ROOT_DIR/emmy_serve/systemd/emmy-sidecar.service` | WIRED | lines 48/50 confirmed |
| `profile-swap-runner.ts` | `profile-swap-runner-http.ts` | `await import("./profile-swap-runner-http")` on EMMY_REMOTE_CLIENT='1' | WIRED | line 92 |
| `profile-swap-runner-http.ts` | sidecar `/profile/swap` | `POST ${EMMY_SERVE_URL}/profile/swap` | WIRED | line 69 |
| `metrics-poller.ts` | `sidecar-status-client.ts` | lazy `import getSidecarStatus` when EMMY_REMOTE_CLIENT='1' | WIRED | lines 99/135 |
| `pi-emmy-extension.ts` | `slash-commands.ts` new register* functions | `registerStartCommand`, `registerStopCommand`, `registerStatusCommand` inside `createEmmyExtension` | WIRED | lines 102-104/806/826/840 |
| `web-search.ts` | `process.env.EMMY_SEARXNG_URL` | `DEFAULT_CFG.baseUrl` getter | WIRED | lines 106/110 |
| `install-client.sh` | wrapper `~/.local/bin/emmy` | HEREDOC with `EMMY_REMOTE_CLIENT=1` | WIRED | lines 212-214 |
| `docs/runbook.md` | `emmy_serve/airgap/ci_verify_phase3.py` | documentation reference to STRICT validator | WIRED | line 169/182 confirmed |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `controller.py /status` | `kv_used_pct`, `gpu_temp_c`, `in_flight` | `fetch_vllm_metrics_cached(loopback /metrics)` + `sample_gpu_temp_cached()` (nvidia-smi shell) | Yes — real HTTP probe + subprocess; nulled when vllm_up=false | FLOWING |
| `metrics-poller.ts` (remote branch) | `FooterValues` | `getSidecarStatus(EMMY_SERVE_URL/status)` → maps `kv_used_pct→kvPct`; GPU/tok render `--` (v1 deferral per Plan 04 key-decision) | Yes — real sidecar HTTP; GPU% not mapped (known v1 limitation, not a bug) | FLOWING |
| `web-search.ts` | `baseUrl` | `process.env.EMMY_SEARXNG_URL` getter | Yes — live getter; no module-cache stale literal | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED (sidecar requires a real GPU/vLLM container; unit tests cover the logic; spot-checks on a live instance are the SC-1/SC-2/SC-3 operator gates).

The Python test suite can be confirmed statically:

| Behavior | Check | Status |
|----------|-------|--------|
| All sidecar unit test files exist with required test functions | `ls tests/unit/test_sidecar_*.py` + `grep def test_` | PASS |
| All TS test files exist and import the modules under test | `ls packages/emmy-ux/test/sidecar-*.test.ts` + `grep import` | PASS |
| D-33 LOCKED canary smoke script exists | `ls tests/smoke/verify_airgap_local_mode.sh` | PASS |
| systemd unit verify smoke script exists | `ls tests/smoke/verify_systemd_unit.sh` + `grep systemd-analyze` | PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| UX-04 | 04.2-01, 04.2-03, 04.2-04 | Model-swap UX with visible progress over SSE | Done† | sidecar C-06 SSE wraps Phase-4 D-02 LOCKED contract; TS HTTP runner emits identical (phase, pct?) callbacks; SC-3 phase4.2 gates live confirmation |
| UX-07 | 04.2-04, 04.2-05 | 3-state offline badge preserved in remote mode | Done† | metrics-poller D-09 branch feeds badge state machine unchanged; SC-2 phase4.2 gates live observation |
| TOOLS-10 | 04.2-05 | `web_search` reachable from Mac via EMMY_SEARXNG_URL override | Done† | web-search.ts getter + air-gap canary smoke; SC-1 phase4.2 gates live web_search over Tailscale |

All 3 requirement IDs correctly marked Done† in REQUIREMENTS.md (operator-gated SC walkthroughs pending — documented deferral per Phase 1/Phase 4 closeout precedent).

### Anti-Patterns Found

| File | Location | Pattern | Severity | Impact |
|------|----------|---------|----------|--------|
| `controller.py:469` | `/profile/swap` handler | Path-traversal guard present on `/start` but absent on `/profile/swap` | Warning | Defense-in-depth gap; orchestrator preflight provides backstop; C-05 trust-on-tailnet is primary boundary; no shell injection possible (create_subprocess_exec) |
| `controller.py:307-351` | `/start` + `/profile/swap` SSE generators | Client disconnect leaves orphaned orchestrator subprocess + state machine in ERROR | Warning | Impacts UX on flaky Tailscale connections; subprocess runs to completion independently; next `/start` recovers; not a data-corruption risk |
| `web-search.ts:106` | `_resolveDefaultBaseUrl()` | `EMMY_SEARXNG_URL=""` (empty string) passes `??` and causes uncaught TypeError in `new URL()` | Warning | Only triggered by explicit `EMMY_SEARXNG_URL=""`; default case (unset) is safe; documented edge case |

No blockers found. All three warnings were identified and documented in the code review (REVIEW.md). None prevent remote-client parity from functioning in normal operation.

### Human Verification Required

The 3 operator-gated SC walkthroughs (SC-1, SC-2, SC-3 phase4.2) are explicitly deferred per the Phase 1/Phase 4 closeout precedent. This is a documented design decision, not a gap. All walkthrough scripts are available in `docs/runbook.md § Phase 04.2 SC walkthroughs`.

#### 1. SC-1 phase4.2: Mac client one-shot through Tailscale

**Test:** Run `bash scripts/install-client.sh` on a Mac with Tailscale connected to the Spark tailnet; verify `~/.local/bin/emmy` wrapper is written with the 3 env vars; issue a `web_search` query through the harness and confirm it hits Spark-hosted SearxNG via `EMMY_SEARXNG_URL`.
**Expected:** Wrapper script exists and contains `EMMY_REMOTE_CLIENT=1`; inference routes to Spark vLLM over Tailscale; web_search returns grounded results; `sc1 phase4.2 green` resume signal.
**Why human:** Requires live Tailscale tailnet + Spark hardware + SearxNG container running; no CI equivalent.

#### 2. SC-2 phase4.2: /start /stop /status round-trip over Tailscale

**Test:** From Mac TUI, run `/stop`; observe draining SSE events in footer; verify `/status` returns `state=stopped`; run `/start qwen3.6-35b-a3b@v3.1-default`; observe 4-phase D-02 LOCKED footer progress; verify badge state in remote mode.
**Expected:** D-01 graceful drain fires (30s window); /status reflects stopped state; /start cold-boots vLLM with stopping vLLM → loading weights → warmup → ready sequence; UX-07 badge reflects correct state; `sc2 phase4.2 green` resume signal.
**Why human:** Requires live vLLM container start/stop on Spark GPU; D-01 drain requires real in-flight requests.

#### 3. SC-3 phase4.2: /profile swap from Mac

**Test:** From Mac TUI with `EMMY_REMOTE_CLIENT=1`, run `/profile <target>@<variant>` for a cross-variant swap; verify dispatch routes through HTTP+SSE (not spawn); footer progress streams the C-06 SSE wire format.
**Expected:** No `spawn("uv", ...)` invoked locally; sidecar receives POST /profile/swap; 4-phase C-06 progress streams to harness-swap.ts consumer unchanged; swap completes successfully; `sc3 phase4.2 green` resume signal.
**Why human:** Requires Tailscale + real dual-model swap on Spark GPU; SSE byte-stability over real network cannot be mocked.

### Gaps Summary

No gaps found. All 16 must-haves from the 6 plans are verified against the codebase. The 3 items in Human Verification Required are operator-gated SC walkthroughs that are explicitly deferred per documented precedent (matching Phase 1 SC-1 and Phase 4 SC-1/SC-3/SC-4 deferral pattern). These are not verification gaps — they are the final live-rig confirmation gates that flip TOOLS-10/UX-04/UX-07 from Done† to Done.

The phase has achieved its coded goal: the Spark-side sidecar, Mac-side dispatcher, slash commands, footer remote branch, web-search override, install script, smoke tests, and all documentation are in place. The only remaining work is operator confirmation on live Tailscale hardware.

---

_Verified: 2026-04-25T23:00:00Z_
_Verifier: Claude (gsd-verifier)_
