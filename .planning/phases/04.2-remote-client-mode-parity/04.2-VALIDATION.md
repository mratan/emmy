---
phase: 04.2
slug: remote-client-mode-parity
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-25
---

# Phase 04.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (Python sidecar) + Bun test (TypeScript dispatcher) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` + `packages/*/test/` |
| **Quick run command** | `pytest tests/unit/test_sidecar_*.py -q && bun test packages/emmy-ux/test/profile-swap-runner.http.test.ts` |
| **Full suite command** | `pytest -q && bun test && pytest -m integration --run-integration` |
| **Estimated runtime** | ~45 s quick / ~120 s full |

---

## Sampling Rate

- **After every task commit:** Run quick command
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green AND operator-gated SC-1/SC-2/SC-3 phase4.2 walkthroughs must be signaled green on the actual tailnet
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Populated by planner during plan creation. Wave 0 (test scaffolds) seeded below; per-plan task IDs filled in once plans exist.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.2-00-01 | 00 | 0 | scaffold | — | N/A | scaffold | `pytest tests/unit/test_sidecar_smoke.py -q` | ❌ W0 | ⬜ pending |
| 04.2-00-02 | 00 | 0 | scaffold | — | N/A | scaffold | `bun test packages/emmy-ux/test/profile-swap-runner.http.test.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_sidecar_smoke.py` — minimal FastAPI TestClient bring-up; asserts `/status` returns `{state: "stopped", vllm_up: false}` when no vLLM is running
- [ ] `tests/conftest.py` — shared fixtures: `sidecar_client` (TestClient), `mock_orchestrator` (subprocess.Popen mock), `frozen_time` (for SSE keepalive timing tests)
- [ ] `tests/integration/conftest.py` — `--run-integration` opt-in marker registration; sidecar boot fixture that does NOT require real vLLM
- [ ] `packages/emmy-ux/test/profile-swap-runner.http.test.ts` — DI seam for `fetchSseImpl`; mocks `eventsource-parser` stream; asserts `(phase, pct?)` callback parity with spawn path
- [ ] `packages/emmy-ux/test/spawn-argv.snapshot.test.ts` — locks the local-mode spawn argv at byte equivalence; protects D-04 dual-path invariant
- [ ] `packages/emmy-ux/test/sidecar-status-client.test.ts` — mocks `fetch` for `/status`; asserts metrics-poller remote branch shape
- [ ] No new framework install — `pytest`, `bun test`, FastAPI's `TestClient` (Starlette), and `prometheus_client` are already pinned

*Wave 0 must land BEFORE Wave 1 sidecar implementation so unit tests RED-then-GREEN drives controller.py.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Mac→Spark Tailscale Serve hop end-to-end (`emmy --base-url https://<spark>.<tailnet>.ts.net`) | D-08 SC-1 | Requires real tailnet + real systemd-managed sidecar + real vLLM swap | Operator runs from Mac: `emmy /status`, `emmy /start qwen3.6-35b-a3b@v3.1-default`, wait for `ready`, `emmy /profile gemma-4-26b-a4b-it@v2-default` and observe SSE progress in footer; signal `sc1 phase4.2 green` on success |
| `/start /stop /status` round-trip with graceful drain | D-01, D-08 SC-2 | Requires multi-minute generation in flight to validate 30 s drain semantics | Operator submits a long generation, hits `/stop`, observes `draining N requests` SSE event, verifies generation completes before SIGTERM (or SIGTERM after 30 s if hung), then `/start` brings it back; signal `sc2 phase4.2 green` |
| `/profile` swap from Mac mid-session | D-02, D-08 SC-3 | Validates the LOCKED Phase-4 progress contract is unchanged over the SSE wire | Operator runs `/profile <other>@<variant>` from Mac, observes the four-phase progress sequence (`stopping vLLM` → `loading weights` 0/50/90 → `warmup` → `ready`) in footer, with no extra phases or shape drift; signal `sc3 phase4.2 green` |
| Tailscale Serve SSE survives ≥10-min idle | A1 risk in RESEARCH.md | Idle-timeout behavior of TS Serve+SSE only verifiable on real tailnet | During SC-3, idle the SSE stream for 10 min between progress phases (e.g. on a 10-min weight load); confirm sse-starlette's 15 s keepalive ping prevents disconnect |
| Air-gap CI continued passage in local mode | C-03 D-33 LOCKED | Requires running `ci_verify_phase3.py` STRICT validator on Spark with `EMMY_REMOTE_CLIENT` UNSET | Spark: `EMMY_WEB_SEARCH=off python -m emmy_serve.airgap.ci_verify_phase3` exits 0; sidecar listening on :8003 must NOT trip egress checks (validator uses `ss -tnp state established` per RESEARCH.md) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (CI determinism)
- [ ] Feedback latency < 60s for quick command
- [ ] Operator-gated SC-1/SC-2/SC-3 walkthroughs documented with green-signal phrases
- [ ] `nyquist_compliant: true` set in frontmatter once planner populates per-task map

**Approval:** pending
