---
status: partial
phase: 04.2-remote-client-mode-parity
source: [04.2-VERIFICATION.md, 04.2-06-PLAN.md]
started: 2026-04-26T00:50:00Z
updated: 2026-04-26T00:50:00Z
deferred_per: "Phase 1/Phase 4 closeout precedent (operator-gated, not blockers)"
runbook_section: "docs/runbook.md ## Phase 04.2 SC walkthroughs"
requirements_pending_done_promotion:
  - TOOLS-10: "promotes Done† → Done on sc1 phase4.2 green"
  - UX-04:    "promotes Done† → Done on sc2 phase4.2 green AND sc3 phase4.2 green"
  - UX-07:    "promotes Done† → Done on sc1 phase4.2 green AND sc2 phase4.2 green"
---

## Current Test

[awaiting operator on Mac+Spark+Tailscale rig — see runbook walkthroughs]

## Tests

### 1. SC-1 phase4.2 — Mac client one-shot through Tailscale

expected: From Mac terminal with `bash scripts/install-client.sh` previously run: `curl -sf https://$EMMY_SPARK_HOST:8003/healthz` returns `{"ok":true,...}`; `curl -sf https://$EMMY_SPARK_HOST:8003/status | jq` returns `state=ready vllm_up=true`; `emmy --print "Reply with: SP_OK_PHASE4_2_GREEN"` includes the canary string in output (Pitfall #6 SP_OK alive on remote path); optional `emmy --print "Use web_search to find: bun 1.3.13 release date"` exercises EMMY_SEARXNG_URL → Spark SearxNG via Tailscale.
runbook: `docs/runbook.md ## Phase 04.2 SC walkthroughs § SC-1`
resume_signal: `sc1 phase4.2 green` | `sc1 phase4.2 red: <reason>`
result: pending
promotes: TOOLS-10 Done† → Done; partial credit for UX-07 (badge state in remote mode visible during inference)

### 2. SC-2 phase4.2 — /start /stop /status round-trip with graceful drain

expected: From Mac TUI: `/status` shows current sidecar state; `/stop` while idle drains within ~2s, footer transitions `draining` → `stopped`; `/status` afterward shows `state=stopped vllm_up=false`; `/start qwen3.6-35b-a3b@v3.1-default` shows D-02 4-phase progress (`stopping vLLM` → `loading weights 0%` → `loading weights 50%` → `warmup` → `ready`); long-gen drain test: kick off long generation in second terminal, `/stop` shows `draining {in_flight: 1}` within 1s, generation either completes <30s or truncates with SIGTERM; `/start` restores ready state.
runbook: `docs/runbook.md ## Phase 04.2 SC walkthroughs § SC-2`
resume_signal: `sc2 phase4.2 green` | `sc2 phase4.2 red: <reason>`
result: pending
promotes: UX-04 Done† → Done (partial); UX-07 Done† → Done (badge state machine in remote mode)

### 3. SC-3 phase4.2 — /profile swap from Mac (C-06 SSE wire format end-to-end)

expected: From Mac TUI with `EMMY_REMOTE_CLIENT=1`: `/profile gemma-4-26b-a4b-it@v2-default` shows the C-06 LOCKED 4-phase progress sequence verbatim in footer (`stopping vLLM` → `loading weights 0%` → `loading weights 50%` → `warmup` → `ready`); 10+ minute idle SSE survives (sse-starlette ping=15 over Tailscale tailnet); inference works on swapped model; `/profile qwen3.6-35b-a3b@v3.1-default` swaps back; `/start` of currently-running variant returns ~1s (D-02 idempotent same-variant short-circuit).
runbook: `docs/runbook.md ## Phase 04.2 SC walkthroughs § SC-3`
resume_signal: `sc3 phase4.2 green` | `sc3 phase4.2 red: <reason>`
result: pending
promotes: UX-04 Done† → Done; partial credit for TOOLS-10 (env override active during swap)

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Promotion path

When all 3 SCs land green:
1. Update `.planning/REQUIREMENTS.md`: TOOLS-10, UX-04, UX-07 → flip Done† to Done (drop dagger)
2. Update `.planning/STATE.md`: change "CLOSURE PENDING" line in Phase 04.2 row to "CLOSED <date>"
3. Update `.planning/ROADMAP.md`: Phase 04.2 row swap "Closure pending" badge for "Closed"
4. Update `.planning/phases/04.2-remote-client-mode-parity/04.2-HUMAN-UAT.md` frontmatter: `status: partial` → `status: resolved`
5. Optional: write a short `04.2-CLOSEOUT.md` mirroring `04-CLOSEOUT.md` structure
