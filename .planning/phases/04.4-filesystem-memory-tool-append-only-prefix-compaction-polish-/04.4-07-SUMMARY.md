---
phase: 04.4-filesystem-memory-tool-append-only-prefix-compaction-polish-
plan: 07
status: complete
completed: 2026-04-26
checkpoint_disposition: auto-approved-via-auto-flag
---

# Plan 04.4-07: v1 compaction prompt + V1/V6/V7 tests — SUMMARY

## Final profile.yaml hashes (post-recompute)

These supersede plan 03's hashes — they encompass BOTH the harness.yaml
memory-block addition AND the prompts/compact.md replacement.

| Profile | Final hash |
|---------|------------|
| `qwen3.6-35b-a3b/v3.1` | `sha256:a3716a86b2b97fe46482e75cc46a23b52f466784fbbd760e95e8a4f19110a272` |
| `qwen3.6-27b/v1.1` | `sha256:cfcfeb97f666eee0d4c347358d27c442b5a81933f5619248cc4a94cc9088254f` |
| `gemma-4-26b-a4b-it/v2` | `sha256:af2e9992980625882f7c146c32d26f5916d2e5e5f375e73402c0d2968b487b2b` |
| `gemma-4-31b-it/v1.1` | `sha256:ffbf7e7d7ab1e83883ba9a05733f01be2fd3bfa1c48ffdce0beec386ac215489` |

## prompts/compact.md sha256 (single value across all 4)

`sha256:90beff104a6b9d7a7659055b31a54a6290a2f6bed7179dc72ddbad6ae7c5f874`

(All 4 profiles' compact.md files are byte-identical; verified by
compaction-prompt-trimmed.test.ts cross-check.)

## v1 prompt token count (split-on-whitespace approximation)

35 tokens (well under the ≤50-token sanity gate).

## Test counts per file

| File | Pass |
|------|------|
| `packages/emmy-context/test/compaction-prompt-trimmed.test.ts` | 9 |
| `packages/emmy-context/test/v6-summarizer-retry.test.ts` | 2 |
| `packages/emmy-context/test/v7-fallback-engages.test.ts` | 3 |

**Total:** 14 / 14 pass (29 expect calls).

## V1 prompt verbatim (calibration-locked)

```
Summarize the conversation above so a fresh context can resume the work. Preserve: explicit goals, decisions made, errors and their resolutions, files modified and their final state. Drop: dead-end exploration, redundant tool calls, transient state.
```

(Single paragraph + trailing newline. NO sectioning, NO Reason: hint, NO
token-budget instruction. Calibration protocol per CONTEXT.md §decisions:
do NOT pre-emptively bloat. Expand only after V2 hand-rating measurement
shows goal/decision/error preservation misses target.)

## Acceptance criteria — verified

- [x] All 4 profiles' prompts/compact.md content equals v1 verbatim
- [x] sha256-equal cross-check across 4 files
- [x] V1 trigger fires within ±5% of `soft_threshold_pct × max_input_tokens`
- [x] V6 first-throw → fallback engages; valid second call → clean
- [x] V7 D-16 structured-pruning engages on persistent throw with elided > 0
- [x] V7 three back-to-back throws emit 3 distinct fallback events
- [x] All 4 profile.yaml hashes recomputed
- [x] `uv run emmy profile validate <each-dir>` exits 0
- [x] Tests use **pinned** signatures (no `...` placeholders, no wrong field names)
- [x] No source extension to `@emmy/context` needed (W4 path **a** confirmed)

## Deviations from Plan

**Test directory: `test/` not `tests/`.** The plan body lists files under
`packages/emmy-context/tests/` — the actual emmy-context package convention
is `packages/emmy-context/test/` (singular, alongside the existing
`compaction-live-wiring.test.ts` etc.). Tests landed in the convention'd
location; functionally identical.

**Profile fixture: 12 entries (not 2).** The plan body example uses 2 entries
in the stub-mode ctx fixture. With only 2 entries and `preserve_recent_turns:
5`, the entire transcript is preserved — `summarizable.length === 0` →
`prepareCompactionLocal` returns null → trigger short-circuits to
`{ran: false}`, breaking the V1 "runs at threshold" assertions. Increased
the fixture to 12 entries so 7+ are summarizable. Functionally identical
intent.

**Auto-approved Task 3 checkpoint per `--auto`.** The plan marks Task 3 as
`checkpoint:human-verify`. With `--auto` set, GSD auto-approves the hash
recompute. 4 fresh hashes recorded above; `uv run emmy profile validate`
exits 0 on all 4 dirs.

**Total deviations:** 0 architectural; 3 minor (path convention, fixture
size, auto-approved checkpoint per flag).

## Self-Check: PASSED

- [x] All 3 tasks executed
- [x] SUMMARY.md created
- [x] All acceptance criteria verified
- [x] 14 tests pass
- [x] No regressions in pre-existing emmy-context tests
