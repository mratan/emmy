---
phase: 04.4-filesystem-memory-tool-append-only-prefix-compaction-polish-
plan: 06
status: complete
completed: 2026-04-26
---

# Plan 04.4-06: Append-only-prefix invariant (D-3X) — SUMMARY

## Files created / modified

| File | Change | Purpose |
|------|--------|---------|
| `packages/emmy-ux/src/prefix-hash.ts` | NEW (104 lines) | `computePrefixHash` + `extractSystemPrefixBytes` — canonical sha256 of system message + leading user preamble + tool catalog (alphabetic-by-name) |
| `packages/emmy-ux/src/pi-emmy-extension.ts` | +35 lines | Emits `compaction.prefix_hash` on every non-canary wire request via existing `emitEvent` path |
| `packages/emmy-ux/scripts/audit_prefix_mutations.ts` | NEW (101 lines, exec) | Static-grep audit; classifies findings as ALLOWED / FORBIDDEN / REVIEW |
| `packages/emmy-ux/tests/prefix-hash.test.ts` | NEW (165 lines) | 12 unit tests — extraction, canonicalization, determinism, V3 invariance |
| `packages/emmy-ux/tests/append-only-prefix-invariant.test.ts` | NEW (290 lines) | 6 integration tests — V3 hash byte-equality across turn 0/5/10, canary skip, regression case, body-growth invariance, session/turn id propagation |
| `packages/emmy-ux/tests/v4-prefix-cache-arch-aware.test.ts` | NEW (140 lines) | 7 tests — classifyArch + v4Pass gate logic (Pitfall #22) |
| `.planning/STATE.md` | +27 lines | D-3X (Phase 04.4) decision block under "Decisions Made During Execution" |
| `docs/runbook.md` | +47 lines | New §"Append-only-prefix invariant (D-3X)" section |

## Audit script output

```
$ bun run packages/emmy-ux/scripts/audit_prefix_mutations.ts
... 30 ALLOWED matches (`before_provider_request` mentions in code & comments) ...
[REVIEW] packages/emmy-provider/src/before-request-hook.ts:144  /messages\.unshift/
    payload.messages.unshift({ role: "system", content: assembledPrompt.text });

AUDIT GREEN — 32 site(s) reviewed; 0 forbidden.
```

The single REVIEW item (`messages.unshift` in `before-request-hook.ts:144`) is **NOT** a D-3X violation: it runs once per request to put the assembled-prompt system message at index 0. The assembled prompt itself is stable across the session (its sha256 is what `harness.assembly` events have always emitted), so prefix caching sees byte-identical bytes from turn 0 to turn N — exactly the D-3X intent.

## Patterns the audit checks

| Pattern | Verdict | Rationale |
|---------|---------|-----------|
| `before_provider_request` | allowed | per-request payload hook; check handler doesn't mutate `payload.messages[0]` |
| `messages\[0\]\.content\s*=` | forbidden | direct system-message mutation — D-3X violation |
| `messages\.unshift` | review | prepend mutates prefix index 0; review whether mid-session |
| `tools\.sort\b` | review | sorting tools mid-session reorders prefix |

## V3 reference hash

For a sample synthetic system prompt + preamble + 2-tool catalog:

```typescript
{
  messages: [
    { role: "system", content: "You are Emmy." },
    { role: "user", content: "AGENTS.md preamble\n" },
  ],
  tools: [
    { function: { name: "edit", description: "edit files" } },
    { function: { name: "bash", description: "run shell" } },
  ],
}
```

→ `emmy.prefix.hash = 82378301ddf19d4ba06d4fc823f4ae25ef2d24c4fc07a3a744fa91e168a8b79b`

(Use this as a reference value for V3 cross-checks.)

## V4 classifier verdicts (all 4 shipped profiles)

| Profile ID | Architecture | V4 gate |
|------------|--------------|---------|
| `qwen3.6-35b-a3b` | mamba-hybrid | measure-and-acknowledge (Pitfall #22) |
| `qwen3.6-27b` | attention-only | hard ≥ 80% post-compaction hit rate |
| `gemma-4-26b-a4b-it` | mamba-hybrid | measure-and-acknowledge (Pitfall #22) |
| `gemma-4-31b-it` | attention-only | hard ≥ 80% post-compaction hit rate |

## D-3X section text (verbatim from STATE.md)

```markdown
### D-3X (Phase 04.4): Append-only-prefix invariant

**Decision:** The system-prompt prefix sent to vLLM never mutates within a session. Compaction replaces conversation-body turns; never edits system prompt, tool descriptions, project preamble (CLAUDE.md / AGENTS.md), or any pre-turn content.

**Rationale:**
- vLLM automatic prefix caching is the largest free-money win in long-context. Any prefix mutation invalidates blocks → full recompute → KV pressure → preemption.
- Cognition's 2026 "clean context for verifiers" lesson — stable prefix is closer to clean than rewriting.
- Reproducibility: prefix mutations interleaved with compactions create non-deterministic histories.

**Allowed:**
- before_provider_request payload mutations (thinking-disable, reactive grammar) — per-request payload, not conversation prefix.
- Compaction body replacement — pi's existing behavior.
- Custom messages appended AFTER system prompt — appending, not mutating.

**Forbidden:**
- Re-rendering system prompt mid-session
- Re-ordering tool descriptions mid-session
- Mutating CLAUDE.md / AGENTS.md and re-loading
- Adding "session summary so far" to system prompt at compaction time

**Enforcement:**
1. One-time audit (Phase 04.4 plan 06): bun run packages/emmy-ux/scripts/audit_prefix_mutations.ts — exits 0 if zero FORBIDDEN findings.
2. Per-request OTel attribute emmy.prefix.hash = sha256(system_prompt_prefix_bytes) — V3 test asserts hash equality at turn 0 and turn N within one session.
3. V4 architecture-aware (per Pitfall #22): attention-only profiles gate at ≥80% prefix-cache hit rate post-compaction; Mamba-hybrid profiles use "measure-and-acknowledge" (boot-log warning detection).
```

## Acceptance criteria — verified

- [x] `packages/emmy-ux/src/prefix-hash.ts` exports `computePrefixHash`, `extractSystemPrefixBytes`
- [x] grep "first assistant turn" → 1 match
- [x] grep "createHash" → 1 match
- [x] `bun test prefix-hash.test.ts` → 12 pass
- [x] `pi-emmy-extension.ts` imports `computePrefixHash` and emits `compaction.prefix_hash`
- [x] `pi-emmy-extension.ts` contains `"emmy.prefix.hash":`
- [x] `bun test append-only-prefix-invariant.test.ts` → 6 pass
- [x] `bun run typecheck` clean across emmy-ux
- [x] `audit_prefix_mutations.ts` exists, executable, exits 0 (AUDIT GREEN)
- [x] STATE.md contains "D-3X (Phase 04.4): Append-only-prefix invariant" + "vLLM automatic prefix caching"
- [x] runbook.md contains "Append-only-prefix invariant (D-3X)" + audit invocation + "Pitfall #22"
- [x] `bun test v4-prefix-cache-arch-aware.test.ts` → 7 pass
- [x] V4 classifier correctly tags all 4 shipped profiles

## Deviations from Plan

**Buffer typing fix (technical, not architectural).** Plan asks for `Buffer.concat([...])`-based assembly in extractSystemPrefixBytes. Strict TypeScript (`@types/node` 24+) rejects `Buffer<ArrayBufferLike>` against `createHash().update()`'s `BinaryLike` parameter due to ArrayBuffer/SharedArrayBuffer variance. Switched to a string-builder approach with `TextEncoder().encode()` for the final byte slab — same canonical bytes, no Buffer typing churn. The hash output is identical for any input.

**Canary handling.** Plan's prefix-hash emit lives AFTER the existing `if (isCanary) return` early-out — so canary requests skip the new emit naturally. No additional guard needed.

**Total deviations:** 0 architectural; 1 type-system technicality (transparent to callers).

## Self-Check: PASSED

- [x] All 3 tasks executed
- [x] Each task committed individually (commits a6f208a → 84d7630 → 7f8da94 → bc0d733 → this Task 3)
- [x] SUMMARY.md created
- [x] All acceptance criteria verified via grep + bun test + script invocation
- [x] Typechecks clean (emmy-tools, emmy-ux, emmy-provider)
- [x] 25+ tests pass total (12 unit + 6 integration + 7 V4)
