[emmy] Langfuse keys not set (LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY) - running JSONL-only
pi-emmy starting (profile=qwen3.6-35b-a3b@v3.1, base_url=http://127.0.0.1:8002, telemetry=JSONL-only)
prompt.assembled sha256=1646541b6a6aef8b4411faf20c7ab5fec049c41b1fe6c4ecdf11a4d82b56e2cf
[32m[emmy] OFFLINE OK[0m
pi-emmy SP_OK canary: OK
pi-emmy session ready (prompt.sha256=1646541b6a6aef8b4411faf20c7ab5fec049c41b1fe6c4ecdf11a4d82b56e2cf, layers=system.md,tool_defs,user)
pi-emmy transcript=/data/projects/emmy/runs/phase2-sc3-capture/session-2026-04-27T01-37-20-013Z.jsonl
Here's the difference between `buildRealPiRuntime` and `buildRealPiRuntimeTui` in `session.ts`:

## Shared setup (identical in both)
Both functions start the same way:
1. **Auth** — seed `EMMY_VLLM_API_KEY` env var, create in-memory `AuthStorage`
2. **ModelRegistry** — register `emmy-vllm` as an `openai-completions` provider with the profile's model
3. **ExtensionFactory** — create the `emmyExtension` with profile, prompt provider, baseUrl, telemetry, etc.
4. **SessionManager** — in-memory session manager

## Key divergence

### `buildRealPiRuntime` (the `--print` / `--json` path)
- Uses **`createAgentSessionServices` → `createAgentSessionFromServices`** directly (the SDK path)
- **Wires the SubAgent (Agent) tool** into `customTools` if the profile has `subagents.enabled: true` (loads personas, creates concurrency governor, etc.)
- Builds a **`runPrint`** function that:
  - Wraps the call in an OTel `startActiveSpan("parent_session")`
  - Subscribes to `agent_end` to collect the final response
  - Calls `session.prompt(prompt)` and resolves with the assistant text
- Returns a `PiRuntime` adapter with `registerProvider`, `registerTool`, `on`, `runPrint`, `session`, and `introspect`

### `buildRealPiRuntimeTui` (the TUI path)
- Uses **`createAgentSessionRuntime`** + **`new InteractiveMode(runtime).run()`** — pi's full interactive runtime, not the SDK path
- **Does NOT wire the SubAgent tool** — `customTools` are passed directly without the persona/governor/Agent tool extension
- Has an **`agentDir`** step (`ensureEmmyAgentDir()`) to create `~/.emmy/agent/` for pi's settings writer (TUI needs a real agent directory)
- Builds a **`runTui`** function instead of `runPrint` — creates `new InteractiveMode(agentSessionRuntime, {verbose: false})` and calls `.run()`
- Passes extra extension options (`profileDir`, `profilesRoot`) that the `--print` path doesn't need (for the `/profile` slash command)
- The `sessionStartEvent` is conditionally passed through the factory closure

## Why two functions?
Per the comments, `buildRealPiRuntime` was **Plan 03-07 certified** and deliberately kept untouched. `buildRealPiRuntimeTui` is a parallel copy that diverges only at the final factory shape — the SDK path vs the full interactive runtime path. The shared auth/modelRegistry/extension wiring (~40 lines) is duplicated rather than refactored to avoid regressing the certified `--print` path.
