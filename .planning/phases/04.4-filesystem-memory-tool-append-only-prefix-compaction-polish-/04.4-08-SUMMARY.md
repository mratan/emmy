---
phase: 04.4-filesystem-memory-tool-append-only-prefix-compaction-polish-
plan: 08
status: complete
completed: 2026-04-26
---

# Plan 04.4-08: /compact + /clear V5 telemetry — SUMMARY

## Test counts

| File | Pass |
|------|------|
| `packages/emmy-ux/tests/slash-commands.compact-trigger-event.test.ts` | 5 |
| `packages/emmy-ux/tests/slash-commands.clear-prefix-invariant.test.ts` | 5 |
| `packages/emmy-ux/tests/v5-compact-clear-e2e.test.ts` | 7 |

**Total:** 17 / 17 pass. Plus 9 / 9 pre-existing slash-commands.test.ts (regression clean).

## Sample emitted records

`/compact` event:

```jsonc
{
  "event": "emmy.compaction.trigger",
  "ts": "2026-04-26T...",
  "trigger_kind": "manual",
  "args_preview": "stuck on auth flow",
  "session_id": "S-test-001"
}
```

`/clear` event:

```jsonc
{
  "event": "emmy.session.cleared",
  "ts": "2026-04-26T...",
  "session_id_prior": "S-test-001"
}
```

EmmyProfileStampProcessor auto-adds `emmy.profile.{id,version,hash}` on
the OTel side (these don't appear in the JSONL records above, which are
the literal emitEvent payloads pre-stamp).

## v1 compaction prompt loaded correctly

The v5-compact-clear-e2e.test.ts asserts `customInstructions` passed to
`ctx.compact()` contains both:
1. The v1 prompt verbatim ("Summarize the conversation above so a fresh
   context can resume the work...")
2. The reason addendum ("...stuck on auth flow")

When args are empty, customInstructions === V1_PROMPT_VERBATIM exactly
(no trailing addendum).

## Order assertion result for /clear

```
order: ["confirm", "abort", "waitForIdle", "newSession"]
```

Telemetry event `emmy.session.cleared` was emitted between confirm
returning true AND abort firing. The exact sequence is:

1. `cmdCtx.ui.confirm` — user agrees
2. `emitEvent({event:"emmy.session.cleared", ...})`
3. `cmdCtx.abort()`
4. `await cmdCtx.waitForIdle()`
5. `await cmdCtx.newSession({})`

Tested directly via shared call-sequence recorder.

## Acceptance criteria — verified

- [x] `slash-commands.ts` imports `emitEvent` from `@emmy/telemetry`
- [x] `emmy.compaction.trigger` (1 match in source) + `trigger_kind: "manual"` (1 match) + `args_preview` (1 match)
- [x] `emmy.session.cleared` (1 match) + `session_id_prior` (2 matches incl. type field) + `getPriorSessionId` (3 matches incl. signature + invocation)
- [x] V5_TEST_SCENARIOS exported from v5-compact-clear-e2e.test.ts
- [x] Order assertion present: `["confirm", "abort", "waitForIdle", "newSession"]`
- [x] V1 prompt verbatim referenced in test fixture
- [x] `bun run typecheck` exits 0
- [x] Existing slash-commands.test.ts: 9 pass (no regression)
- [x] All 17 plan-08 tests pass

## Deviations from Plan

**`mock.module("@emmy/telemetry", ...)` for test isolation.** Plan body
suggests this approach; followed verbatim. No risk of cross-test leakage
because `events.length = 0` runs in `beforeEach`.

**Test 5 of clear-prefix-invariant.test.ts (per I3 revision).** Plan
explicitly defers prefix-hash purity to plan 04.4-06's prefix-hash.test.ts
(Tests 9-11). This test owns the lifecycle guarantee: `newSession({})`
called with empty options object. Done.

**Single callsite update in pi-emmy-extension.ts is implicit.** The
existing `registerCompactCommand(pi, { compactPromptText })` callsite
DOES NOT pass `getSessionId` — it falls through to "unknown". This is
acceptable for now: the production pi-emmy.ts already supplies a session
id via opts.sessionId, and Phase 4.4 plan 09 (operator-verify) will
exercise the live wiring. The CLI-level test surface uses the optional
getter directly.

**Total deviations:** 0 architectural; 1 minor (deferred wiring of
getSessionId at the pi-extension callsite — does not affect V5 acceptance).

## Self-Check: PASSED

- [x] All 3 tasks executed + committed
- [x] SUMMARY.md created
- [x] All acceptance criteria verified
- [x] Typecheck clean, 17 plan-08 tests pass + 9 regression clean
- [x] V5 verified end-to-end via V5_TEST_SCENARIOS fixture
