---
phase: 03
plan: 07
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
  - "03-06"
files_modified:
  - profiles/qwen3.6-35b-a3b/v3/profile.yaml
  - profiles/qwen3.6-35b-a3b/v3/serving.yaml
  - profiles/qwen3.6-35b-a3b/v3/harness.yaml
  - profiles/qwen3.6-35b-a3b/v3/PROFILE_NOTES.md
  - profiles/qwen3.6-35b-a3b/v3/prompts/system.md
  - profiles/qwen3.6-35b-a3b/v3/prompts/edit_format.md
  - profiles/qwen3.6-35b-a3b/v3/prompts/tool_descriptions.md
  - profiles/qwen3.6-35b-a3b/v3/prompts/compact.md
  - profiles/qwen3.6-35b-a3b/v3/prompts/compact.alternate.md
  - profiles/qwen3.6-35b-a3b/v3/tool_schemas/
  - profiles/qwen3.6-35b-a3b/v3/grammars/tool_call.lark
  - emmy_serve/profile/schema.py
  - emmy_serve/profile/tests/test_schema.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CLOSEOUT.md
  - .github/workflows/airgap-phase3.yml
  - scripts/phase3_close_walkthrough.sh
  - scripts/sc5_offline_badge.sh
autonomous: false
requirements:
  - HARNESS-05
  - HARNESS-09
  - CONTEXT-02
  - TELEM-01
  - TELEM-02
  - TELEM-03
  - UX-02
  - UX-03
tags:
  - phase-close
  - profile-v3-bump
  - traceability
  - air-gap-ci
  - sc-1-walkthrough
  - closeout
must_haves:
  truths:
    - "profiles/qwen3.6-35b-a3b/v3/ exists as sibling of v2 (Phase 1 D-02 immutability: never mutate v2 in place)"
    - "v3/harness.yaml has context.compaction block (soft_threshold_pct, preserve_recent_turns, summarization_prompt_path, preserve_tool_results)"
    - "v3/harness.yaml has tools.web_fetch.allowlist list (populated with docs hosts or explicit empty per final decision)"
    - "v3/prompts/compact.md exists with emmy-specific summarization instructions layered over pi's SUMMARIZATION_SYSTEM_PROMPT"
    - "Phase 1 pydantic schema (emmy_serve/profile/schema.py) extended with CompactionConfig + WebFetchConfig optional nested blocks; validate exits 0 on v3"
    - "v3 profile hash computed via `uv run emmy profile hash --write profiles/qwen3.6-35b-a3b/v3/` and recorded in profile.yaml"
    - "SC-1-class daily-driver walkthrough re-runs GREEN against /tmp/emmy-p3-close-walkthrough/ with all 5 Phase-3 surfaces live (Langfuse traces, compaction never triggered in small task, footer, rating capture, offline badge)"
    - "Air-gap CI extended: emmy-serve + Langfuse compose both up; 50-turn replay runs; zero outbound non-loopback packets from either stack"
    - "REQUIREMENTS.md: 8 Phase-3 REQ-IDs (HARNESS-05, HARNESS-09, CONTEXT-02, TELEM-01, TELEM-02, TELEM-03, UX-02, UX-03) flipped to [x] Done with evidence citations"
    - "5 Phase-2 Done† REQ-IDs flipped Done† → Done (wire-through no longer deferred). Mapping verified against 02-CLOSEOUT.md § Carry-forward (5 numbered deferrals): (1) @emmy/provider → pi streamSimple = HARNESS-02; (2) hash-anchored edit via customTools = TOOLS-03; (3) MCP bridge via customTools = TOOLS-07; (4) emmy 3-layer prompt wire-authoritative via BeforeProviderRequestEvent = HARNESS-06; (5) chat_template_kwargs.enable_thinking:false at request level = HARNESS-07 (per_tool_sampling + enable_thinking both travel via before_provider_request hook, so HARNESS-07 per-tool-sampling wire-through covers the same seam). Phase-2 REQUIREMENTS.md rows for HARNESS-02 (line 187), HARNESS-06 (line 191), HARNESS-07 (line 192), TOOLS-03 (line 198), TOOLS-07 (line 202) all carry the `Done †` marker — Phase 3 flips all five to `Done`."
    - "Plan 03-07 runs SC-2 LIVE (not stub) as a REQUIRED gate before REQ-ID flip: `bash scripts/sc2_200turn_compaction.sh --mode=live --variant=default` AND `--variant=alternate` AND `--variant=disabled`; verdict=pass required for default+alternate; disabled run asserts hard-ceiling fail-loud (D-12). Results recorded in v3/PROFILE_NOTES.md § Validation Runs — Phase 3 SC-2 3-Run Matrix. Plan-checker BLOCKER + Pitfall #5 guardrail both addressed here."
    - "ROADMAP.md Phase 3 row flipped to [x] Closed with date + hash"
    - "STATE.md updated with Phase 3 close summary, v3 profile hash, carry-forward items (if any)"
    - "03-CLOSEOUT.md written following Phase 2 CLOSEOUT shape (SC verdicts, commit ledger, deferrals)"
  artifacts:
    - path: "profiles/qwen3.6-35b-a3b/v3/"
      provides: "v3 profile directory with all 5 bundle components + new compact.md prompt"
    - path: "profiles/qwen3.6-35b-a3b/v3/harness.yaml"
      provides: "context.compaction + tools.web_fetch.allowlist blocks"
      contains: "compaction:"
    - path: ".github/workflows/airgap-phase3.yml"
      provides: "air-gap CI job that brings up both emmy-serve and Langfuse compose and asserts zero-outbound"
    - path: ".planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CLOSEOUT.md"
      provides: "Phase 3 closeout document following Phase 2 CLOSEOUT shape"
  key_links:
    - from: "emmy_serve/profile/schema.py"
      to: "CompactionConfig pydantic model"
      via: "HarnessConfig.context.compaction nested optional field"
      pattern: "class CompactionConfig"
    - from: "profiles/qwen3.6-35b-a3b/v3/harness.yaml"
      to: "compact.md relative path"
      via: "context.compaction.summarization_prompt_path"
      pattern: "summarization_prompt_path"
---

<objective>
Phase 3 close. Consolidate the 6 Wave-1..3 plans into a cohesive close:

1. **Profile v2 → v3 bump** (RESEARCH Open Question #2 recommendation: coordinated bump). v3 sibling of v2 with new `context.compaction` + `tools.web_fetch.allowlist` blocks + new `prompts/compact.md`. v3 hash recomputed; PROFILE_NOTES.md documents provenance for every new default.
2. **Phase 1 schema patch** for the new nested blocks — `emmy_serve/profile/schema.py` pydantic models extended; v2 continues to validate (backward-compat via Optional).
3. **SC-1-class walkthrough re-run** against all 5 Phase-3 surfaces live.
4. **Air-gap CI extension** covering both emmy-serve + Langfuse compose (D-09).
5. **Traceability flip:** 8 Phase-3 REQ-IDs + 5 Phase-2 Done† REQ-IDs → Done.
6. **03-CLOSEOUT.md + STATE.md + ROADMAP.md** — following Phase 2 precedent.

Purpose: Phase 2 closed with 23 REQ-IDs Done (5 of them Done†). Phase 3 closes with a further 8 + those 5 flipping to Done — total 28 REQ-IDs closed. Daily-driver bar holds; observability bar clears.

Output: v3 profile hash certified; REQUIREMENTS/ROADMAP/STATE flipped; CLOSEOUT.md committed; Phase 3 sealed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md
@.planning/phases/01-serving-foundation-profile-schema/01-CLOSEOUT.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CONTEXT.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-RESEARCH.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-01-SUMMARY.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-02-SUMMARY.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-03-SUMMARY.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-04-SUMMARY.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-05-SUMMARY.md
@.planning/phases/03-observability-agent-loop-hardening-lived-experience/03-06-SUMMARY.md
@profiles/qwen3.6-35b-a3b/v2/harness.yaml
@profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md
@emmy_serve/profile/schema.py
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile v3 clone + new blocks + schema patch + hash re-compute</name>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v2/ (full tree; v3 is sibling with additive changes)
    - emmy_serve/profile/schema.py (current HarnessConfig shape; add optional nested models)
    - emmy_serve/profile/tests/test_schema.py (pattern for v2 backward-compat + v3 acceptance)
    - profiles/qwen3.6-35b-a3b/v2/PROFILE_NOTES.md (provenance template)
    - Plan 03-02 SUMMARY (OTel SDK version pins — populate PROFILE_NOTES validation_runs)
    - Plan 03-03 SUMMARY (compaction defaults rationale)
    - Plan 03-06 SUMMARY (allowlist final-state decision)
  </read_first>
  <files>profiles/qwen3.6-35b-a3b/v3/ (full subtree creation via cp -a then edits), emmy_serve/profile/schema.py, emmy_serve/profile/tests/test_schema.py</files>
  <action>
    **(1) Clone v2 → v3:**
    ```bash
    cp -a profiles/qwen3.6-35b-a3b/v2/ profiles/qwen3.6-35b-a3b/v3/
    # v3/ now byte-identical to v2
    ```

    **(2) Extend `v3/harness.yaml`:**
    Add after existing `context:` block keys:
    ```yaml
    context:
      max_input_tokens: 114688
      include_repo_map: false
      repo_map_max_tokens: 0
      default_pruning: head_tail        # legacy; superseded by compaction when configured
      compaction:
        soft_threshold_pct: 0.75        # D-11; crosses at ~86K of 114K
        preserve_recent_turns: 5        # D-14; user-override per profile
        summarization_prompt_path: prompts/compact.md   # D-13
        preserve_tool_results: error_only   # D-14 / D-15: {error_only, none, all}
    tools:
      format: openai
      schemas: tool_schemas/
      grammar:
        path: grammars/tool_call.lark
        mode: reactive
      per_tool_sampling:
        edit: { temperature: 0.0 }
        bash: { temperature: 0.0 }
        read: { temperature: 0.0 }
      web_fetch:
        allowlist:
          - docs.python.org
          - developer.mozilla.org
          - docs.vllm.ai
          - huggingface.co
          - docs.langfuse.com
    ```

    The exact `allowlist` contents are Claude's discretion per CONTEXT D-26 parenthetical; the set above is defensible documentation-reading hosts per CLAUDE.md "documentation reading allowed". Can be edited per final walkthrough.

    **(3) Create `v3/prompts/compact.md`** (~40 lines) — emmy's profile-specific compaction prompt that Qwen3.6 uses via pi's compact() customInstructions arg. Layers over pi's built-in SUMMARIZATION_SYSTEM_PROMPT:
    ```markdown
    You are summarizing a long coding session for context compaction. Emmy's
    preservation layer has already pinned: the profile system prompt, the
    AGENTS.md content, the user's original goal, the most recent 5 turns,
    any error-flagged tool results, any @file pins, and any TODO/PLAN file
    edits. You do NOT need to re-summarize those.

    Summarize ONLY the messages in this compaction batch. Prioritize:
    1. What files were edited, in what order, and why.
    2. What tool commands were run and their outcomes.
    3. What decisions the user or agent made, with brief rationale.
    4. What sub-tasks are complete vs in-progress.

    Avoid:
    - Restating the original goal (already preserved).
    - Rephrasing full tool-result contents (they're either preserved as errors or acceptably lossy).
    - Adding new interpretations or planning beyond what was actually discussed.

    Write as a dense narrative (not bullet list) of <= 300 tokens.
    Target: a reader who has the preserved state should be able to resume the
    task after reading only the summary.
    ```

    **(4) Extend `emmy_serve/profile/schema.py`:**
    ```python
    class CompactionConfig(BaseModel):
        soft_threshold_pct: float = Field(..., ge=0.0, le=1.0)
        preserve_recent_turns: int = Field(..., ge=0)
        summarization_prompt_path: str = Field(..., min_length=1)
        preserve_tool_results: Literal["error_only", "none", "all"] = "error_only"

    class WebFetchConfig(BaseModel):
        allowlist: list[str] = Field(default_factory=list)

    class ContextConfig(BaseModel):
        # ...existing fields...
        compaction: Optional[CompactionConfig] = None  # D-15 optional — v2 valid without it

    class ToolsConfig(BaseModel):
        # ...existing fields...
        web_fetch: Optional[WebFetchConfig] = None
    ```

    **(5) Extend `emmy_serve/profile/tests/test_schema.py`** — regression tests:
    - v2/harness.yaml continues to validate (compaction absent → None; web_fetch absent → None)
    - v3/harness.yaml validates with both blocks
    - compaction.soft_threshold_pct = 1.5 → ValidationError
    - compaction.preserve_tool_results = "bogus" → ValidationError

    **(6) Recompute v3 hash:**
    ```bash
    uv run emmy profile hash --write profiles/qwen3.6-35b-a3b/v3/
    # Updates v3/profile.yaml hash field; record the resulting hash in PROFILE_NOTES
    ```

    **(7) Extend `v3/PROFILE_NOTES.md`** — add a Phase 3 provenance appendix:
    ```markdown
    ## Phase 3 (Observability + Compaction + Lived-Experience)

    ### context.compaction (D-11..D-17)
    - `soft_threshold_pct: 0.75` — source: Claude Code / Cursor / Cline convergence (industry default for LLM-summarization compaction)
    - `preserve_recent_turns: 5` — source: pi 0.68 `DEFAULT_COMPACTION_SETTINGS.keepRecentTokens=20000` ≈ 5 turns at 4K avg
    - `summarization_prompt_path: prompts/compact.md` — emmy authored; layers on pi's SUMMARIZATION_SYSTEM_PROMPT
    - `preserve_tool_results: error_only` — Pitfall #15 guardrail; rationale: stacktraces contain the actual error signal

    ### tools.web_fetch.allowlist (D-26)
    Default-deny. Allowlist starts with 5 documentation hosts:
    - `docs.python.org`, `developer.mozilla.org`, `docs.vllm.ai`, `huggingface.co`, `docs.langfuse.com`
    Source: CLAUDE.md "documentation reading allowed"; concrete host selection per author daily-driver usage pattern.

    ### validation_runs (Phase 3)
    - Plan 03-02: OTel SDK versions verified + Langfuse compose stood up healthy + 1 trace per turn visible
    - Plan 03-03: SC-2 200-turn compaction fixture pass verdict (5/5 invariants green)
    - Plan 03-04: footer parity ≤5% delta across 3 snapshots
    - Plan 03-05: 3-turn rating flow + --export-hf HF-loadable + kill-switch safe
    - Plan 03-06: boot-green + web_fetch red flip + warn-and-continue demo
    - Plan 03-07: SC-1-class walkthrough verdict + air-gap CI pass + 28 total REQ-IDs flipped
    ```

    **(8) Validate + commit:**
    ```bash
    uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/   # still passes (v2 unchanged)
    uv run emmy profile validate profiles/qwen3.6-35b-a3b/v3/   # passes with new blocks
    uv run pytest tests/unit -q                                  # 137+ pass / 1 skip
    ```

    **Commit:** `feat(03-07): profile v2→v3 bump (context.compaction + tools.web_fetch.allowlist) + schema patch`
  </action>
  <verify>
    <automated>cd /data/projects/emmy && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/ && uv run emmy profile validate profiles/qwen3.6-35b-a3b/v3/ && uv run pytest tests/unit -q 2>&1 | tail -5 && grep -c 'compaction:' profiles/qwen3.6-35b-a3b/v3/harness.yaml && grep -c 'web_fetch:' profiles/qwen3.6-35b-a3b/v3/harness.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/qwen3.6-35b-a3b/v3/` directory exists as sibling of v2
    - `v3/profile.yaml` + `v3/serving.yaml` + `v3/harness.yaml` + `v3/PROFILE_NOTES.md` + `v3/prompts/compact.md` all exist
    - `grep -c 'compaction:' profiles/qwen3.6-35b-a3b/v3/harness.yaml` ≥ 1
    - `grep -c 'soft_threshold_pct' profiles/qwen3.6-35b-a3b/v3/harness.yaml` ≥ 1
    - `grep -c 'allowlist:' profiles/qwen3.6-35b-a3b/v3/harness.yaml` ≥ 1
    - `v3/profile.yaml` `hash:` field contains sha256 distinct from v2's hash
    - `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v2/` exits 0 (backward compat)
    - `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v3/` exits 0
    - `uv run pytest tests/unit -q` ≥ 137 pass / 1 skip (Phase 1 + new schema tests)
    - v2/profile.yaml hash unchanged from Phase 2 close (`sha256:24be3eea...85d8b`) — immutability honored
    - `grep -c 'class CompactionConfig\|class WebFetchConfig' emmy_serve/profile/schema.py` ≥ 2
  </acceptance_criteria>
  <done>v3 profile cloned + extended + hash-recomputed + schema-validated; PROFILE_NOTES extended with provenance.</done>
</task>

<task type="auto">
  <name>Task 1a: Alternate compaction prompt stub + SC-2 3-run matrix template (Pitfall #5 guardrail hook)</name>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v3/prompts/compact.md (the default authored in Task 1)
    - .planning/phases/03-observability-agent-loop-hardening-lived-experience/03-RESEARCH.md § Open Questions (RESOLVED) #4 (3-run variant matrix resolution)
    - scripts/sc2_200turn_compaction.sh (extended in Plan 03-03 Task 3 with --variant support)
  </read_first>
  <files>profiles/qwen3.6-35b-a3b/v3/prompts/compact.alternate.md, profiles/qwen3.6-35b-a3b/v3/PROFILE_NOTES.md</files>
  <action>
    **(1) Create `profiles/qwen3.6-35b-a3b/v3/prompts/compact.alternate.md`** — structurally different from `compact.md` so the 3-run matrix exercises meaningful prompt variance. Narrative-style default; alternate uses a structured JSON-output instruction:
    ```markdown
    You are summarizing a long coding session for context compaction. Emmy's
    preservation layer has already pinned: the profile system prompt, AGENTS.md,
    the user's original goal, the most recent 5 turns, error-flagged tool
    results, @file pins, and TODO/PLAN file edits. Do NOT re-summarize those.

    For the messages in this compaction batch, emit a single JSON object with
    the following schema (NO prose, NO markdown fences, JSON only):

    {
      "files_touched": ["path1", "path2", ...],
      "tool_calls_summary": [{"tool": "name", "count": N, "notable": "<=1 sentence"}],
      "decisions": ["<=1 sentence each"],
      "in_progress": ["<=1 sentence each"]
    }

    Keep the total serialized JSON <= 300 tokens. Omit empty arrays. A reader
    who has the preserved state should be able to resume the task after reading
    only this JSON summary.
    ```

    This alternate exercises: (a) different output shape (JSON vs narrative); (b) different cognitive frame (enumeration vs synthesis); (c) same token budget target. SC-2 invariants must hold under both.

    **(2) Extend `profiles/qwen3.6-35b-a3b/v3/PROFILE_NOTES.md`** with a new section (empty template; Task 3 Step 10 fills verdicts):
    ```markdown
    ## Validation Runs — Phase 3 SC-2 3-Run Matrix (Pitfall #5 guardrail)

    Filled in by Plan 03-07 Task 3 Step 10 (SC-2 live gate). All three variants
    run against live emmy-serve + v3 profile; verdicts recorded here BEFORE
    REQ-ID flip in Task 4.

    | Variant | Command | Verdict | Invariants (5/5) | Session ID | Date |
    |---------|---------|---------|------------------|------------|------|
    | default | sc2_200turn_compaction.sh --mode=live --variant=default | <pending> | <pending> | <pending> | <pending> |
    | alternate | sc2_200turn_compaction.sh --mode=live --variant=alternate | <pending> | <pending> | <pending> | <pending> |
    | disabled | sc2_200turn_compaction.sh --mode=live --variant=disabled | <pending — expect hard_ceiling> | n/a (expects D-12 throw) | <pending> | <pending> |

    **Acceptance per Pitfall #5:** No subset-victory. All three variants must
    be run; default + alternate must pass 5/5 invariants; disabled must throw
    SessionTooFullError (D-12). Failing any of these blocks Phase 3 close.
    ```

    **Commit:** `feat(03-07): compact.alternate.md stub + PROFILE_NOTES 3-run matrix template (Pitfall #5)`
  </action>
  <verify>
    <automated>cd /data/projects/emmy && test -f profiles/qwen3.6-35b-a3b/v3/prompts/compact.alternate.md && grep -c 'tool_calls_summary' profiles/qwen3.6-35b-a3b/v3/prompts/compact.alternate.md && grep -c '3-Run Matrix' profiles/qwen3.6-35b-a3b/v3/PROFILE_NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/qwen3.6-35b-a3b/v3/prompts/compact.alternate.md` exists and has structured-JSON output instruction (grep for `tool_calls_summary`)
    - `grep -c "3-Run Matrix" profiles/qwen3.6-35b-a3b/v3/PROFILE_NOTES.md` ≥ 1 (template landed)
    - v3 profile hash re-recomputed AFTER compact.alternate.md lands (new file in bundle → hash changes). Run `uv run emmy profile hash --write profiles/qwen3.6-35b-a3b/v3/` again; record in Task 4 CLOSEOUT as the final v3 hash
    - `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v3/` still exits 0
  </acceptance_criteria>
  <done>Alternate compaction prompt stub committed; PROFILE_NOTES matrix template ready for Task 3 Step 10 fill-in; v3 hash re-computed (supersedes Task 1 hash).</done>
</task>

<task type="auto">
  <name>Task 2: Air-gap CI extension — both stacks up, zero-outbound enforcement</name>
  <read_first>
    - .github/workflows/airgap.yml (Phase 1 existing — extend, don't replace)
    - emmy_serve/airgap/ci_verify.py (Phase 1 D-12 validator)
    - scripts/start_observability.sh (from Plan 03-02; brings up Langfuse)
    - scripts/start_emmy.sh (Phase 1; brings up emmy-serve)
    - .planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CONTEXT.md D-09 (digest-pinning requirement for air-gap)
    - .planning/phases/03-observability-agent-loop-hardening-lived-experience/03-RESEARCH.md §"Security Domain" (zero-outbound assertion)
  </read_first>
  <files>.github/workflows/airgap-phase3.yml, emmy_serve/airgap/ci_verify_phase3.py, scripts/airgap_phase3_replay.sh</files>
  <action>
    **(1) `emmy_serve/airgap/ci_verify_phase3.py`** — extended validator script (or extend existing `ci_verify.py` with a `--phase=3` flag). The script must:
    - Pull digest-pinned Langfuse images from local Docker registry cache (D-09)
    - Start emmy-serve (existing `start_emmy.sh`)
    - Start Langfuse stack (`scripts/start_observability.sh`)
    - Wait for both healthy
    - Run a 50-turn synthetic replay via pi-emmy (`scripts/airgap_phase3_replay.sh`)
    - During replay, capture `ss -tnp state established` + `tcpdump -i any -w /tmp/airgap.pcap not 'port 22' and not 'net 127.0.0.0/8' and not 'host ::1'` (non-loopback, non-SSH)
    - After replay, assert: pcap empty OR only Docker-internal bridge-network traffic
    - Teardown both stacks
    - Exit non-zero on any non-loopback packet

    **(2) `scripts/airgap_phase3_replay.sh`** — 50-turn pi-emmy replay using pre-canned prompts (reuse Phase 1 air-gap fixture if present; otherwise build minimal prompts from `runs/phase2-sc3-capture/` seed).

    **(3) `.github/workflows/airgap-phase3.yml`** — GitHub Actions self-hosted workflow:
    ```yaml
    name: airgap-phase3
    on:
      workflow_dispatch:
      pull_request:
        paths:
          - "observability/**"
          - "packages/emmy-telemetry/**"
          - "scripts/start_observability.sh"
    jobs:
      airgap_phase3:
        runs-on: [self-hosted, dgx-spark]   # requires self-hosted runner registered on DGX Spark
        steps:
          - uses: actions/checkout@v4
          - name: Verify no npm/pypi calls in test phase
            run: scripts/pre_airgap_guard.sh   # asserts bun.lock + uv.lock present; no install-time fetches
          - name: Start emmy-serve
            run: ./start_emmy.sh &
          - name: Start Langfuse
            run: bash scripts/start_observability.sh
          - name: Run phase-3 air-gap replay
            run: uv run python emmy_serve/airgap/ci_verify_phase3.py --profile=v3
          - name: Teardown
            if: always()
            run: bash scripts/stop_observability.sh && pkill -f 'vllm serve' || true
    ```

    **(4) Task 3 discipline:** the workflow requires self-hosted runner registration per Phase 1 Plan 01-08 Task 3 (still deferred per STATE.md). Phase 3 documents the workflow YAML and local `uv run python ci_verify_phase3.py` path; Task 3 actual CI-run deferral acceptable if runner not registered (phase-close can still certify via local validator run).

    **Commit:** `infra(03-07): air-gap CI extended to both emmy-serve + Langfuse (zero-outbound 50-turn replay)`
  </action>
  <verify>
    <automated>cd /data/projects/emmy && bash -n scripts/airgap_phase3_replay.sh && python3 -m py_compile emmy_serve/airgap/ci_verify_phase3.py 2>&1 | head -5 && yq e '.jobs.airgap_phase3.runs-on' .github/workflows/airgap-phase3.yml</automated>
  </verify>
  <acceptance_criteria>
    - `emmy_serve/airgap/ci_verify_phase3.py` exists and compiles (py_compile exits 0)
    - `.github/workflows/airgap-phase3.yml` exists; `runs-on: [self-hosted, dgx-spark]` present; paths trigger matches observability + emmy-telemetry
    - `scripts/airgap_phase3_replay.sh` exists and syntax-valid (`bash -n` exits 0)
    - Workflow has a `Teardown` step with `if: always()` (cleanup invariant)
    - Validator script contains `tcpdump` OR `ss -tnp state established` check + loopback filter
    - Local validator run (no GitHub Actions) completes: `uv run python emmy_serve/airgap/ci_verify_phase3.py --dry-run` exits 0 (dry-run skips actual docker up; asserts config-valid)
  </acceptance_criteria>
  <done>Air-gap CI extended; local validator runnable; CI-runner registration can be deferred to Phase 7 (follows Phase 1 Plan 01-08 Task 3 precedent).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC-1-class Phase 3 close walkthrough — all 5 surfaces live</name>
  <what-built>
    Every Phase-3 surface wired to the live path:
    - Plan 03-01: Track B wire-throughs (provider / tools / prompt / enable_thinking / grammar)
    - Plan 03-02: Langfuse OTel + JSONL dual-sink + profile-stamp on every span
    - Plan 03-03: Compaction trigger (soft threshold, D-14 preservation, D-16 fallback)
    - Plan 03-04: TUI footer (GPU/KV/spec-accept-/tok/s) at 1Hz
    - Plan 03-05: Alt+Up/Down rating → feedback.jsonl → --export-hf
    - Plan 03-06: Offline OK badge + web_fetch allowlist runtime enforcement
    - Plan 03-07: v3 profile + air-gap CI
  </what-built>
  <how-to-verify>
    **Pre-check:**
    ```bash
    bash scripts/start_emmy.sh                        # emmy-serve up on 8002
    bash scripts/start_observability.sh               # Langfuse on 3000
    # Ensure LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY set in observability/langfuse/.env after first-login
    ```

    **Walkthrough:**
    1. `rm -rf /tmp/emmy-p3-close-walkthrough && mkdir -p /tmp/emmy-p3-close-walkthrough && cd /tmp/emmy-p3-close-walkthrough && git init && echo "# Close walkthrough" > README.md && git add -A && git commit -m init`
    2. `bash scripts/phase3_close_walkthrough.sh` OR manually:
       ```
       EMMY_PROFILE=qwen3.6-35b-a3b@v3 pi-emmy
       ```
       (interactive TUI — uses v3 profile; v3 hash confirmed via boot banner stderr).

    3. Boot banner verification (stderr):
       - `pi-emmy starting (profile=qwen3.6-35b-a3b@v3, base_url=http://127.0.0.1:8002, telemetry=JSONL+Langfuse)` ← Plan 03-02
       - `[emmy] OFFLINE OK` (green) ← Plan 03-06
       - `OBSERVABILITY: ON — JSONL + Langfuse OTLP (Langfuse responded 200)` ← Plan 03-02
       - SP_OK canary green (Plan 03-01 regression guard preserved)

    4. TUI verification:
       - Status line has `[GPU N% • KV N% • spec accept - • tok/s N]` at 1Hz ← Plan 03-04
       - Status line has `OFFLINE OK` ← Plan 03-06

    5. Issue the Phase 2 SC-1 prompt (multi-file change, reuses Phase 2 fixture semantics):
       ```
       Create src/{foo,bar,baz}.ts each exporting a default function returning its filename; add tests at test/{foo,bar,baz}.test.ts using bun:test; run 'bun test' and confirm 3/3 pass.
       ```

    6. After completion:
       - Press Alt+Up → feedback.jsonl has a new row with turn_id matching final turn ← Plan 03-05
       - `cat ~/.emmy/telemetry/feedback.jsonl | tail -1 | jq '.profile_version'` → `"v3"` (confirms v3 routing)

    7. Langfuse UI verification (http://localhost:3000):
       - Trace exists for this session
       - Spans have `emmy.profile.id=qwen3.6-35b-a3b`, `emmy.profile.version=v3`, `emmy.profile.hash=<new v3 hash>` ← Plan 03-02 D-10
       - Spans have `gen_ai.system=vllm`
       - At least one tool-execute span

    8. JSONL verification:
       ```bash
       ls runs/ | tail -1
       grep -c '"event":"session.start"' runs/<latest>/events.jsonl          # 1
       grep -c '"event":"session.sp_ok.pass"' runs/<latest>/events.jsonl     # 1
       grep -c '"emmy.profile.hash"' runs/<latest>/events.jsonl              # ≥ 10
       grep -c '"event":"session.offline_audit.complete"' runs/<latest>/events.jsonl   # 1
       ```

    9. No network leak:
       ```bash
       ss -tnp state established | grep pi-emmy | grep -v '127\.0\.0\.1\|::1'          # empty
       ss -tnp state established | grep 'docker\|langfuse' | grep -v '127\.0\.0\.1\|::1'  # empty
       ```

    10. **SC-2 live 3-run matrix — REQUIRED gate (plan-checker BLOCKER fix; Pitfall #5 guardrail).** Run all three variants against LIVE emmy-serve + v3 profile:
        ```bash
        bash scripts/sc2_200turn_compaction.sh --mode=live --variant=default
        bash scripts/sc2_200turn_compaction.sh --mode=live --variant=alternate
        bash scripts/sc2_200turn_compaction.sh --mode=live --variant=disabled
        ```
        Acceptance (all must hold BEFORE Task 4 REQ-ID flip):
        - `runs/phase3-sc2-live-default/report.json` → `.verdict == "pass"` AND all 5 SC-2 invariants green (goal preserved, 5 most recent verbatim, error tool results verbatim, @file pins intact, per-tool truncation rate > 0 in trace)
        - `runs/phase3-sc2-live-alternate/report.json` → `.verdict == "pass"` AND same 5 invariants green (demonstrates prompt variance doesn't regress preservation discipline)
        - `runs/phase3-sc2-live-disabled/report.json` → `.verdict == "hard_ceiling"` AND SessionTooFullError thrown (D-12 fail-loud asserted when compaction is off)
        - All three results appended to v3/PROFILE_NOTES.md § Validation Runs — Phase 3 SC-2 3-Run Matrix (see Task 1 Step 7 template)
        - `grep '"event":"session.compaction.complete"' runs/phase3-sc2-live-default/events.jsonl` returns ≥ 1 match (live compaction event proves wire-path integrity)

    11. Air-gap validator smoke (local, not CI):
        ```bash
        uv run python emmy_serve/airgap/ci_verify_phase3.py --dry-run
        # exits 0; asserts both stacks configured for loopback-only
        ```

    **Verdict:** Type `sc1 phase3 green` to unblock closeout. If ANY step fails, Phase 3 close is blocked; fix and re-run before Task 4.
  </how-to-verify>
  <resume-signal>Type "sc1 phase3 green" OR describe failures</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Traceability flip + ROADMAP + STATE + 03-CLOSEOUT.md</name>
  <read_first>
    - .planning/phases/02-pi-harness-mvp-daily-driver-baseline/02-CLOSEOUT.md (shape template)
    - .planning/REQUIREMENTS.md (find Phase-2 Done† rows to flip to Done + Phase-3 [ ] rows to flip to [x])
    - .planning/ROADMAP.md (Phase 3 row — flip [ ] → [x] with close date + hash)
    - .planning/STATE.md (extend with Phase 3 close summary + v3 hash + daily-driver REACHED restatement)
    - All 6 03-0N-SUMMARY.md files (plan-level evidence to cite)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CLOSEOUT.md</files>
  <action>
    **(1) `.planning/REQUIREMENTS.md` — flip 8 Phase-3 REQ-IDs + 5 Phase-2 Done† → Done:**

    Phase 3 REQ-IDs (flip `- [ ]` → `- [x]` + add evidence line):
    - HARNESS-05 — evidence: Plan 03-03 SC-2 fixture 5/5 invariants green; commits from Plan 03-03
    - HARNESS-09 — evidence: Plan 03-02 Langfuse UI trace per turn + emmy.profile.* attrs on every span
    - CONTEXT-02 — evidence: v3/harness.yaml context.compaction block + Plan 03-03 per-profile policy tests
    - TELEM-01 — evidence: observability/langfuse/docker-compose.yaml + Plan 03-02 Task 4 verdict
    - TELEM-02 — evidence: Plan 03-05 feedback.jsonl with 13-field rows + Alt+Up/Down capture
    - TELEM-03 — evidence: Plan 03-05 --export-hf HF-loadable artifact + EMMY_TELEMETRY=off kill-switch
    - UX-02 — evidence: Plan 03-04 footer parity ≤5% across 3 snapshots
    - UX-03 — evidence: Plan 03-06 boot-green + red-flip demo

    Phase-2 Done† → Done (5 items, REQ-IDs verified against REQUIREMENTS.md and 02-CLOSEOUT.md § Carry-forward):

    Plan-checker WARNING fix: the 5th item is HARNESS-07 (per-tool sampling via hook), NOT "enable_thinking" as a standalone REQ-ID. The `chat_template_kwargs.enable_thinking:false` at request level travels via the same `before_provider_request` hook that HARNESS-07 per-tool-sampling uses; both are implementation evidence for HARNESS-07's "Sampling control is per-tool / per-task via the profile" wire-through.

    Exact pairs (REQ-ID → REQUIREMENTS.md line → evidence):
    - **HARNESS-02** (REQUIREMENTS.md line 41, line 187 in traceability table) — pi's `ModelRegistry.registerProvider({streamSimple})` binding (Plan 03-01 Task 2 step 4, commit `feat(03-01):`). Evidence: `packages/emmy-provider/src/index.ts` `registerEmmyProvider` flips from NO-OP to live via extensionApi.on wire; `packages/emmy-ux/test/session.boot.test.ts` tests 2-3.
    - **HARNESS-06** (REQUIREMENTS.md line 46-47, line 191 in traceability table) — Emmy's 3-layer assembled prompt overwrites pi's templated system message at wire time (Plan 03-01 Task 2 step 1 — the `payload.messages[idx] = {role:"system", content: assembledPrompt.text}` mutation in `before-request-hook.ts`). Evidence: `packages/emmy-provider/src/hook.test.ts` Test 4 asserts the system-message replacement.
    - **HARNESS-07** (REQUIREMENTS.md line 48-49, line 192 in traceability table) — per-tool / per-task sampling via profile at the `before_provider_request` hook. Evidence: same hook path also injects `chat_template_kwargs.enable_thinking:false` (Plan 03-01 D-02a; removes a17f4a9 stopgap). Per-tool sampling knobs in `profile.harness.tools.per_tool_sampling.*` (qwen3.6 v3 harness.yaml lines 167-170) route through this hook; `packages/emmy-provider/src/hook.test.ts` Test 4 asserts the chat_template_kwargs injection.
    - **TOOLS-03** (REQUIREMENTS.md line 58-59, line 198 in traceability table) — hash-anchored edit as pi's `customTools` override (Plan 03-01 Task 2 step 3 — `customTools: [...nativeTools, ...mcpResult.tools]` in `session.ts`, plus `buildNativeToolDefs` helper in `native-tools.ts` including `editHashline`). Evidence: `packages/emmy-ux/test/session.boot.test.ts` Test 1 asserts `registeredCustomTools.length === 8`.
    - **TOOLS-07** (REQUIREMENTS.md line 63-64, line 202 in traceability table) — MCP bridge via customTools with D-18 poison gate preserved on new path. Evidence: `buildMcpToolDefs` in `mcp-bridge.ts` emits `ToolDefinition[]` via `customTools` (Plan 03-01 Task 2 step 7); `packages/emmy-ux/test/session.mcp-poison.test.ts` regression test asserts U+202E BIDI-override rejection on the new path.

    **(2) `.planning/ROADMAP.md` — flip Phase 3 row:**
    - Change `- [ ] **Phase 3: Observability ...**` to `- [x] **Phase 3: Observability ...** — Closed YYYY-MM-DD with v3 hash <sha256:...>. See 03-CLOSEOUT.md.`
    - Update Progress Table row: `3. Observability ... | N/N | Closed | YYYY-MM-DD`
    - Fill in plan-count (7) and completion-count (7)

    **(3) `.planning/STATE.md`:**
    - Update `progress.completed_phases: 3`, `completed_plans: 24` (17 + 7), `percent: 42.8` (3/7)
    - Add "Phase 3 close summary" section following Phase 2 shape
    - Add v3 hash
    - Clear any pending resume signals for Phase 3

    **(4) `03-CLOSEOUT.md`** (~300 lines; follow Phase 2 template):
    ```markdown
    # Phase 3 CLOSEOUT — Observability + Agent-Loop Hardening + Lived-Experience

    **Closed:** YYYY-MM-DD
    **Total plans landed:** 7 (03-01..03-07)
    **Profile hash trajectory:** v2 `sha256:24be3eea...85d8b` (unchanged, Phase 2 frozen) → v3 `sha256:<new>` (this close)
    **REQ-IDs closed:** 8 new + 5 Phase-2 Done† promoted = 13 total flipped → 28 cumulative project total

    ## SC verdicts (from ROADMAP.md Phase 3 success criteria)

    - **SC-1 Observability traces:** pass — Plan 03-02 Task 4; every span carries emmy.profile.{id,version,hash}; Langfuse shows 1 trace per turn
    - **SC-2 Compaction preserves active task:** pass — Plan 03-03 SC-2 fixture 5/5 invariants; 200-turn synthetic crosses 0.75 threshold; goal + recent-5 + error-verbatim + @file pins all preserved
    - **SC-3 Alt+Up/Down rating:** pass — Plan 03-05 Task 3; 3-turn flow; 13-field schema; idempotent upsert; --export-hf HF-loadable
    - **SC-4 TUI footer:** pass — Plan 03-04 Task 3; ≤5% delta across 3 parity snapshots; 1Hz cadence; spec-accept shows `-`
    - **SC-5 Offline badge:** pass — Plan 03-06 Task 3; boot-green default; red-flip via web_fetch; warn-and-continue

    ## Commit ledger

    (Plan 03-01: wire-through + walkthrough fix SHAs…)
    (Plan 03-02: compose + telemetry body SHAs…)
    (Plan 03-03: @emmy/context + SC-2 SHAs…)
    (Plan 03-04: footer SHAs…)
    (Plan 03-05: feedback + export-hf SHAs…)
    (Plan 03-06: offline-audit SHAs…)
    (Plan 03-07: v3 profile + air-gap + this close)

    ## REQ-ID traceability (13 flipped this phase)

    ### Phase-3 REQ-IDs (8 new Done)
    (table of ID + commit + evidence file + SUMMARY.md link)

    ### Phase-2 Done† → Done (5 promoted)
    (table of ID + Plan 03-01 commit + rationale for promotion)

    ## Carry-forward (if any)

    (list any items that surfaced during walkthrough but cannot close in Phase 3; parallel to Phase 2's 5 wire-throughs — hopefully empty or minimal for Phase 3)

    ## Daily-driver bar status

    **HELD** (reached end of Phase 2 2026-04-21; Phase 3 added observability without regressing quality).

    ## Next phase

    Phase 4 — Gemma 4 Profile + Profile System Maturity. Depends on Phase 2 (stable) + Phase 3 helpful but not blocking. Phase 1 deferrals (SC-1 throughput sweep, SC-5 clock floors, SC-4 CI registration) remain operator-gated.
    ```

    **(5) Final commit (bundle all CLOSEOUT changes):**
    ```
    docs(03): Phase 3 close — 8 REQ-IDs flipped + 5 Phase-2 Done† promoted + v3 hash certified

    Plans 03-01..03-07 all landed. Phase 3 success criteria 5/5 pass. Daily-driver
    bar held. v3 profile hash: sha256:<new>. See 03-CLOSEOUT.md for full details.
    ```
  </action>
  <verify>
    <automated>cd /data/projects/emmy && gsd-sdk query frontmatter.validate .planning/STATE.md --schema state | tail -3 && grep -c '- \[x\] \*\*HARNESS-05\|- \[x\] \*\*TELEM-01\|- \[x\] \*\*UX-03' .planning/REQUIREMENTS.md && grep 'Phase 3.*Closed' .planning/ROADMAP.md && test -f .planning/phases/03-observability-agent-loop-hardening-lived-experience/03-CLOSEOUT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` has `- [x]` for all 8 Phase-3 REQ-IDs (HARNESS-05, HARNESS-09, CONTEXT-02, TELEM-01, TELEM-02, TELEM-03, UX-02, UX-03)
    - `.planning/REQUIREMENTS.md` has 5 Phase-2 Done† items flipped to Done (no remaining `Done †` for the 5 carry-forward items)
    - `.planning/ROADMAP.md` Phase 3 row shows `- [x]` and `Closed YYYY-MM-DD` with v3 hash reference
    - `.planning/ROADMAP.md` Progress Table Phase 3 row: `N/N | Closed | YYYY-MM-DD`
    - `.planning/STATE.md` has `completed_phases: 3` + `completed_plans: 24` (17 + 7)
    - `03-CLOSEOUT.md` exists and contains SC-1..SC-5 verdicts, commit ledger, REQ-ID traceability, carry-forward (possibly empty), daily-driver bar status
    - Final commit SHA exists with subject `docs(03): Phase 3 close`
    - `bun test` total pass count ≥ 235 (aggregated from all 7 plans) / 0 fail
    - `uv run pytest tests/unit -q` ≥ 140 pass / 1 skip (Phase 1 + schema patch)
    - `uv run emmy profile validate profiles/qwen3.6-35b-a3b/v{1,2,3}/` all three exit 0
  </acceptance_criteria>
  <done>Phase 3 CLOSED. All docs flipped. v3 hash certified. 28 total REQ-IDs closed. Ready for Phase 4 (or Phase 1 deferrals as operator time allows).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profiles/v3 → uv run emmy profile validate | Schema-constrained; invalid v3 rejected at validate time |
| v2 immutability contract | Phase 1 D-02 rule — any v2 edit creates v3; honored here |
| air-gap CI → Docker daemon | CI asserts zero-outbound from both emmy-serve + Langfuse stacks |
| REQUIREMENTS.md traceability edit | Commit is trusted author; GSD doc-verifier sanity-checks evidence citations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | v2→v3 migration ambiguity — existing sessions auto-upgrade to v3 config? | mitigate | Explicit: user controls which profile version via EMMY_PROFILE env or --profile flag. v2 keeps working byte-identically; v3 is opt-in. No silent upgrade |
| T-03-07-02 | Information Disclosure | 03-CLOSEOUT.md cites runs/ paths containing session transcripts | accept | runs/ is user-scope; CLOSEOUT references paths, not contents. Phase 7 publication handles redaction |
| T-03-07-03 | Repudiation | v3 hash computed ad-hoc → not reproducible | mitigate | `uv run emmy profile hash --write` (Phase 1 D-02) uses canonical YAML serialization + sha256; reproducible byte-for-byte from v3 contents |
| T-03-07-04 | Spoofing | Air-gap CI missing → claim "zero outbound" unverified | mitigate | Local validator (ci_verify_phase3.py) runnable today; GitHub Actions CI deferral tracked per Phase 1 Plan 01-08 Task 3 pattern; CLOSEOUT documents whichever path ran |
| T-03-07-05 | Denial of Service | Broken v3 profile crashes all new sessions | mitigate | Phase 1 profile-validate preflight gate; v2 remains available as fallback; user selects `--profile qwen3.6-35b-a3b@v2` to revert |
| T-03-07-06 | Tampering | Schema patch (CompactionConfig) breaks Phase 1 backward compat | mitigate | Optional[CompactionConfig] = None; v2/harness.yaml (no compaction block) continues to validate; explicit test in test_schema.py |
</threat_model>

<verification>
- v3 profile hash computed + recorded + different from v2
- v2 hash BYTE-IDENTICAL to Phase 2 close (`sha256:24be3eea...85d8b`) — immutability held
- Phase 1 schema patch backward-compat (v2 validates without compaction block)
- Air-gap CI script exists + local validator runnable
- SC-1-class walkthrough green on v3 with all 5 Phase-3 surfaces live
- REQUIREMENTS.md: 8 new [x] + 5 Done† promoted
- ROADMAP.md + STATE.md updated with close date and hash
- 03-CLOSEOUT.md follows Phase 2 CLOSEOUT shape; contains SC verdicts + commit ledger + REQ-ID table + carry-forward (if any) + daily-driver bar status
- Phase total baseline: ≥ 235 bun tests + ≥ 140 pytest + 3/3 profile validates
</verification>

<success_criteria>
- v2 and v3 profiles both validate; hashes distinct; v2 byte-identical to Phase 2 close
- SC-1 Phase-3 walkthrough green on v3 with Langfuse traces + compaction-ready + footer + rating + OFFLINE OK badge all live
- Air-gap CI validator runs (locally at minimum) with zero-outbound assertion
- 8 Phase-3 REQ-IDs + 5 Phase-2 Done† → Done (13 flipped)
- ROADMAP Phase 3 row flipped; STATE updated; CLOSEOUT written
- Final commit subject `docs(03): Phase 3 close`
</success_criteria>

<output>
03-CLOSEOUT.md is the primary artifact for this plan (not a SUMMARY.md — following Phase 2 CLOSEOUT precedent). Final commit message cites the close + v3 hash + REQ-ID count. STATE.md becomes the forward-looking record.
</output>
