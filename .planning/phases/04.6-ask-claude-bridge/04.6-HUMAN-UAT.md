---
status: partial
phase: 04.6-ask-claude-bridge
source: [04.6-VERIFICATION.md]
started: 2026-04-30T18:35:00Z
updated: 2026-04-30T18:35:00Z
---

## Current Test

[awaiting human testing — 6 operator-gated walkthroughs pending; precedent-deferred per Phase 1/04/04.2 closeout pattern]

## Tests

### 1. SC-1 — pi-emmy slash command live walkthrough
expected: Operator runs `/ask-claude '<question>'` from a Spark-side `pi-emmy` session; receives coherent answer in TUI within ~30s; `emmy.ask_claude.response` event captured in JSONL with `prompt_sha256` + `response_sha256` (NO raw content). Closure signal: `ask-claude pi-emmy green`.
result: [pending]

### 2. SC-2 — emmy (Mac client) slash command live walkthrough
expected: Operator runs `/ask-claude` from Mac-side `emmy` session over Tailscale; identical UX to `pi-emmy` (D-14); JSONL event captured on Spark side (single chokepoint per D-01). Closure signal: `ask-claude emmy green`.
result: [pending]

### 3. SC-3 — Tool form discipline check (30-min real coding session)
expected: Operator flips `tools.ask_claude.enabled: true` in `profiles/gemma-4-26b-a4b-it/v2.1/harness.yaml` + recomputes hash; runs 30-minute real coding session; model uses `ask_claude` ≤ 5 times AND only on hard reasoning problems (not routine lookups). Closure signal: `ask-claude discipline green`. (Phase 5 follow-up per D-15 LOCKED — eval-calibration deferred.)
result: [pending]

### 4. SC-4 — Scrubber blocking test (operator manual)
expected: Operator types `/ask-claude 'my OPENAI_API_KEY=sk-fake1234...'`; TUI shows error: `prompt blocked by scrubber (sk_prefixed_key)`; JSONL event captures `pattern_class` with NO response/prompt content. Closure signal: `ask-claude scrubber green`.
result: [pending]
blocker: CR-01 must be remediated before this test — currently scrubber writes the literal matched_excerpt into JSONL + HTTP body, so the live test would itself create a privacy-violating audit entry.

### 5. SC-5 — Rate-limit test (32 calls in an hour)
expected: Operator scripts 32 sequential calls (with 4s sleep to bypass min-gap); calls 1-30 return 200; call 31+ returns 429 with `reason: 'rate_limited_hourly'`. JSONL captures 30 request/response pairs and 2 `rate_limited` events. Closure signal: `ask-claude ratelimit green`.
result: [pending]

### 6. Privacy attestation (D-10) signed by operator
expected: Operator visits claude.ai → Settings → Privacy → 'Help improve Claude' toggle confirmed OFF; checkbox in `04.6-07-OPERATOR-PROTOCOLS.md` signed with date + initials before flipping `EMMY_ASK_CLAUDE=on` for the first time.
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
