---
phase: 04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-
plan: 03
type: execute
wave: 2
depends_on: [04.1-01, 04.1-02]
files_modified:
  - profiles/routes.yaml
  - eval/MATRIX.md
  - docs/runbook.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "profiles/routes.yaml optionally exposes a 'dense' role token mapping to qwen3.6-27b (commented as opt-in, does not affect default)"
    - "eval/MATRIX.md exists and enumerates all four Phase-5 participant profiles (qwen3.6-35b-a3b, qwen3.6-27b, gemma-4-26b-a4b-it, gemma-4-31b-it)"
    - "docs/runbook.md § Swapping profiles is updated with the two new profile names + HF weight paths + container-per-family note"
    - "Daily-driver default UNCHANGED — profiles/routes.yaml:default still resolves to qwen3.6-35b-a3b@v3.1-default"
  artifacts:
    - path: profiles/routes.yaml
      provides: "Within-model role routing with optional dense role token"
      contains: "dense:"
    - path: eval/MATRIX.md
      provides: "Phase 5 eval matrix enumerating all 4 participant profiles with container, param count, quant, expected tok/s, served_model_name columns"
      contains: "qwen3.6-27b"
    - path: docs/runbook.md
      provides: "Operator reference for /profile swap including the two new dense profile names"
      contains: "gemma-4-31b-it"
  key_links:
    - from: "eval/MATRIX.md"
      to: "profiles/qwen3.6-27b/v1, profiles/gemma-4-31b-it/v1"
      via: "by-name enumeration for Phase 5 eval harness consumption"
      pattern: "qwen3.6-27b|gemma-4-31b-it"
    - from: "profiles/routes.yaml"
      to: "profiles/qwen3.6-27b/v1 (via family DEFAULT_VARIANT resolver)"
      via: "optional 'dense' role role→profile_id mapping"
      pattern: "dense:.*qwen3.6-27b"
---

<objective>
Wire the two new dense profiles into routing + eval-matrix docs so Phase 5 can consume them as first-class participants. This plan is docs + config only (minutes, not hours) — no container runs, no profile hashing, no thermal.

Purpose: make the dense variants discoverable to the Phase 5 eval harness and to operators via the runbook, without changing any running behavior.
Output: updated routes.yaml, new eval/MATRIX.md, updated runbook.md § Swapping profiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-/04.1-CONTEXT.md
@profiles/routes.yaml
@docs/runbook.md
@profiles/qwen3.6-27b/v1/profile.yaml
@profiles/qwen3.6-27b/v1/serving.yaml
@profiles/gemma-4-31b-it/v1/profile.yaml
@profiles/gemma-4-31b-it/v1/serving.yaml

<interfaces>
Current routes.yaml structure (preserve everything; add a commented `dense:` role under `roles:`):
```yaml
default: qwen3.6-35b-a3b@v3.1-default   # DO NOT CHANGE — daily-driver default preserved per operator directive
roles:
  plan:   qwen3.6-35b-a3b@v3.1-reason
  edit:   qwen3.6-35b-a3b@v3.1-precise
  critic: qwen3.6-35b-a3b@v3.1-default
  # ADD: dense role (opt-in)
```

Four Phase 5 eval participants:
| Profile ID | Bundle path | Container | Params | Quant | Expected tok/s | served_model_name |
|---|---|---|---|---|---|---|
| qwen3.6-35b-a3b | profiles/qwen3.6-35b-a3b/v3.1 | emmy-serve/vllm:26.03.post1-fst (NGC-derived) | 35B MoE (3B active) | FP8 (publisher) | ~75 | qwen3.6-35b-a3b |
| qwen3.6-27b | profiles/qwen3.6-27b/v1 | emmy-serve/vllm:26.03.post1-fst (NGC-derived) | 27B dense | FP8 (publisher) | ~40-55 | qwen3.6-27b |
| gemma-4-26b-a4b-it | profiles/gemma-4-26b-a4b-it/v2 | vllm/vllm-openai:gemma4-0409-arm64-cu130 (upstream) | 26B MoE (4B active) | FP8 (runtime) | ~35-52 | gemma-4-26b-a4b-it |
| gemma-4-31b-it | profiles/gemma-4-31b-it/v1 | vllm/vllm-openai:gemma4-0409-arm64-cu130 (upstream) | 30.7B dense | FP8 (runtime, BF16 source) | ~6-10 (bandwidth-bound; NOT a gate) | gemma-4-31b-it |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional 'dense' role to profiles/routes.yaml</name>
  <files>profiles/routes.yaml</files>
  <read_first>
    - profiles/routes.yaml (entire file)
    - profiles/qwen3.6-27b/DEFAULT_VARIANT (should exist with content `v1` after Plan 04.1-1)
    - profiles/gemma-4-31b-it/DEFAULT_VARIANT (should exist with content `v1` after Plan 04.1-2)
  </read_first>
  <action>
    Edit `profiles/routes.yaml` to add an optional `dense:` role under the existing `roles:` map. DO NOT change the `default:` line — daily-driver default must stay on `qwen3.6-35b-a3b@v3.1-default` per operator directive.

    Append to the `roles:` block (after the `critic:` line):

    ```yaml
      # Phase 4.1 dense sibling opt-in. Resolves via family DEFAULT_VARIANT to profiles/qwen3.6-27b/v1.
      # Qwen 27B dense is picked over Gemma 31B dense here because it has higher expected throughput
      # (~40-55 tok/s vs ~6-10). Operator can override at any turn via /profile gemma-4-31b-it.
      # Role selection is TOOL-NAME or MESSAGE-REGEX based (D-19 — never model-name based);
      # harness classifies turn → dense when the operator explicitly opts in.
      dense:  qwen3.6-27b
    ```

    Concrete acceptance: the exact token `dense:  qwen3.6-27b` (two spaces for column alignment with `plan`/`edit`/`critic`) appears under `roles:`.

    DO NOT edit the existing `default:` line. DO NOT touch the commented-out `# gemma-4-26b-a4b-it-default: gemma-4-26b-a4b-it@v1` block at the bottom (that's Gemma MoE routing scaffolding owned by a future phase).
  </action>
  <verify>
    <automated>grep -qE "^  dense:\s+qwen3\.6-27b" profiles/routes.yaml &amp;&amp; grep -q "^default: qwen3.6-35b-a3b@v3.1-default" profiles/routes.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/routes.yaml` contains `default: qwen3.6-35b-a3b@v3.1-default` (unchanged — `git diff` shows no modification to this line)
    - `profiles/routes.yaml` contains a `dense:` entry under `roles:` with value `qwen3.6-27b`
    - `profiles/routes.yaml` still contains the three original roles (`plan:`, `edit:`, `critic:`) unmodified
    - `profiles/routes.yaml` still contains the commented-out Gemma 4 routing scaffolding block at the bottom unmodified
  </acceptance_criteria>
  <done>routes.yaml has optional dense role token; daily-driver default preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create eval/MATRIX.md enumerating all 4 Phase-5 participant profiles</name>
  <files>eval/MATRIX.md (new)</files>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v3.1/profile.yaml (for the 35B MoE baseline row)
    - profiles/qwen3.6-35b-a3b/v3.1/serving.yaml
    - profiles/qwen3.6-27b/v1/profile.yaml (exists after Plan 04.1-1)
    - profiles/qwen3.6-27b/v1/serving.yaml
    - profiles/gemma-4-26b-a4b-it/v2/profile.yaml
    - profiles/gemma-4-26b-a4b-it/v2/serving.yaml
    - profiles/gemma-4-31b-it/v1/profile.yaml (exists after Plan 04.1-2)
    - profiles/gemma-4-31b-it/v1/serving.yaml
  </read_first>
  <action>
    Create the `eval/` directory if it does not exist: `mkdir -p eval/`

    Write `eval/MATRIX.md` with EXACTLY this structure (substitute actual `profile_hash` values read from the four profile.yaml files):

    ```markdown
    # Phase 5 — Eval Matrix

    *Created 2026-04-24 by Phase 4.1. Consumed by the Phase 5 eval harness.*

    This file enumerates the four model profiles that Phase 5 will evaluate as first-class
    participants. Each row is a pinned bundle; the eval harness MUST import the harness as
    a library (not bypass it) per CLAUDE.md § Design Principles.

    ## Participants

    | Profile ID | Bundle path | Container | Params | Quant | Expected tok/s | served_model_name | Profile hash |
    |---|---|---|---|---|---|---|---|
    | qwen3.6-35b-a3b | profiles/qwen3.6-35b-a3b/v3.1 | emmy-serve/vllm:26.03.post1-fst (NGC-derived, fastsafetensors) | 35B MoE (3B active) | FP8 (publisher) | ~75 | qwen3.6-35b-a3b | `sha256:<actual-hash-from-profile.yaml>` |
    | qwen3.6-27b | profiles/qwen3.6-27b/v1 | emmy-serve/vllm:26.03.post1-fst (NGC-derived, fastsafetensors) | 27B dense | FP8 (publisher) | ~40-55 | qwen3.6-27b | `sha256:<actual-hash>` |
    | gemma-4-26b-a4b-it | profiles/gemma-4-26b-a4b-it/v2 | vllm/vllm-openai:gemma4-0409-arm64-cu130 (upstream, plain safetensors) | 26B MoE (4B active) | FP8 (runtime quant of BF16) | ~35-52 | gemma-4-26b-a4b-it | `sha256:<actual-hash>` |
    | gemma-4-31b-it | profiles/gemma-4-31b-it/v1 | vllm/vllm-openai:gemma4-0409-arm64-cu130 (upstream, plain safetensors) | 30.7B dense | FP8 (runtime quant of BF16) | ~6-10 (bandwidth-bound; NOT a gate) | gemma-4-31b-it | `sha256:<actual-hash>` |

    ## Dense-vs-MoE A/B axis

    Phase 5 can run a same-family dense-vs-MoE comparison by pairing rows above:

    - **Qwen family:** qwen3.6-35b-a3b (MoE) vs qwen3.6-27b (dense) — roughly matched total-param budget (35B vs 27B); huge active-param delta (3B MoE active vs 27B dense).
    - **Gemma family:** gemma-4-26b-a4b-it (MoE) vs gemma-4-31b-it (dense) — matched generation (Gemma 4) and deployment stack; total-param delta is 26B vs 30.7B.

    ## Throughput policy (binding, per operator directive 2026-04-24)

    Dense variants (qwen3.6-27b, gemma-4-31b-it) are bandwidth-bound by design.
    Phase 5 MUST NOT gate on tok/s for dense profiles — slow is expected. Gates are
    thermal stability (zero preemptions, zero OOM) and coding-correctness metrics
    (terminal-bench 2.0, SWE-bench Verified, LiveCodeBench). See CLAUDE.md §
    "Design Principles" and user memory feedback_dense_model_throughput.md.

    ## Profile resolution

    - Default (daily driver, unchanged): `qwen3.6-35b-a3b@v3.1-default` (via `profiles/routes.yaml`)
    - Dense opt-in role (new): `roles.dense: qwen3.6-27b` (via `profiles/routes.yaml`)
    - Explicit swap: `/profile <profile-id>` in pi-emmy — resolves via family `DEFAULT_VARIANT` file (Phase 4 follow-up commit `61ef905` pattern)

    ## Container lifecycle note

    Qwen profiles share the NGC-derived `emmy-serve/vllm:26.03.post1-fst` image.
    Gemma profiles share the upstream `vllm/vllm-openai:gemma4-0409-arm64-cu130`
    image. Cross-family swaps (Qwen → Gemma or Gemma → Qwen) DO restart the
    container. Same-family swaps (35B MoE → 27B dense, or 26B MoE → 31B dense)
    also restart because `engine.model` path differs per bundle.

    ## Evidence links

    - Qwen 27B profile: `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` § KV bisection result + Thermal validation
    - Gemma 31B profile: `profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md` § KV bisection result + Thermal validation + Known-risk fallback branch (if fired)
    - Qwen 35B MoE baseline: `profiles/qwen3.6-35b-a3b/v3.1/PROFILE_NOTES.md`
    - Gemma 26B MoE baseline: `profiles/gemma-4-26b-a4b-it/v2/PROFILE_NOTES.md`
    ```

    To populate the `Profile hash` column, read each bundle's `profile.yaml` and extract the `hash:` field:
    ```bash
    grep -E "^  hash: sha256:" profiles/qwen3.6-35b-a3b/v3.1/profile.yaml
    grep -E "^  hash: sha256:" profiles/qwen3.6-27b/v1/profile.yaml
    grep -E "^  hash: sha256:" profiles/gemma-4-26b-a4b-it/v2/profile.yaml
    grep -E "^  hash: sha256:" profiles/gemma-4-31b-it/v1/profile.yaml
    ```

    Substitute these exact hash values into the table. If any of the new-profile hash reads are empty (Plans 04.1-1 / 04.1-2 did not complete), STOP — this plan's wave: 2 depends_on guarantee has been violated.
  </action>
  <verify>
    <automated>test -f eval/MATRIX.md &amp;&amp; grep -q "qwen3.6-35b-a3b" eval/MATRIX.md &amp;&amp; grep -q "qwen3.6-27b" eval/MATRIX.md &amp;&amp; grep -q "gemma-4-26b-a4b-it" eval/MATRIX.md &amp;&amp; grep -q "gemma-4-31b-it" eval/MATRIX.md &amp;&amp; grep -q "Throughput policy" eval/MATRIX.md &amp;&amp; grep -cE "^\| \`?sha256:|sha256:[0-9a-f]{16}" eval/MATRIX.md | awk '{ exit ($1 &lt; 4) }'</automated>
  </verify>
  <acceptance_criteria>
    - `eval/MATRIX.md` exists
    - `eval/MATRIX.md` contains all four profile IDs: `qwen3.6-35b-a3b`, `qwen3.6-27b`, `gemma-4-26b-a4b-it`, `gemma-4-31b-it`
    - `eval/MATRIX.md` contains the explicit throughput policy section stating dense is not gated on tok/s
    - `eval/MATRIX.md` contains four distinct `sha256:` profile-hash values (no placeholder `<actual-hash>` strings remaining)
    - `eval/MATRIX.md` contains a Dense-vs-MoE A/B axis section
    - `eval/MATRIX.md` contains the Container lifecycle note differentiating Qwen vs Gemma containers
  </acceptance_criteria>
  <done>Eval matrix document enumerates all four Phase 5 participants with resolved hashes; throughput policy recorded; Phase 5 can consume.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/runbook.md § Swapping profiles with the two new profile names</name>
  <files>docs/runbook.md</files>
  <read_first>
    - docs/runbook.md § "Swapping profiles (`/profile`)" (starts ~line 215)
    - profiles/qwen3.6-27b/DEFAULT_VARIANT
    - profiles/gemma-4-31b-it/DEFAULT_VARIANT
  </read_first>
  <action>
    Locate the `## Swapping profiles (\`/profile\`)` section in `docs/runbook.md` (around line 215 per existing structure). APPEND a subsection at the end of this section (before the next `##` heading) titled `### Available profiles`:

    ```markdown
    ### Available profiles (as of Phase 4.1)

    Four profiles are in-tree and Phase-5-eval-ready. All four share the atomic `/profile <id>` swap
    path; each resolves via its family `DEFAULT_VARIANT` file. Dense variants are opt-in — the
    daily-driver default remains `qwen3.6-35b-a3b@v3.1-default` (see `profiles/routes.yaml`).

    | Profile ID | Type | Weight path | Container |
    |---|---|---|---|
    | `qwen3.6-35b-a3b` (default) | MoE, 35B / 3B active | `/models/Qwen3.6-35B-A3B-FP8` | `emmy-serve/vllm:26.03.post1-fst` (NGC-derived, fastsafetensors) |
    | `qwen3.6-27b` | Dense, 27B | `/models/Qwen3.6-27B-FP8` | `emmy-serve/vllm:26.03.post1-fst` (NGC-derived, fastsafetensors) |
    | `gemma-4-26b-a4b-it` | MoE, 26B / 4B active | `/models/gemma-4-26B-A4B-it` | `vllm/vllm-openai:gemma4-0409-arm64-cu130` (upstream, plain safetensors) |
    | `gemma-4-31b-it` | Dense, 30.7B | `/models/gemma-4-31B-it` | `vllm/vllm-openai:gemma4-0409-arm64-cu130` (upstream, plain safetensors) |

    **Container-per-family note:** swapping within a family (Qwen → Qwen or Gemma → Gemma)
    restarts the vLLM engine because `engine.model` path differs per bundle, but reuses the
    same local container image. Swapping *across* families (Qwen ↔ Gemma) restarts the engine
    AND loads a different container image — expect a longer swap (plain safetensors Gemma
    cold-boot is ~8 min vs fastsafetensors Qwen cold-boot ~3 min).

    **Dense-variant throughput:** Qwen 27B dense lands around 40-55 tok/s (bandwidth-bound
    vs MoE ~75); Gemma 4 31B dense lands around 6-10 tok/s. This is expected — dense models
    on UMA hardware are bandwidth-bound. Use dense variants when Phase 5 eval explicitly
    asks for a dense-vs-MoE A/B, or when comparing to external baselines measured on dense
    architectures.

    **First-time setup per family:** make sure the weight dir and container image are present
    on this host. For Qwen profiles: `huggingface-cli download Qwen/Qwen3.6-{35B-A3B-FP8|27B-FP8}
    --local-dir /models/Qwen3.6-{...}` and `docker inspect emmy-serve/vllm:26.03.post1-fst`.
    For Gemma profiles: `huggingface-cli download google/gemma-4-{26B-A4B-it|31B-it} --local-dir
    /models/gemma-4-{...}` and `docker pull vllm/vllm-openai:gemma4-0409-arm64-cu130`.
    ```

    DO NOT rewrite the existing `## Swapping profiles (\`/profile\`)` preamble, the status-row state table, the exit-code table, or the error-messages-and-resolutions sub-subsections. ONLY append the new `### Available profiles` subsection.

    If `docs/runbook.md` does not exist on this checkout (it does per file listing at plan time, but as a defensive fallback), create it with just the `## Swapping profiles` section header + the appended content. If it exists but lacks a `## Swapping profiles` section, append the new content as a top-level `## Profiles` section instead of under swap.
  </action>
  <verify>
    <automated>test -f docs/runbook.md &amp;&amp; grep -q "Available profiles" docs/runbook.md &amp;&amp; grep -q "qwen3.6-27b" docs/runbook.md &amp;&amp; grep -q "gemma-4-31b-it" docs/runbook.md &amp;&amp; grep -q "Container-per-family note" docs/runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbook.md` contains `### Available profiles` (or `## Profiles` fallback)
    - `docs/runbook.md` contains `qwen3.6-27b` AND `/models/Qwen3.6-27B-FP8`
    - `docs/runbook.md` contains `gemma-4-31b-it` AND `/models/gemma-4-31B-it`
    - `docs/runbook.md` contains the Container-per-family note
    - `docs/runbook.md` `## Swapping profiles` preamble (lines about 4-state TUI status row, exit codes 0/5/6) is UNCHANGED
  </acceptance_criteria>
  <done>Runbook documents the two new dense profiles under swapping; operator has the exact weight paths + container per family.</done>
</task>

</tasks>

<verification_criteria>
1. `profiles/routes.yaml` has a `dense:` role entry under `roles:` mapping to `qwen3.6-27b`
2. `profiles/routes.yaml` `default:` line is byte-identical to the pre-plan value (`qwen3.6-35b-a3b@v3.1-default`) — daily-driver default preserved
3. `eval/MATRIX.md` exists and enumerates all four participant profiles with resolved sha256 hashes
4. `eval/MATRIX.md` contains the binding throughput policy that dense variants are NOT gated on tok/s
5. `docs/runbook.md § Swapping profiles` has a new `### Available profiles` subsection listing all four profile IDs + weight paths + container-per-family note
6. `docs/runbook.md` existing swap-workflow content (status row states, exit codes, error tables) is unchanged
7. No profile bundles under `profiles/qwen3.6-35b-a3b/`, `profiles/qwen3.6-27b/`, `profiles/gemma-4-26b-a4b-it/`, `profiles/gemma-4-31b-it/` were modified by this plan (docs + routes only)
</verification_criteria>

<success_criteria>
Plan complete when all 7 verification_criteria items pass. Phase 5 eval harness can now consume `eval/MATRIX.md` to enumerate participants; operators can discover the new dense profiles from `docs/runbook.md`; the optional `dense` role in `routes.yaml` gives a quick opt-in path without affecting the daily-driver default.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-/04.1-03-SUMMARY.md` capturing:
- Final routes.yaml dense role target (qwen3.6-27b)
- eval/MATRIX.md four resolved profile hashes
- docs/runbook.md new-subsection line range
- Confirmation that existing profiles and default route were not touched
</output>
