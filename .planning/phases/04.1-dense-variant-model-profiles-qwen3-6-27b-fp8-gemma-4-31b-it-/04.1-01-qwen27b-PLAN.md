---
phase: 04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - profiles/qwen3.6-27b/v1/profile.yaml
  - profiles/qwen3.6-27b/v1/serving.yaml
  - profiles/qwen3.6-27b/v1/harness.yaml
  - profiles/qwen3.6-27b/v1/PROFILE_NOTES.md
  - profiles/qwen3.6-27b/v1/prompts/system.md
  - profiles/qwen3.6-27b/v1/prompts/edit_format.md
  - profiles/qwen3.6-27b/v1/prompts/tool_descriptions.md
  - profiles/qwen3.6-27b/v1/prompts/compact.md
  - profiles/qwen3.6-27b/v1/prompts/compact.alternate.md
  - profiles/qwen3.6-27b/v1/tool_schemas/bash.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/edit.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/find.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/grep.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/ls.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/read.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/web_fetch.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/web_search.schema.json
  - profiles/qwen3.6-27b/v1/tool_schemas/write.schema.json
  - profiles/qwen3.6-27b/v1/grammars/tool_call.lark
  - profiles/qwen3.6-27b/DEFAULT_VARIANT
  - runs/phase4.1-qwen-kv/**
  - runs/phase4.1-qwen-thermal/pass1-record-floors/**
  - runs/phase4.1-qwen-thermal/pass2-assert-floors/**
autonomous: false
requirements: []

must_haves:
  truths:
    - "profiles/qwen3.6-27b/v1/ exists and validates cleanly (emmy profile validate exits 0)"
    - "profile.yaml.hash matches output of emmy profile hash"
    - "KV bisection produced a final gpu_memory_utilization written by find_kv_budget.py (sole sanctioned writer)"
    - "2x2 h thermal replay: pass 1 records floors, pass 2 asserts floors with zero preemptions and zero OOM"
    - "profiles/qwen3.6-27b/DEFAULT_VARIANT contains v1"
    - "/profile qwen3.6-27b resolves to profiles/qwen3.6-27b/v1 end-to-end on live DGX Spark"
    - "profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT is UNCHANGED (still v3.1) — daily-driver default preserved"
    - "profile passes ci_verify_phase3 (STRICT air-gap) — no new outbound endpoints"
  artifacts:
    - path: profiles/qwen3.6-27b/v1/profile.yaml
      provides: "Bundle identity, immutable versioning, content hash"
      contains: "id: qwen3.6-27b"
    - path: profiles/qwen3.6-27b/v1/serving.yaml
      provides: "vLLM engine args + sampling defaults for Qwen3.6-27B-FP8 dense"
      contains: "served_model_name: qwen3.6-27b"
    - path: profiles/qwen3.6-27b/v1/PROFILE_NOTES.md
      provides: "Provenance for every default + KV-bisection + thermal outcome"
      contains: "profile_id: qwen3.6-27b"
    - path: profiles/qwen3.6-27b/DEFAULT_VARIANT
      provides: "Family resolver default for /profile qwen3.6-27b"
      contains: "v1"
    - path: runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json
      provides: "Thermal pass-2 outcome JSON (zero preemptions, zero OOM)"
      contains: '"preemptions_hour2": 0'
  key_links:
    - from: "profiles/qwen3.6-27b/v1/serving.yaml"
      to: "/models/Qwen3.6-27B-FP8"
      via: "engine.model path mount"
      pattern: "model: /models/Qwen3\\.6-27B-FP8"
    - from: "profiles/qwen3.6-27b/v1/serving.yaml"
      to: "emmy-serve/vllm:26.03.post1-fst container image"
      via: "engine.container_image_digest digest pin"
      pattern: "container_image_digest: sha256:"
    - from: "scripts/find_kv_budget.py"
      to: "profiles/qwen3.6-27b/v1/serving.yaml:engine.gpu_memory_utilization"
      via: "sole sanctioned writer — Pitfall #1 discipline"
      pattern: "gpu_memory_utilization:"
---

<objective>
Land `profiles/qwen3.6-27b/v1/` as a full profile bundle for `Qwen/Qwen3.6-27B-FP8` (dense sibling of the `qwen3.6-35b-a3b` MoE). Clone-and-retarget from the current daily-driver `profiles/qwen3.6-35b-a3b/v3.1/`, then run the standard post-authoring chain: hash → validate → smoke → KV bisection → 2×2h thermal replay. Daily-driver default stays on `qwen3.6-35b-a3b@v3.1` — this profile is additive and opt-in via `/profile`.

Purpose: give Phase 5 eval a dense-vs-MoE same-family sibling in-tree with the same discipline as the existing Phase 1 / Phase 4 bundles.
Output: validated v1 bundle, DEFAULT_VARIANT marker, KV-bisection evidence in `runs/phase4.1-qwen-kv/`, 2×2h thermal evidence in `runs/phase4.1-qwen-thermal/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-/04.1-CONTEXT.md
@profiles/qwen3.6-35b-a3b/v3.1/profile.yaml
@profiles/qwen3.6-35b-a3b/v3.1/serving.yaml
@profiles/qwen3.6-35b-a3b/v3.1/PROFILE_NOTES.md
@profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT
@scripts/find_kv_budget.py
@scripts/thermal_replay.py
@scripts/smoke_test.py
@scripts/start_emmy.sh

<interfaces>
Clone source (identical files copy verbatim unless listed as RETARGETED):

profile.yaml (RETARGET):
```yaml
profile:
  id: qwen3.6-27b           # was qwen3.6-35b-a3b
  version: v1               # was v3.1
  family: qwen3.6           # unchanged
  base_model: Qwen/Qwen3.6-27B-FP8   # was Qwen/Qwen3.6-35B-A3B-FP8
  description: "Qwen3.6-27B-FP8 dense profile — sibling to qwen3.6-35b-a3b@v3.1 for Phase 5 dense-vs-MoE A/B; clone-and-retarget from v3.1 with dense weights; KV bisected + 2x2h thermal validated"
  created: '2026-04-24'
  hash: sha256:<RECOMPUTED>  # MUST be recomputed via emmy profile hash --write; NEVER reuse v3.1 hash
  hash_algorithm: sha256
  hash_manifest_version: 1
  tags: [coding, dgx-spark, fp8, qwen3.6, phase-4.1, dense]
  community_sources:
    - title: Qwen3.6-27B-FP8 HF model card
      url: https://huggingface.co/Qwen/Qwen3.6-27B-FP8
      retrieved: '2026-04-24'
    - title: vLLM Qwen3.5/3.6 recipes
      url: https://docs.vllm.ai/projects/recipes/en/latest/Qwen/Qwen3.5.html
      retrieved: '2026-04-24'
```

serving.yaml — RETARGETED fields only; all other fields byte-identical clone from v3.1:
```yaml
engine:
  model: /models/Qwen3.6-27B-FP8              # was /models/Qwen3.6-35B-A3B-FP8
  model_hf_id: Qwen/Qwen3.6-27B-FP8            # was Qwen/Qwen3.6-35B-A3B-FP8
  served_model_name: qwen3.6-27b               # was qwen3.6-35b-a3b
  container_image: emmy-serve/vllm:26.03.post1-fst   # SAME as v3.1 (NGC-derived with fastsafetensors)
  container_image_digest: sha256:<VERIFIED>    # MUST verify current local image digest; may or may not match v3.1's sha256:77321e416cf4... — re-run `docker inspect --format '{{.Id}}' emmy-serve/vllm:26.03.post1-fst` and write fresh value
  gpu_memory_utilization: 0.55                 # SEED ONLY — find_kv_budget.py overwrites this
  load_format: fastsafetensors                 # NGC image supports fastsafetensors
  quantization: fp8                            # publisher FP8 weights
  tool_call_parser: qwen3_coder                # same parser family
  # All other engine fields (max_model_len 131072, kv_cache_dtype fp8, enable_prefix_caching,
  # enable_chunked_prefill, max_num_batched_tokens 8192, enable_auto_tool_choice,
  # attention_backend flashinfer, host, port) byte-identical to v3.1.
# sampling_defaults: byte-identical to v3.1 (temperature 0.2, top_p 0.95, top_k 40, repetition_penalty 1.05, max_tokens 8192, stop [])
# speculative: null
# guided_decoding: xgrammar
# quirks: byte-identical to v3.1
# env: byte-identical to v3.1
```

harness.yaml + prompts/ + tool_schemas/ + grammars/: byte-identical clone from v3.1.

Thermal summary.json contract (from runs/phase4-thermal/pass2-assert-floors/summary.json — confirmed shape):
```json
{
  "preemptions_hour2": 0,
  "oom_events": 0,
  "profile_id": "qwen3.6-27b",
  "profile_version": "v1",
  "target_wall_time_s": 7200,
  "actual_wall_time_s": <~7200>,
  "profile_hash": "sha256:<matches profile.yaml.hash>"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull Qwen3.6-27B-FP8 weights from HuggingFace</name>
  <files>/models/Qwen3.6-27B-FP8/** (weight cache; NOT in git)</files>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v3.1/serving.yaml (for the model mount-path convention)
    - 04.1-CONTEXT.md § "Research already done" Qwen 3.6 dense bullets
  </read_first>
  <action>
    Download the Qwen3.6-27B-FP8 weights to the container-internal-compatible host path:

    ```bash
    huggingface-cli download Qwen/Qwen3.6-27B-FP8 --local-dir /models/Qwen3.6-27B-FP8 --local-dir-use-symlinks False
    ```

    HF auth is already configured on this host (per Phase 1). If the download fails with 401/403, fall back to `HF_TOKEN=$(cat ~/.cache/huggingface/token) huggingface-cli download ...`. DO NOT use `--resume-download` flag (deprecated in recent CLI); resume is automatic.

    After download, record the weights directory size and file listing in `runs/phase4.1-qwen-kv/download-logs/weight-inventory.txt` via `ls -la /models/Qwen3.6-27B-FP8/ | tee runs/phase4.1-qwen-kv/download-logs/weight-inventory.txt`. This is evidence for PROFILE_NOTES.md provenance.

    Verify the HF config.json quantization shape matches vLLM's expected FP8 (per 04.1-CONTEXT.md § Known risks — "Qwen 27B-FP8 quant format"): `grep -iE "quant|fp8" /models/Qwen3.6-27B-FP8/config.json` — expect an entry like `"quantization_config": {"quant_method": "fp8", ...}`. If the shape looks off (e.g. NVFP4, INT4, unexpected scale_format), STOP and file a PROFILE_NOTES.md note; do not proceed to profile writing.
  </action>
  <verify>
    <automated>test -f /models/Qwen3.6-27B-FP8/config.json &amp;&amp; test -f /models/Qwen3.6-27B-FP8/tokenizer.json &amp;&amp; grep -q '"quant_method"' /models/Qwen3.6-27B-FP8/config.json</automated>
  </verify>
  <acceptance_criteria>
    - `/models/Qwen3.6-27B-FP8/config.json` exists
    - `/models/Qwen3.6-27B-FP8/tokenizer.json` exists
    - At least one `.safetensors` shard exists under `/models/Qwen3.6-27B-FP8/`
    - `/models/Qwen3.6-27B-FP8/config.json` contains `"quant_method"` (FP8-compatible quant declaration)
    - `runs/phase4.1-qwen-kv/download-logs/weight-inventory.txt` exists and is non-empty
  </acceptance_criteria>
  <done>Weights present at /models/Qwen3.6-27B-FP8/, FP8 quant shape confirmed, inventory logged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify + pin container image digest (NGC fastsafetensors-derived)</name>
  <files>runs/phase4.1-qwen-kv/container-inspect.txt (new)</files>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v3.1/serving.yaml (engine.container_image and engine.container_image_digest)
    - scripts/build_emmy_image.sh (to understand emmy-serve/vllm:26.03.post1-fst derivation)
  </read_first>
  <action>
    The Qwen dense profile reuses the SAME derived NGC image (`emmy-serve/vllm:26.03.post1-fst` = NGC `nvcr.io/nvidia/vllm:26.03.post1-py3` + fastsafetensors layer) as qwen3.6-35b-a3b@v3.1. Verify the image is still present with the same digest:

    ```bash
    docker inspect --format '{{.Id}}' emmy-serve/vllm:26.03.post1-fst | tee runs/phase4.1-qwen-kv/container-inspect.txt
    ```

    Expected: output matches v3.1's `container_image_digest` value `sha256:77321e416cf49702ed6f04af9e5d39945726fea48970bb013617fddc659f9486`.

    If the output does NOT match (image rebuilt since v3.1 landed), this is fine — capture the new digest; the new profile will pin whatever digest is currently on disk. Write the captured digest to `runs/phase4.1-qwen-kv/container-inspect.txt` for later reference when writing serving.yaml.

    If the image is missing entirely (`docker inspect` fails), rebuild via `bash scripts/build_emmy_image.sh` (same as Phase 1), then re-capture. This is expected on a fresh machine.
  </action>
  <verify>
    <automated>test -s runs/phase4.1-qwen-kv/container-inspect.txt &amp;&amp; grep -q "^sha256:" runs/phase4.1-qwen-kv/container-inspect.txt</automated>
  </verify>
  <acceptance_criteria>
    - `runs/phase4.1-qwen-kv/container-inspect.txt` exists and starts with `sha256:`
    - `docker images emmy-serve/vllm:26.03.post1-fst --format '{{.ID}}'` returns a non-empty result
  </acceptance_criteria>
  <done>Container image present locally, digest captured for use in serving.yaml engine.container_image_digest.</done>
</task>

<task type="auto">
  <name>Task 3: Write profiles/qwen3.6-27b/v1/ bundle (clone-and-retarget from v3.1)</name>
  <files>
    profiles/qwen3.6-27b/v1/profile.yaml,
    profiles/qwen3.6-27b/v1/serving.yaml,
    profiles/qwen3.6-27b/v1/harness.yaml,
    profiles/qwen3.6-27b/v1/PROFILE_NOTES.md,
    profiles/qwen3.6-27b/v1/prompts/system.md,
    profiles/qwen3.6-27b/v1/prompts/edit_format.md,
    profiles/qwen3.6-27b/v1/prompts/tool_descriptions.md,
    profiles/qwen3.6-27b/v1/prompts/compact.md,
    profiles/qwen3.6-27b/v1/prompts/compact.alternate.md,
    profiles/qwen3.6-27b/v1/tool_schemas/*.schema.json (9 files),
    profiles/qwen3.6-27b/v1/grammars/tool_call.lark,
    profiles/qwen3.6-27b/DEFAULT_VARIANT
  </files>
  <read_first>
    - profiles/qwen3.6-35b-a3b/v3.1/profile.yaml (entire file)
    - profiles/qwen3.6-35b-a3b/v3.1/serving.yaml (entire file)
    - profiles/qwen3.6-35b-a3b/v3.1/harness.yaml (entire file)
    - profiles/qwen3.6-35b-a3b/v3.1/PROFILE_NOTES.md (entire file)
    - profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT
    - runs/phase4.1-qwen-kv/container-inspect.txt (from Task 2 — for digest)
  </read_first>
  <action>
    Perform a clone-and-retarget.

    Step 1 — copy the entire v3.1 bundle as the starting template:
    ```bash
    mkdir -p profiles/qwen3.6-27b
    cp -R profiles/qwen3.6-35b-a3b/v3.1/ profiles/qwen3.6-27b/v1/
    ```

    Step 2 — edit `profiles/qwen3.6-27b/v1/profile.yaml` with these EXACT field values (all other lines byte-identical to v3.1):
    - `id: qwen3.6-27b`
    - `version: v1`
    - `base_model: Qwen/Qwen3.6-27B-FP8`
    - `description: "Qwen3.6-27B-FP8 dense profile — sibling to qwen3.6-35b-a3b@v3.1 for Phase 5 dense-vs-MoE A/B; clone-and-retarget from v3.1 with dense weights; KV bisected + 2x2h thermal validated"`
    - `created: '2026-04-24'`
    - DELETE the existing `hash:` line's value (leave the key with placeholder `sha256:PENDING` — Task 4 rewrites it via `emmy profile hash --write`)
    - Replace `tags:` with `[coding, dgx-spark, fp8, qwen3.6, phase-4.1, dense]`
    - Replace `community_sources:` with a list containing: (a) `Qwen3.6-27B-FP8 HF model card` at `https://huggingface.co/Qwen/Qwen3.6-27B-FP8` retrieved `2026-04-24`, (b) `vLLM Qwen3.5/3.6 recipes` at `https://docs.vllm.ai/projects/recipes/en/latest/Qwen/Qwen3.5.html` retrieved `2026-04-24`

    Step 3 — edit `profiles/qwen3.6-27b/v1/serving.yaml`. Change ONLY these fields; leave every other line byte-identical to v3.1:
    - `engine.model: /models/Qwen3.6-27B-FP8` (was `/models/Qwen3.6-35B-A3B-FP8`)
    - `engine.model_hf_id: Qwen/Qwen3.6-27B-FP8` (was `Qwen/Qwen3.6-35B-A3B-FP8`)
    - `engine.served_model_name: qwen3.6-27b` (was `qwen3.6-35b-a3b`)
    - `engine.container_image_digest: sha256:<value from runs/phase4.1-qwen-kv/container-inspect.txt>` — substitute the actual digest captured in Task 2
    - `engine.gpu_memory_utilization: 0.55` — THIS IS A SEED VALUE ONLY. `scripts/find_kv_budget.py` is the ONLY sanctioned writer to this field (per CLAUDE.md Pitfall #1 + 04.1-CONTEXT.md § Constraints). Task 7 will overwrite this value. Do NOT hand-edit after Task 4.
    - Update the header comment block to reference Phase 4.1 provenance (replace `# Phase 3.1 operational polish (2026-04-22) — D-29 RAM headroom tuning.` and the v3-delta lines with: `# Phase 4.1 (2026-04-24) — Qwen3.6-27B-FP8 dense sibling of qwen3.6-35b-a3b@v3.1. Clone-and-retarget: model path, served_model_name, model_hf_id, container digest (re-verified). gpu_memory_utilization seeded at 0.55; find_kv_budget.py will overwrite. See PROFILE_NOTES.md § "Phase 4.1 clone provenance".`)

    KEEP byte-identical from v3.1:
    - `max_model_len: 131072`
    - `kv_cache_dtype: fp8`
    - `enable_prefix_caching: true`, `enable_chunked_prefill: true`
    - `max_num_batched_tokens: 8192`
    - `load_format: fastsafetensors` (NGC image supports it)
    - `quantization: fp8`
    - `tool_call_parser: qwen3_coder`, `enable_auto_tool_choice: true`
    - `attention_backend: flashinfer`
    - `host: 0.0.0.0`, `port: 8000`
    - Entire `sampling_defaults:` block (temperature 0.2, top_p 0.95, top_k 40, repetition_penalty 1.05, max_tokens 8192, stop [])
    - `speculative: null`
    - Entire `guided_decoding:`, `quirks:`, `env:` blocks

    Step 4 — leave `harness.yaml`, `prompts/*`, `tool_schemas/*`, `grammars/tool_call.lark` BYTE-IDENTICAL to v3.1 (no dense-specific changes expected; Qwen 3.6 dense shares tokenizer + chat template + tool format with the MoE per 04.1-CONTEXT.md research).

    Step 5 — rewrite `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` preserving the provenance-table structure from v3.1, but updating:
    - Header: `# Qwen3.6-27B-FP8 — v1 Profile Notes`
    - Add a top paragraph: `Phase 4.1 dense sibling profile. Qwen3.6-27B-FP8 (Qwen dense, 27B params) served in the emmy-serve NGC fastsafetensors image on DGX Spark GB10. Clone-and-retarget from qwen3.6-35b-a3b@v3.1; diffs limited to model path, served_model_name, container digest (re-verified), and the post-bisection gpu_memory_utilization.`
    - Add section `## Phase 4.1 clone provenance` listing the exact field retargets from Step 3
    - Add section `## KV bisection result` with placeholder `<to be populated by scripts/find_kv_budget.py in Task 7>`
    - Add section `## Thermal validation` with placeholder `<to be populated after Tasks 8 and 9>`
    - Add section `## Throughput observation (informational, NOT a gate)` with placeholder and a verbatim line: `Per operator directive 2026-04-24 (feedback_dense_model_throughput.md), dense variants are bandwidth-bound by design. Observed tok/s is recorded here for Phase 5 eval context but is NOT an acceptance gate for this profile.`
    - In `measured_values:`, set `gpu_memory_utilization: <will-be-written-by-find_kv_budget>` (placeholder string); find_kv_budget.py overwrites

    Step 6 — write `profiles/qwen3.6-27b/DEFAULT_VARIANT` as a new file with single line content `v1` and NO trailing newline-content (match the existing family convention: `cat profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT` = exactly `v3.1`). Use:
    ```bash
    printf 'v1' > profiles/qwen3.6-27b/DEFAULT_VARIANT
    ```

    Step 7 — DO NOT touch `profiles/qwen3.6-35b-a3b/` or `profiles/gemma-4-26b-a4b-it/` in any way. All writes in this task are to NEW paths under `profiles/qwen3.6-27b/`.
  </action>
  <verify>
    <automated>test -f profiles/qwen3.6-27b/v1/profile.yaml &amp;&amp; test -f profiles/qwen3.6-27b/v1/serving.yaml &amp;&amp; test -f profiles/qwen3.6-27b/v1/harness.yaml &amp;&amp; test -f profiles/qwen3.6-27b/v1/PROFILE_NOTES.md &amp;&amp; test -f profiles/qwen3.6-27b/DEFAULT_VARIANT &amp;&amp; grep -q "^id: qwen3.6-27b$" profiles/qwen3.6-27b/v1/profile.yaml &amp;&amp; grep -q "served_model_name: qwen3.6-27b" profiles/qwen3.6-27b/v1/serving.yaml &amp;&amp; [ "$(cat profiles/qwen3.6-27b/DEFAULT_VARIANT)" = "v1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/qwen3.6-27b/v1/profile.yaml` contains `id: qwen3.6-27b`
    - `profiles/qwen3.6-27b/v1/profile.yaml` contains `version: v1`
    - `profiles/qwen3.6-27b/v1/profile.yaml` contains `base_model: Qwen/Qwen3.6-27B-FP8`
    - `profiles/qwen3.6-27b/v1/serving.yaml` contains `model: /models/Qwen3.6-27B-FP8`
    - `profiles/qwen3.6-27b/v1/serving.yaml` contains `served_model_name: qwen3.6-27b`
    - `profiles/qwen3.6-27b/v1/serving.yaml` contains `gpu_memory_utilization: 0.55`
    - `profiles/qwen3.6-27b/v1/serving.yaml` contains `load_format: fastsafetensors`
    - `profiles/qwen3.6-27b/v1/serving.yaml` contains `tool_call_parser: qwen3_coder`
    - `profiles/qwen3.6-27b/v1/tool_schemas/` contains 9 `.schema.json` files
    - `profiles/qwen3.6-27b/v1/grammars/tool_call.lark` exists
    - `profiles/qwen3.6-27b/DEFAULT_VARIANT` contains exactly `v1` (no newline)
    - `profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT` still contains `v3.1` (UNCHANGED — `git diff profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT` is empty)
  </acceptance_criteria>
  <done>Bundle files exist with retargeted identity; unchanged fields byte-identical to v3.1; hash placeholder in profile.yaml; DEFAULT_VARIANT set to v1; existing profiles untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Compute content hash and validate profile bundle</name>
  <files>profiles/qwen3.6-27b/v1/profile.yaml (hash field rewritten)</files>
  <read_first>
    - profiles/qwen3.6-27b/v1/profile.yaml (current state after Task 3)
    - scripts/hash_profile.py (hash canonicalization logic)
    - scripts/validate_profile.py (validation surface)
  </read_first>
  <action>
    Compute the content hash and write it into `profile.yaml`:

    ```bash
    uv run emmy profile hash --write profiles/qwen3.6-27b/v1
    ```

    Then validate the bundle against the schema:

    ```bash
    uv run emmy profile validate profiles/qwen3.6-27b/v1
    ```

    Expected: `validate` exits 0 and prints a success message (reference v3.1 output shape). If validate fails, DO NOT proceed to smoke-test; fix the schema violation first.

    The hash in `profile.yaml` MUST NOT equal the v3.1 hash (`sha256:f9dcabd1dbee8f29b7ee8439140da83d84e9784dab08a1304474e1d06901fc73`) — the base_model, id, served_model_name, and container_image_digest all differ, so the canonicalized bytes are different. If the hash accidentally matches, something is wrong (likely Task 3 didn't actually retarget the fields).
  </action>
  <verify>
    <automated>uv run emmy profile validate profiles/qwen3.6-27b/v1 &amp;&amp; ! grep -q "f9dcabd1dbee8f29b7ee8439140da83d84e9784dab08a1304474e1d06901fc73" profiles/qwen3.6-27b/v1/profile.yaml &amp;&amp; grep -qE "^  hash: sha256:[0-9a-f]{64}$" profiles/qwen3.6-27b/v1/profile.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run emmy profile validate profiles/qwen3.6-27b/v1` exits 0
    - `profiles/qwen3.6-27b/v1/profile.yaml` contains a `hash: sha256:<64-hex>` line
    - Hash value differs from v3.1's `sha256:f9dcabd1dbee8f29b7ee8439140da83d84e9784dab08a1304474e1d06901fc73`
  </acceptance_criteria>
  <done>Bundle hash computed, written, schema-validated; ready for smoke test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Smoke test — boot emmy-serve against the new Qwen dense profile</name>
  <what-built>
    Profile bundle `profiles/qwen3.6-27b/v1/` is authored, hashed, and schema-validated. Now verify it boots end-to-end on the live DGX Spark and responds to a probe.
  </what-built>
  <how-to-verify>
    Operator runs from the emmy repo root:

    ```bash
    scripts/start_emmy.sh --profile profiles/qwen3.6-27b/v1 2>&amp;1 | tee runs/phase4.1-qwen-kv/smoke.log
    ```

    Expected outcomes (in order):
    1. Script loads profile + renders docker argv from serving.yaml
    2. Container boots (cold start expected ~3 min with fastsafetensors on 27B FP8 weights — NGC image + fastsafetensors layer)
    3. `scripts/smoke_test.py` (embedded in start_emmy.sh) probes `/v1/models` and `/v1/chat/completions` with 900s timeout
    4. Final line of smoke.log: `smoke ok: tok/s=<some number>` — the tok/s value is informational ONLY (per operator directive, dense throughput is NOT a gate; expected range 40-55 tok/s but lower is fine)

    If the boot fails (container exits, timeout, weight-load error): capture the relevant section of `docker logs emmy-serve` into `runs/phase4.1-qwen-kv/boot-failure-<iso>.log` and STOP — diagnose before re-running. Common causes: weight path mismatch (`/models/Qwen3.6-27B-FP8` vs actual), container digest drift, FP8 quant shape mismatch (Task 1 safeguard should have caught it; re-check).

    After success: tear down via `docker stop emmy-serve` (the next task's KV bisection will spawn its own container lifecycle).
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-qwen-kv/smoke.log` exists
    - `runs/phase4.1-qwen-kv/smoke.log` contains `smoke ok`
    - `docker ps -a --filter name=emmy-serve --format '{{.Status}}'` shows a completed container (either running or stopped — not "Exited (<non-zero>)")
  </acceptance_criteria>
  <resume-signal>Type "approved" after smoke.log contains `smoke ok`, or describe any boot failure for diagnosis.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: (placeholder — intentionally omitted; numbering aligns with CONTEXT.md task list)</name>
  <files>(none)</files>
  <read_first>(none)</read_first>
  <action>This task intentionally left blank. CONTEXT.md numbers tasks 1-9; executor should treat this row as a marker so downstream task numbers (7=KV, 8=thermal-pass-1, 9=thermal-pass-2) match CONTEXT.md semantically.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <acceptance_criteria>
    - No artifacts produced
  </acceptance_criteria>
  <done>No-op.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: KV budget bisection (sole sanctioned writer to gpu_memory_utilization)</name>
  <what-built>
    The seed `gpu_memory_utilization: 0.55` in serving.yaml is a placeholder. `scripts/find_kv_budget.py` is the ONLY sanctioned writer (per CLAUDE.md Pitfall #1 + 04.1-CONTEXT.md § Constraints). This task invokes it, producing a measured final value + rehashing the profile + writing a KV-result block into PROFILE_NOTES.md.
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    uv run python scripts/find_kv_budget.py \
      --profile profiles/qwen3.6-27b/v1 \
      --drive-minutes 10 \
      --max-iters 12 \
      2>&amp;1 | tee runs/phase4.1-qwen-kv/find_kv_budget-stdout.log
    ```

    Estimated wall-clock: ~3.5 h (boot ~3 min/iter × up to 12 iters + 10 min drive each). Operator attention required: watch for container preemptions in `docker logs emmy-serve` and OOM events in `dmesg`.

    Expected behavior (reference: `runs/phase4-kv/final-run/` from Phase 4):
    - Bisection converges on a gpu_memory_utilization value (typically in [0.65, 0.90] range for FP8 dense on 128 GB UMA; actual value depends on Qwen 27B weight footprint)
    - `scripts/find_kv_budget.py` writes the converged value into `profiles/qwen3.6-27b/v1/serving.yaml:engine.gpu_memory_utilization`
    - Script re-invokes `emmy profile hash --write` so the profile.yaml hash reflects the new serving.yaml bytes
    - Script appends a KV-result block to `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` containing the iteration table + final value + any preemption/OOM observations
    - Stdout ends with a clear "converged: gpu_memory_utilization=<value>" line

    If bisection fails (every tested value OOMs or preempts): reduce `max_model_len` from 131072 to 65536 in serving.yaml, recompute hash + re-validate, and re-run bisection. Document this fallback in PROFILE_NOTES.md if it fires.

    After completion: `uv run emmy profile validate profiles/qwen3.6-27b/v1` must still exit 0 (find_kv_budget rehashes; validate confirms hash-bytes alignment).
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-qwen-kv/find_kv_budget-stdout.log` exists and ends with a `converged:` line
    - `profiles/qwen3.6-27b/v1/serving.yaml` contains `gpu_memory_utilization:` with a value that is NOT `0.55` (seed has been overwritten by find_kv_budget.py)
    - `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` contains `## KV bisection result` with iteration-table content populated (not the "<to be populated>" placeholder from Task 3)
    - `uv run emmy profile validate profiles/qwen3.6-27b/v1` exits 0 (hash realigned)
  </acceptance_criteria>
  <resume-signal>Type "approved" after bisection converges and validate passes; or describe any preemption/OOM pattern observed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Thermal replay pass 1 — record floors (2 h sustained load)</name>
  <what-built>
    With KV budget locked, run a 2-hour sustained-load replay to record thermal + throughput floors. Reference: Phase 4 `runs/phase4-thermal/pass1-record-floors/`. Per operator directive, throughput values recorded here are informational; the gate is zero preemptions + zero OOM (which pass 2 will assert).
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    uv run python scripts/thermal_replay.py \
      --profile profiles/qwen3.6-27b/v1 \
      --target-wall-time-s 7200 \
      --record-floors \
      --out-dir runs/phase4.1-qwen-thermal/pass1-record-floors \
      2>&amp;1 | tee runs/phase4.1-qwen-thermal/replay-pass1-stdout.log
    ```

    Expected outcomes:
    1. Container boots under the bisected gpu_memory_utilization
    2. Script drives sustained load for ~7200s
    3. On completion, writes `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` with fields matching the Phase-4 shape: `preemptions_hour2`, `oom_events`, `decode_throughput_p1_hour2_tokps`, `decode_throughput_p50_hour2_tokps`, `gpu_clock_p5_hour2_mhz`, `gpu_clock_p50_hour2_mhz`, `gpu_temp_p95_hour2_c`, `profile_hash`, `profile_id`, `profile_version`, `actual_wall_time_s`, `target_wall_time_s`, `run_id`
    4. `--record-floors` mode PERSISTS the observed floors into `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md § Thermal validation` for pass 2 to assert against

    Required acceptance: `preemptions_hour2: 0` AND `oom_events: 0`. Tok/s values are informational — ANY positive number passes this gate per operator directive.

    If preemptions > 0 or OOM > 0: back off gpu_memory_utilization (re-run Task 7 with a narrower upper bound) and repeat. DO NOT manually edit serving.yaml.

    Operator evidence to commit: `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json`, `runs/phase4.1-qwen-thermal/pass1-record-floors/README.md` (narrative if anything notable happened), `runs/phase4.1-qwen-thermal/replay-pass1-stdout.log`.
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` exists
    - `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` contains `"preemptions_hour2": 0`
    - `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` contains `"oom_events": 0`
    - `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` contains `"profile_id": "qwen3.6-27b"`
    - `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` contains `"profile_version": "v1"`
    - `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` § Thermal validation contains the recorded floor values (no longer the placeholder)
  </acceptance_criteria>
  <resume-signal>Type "approved" after summary.json shows zero preemptions and zero OOM; otherwise describe the pressure pattern.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Thermal replay pass 2 — assert floors (2 h validation run)</name>
  <what-built>
    Second 2-hour replay against the floors recorded in Task 8. Pass condition (per operator directive): zero preemptions + zero OOM. Tok/s floors are asserted by the script's default `--assert-floors` logic but even if tok/s floors fail, manual operator review MUST classify as informational only — the gate for this plan is preemptions_hour2=0 and oom_events=0.
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    uv run python scripts/thermal_replay.py \
      --profile profiles/qwen3.6-27b/v1 \
      --target-wall-time-s 7200 \
      --assert-floors \
      --out-dir runs/phase4.1-qwen-thermal/pass2-assert-floors \
      2>&amp;1 | tee runs/phase4.1-qwen-thermal/replay-pass2-stdout.log
    ```

    Expected outcomes:
    1. Script boots a fresh container under the bisected gpu_memory_utilization
    2. Drives 2-hour sustained load with the same fixture set as pass 1
    3. Writes `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` with the same schema as pass 1
    4. Stdout includes a final status line (shape: `All floors pass` from reference Phase 4 run)

    Gate (binding):
    - `summary.json` contains `"preemptions_hour2": 0`
    - `summary.json` contains `"oom_events": 0`

    Non-gate (informational per operator directive):
    - Decode-throughput values MUST be recorded in PROFILE_NOTES.md § "Throughput observation (informational, NOT a gate)" but are NOT a pass/fail criterion for this plan
    - GPU clock floors are informational only

    If preemptions > 0 or OOM > 0: bisection was optimistic. Return to Task 7 with a lower upper bound; re-run Tasks 7-9.

    After success: update `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` § Thermal validation to reference both run_ids (pass-1 and pass-2) + a summary line "2x2h thermal replay: pass1 recorded floors, pass2 asserted with zero preemptions and zero OOM".
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` exists
    - `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` contains `"preemptions_hour2": 0`
    - `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` contains `"oom_events": 0`
    - `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` contains `"profile_id": "qwen3.6-27b"`
    - `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` contains `"profile_version": "v1"`
    - `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` contains a `profile_hash` matching `profiles/qwen3.6-27b/v1/profile.yaml:hash`
    - `runs/phase4.1-qwen-thermal/replay-pass2-stdout.log` contains `All floors pass`
    - `profiles/qwen3.6-27b/v1/PROFILE_NOTES.md` § Thermal validation references both pass-1 and pass-2 run_ids
  </acceptance_criteria>
  <resume-signal>Type "approved" after pass 2's summary.json shows zero preemptions + zero OOM AND stdout contains "All floors pass"; or describe throughput floor deltas for informational capture.</resume-signal>
</task>

<task type="auto">
  <name>Task 10: Air-gap STRICT re-verify (ci_verify_phase3)</name>
  <files>runs/phase4.1-qwen-kv/airgap-verify.log (new)</files>
  <read_first>
    - scripts/verify_airgap_ci.sh
    - scripts/airgap_probe.py
    - CLAUDE.md § "Hidden cloud dependencies" Pitfall #5
  </read_first>
  <action>
    Confirm the new profile introduces zero outbound endpoints relative to STRICT baseline:

    ```bash
    bash scripts/verify_airgap_ci.sh --profile profiles/qwen3.6-27b/v1 --mode strict 2>&amp;1 | tee runs/phase4.1-qwen-kv/airgap-verify.log
    ```

    If the script signature differs, fall back to invoking the Python probe directly:
    ```bash
    uv run python scripts/airgap_probe.py --profile profiles/qwen3.6-27b/v1 --strict 2>&amp;1 | tee runs/phase4.1-qwen-kv/airgap-verify.log
    ```

    Expected: exit 0 and log ends with a clear STRICT-pass line (match the Phase 3/4 precedent).
  </action>
  <verify>
    <automated>test -s runs/phase4.1-qwen-kv/airgap-verify.log &amp;&amp; ( grep -qiE "strict.*pass|airgap.*(pass|ok)|0 outbound" runs/phase4.1-qwen-kv/airgap-verify.log )</automated>
  </verify>
  <acceptance_criteria>
    - `runs/phase4.1-qwen-kv/airgap-verify.log` exists and contains a STRICT-mode pass indicator
    - No new outbound endpoints introduced relative to existing Qwen v3.1 profile
  </acceptance_criteria>
  <done>Profile passes ci_verify_phase3 STRICT unchanged.</done>
</task>

</tasks>

<verification_criteria>
1. `profiles/qwen3.6-27b/v1/` bundle exists with profile.yaml, serving.yaml, harness.yaml, PROFILE_NOTES.md, prompts/, tool_schemas/, grammars/
2. `uv run emmy profile validate profiles/qwen3.6-27b/v1` exits 0
3. `profile.yaml.hash` matches `emmy profile hash` output (hash re-written by Task 4 AND again by find_kv_budget.py in Task 7)
4. `profiles/qwen3.6-27b/v1/serving.yaml:engine.gpu_memory_utilization` value ≠ 0.55 (seed has been overwritten by find_kv_budget.py — the ONLY sanctioned writer per Pitfall #1)
5. `profiles/qwen3.6-27b/DEFAULT_VARIANT` contains exactly `v1` (no newline)
6. `profiles/qwen3.6-35b-a3b/DEFAULT_VARIANT` is UNCHANGED (still `v3.1`) — daily-driver default preserved
7. `runs/phase4.1-qwen-kv/find_kv_budget-stdout.log` shows converged value
8. `runs/phase4.1-qwen-thermal/pass1-record-floors/summary.json` shows `preemptions_hour2: 0` AND `oom_events: 0`
9. `runs/phase4.1-qwen-thermal/pass2-assert-floors/summary.json` shows `preemptions_hour2: 0` AND `oom_events: 0`
10. `runs/phase4.1-qwen-thermal/replay-pass2-stdout.log` contains `All floors pass`
11. `runs/phase4.1-qwen-kv/airgap-verify.log` confirms STRICT air-gap pass
12. NO throughput floor is asserted as a blocking gate at any step (per operator directive; dense variants are bandwidth-bound and tok/s is informational only)
13. Fallback branch: if Task 7 KV bisection fails at every value AND reducing `max_model_len` 131072→65536 also fails, document the failure in PROFILE_NOTES.md and defer the profile to a future hardware upgrade — this is an acceptable exit per 04.1-CONTEXT.md § Known risks
</verification_criteria>

<success_criteria>
Plan complete when all 13 verification_criteria items pass. The bundle is Phase-5-eval-ready when `/profile qwen3.6-27b` resolves to `profiles/qwen3.6-27b/v1/` end-to-end, KV is bisected, thermal is 2x2h clean, and air-gap STRICT holds.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-/04.1-01-SUMMARY.md` capturing:
- Final gpu_memory_utilization value chosen by find_kv_budget.py
- Observed tok/s at p50 / p1 hour-2 (informational only)
- Any max_model_len fallback applied
- Final profile hash
- Links to all run evidence paths
</output>
