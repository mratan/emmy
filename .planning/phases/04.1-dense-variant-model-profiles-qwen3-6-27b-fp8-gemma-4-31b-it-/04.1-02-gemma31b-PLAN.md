---
phase: 04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - profiles/gemma-4-31b-it/v1/profile.yaml
  - profiles/gemma-4-31b-it/v1/serving.yaml
  - profiles/gemma-4-31b-it/v1/harness.yaml
  - profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md
  - profiles/gemma-4-31b-it/v1/prompts/**
  - profiles/gemma-4-31b-it/v1/tool_schemas/**
  - profiles/gemma-4-31b-it/v1/grammars/tool_call.lark
  - profiles/gemma-4-31b-it/DEFAULT_VARIANT
  - runs/phase4.1-gemma-kv/**
  - runs/phase4.1-gemma-thermal/pass1-record-floors/**
  - runs/phase4.1-gemma-thermal/pass2-assert-floors/**
autonomous: false
requirements: []

must_haves:
  truths:
    - "profiles/gemma-4-31b-it/v1/ exists and validates cleanly (emmy profile validate exits 0)"
    - "profile.yaml.hash matches output of emmy profile hash"
    - "KV bisection produced a final gpu_memory_utilization written by find_kv_budget.py (sole sanctioned writer)"
    - "2x2 h thermal replay: pass 1 records floors, pass 2 asserts floors with zero preemptions and zero OOM — REGARDLESS of tok/s figure (dense bandwidth-bound is expected; tok/s NOT a gate)"
    - "profiles/gemma-4-31b-it/DEFAULT_VARIANT contains v1"
    - "/profile gemma-4-31b-it resolves to profiles/gemma-4-31b-it/v1 end-to-end on live DGX Spark"
    - "profiles/gemma-4-26b-a4b-it/DEFAULT_VARIANT is UNCHANGED (still v2) — Gemma MoE default preserved"
    - "profile passes ci_verify_phase3 (STRICT air-gap) — no new outbound endpoints"
  artifacts:
    - path: profiles/gemma-4-31b-it/v1/profile.yaml
      provides: "Bundle identity, immutable versioning, content hash"
      contains: "id: gemma-4-31b-it"
    - path: profiles/gemma-4-31b-it/v1/serving.yaml
      provides: "vLLM engine args + sampling defaults for google/gemma-4-31B-it dense"
      contains: "served_model_name: gemma-4-31b-it"
    - path: profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md
      provides: "Provenance + KV result + thermal outcome + known-risk deferral branch"
      contains: "profile_id: gemma-4-31b-it"
    - path: profiles/gemma-4-31b-it/DEFAULT_VARIANT
      provides: "Family resolver default for /profile gemma-4-31b-it"
      contains: "v1"
    - path: runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json
      provides: "Thermal pass-2 outcome JSON (zero preemptions, zero OOM)"
      contains: '"preemptions_hour2": 0'
  key_links:
    - from: "profiles/gemma-4-31b-it/v1/serving.yaml"
      to: "/models/gemma-4-31B-it"
      via: "engine.model path mount"
      pattern: "model: /models/gemma-4-31B-it"
    - from: "profiles/gemma-4-31b-it/v1/serving.yaml"
      to: "vllm/vllm-openai:gemma4-0409-arm64-cu130"
      via: "engine.container_image_digest (reuses Phase 4 v2 local digest sha256:db59febc6c47...)"
      pattern: "container_image_digest: sha256:db59febc6c47"
    - from: "scripts/find_kv_budget.py"
      to: "profiles/gemma-4-31b-it/v1/serving.yaml:engine.gpu_memory_utilization"
      via: "sole sanctioned writer — Pitfall #1 discipline"
      pattern: "gpu_memory_utilization:"
---

<objective>
Land `profiles/gemma-4-31b-it/v1/` as a full profile bundle for `google/gemma-4-31B-it` (dense sibling of the `gemma-4-26b-a4b-it` MoE). Clone-and-retarget from `profiles/gemma-4-26b-a4b-it/v2/`, then run the standard post-authoring chain: hash → validate → smoke → KV bisection → 2×2h thermal replay. Gemma MoE default stays on `gemma-4-26b-a4b-it@v2`.

Purpose: give Phase 5 eval a dense-vs-MoE same-family sibling for Gemma 4. Dense Gemma is bandwidth-bound (~6-10 tok/s expected per CLAUDE.md + research); this is NOT a failure — the gate is thermal stability, not throughput.
Output: validated v1 bundle, DEFAULT_VARIANT marker, KV evidence in `runs/phase4.1-gemma-kv/`, thermal evidence in `runs/phase4.1-gemma-thermal/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-/04.1-CONTEXT.md
@profiles/gemma-4-26b-a4b-it/v2/profile.yaml
@profiles/gemma-4-26b-a4b-it/v2/serving.yaml
@profiles/gemma-4-26b-a4b-it/v2/PROFILE_NOTES.md
@profiles/gemma-4-26b-a4b-it/DEFAULT_VARIANT
@scripts/find_kv_budget.py
@scripts/thermal_replay.py
@scripts/smoke_test.py
@scripts/start_emmy.sh

<interfaces>
Clone source field map (identical files copy verbatim unless listed as RETARGETED):

profile.yaml RETARGET:
- id: gemma-4-31b-it (was gemma-4-26b-a4b-it)
- version: v1 (was v2)
- base_model: google/gemma-4-31B-it (was google/gemma-4-26B-A4B-it)
- hash: recomputed via emmy profile hash --write (NEVER reuse v2 hash sha256:ec14fb09...)

serving.yaml RETARGET:
- engine.model: /models/gemma-4-31B-it
- engine.model_hf_id: google/gemma-4-31B-it
- engine.served_model_name: gemma-4-31b-it
- engine.container_image: vllm/vllm-openai:gemma4-0409-arm64-cu130 (SAME as v2)
- engine.container_image_digest: sha256:db59febc6c476c9c93701cc9f54917533cee774d4e4bb27eff6fafeae37509f8 (SAME local digest as v2)
- engine.container_entrypoint_override: "" (SAME as v2; required for upstream image's baked ENTRYPOINT)
- engine.gpu_memory_utilization: 0.45 SEED ONLY (more conservative than v2's 0.86 due to 31B BF16 weights ≈ 61 GB of 128 GB UMA leaving less KV headroom; find_kv_budget.py overwrites)
- engine.load_format: safetensors (SAME as v2; upstream image doesn't bundle fastsafetensors)
- engine.quantization: fp8 (runtime quant of BF16, SAME pattern as v2)
- engine.tool_call_parser: gemma4 (SAME as v2)
- engine.reasoning_parser: gemma4 (SAME as v2)
- quirks.strip_thinking_tags: true (SAME as v2; channel-bleed bug affects dense equally)

All other fields byte-identical to v2 including sampling_defaults (Google card: temperature 1.0, top_p 0.95, top_k 64, repetition_penalty 1.0, max_tokens 8192), speculative null, guided_decoding xgrammar, env block.

harness.yaml + prompts/ + tool_schemas/ + grammars/: byte-identical clone from v2.

Thermal summary.json contract (confirmed shape from runs/phase4-thermal/pass2-assert-floors/summary.json):
- preemptions_hour2: 0  (GATE)
- oom_events: 0         (GATE)
- profile_id: gemma-4-31b-it
- profile_version: v1
- profile_hash: sha256:<matches profile.yaml.hash>
- target_wall_time_s: 7200
- decode_throughput_p1_hour2_tokps / decode_throughput_p50_hour2_tokps (INFORMATIONAL; NOT a gate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull google/gemma-4-31B-it weights from HuggingFace</name>
  <files>/models/gemma-4-31B-it/** (weight cache; NOT in git)</files>
  <read_first>
    - profiles/gemma-4-26b-a4b-it/v2/serving.yaml (model mount-path convention)
    - 04.1-CONTEXT.md § "Research already done" Gemma 4 dense bullets (BF16 only, ~61 GB weights, no publisher FP8)
  </read_first>
  <action>
    Download BF16 weights (Google ships BF16 only; FP8 is runtime quant at vLLM load):

    ```bash
    huggingface-cli download google/gemma-4-31B-it --local-dir /models/gemma-4-31B-it --local-dir-use-symlinks False
    ```

    Gemma 4 is a gated HuggingFace repo. Auth is already configured on this host (Phase 4 downloaded the 26B MoE sibling from the same org). If you get a 401/403, re-authenticate with `huggingface-cli login`.

    Expected download size: ~61 GB. Plan for 10-30 min transfer. Monitor for stalls.

    Inventory: `ls -la /models/gemma-4-31B-it/ | tee runs/phase4.1-gemma-kv/download-logs/weight-inventory.txt`

    Verify config: `grep -iE "torch_dtype|quant" /models/gemma-4-31B-it/config.json`. Expect `"torch_dtype": "bfloat16"` with NO `quantization_config` block.
  </action>
  <verify>
    <automated>test -f /models/gemma-4-31B-it/config.json &amp;&amp; test -f /models/gemma-4-31B-it/tokenizer.json &amp;&amp; grep -q '"torch_dtype": "bfloat16"' /models/gemma-4-31B-it/config.json</automated>
  </verify>
  <acceptance_criteria>
    - `/models/gemma-4-31B-it/config.json` exists
    - `/models/gemma-4-31B-it/tokenizer.json` exists
    - At least one `.safetensors` shard exists under `/models/gemma-4-31B-it/`
    - `/models/gemma-4-31B-it/config.json` contains `"torch_dtype": "bfloat16"`
    - `runs/phase4.1-gemma-kv/download-logs/weight-inventory.txt` exists and is non-empty
  </acceptance_criteria>
  <done>Weights present at /models/gemma-4-31B-it/, BF16 shape confirmed, inventory logged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify container image (reuse Phase 4 v2 upstream vllm-openai image)</name>
  <files>runs/phase4.1-gemma-kv/container-inspect.txt (new)</files>
  <read_first>
    - profiles/gemma-4-26b-a4b-it/v2/serving.yaml (engine.container_image + container_image_digest)
  </read_first>
  <action>
    Same upstream vllm-openai image as the 26B MoE v2 (both require Transformers 5.5.0 + vLLM 0.19+ for Gemma4ForCausalLM). Verify:

    ```bash
    docker inspect --format '{{.Id}}' vllm/vllm-openai:gemma4-0409-arm64-cu130 | tee runs/phase4.1-gemma-kv/container-inspect.txt
    ```

    Expected output: `sha256:db59febc6c476c9c93701cc9f54917533cee774d4e4bb27eff6fafeae37509f8` (matches v2).

    If the digest does NOT match, capture whatever is on disk and FLAG to operator — v2 would be running against drifted image. For THIS plan, proceed with the current local digest.

    If image missing, `docker pull vllm/vllm-openai:gemma4-0409-arm64-cu130` then re-inspect.
  </action>
  <verify>
    <automated>test -s runs/phase4.1-gemma-kv/container-inspect.txt &amp;&amp; grep -q "^sha256:" runs/phase4.1-gemma-kv/container-inspect.txt</automated>
  </verify>
  <acceptance_criteria>
    - `runs/phase4.1-gemma-kv/container-inspect.txt` exists and starts with `sha256:`
    - `docker images vllm/vllm-openai:gemma4-0409-arm64-cu130 --format '{{.ID}}'` returns a non-empty result
  </acceptance_criteria>
  <done>Gemma 4 upstream container present locally, digest captured.</done>
</task>

<task type="auto">
  <name>Task 3: Write profiles/gemma-4-31b-it/v1/ bundle (clone-and-retarget from v2)</name>
  <files>
    profiles/gemma-4-31b-it/v1/profile.yaml,
    profiles/gemma-4-31b-it/v1/serving.yaml,
    profiles/gemma-4-31b-it/v1/harness.yaml,
    profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md,
    profiles/gemma-4-31b-it/v1/prompts/*.md,
    profiles/gemma-4-31b-it/v1/tool_schemas/*.schema.json,
    profiles/gemma-4-31b-it/v1/grammars/tool_call.lark,
    profiles/gemma-4-31b-it/DEFAULT_VARIANT
  </files>
  <read_first>
    - profiles/gemma-4-26b-a4b-it/v2/profile.yaml
    - profiles/gemma-4-26b-a4b-it/v2/serving.yaml
    - profiles/gemma-4-26b-a4b-it/v2/harness.yaml
    - profiles/gemma-4-26b-a4b-it/v2/PROFILE_NOTES.md
    - profiles/gemma-4-26b-a4b-it/DEFAULT_VARIANT
    - runs/phase4.1-gemma-kv/container-inspect.txt (from Task 2)
  </read_first>
  <action>
    Step 1 — copy entire v2 bundle:
    ```bash
    mkdir -p profiles/gemma-4-31b-it
    cp -R profiles/gemma-4-26b-a4b-it/v2/ profiles/gemma-4-31b-it/v1/
    ```

    Step 2 — edit `profiles/gemma-4-31b-it/v1/profile.yaml`:
    - `id: gemma-4-31b-it`
    - `version: v1`
    - `base_model: google/gemma-4-31B-it`
    - `description: "Gemma 4 31B dense profile — sibling to gemma-4-26b-a4b-it@v2 for Phase 5 dense-vs-MoE A/B; clone-and-retarget from v2; BF16 weights runtime-quantized to FP8; KV bisected + 2x2h thermal validated; bandwidth-bound (~6-10 tok/s expected, NOT a gate)"`
    - `created: '2026-04-24'`
    - Set `hash: sha256:PENDING` — Task 4 rewrites
    - `tags: [coding, dgx-spark, fp8, gemma-4, phase-4.1, dense]`
    - Keep existing `community_sources:` from v2 and ADD at top: `{title: "Google Gemma 4 31B-it HF model card", url: "https://huggingface.co/google/gemma-4-31B-it", retrieved: "2026-04-24"}`

    Step 3 — edit `profiles/gemma-4-31b-it/v1/serving.yaml`. Change ONLY these fields; leave every other line byte-identical to v2:
    - `engine.model: /models/gemma-4-31B-it`
    - `engine.model_hf_id: google/gemma-4-31B-it`
    - `engine.served_model_name: gemma-4-31b-it`
    - `engine.container_image_digest: sha256:<value from runs/phase4.1-gemma-kv/container-inspect.txt>` (typically `sha256:db59febc6c476c9c93701cc9f54917533cee774d4e4bb27eff6fafeae37509f8`)
    - `engine.gpu_memory_utilization: 0.45` — SEED VALUE ONLY. `scripts/find_kv_budget.py` is the ONLY sanctioned writer per CLAUDE.md Pitfall #1; Task 7 overwrites.
    - Update the header comment block: replace v2's post-close-follow-up narrative with: `# Phase 4.1 (2026-04-24) — Gemma 4 31B dense sibling of gemma-4-26b-a4b-it@v2. Clone-and-retarget: model path, served_model_name, model_hf_id. Container digest reused (same upstream vllm-openai Day-1 Gemma 4 image). gpu_memory_utilization seeded at 0.45 (more conservative than v2's 0.86 due to dense weight footprint); find_kv_budget.py will overwrite. See PROFILE_NOTES.md § "Phase 4.1 clone provenance".`

    KEEP byte-identical from v2:
    - `max_model_len: 131072`
    - `kv_cache_dtype: fp8`
    - `enable_prefix_caching: true`, `enable_chunked_prefill: true`
    - `max_num_batched_tokens: 8192`, `max_num_seqs: 4`
    - `load_format: safetensors`
    - `quantization: fp8`
    - `tool_call_parser: gemma4`, `reasoning_parser: gemma4`, `enable_auto_tool_choice: true`
    - `container_entrypoint_override: ""` (critical; without it start_emmy.sh produces `vllm serve vllm serve` collision)
    - attention_backend UNSET (v2 finding: FlashInfer not supported on Gemma 4 head_size in this image)
    - `host: 0.0.0.0`, `port: 8000`
    - Entire `sampling_defaults:` (Google card defaults)
    - `speculative: null`
    - Entire `guided_decoding:` block
    - Entire `quirks:` block including `strip_thinking_tags: true` (channel-bleed bug affects dense equally per v2 PROFILE_NOTES.md)
    - Entire `env:` block (VLLM_LOAD_FORMAT: safetensors)

    Step 4 — leave `harness.yaml`, `prompts/*`, `tool_schemas/*`, `grammars/tool_call.lark` BYTE-IDENTICAL to v2.

    Step 5 — rewrite `profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md` preserving v2's provenance-table structure. Update:
    - Header: `# Gemma 4 31B-it — v1 Profile Notes`
    - Top paragraph: `Phase 4.1 dense sibling profile. google/gemma-4-31B-it (dense, 30.7B params, BF16 weights runtime-quantized to FP8) served in upstream vllm/vllm-openai:gemma4-0409-arm64-cu130 on DGX Spark GB10. Clone-and-retarget from gemma-4-26b-a4b-it@v2; diffs limited to model path, served_model_name, and post-bisection gpu_memory_utilization.`
    - Section `## Phase 4.1 clone provenance` listing the exact field retargets from Step 3
    - Section `## KV bisection result` with placeholder `<to be populated by scripts/find_kv_budget.py in Task 7>`
    - Section `## Thermal validation` with placeholder
    - Section `## Throughput observation (informational, NOT a gate)` verbatim: `Per operator directive 2026-04-24 (feedback_dense_model_throughput.md + 04.1-CONTEXT.md § Plan 04.1-2 explicit throughput policy), Gemma-4-31B dense is expected to land in the 6-10 tok/s bandwidth-bound zone. This is NOT a failure. The thermal gate is: zero preemptions + zero OOM + recorded floors. Throughput observation is recorded here as informational context for Phase 5 eval.`
    - Section `## Known-risk fallback branch` verbatim: `If KV bisection fails at every tested value AND subsequent max_model_len reduction (131072 -> 65536 -> 32768) still fails, the model genuinely does not fit on this 128 GB UMA hardware. Document as deferral here and escalate to operator; do NOT attempt further workarounds. Per 04.1-CONTEXT.md § Known risks, this is an acceptable exit.`

    Step 6 — write `profiles/gemma-4-31b-it/DEFAULT_VARIANT`:
    ```bash
    printf 'v1' > profiles/gemma-4-31b-it/DEFAULT_VARIANT
    ```

    Step 7 — DO NOT touch `profiles/gemma-4-26b-a4b-it/` or `profiles/qwen3.6-35b-a3b/` in any way. All writes are to NEW paths under `profiles/gemma-4-31b-it/`.
  </action>
  <verify>
    <automated>test -f profiles/gemma-4-31b-it/v1/profile.yaml &amp;&amp; test -f profiles/gemma-4-31b-it/v1/serving.yaml &amp;&amp; test -f profiles/gemma-4-31b-it/v1/harness.yaml &amp;&amp; test -f profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md &amp;&amp; test -f profiles/gemma-4-31b-it/DEFAULT_VARIANT &amp;&amp; grep -q "^id: gemma-4-31b-it$" profiles/gemma-4-31b-it/v1/profile.yaml &amp;&amp; grep -q "served_model_name: gemma-4-31b-it" profiles/gemma-4-31b-it/v1/serving.yaml &amp;&amp; [ "$(cat profiles/gemma-4-31b-it/DEFAULT_VARIANT)" = "v1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/gemma-4-31b-it/v1/profile.yaml` contains `id: gemma-4-31b-it`
    - `profiles/gemma-4-31b-it/v1/profile.yaml` contains `version: v1`
    - `profiles/gemma-4-31b-it/v1/profile.yaml` contains `base_model: google/gemma-4-31B-it`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `model: /models/gemma-4-31B-it`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `served_model_name: gemma-4-31b-it`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `gpu_memory_utilization: 0.45`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `load_format: safetensors`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `tool_call_parser: gemma4`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `reasoning_parser: gemma4`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `strip_thinking_tags: true`
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `container_entrypoint_override: ""`
    - `profiles/gemma-4-31b-it/v1/tool_schemas/` contains 9 `.schema.json` files
    - `profiles/gemma-4-31b-it/v1/grammars/tool_call.lark` exists
    - `profiles/gemma-4-31b-it/DEFAULT_VARIANT` contains exactly `v1` (no newline)
    - `profiles/gemma-4-26b-a4b-it/DEFAULT_VARIANT` still contains `v2` (UNCHANGED)
  </acceptance_criteria>
  <done>Bundle files exist with retargeted identity; unchanged fields byte-identical to v2 including entrypoint override + strip_thinking_tags; hash placeholder; DEFAULT_VARIANT set to v1; existing profiles untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Compute content hash and validate profile bundle</name>
  <files>profiles/gemma-4-31b-it/v1/profile.yaml (hash field rewritten)</files>
  <read_first>
    - profiles/gemma-4-31b-it/v1/profile.yaml
    - scripts/hash_profile.py
    - scripts/validate_profile.py
  </read_first>
  <action>
    ```bash
    uv run emmy profile hash --write profiles/gemma-4-31b-it/v1
    uv run emmy profile validate profiles/gemma-4-31b-it/v1
    ```

    Expected: `validate` exits 0. Fix any schema violation before proceeding.

    The hash MUST NOT equal the v2 hash (`sha256:ec14fb0980a1a5bbf41ee062d6c007d68a3ec1b872d47c5065215dfdf356fe6f`). If it does, Task 3 failed to retarget the fields.
  </action>
  <verify>
    <automated>uv run emmy profile validate profiles/gemma-4-31b-it/v1 &amp;&amp; ! grep -q "ec14fb0980a1a5bbf41ee062d6c007d68a3ec1b872d47c5065215dfdf356fe6f" profiles/gemma-4-31b-it/v1/profile.yaml &amp;&amp; grep -qE "^  hash: sha256:[0-9a-f]{64}$" profiles/gemma-4-31b-it/v1/profile.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run emmy profile validate profiles/gemma-4-31b-it/v1` exits 0
    - `profiles/gemma-4-31b-it/v1/profile.yaml` contains `hash: sha256:<64-hex>`
    - Hash value differs from v2's `sha256:ec14fb0980a1a5bbf41ee062d6c007d68a3ec1b872d47c5065215dfdf356fe6f`
  </acceptance_criteria>
  <done>Bundle hash computed, written, schema-validated; ready for smoke test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Smoke test — boot emmy-serve against the new Gemma dense profile</name>
  <what-built>
    Profile bundle authored, hashed, validated. Verify end-to-end boot + probe on live DGX Spark. Note: upstream image lacks fastsafetensors so cold start is ~8 min (vs ~3 min for Qwen NGC image) — don't confuse that with a stall.
  </what-built>
  <how-to-verify>
    Operator runs from the emmy repo root:

    ```bash
    scripts/start_emmy.sh --profile profiles/gemma-4-31b-it/v1 2>&amp;1 | tee runs/phase4.1-gemma-kv/smoke.log
    ```

    Expected outcomes:
    1. Profile loads + docker argv rendered from serving.yaml
    2. Container boots (~8 min cold start — plain safetensors loader on 61 GB BF16 weights runtime-quantizing to FP8)
    3. `scripts/smoke_test.py` probes `/v1/models` and `/v1/chat/completions` with 900s timeout (already phase-4-tuned for Gemma cold boots per CONTEXT.md § Constraints)
    4. Final line of smoke.log: `smoke ok: tok/s=<some number>` — tok/s is informational; expected range 6-10 tok/s; lower is fine per operator directive

    If boot fails: capture `docker logs emmy-serve` into `runs/phase4.1-gemma-kv/boot-failure-<iso>.log`. Common Gemma-specific causes: (a) weight path mismatch, (b) missing `container_entrypoint_override` leading to `vllm serve vllm serve` (check serving.yaml — this override must be exactly `""`), (c) Transformers version mismatch (should be impossible with pinned upstream image).

    After success: `docker stop emmy-serve` to release GPU before KV bisection.
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-gemma-kv/smoke.log` exists
    - `runs/phase4.1-gemma-kv/smoke.log` contains `smoke ok`
    - `docker ps -a --filter name=emmy-serve --format '{{.Status}}'` does NOT show `Exited (<non-zero>)`
  </acceptance_criteria>
  <resume-signal>Type "approved" after smoke.log contains `smoke ok`, or describe any boot failure for diagnosis.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: KV budget bisection (sole sanctioned writer to gpu_memory_utilization)</name>
  <what-built>
    Seed `gpu_memory_utilization: 0.45` is a placeholder. `scripts/find_kv_budget.py` is the ONLY sanctioned writer per CLAUDE.md Pitfall #1 + 04.1-CONTEXT.md § Constraints. This task invokes it, producing a measured final value + rehashing the profile + writing a KV-result block into PROFILE_NOTES.md. Dense 31B BF16 weights leave less KV headroom than the MoE v2 (0.86 was fine for 26B MoE), so a lower final value is expected.
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    uv run python scripts/find_kv_budget.py \
      --profile profiles/gemma-4-31b-it/v1 \
      --drive-minutes 10 \
      --max-iters 12 \
      2>&amp;1 | tee runs/phase4.1-gemma-kv/find_kv_budget-stdout.log
    ```

    Estimated wall-clock: ~3.5-5 h (boot ~8 min/iter × up to 12 iters + 10 min drive each — longer than Qwen because plain safetensors loader). Monitor for preemptions and OOM.

    Expected behavior:
    - Bisection converges on a gpu_memory_utilization value (probably noticeably lower than v2's 0.86 — weights+KV footprint is much larger)
    - Script writes the final value into `profiles/gemma-4-31b-it/v1/serving.yaml:engine.gpu_memory_utilization`
    - Script rehashes profile + writes KV-result block to PROFILE_NOTES.md
    - Stdout ends with `converged: gpu_memory_utilization=<value>`

    Known-risk fallback (per 04.1-CONTEXT.md § Known risks): if every tested value OOMs/preempts, reduce `max_model_len` from 131072 → 65536 (edit in serving.yaml, recompute hash via `emmy profile hash --write`, re-validate), then re-run bisection. If 65536 also fails, try 32768. If 32768 also fails, the model genuinely does not fit on this 128 GB UMA hardware — document as deferral in PROFILE_NOTES.md § "Known-risk fallback branch" and escalate.

    After success: `uv run emmy profile validate profiles/gemma-4-31b-it/v1` must exit 0.
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-gemma-kv/find_kv_budget-stdout.log` exists and ends with a `converged:` line OR contains an explicit fallback-branch outcome (max_model_len reduced and re-converged, or deferred)
    - `profiles/gemma-4-31b-it/v1/serving.yaml` contains `gpu_memory_utilization:` with a value that is NOT `0.45` (seed overwritten)
    - `profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md` contains `## KV bisection result` with populated content (not placeholder)
    - `uv run emmy profile validate profiles/gemma-4-31b-it/v1` exits 0
    - IF fallback branch fired: `profiles/gemma-4-31b-it/v1/serving.yaml` shows reduced `max_model_len` (65536 or 32768) AND PROFILE_NOTES.md § Known-risk fallback branch documents the path taken
  </acceptance_criteria>
  <resume-signal>Type "approved" after bisection converges and validate passes; or describe the preemption/OOM pattern + fallback outcome.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Thermal replay pass 1 — record floors (2 h sustained load)</name>
  <what-built>
    With KV budget locked, run 2-hour sustained-load replay to record thermal + throughput floors. Reference shape: Phase 4 `runs/phase4-thermal/pass1-record-floors/`. Per operator directive, throughput values here are informational; the gate is zero preemptions + zero OOM (pass 2 asserts).
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    uv run python scripts/thermal_replay.py \
      --profile profiles/gemma-4-31b-it/v1 \
      --target-wall-time-s 7200 \
      --record-floors \
      --out-dir runs/phase4.1-gemma-thermal/pass1-record-floors \
      2>&amp;1 | tee runs/phase4.1-gemma-thermal/replay-pass1-stdout.log
    ```

    Expected:
    1. Container boots at bisected gpu_memory_utilization
    2. Script drives sustained load for ~7200s
    3. Writes `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` with full Phase-4 schema (preemptions_hour2, oom_events, decode_throughput_p1/p50, gpu_clock_p5/p50, gpu_temp_p95, profile_hash, profile_id, profile_version, run_id, wall-times)
    4. `--record-floors` persists observed floors into `PROFILE_NOTES.md § Thermal validation` for pass 2

    Required acceptance: `preemptions_hour2: 0` AND `oom_events: 0`. Tok/s values (likely 6-10 tok/s or even lower) are informational; ANY positive decode-throughput passes this gate per operator directive.

    If preemptions > 0 or OOM > 0: back off gpu_memory_utilization (re-run Task 7 with narrower upper bound). DO NOT manually edit serving.yaml.

    Evidence to commit: `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json`, `runs/phase4.1-gemma-thermal/pass1-record-floors/README.md` (narrative if anything notable happened), `runs/phase4.1-gemma-thermal/replay-pass1-stdout.log`.
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` exists
    - `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` contains `"preemptions_hour2": 0`
    - `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` contains `"oom_events": 0`
    - `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` contains `"profile_id": "gemma-4-31b-it"`
    - `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` contains `"profile_version": "v1"`
    - `profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md` § Thermal validation contains recorded floor values (not placeholder)
  </acceptance_criteria>
  <resume-signal>Type "approved" after summary.json shows zero preemptions + zero OOM; otherwise describe the pressure pattern.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Thermal replay pass 2 — assert floors (2 h validation run)</name>
  <what-built>
    Second 2-hour replay asserting against floors from Task 8. Pass condition per operator directive: zero preemptions + zero OOM. Tok/s floors recorded but NOT a pass/fail criterion for this plan.
  </what-built>
  <how-to-verify>
    Operator runs:

    ```bash
    uv run python scripts/thermal_replay.py \
      --profile profiles/gemma-4-31b-it/v1 \
      --target-wall-time-s 7200 \
      --assert-floors \
      --out-dir runs/phase4.1-gemma-thermal/pass2-assert-floors \
      2>&amp;1 | tee runs/phase4.1-gemma-thermal/replay-pass2-stdout.log
    ```

    Expected:
    1. Fresh container boot at bisected gpu_memory_utilization
    2. 2-hour sustained load against same fixture set as pass 1
    3. `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` with same schema as pass 1
    4. Stdout final status line: `All floors pass` (reference Phase 4 shape)

    Gate (binding):
    - `summary.json` contains `"preemptions_hour2": 0`
    - `summary.json` contains `"oom_events": 0`

    Non-gate (informational per operator directive — record in PROFILE_NOTES.md but do not pass/fail on):
    - Decode-throughput p50 / p1 values (expected ~6-10 tok/s)
    - GPU clock floors

    If preemptions > 0 or OOM > 0: bisection optimistic; return to Task 7 with lower upper bound; re-run Tasks 7-9.

    After success: update `profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md § Thermal validation` to reference both pass-1 and pass-2 run_ids + summary line "2x2h thermal replay: pass1 recorded floors, pass2 asserted with zero preemptions and zero OOM (throughput observations captured informationally — NOT a gate per operator directive)".
  </how-to-verify>
  <acceptance_criteria>
    - `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` exists
    - `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` contains `"preemptions_hour2": 0`
    - `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` contains `"oom_events": 0`
    - `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` contains `"profile_id": "gemma-4-31b-it"`
    - `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` contains `"profile_version": "v1"`
    - `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` contains a `profile_hash` matching `profiles/gemma-4-31b-it/v1/profile.yaml:hash`
    - `runs/phase4.1-gemma-thermal/replay-pass2-stdout.log` contains `All floors pass`
    - `profiles/gemma-4-31b-it/v1/PROFILE_NOTES.md` § Thermal validation references both run_ids
  </acceptance_criteria>
  <resume-signal>Type "approved" after pass 2's summary.json shows zero preemptions + zero OOM AND stdout contains "All floors pass"; or describe throughput floor deltas.</resume-signal>
</task>

<task type="auto">
  <name>Task 10: Air-gap STRICT re-verify (ci_verify_phase3)</name>
  <files>runs/phase4.1-gemma-kv/airgap-verify.log (new)</files>
  <read_first>
    - scripts/verify_airgap_ci.sh
    - scripts/airgap_probe.py
    - CLAUDE.md § Pitfall #5
  </read_first>
  <action>
    Confirm the new profile introduces zero outbound endpoints:

    ```bash
    bash scripts/verify_airgap_ci.sh --profile profiles/gemma-4-31b-it/v1 --mode strict 2>&amp;1 | tee runs/phase4.1-gemma-kv/airgap-verify.log
    ```

    Fallback if script signature differs:
    ```bash
    uv run python scripts/airgap_probe.py --profile profiles/gemma-4-31b-it/v1 --strict 2>&amp;1 | tee runs/phase4.1-gemma-kv/airgap-verify.log
    ```

    Expected: exit 0 + STRICT-pass line.
  </action>
  <verify>
    <automated>test -s runs/phase4.1-gemma-kv/airgap-verify.log &amp;&amp; ( grep -qiE "strict.*pass|airgap.*(pass|ok)|0 outbound" runs/phase4.1-gemma-kv/airgap-verify.log )</automated>
  </verify>
  <acceptance_criteria>
    - `runs/phase4.1-gemma-kv/airgap-verify.log` exists and contains a STRICT-mode pass indicator
    - No new outbound endpoints introduced relative to existing Gemma v2 profile
  </acceptance_criteria>
  <done>Profile passes ci_verify_phase3 STRICT unchanged.</done>
</task>

</tasks>

<verification_criteria>
1. `profiles/gemma-4-31b-it/v1/` bundle exists with profile.yaml, serving.yaml, harness.yaml, PROFILE_NOTES.md, prompts/, tool_schemas/, grammars/
2. `uv run emmy profile validate profiles/gemma-4-31b-it/v1` exits 0
3. `profile.yaml.hash` matches `emmy profile hash` output (rewritten by Task 4, again by find_kv_budget.py in Task 7)
4. `profiles/gemma-4-31b-it/v1/serving.yaml:engine.gpu_memory_utilization` value ≠ 0.45 (seed overwritten by find_kv_budget.py — ONLY sanctioned writer)
5. `profiles/gemma-4-31b-it/DEFAULT_VARIANT` contains exactly `v1` (no newline)
6. `profiles/gemma-4-26b-a4b-it/DEFAULT_VARIANT` is UNCHANGED (still `v2`)
7. `runs/phase4.1-gemma-kv/find_kv_budget-stdout.log` shows converged value (or documented fallback branch)
8. `runs/phase4.1-gemma-thermal/pass1-record-floors/summary.json` shows `preemptions_hour2: 0` AND `oom_events: 0`
9. `runs/phase4.1-gemma-thermal/pass2-assert-floors/summary.json` shows `preemptions_hour2: 0` AND `oom_events: 0`
10. `runs/phase4.1-gemma-thermal/replay-pass2-stdout.log` contains `All floors pass`
11. `runs/phase4.1-gemma-kv/airgap-verify.log` confirms STRICT air-gap pass
12. NO throughput floor asserted as a blocking gate at any step (operator directive)
13. Known-risk fallback branch: if KV bisection fails at every max_model_len down to 32768, documented deferral in PROFILE_NOTES.md § "Known-risk fallback branch" is an acceptable exit per 04.1-CONTEXT.md § Known risks
</verification_criteria>

<success_criteria>
Plan complete when all 13 verification_criteria items pass. The bundle is Phase-5-eval-ready when `/profile gemma-4-31b-it` resolves to `profiles/gemma-4-31b-it/v1/` end-to-end, KV bisection produced a written value (or fallback documented), thermal is 2x2h clean, and air-gap STRICT holds.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dense-variant-model-profiles-qwen3-6-27b-fp8-gemma-4-31b-it-/04.1-02-SUMMARY.md` capturing:
- Final gpu_memory_utilization value chosen by find_kv_budget.py
- Observed tok/s at p50 / p1 hour-2 (informational only)
- Any max_model_len fallback applied (131072 / 65536 / 32768 / deferred)
- Final profile hash
- Links to all run evidence paths
</output>
