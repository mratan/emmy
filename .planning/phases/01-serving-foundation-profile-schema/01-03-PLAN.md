---
phase: 01-serving-foundation-profile-schema
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - emmy_serve/canary/__init__.py
  - emmy_serve/canary/sp_ok.py
  - emmy_serve/canary/tool_call.py
  - emmy_serve/canary/generate.py
  - emmy_serve/canary/logging.py
  - emmy_serve/canary/replay.py
  - emmy_serve/canary/tool_schemas/read_file.json
  - emmy_serve/boot/__init__.py
  - emmy_serve/boot/probe.py
  - emmy_serve/boot/runner.py
  - emmy_serve/diagnostics/bundle.py
  - scripts/smoke_test.py
  - scripts/start_emmy.sh
  - profiles/qwen3.6-35b-a3b/v1/serving.yaml
  - profiles/qwen3.6-35b-a3b/v1/profile.yaml
autonomous: false
requirements:
  - SERVE-01
  - SERVE-02
  - SERVE-04
  - SERVE-10
  - PROFILE-09
  - EVAL-07
  - REPRO-01

must_haves:
  truths:
    - "`scripts/start_emmy.sh` with a captured NGC digest boots the NGC container, loads Qwen3.6 via fastsafetensors, serves /v1/chat/completions on loopback (SC-1)"
    - "Measured 100-token generation throughput ≥ 60 tok/s on DGX Spark in smoke test (SC-1)"
    - "Boot-time smoke test gates startup on SP_OK + tool_call parse + 100-token generation; fail-loud + rollback + D-06 diagnostic bundle on any failure (SC-3)"
    - "`emmy.canary` module importable and re-used by later phases (EVAL-07); `CanaryResult` dataclass has all 8 fields"
    - "`start_emmy.sh` exits 0 on success, 1/2/3/4 per RESEARCH.md §14 contract"
    - "All Plan-01 canary + boot-related unit tests GREEN; digest-placeholder xfail cleared once operator captures real digest"
  artifacts:
    - path: "emmy_serve/canary/sp_ok.py"
      provides: "SP_OK canary (D-07) — exact prompt from RESEARCH.md §7.2"
      exports: ["run_sp_ok", "SP_OK_SYSTEM_PROMPT", "SP_OK_ASSERTION_SUBSTR"]
    - path: "emmy_serve/canary/tool_call.py"
      provides: "One-tool read_file canary (D-08) — RESEARCH.md §7.3"
      exports: ["run_tool_call", "TOOL_CALL_SYSTEM_PROMPT", "load_default_tool_schema"]
    - path: "emmy_serve/canary/generate.py"
      provides: "100-token decode smoke (RESEARCH.md §7.4)"
      exports: ["run_generate"]
    - path: "emmy_serve/canary/logging.py"
      provides: "CanaryResult dataclass (8 fields) + log_canary_event (append_jsonl_atomic)"
      exports: ["CanaryResult", "log_canary_event"]
    - path: "emmy_serve/boot/probe.py"
      provides: "wait_for_vllm(base_url, timeout_s=300, interval_s=0.5) per RESEARCH.md §7.1"
      exports: ["wait_for_vllm"]
    - path: "emmy_serve/boot/runner.py"
      provides: "render_docker_args(profile_path, run_dir, port, airgap) — CLI subcommand + importable function"
      exports: ["render_docker_args", "main"]
    - path: "emmy_serve/diagnostics/bundle.py"
      provides: "D-06 boot-failure diagnostic bundle writer per RESEARCH.md §7.5"
      exports: ["write_boot_failure_bundle"]
    - path: "scripts/start_emmy.sh"
      provides: "One-command contract per REPRO-01 / RESEARCH.md §14"
      contains: "set -euo pipefail"
    - path: "emmy_serve/canary/tool_schemas/read_file.json"
      provides: "Phase-1 read_file tool schema for tool_call canary (Option A per §7.7)"
      contains: "\"type\": \"function\""
  key_links:
    - from: "scripts/start_emmy.sh"
      to: "emmy_serve.boot.runner:render_docker_args"
      via: "$(python3 -m emmy_serve.boot.runner render-docker-args --profile PATH ...)"
      pattern: "render-docker-args"
    - from: "scripts/start_emmy.sh"
      to: "scripts/smoke_test.py"
      via: "subprocess invocation after /v1/models 200"
      pattern: "scripts/smoke_test.py"
    - from: "scripts/smoke_test.py"
      to: "emmy_serve.canary (run_sp_ok + run_tool_call + run_generate)"
      via: "library import"
      pattern: "from emmy_serve.canary import"
    - from: "scripts/start_emmy.sh"
      to: "runs/boot-failures/<iso>/"
      via: "D-06 bundle write + docker stop + exit 1 on smoke failure"
      pattern: "runs/boot-failures"
---

<objective>
Make the profile boot-able. This plan implements the SP_OK canary library (EVAL-07 — used by every later phase), the boot probe, the docker-args renderer, the D-06 diagnostic bundle, the smoke-test orchestrator, and `scripts/start_emmy.sh` — the one-command REPRO-01 contract. A human checkpoint captures the NGC digest on first `docker pull` (clearing the `test_digest_not_placeholder` xfail) and runs the boot + smoke + throughput integration tests on the real Spark.

Purpose: Success Criteria 1 and 3 of Phase 1's ROADMAP ride on this plan. SC-1 requires `start_emmy.sh` to boot + load weights + serve /v1/chat/completions with ≥60 tok/s measured; SC-3 requires the three-check smoke (SP_OK echo + tool_call parse + 100-token generation) with fail-loud + rollback on any failure. This plan also ships EVAL-07 (the canary library that every benchmark row will re-use from Phase 5 onward).

Output: `./scripts/start_emmy.sh` boots the vLLM container, the D-06 rollback path works when any smoke check fails, runs/<iso>-boot/ contains diagnostic artifacts on success, and the `tests/integration/test_boot.py` suite is GREEN on DGX Spark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-serving-foundation-profile-schema/01-CONTEXT.md
@.planning/phases/01-serving-foundation-profile-schema/01-RESEARCH.md
@.planning/phases/01-serving-foundation-profile-schema/01-VALIDATION.md
@.planning/phases/01-serving-foundation-profile-schema/01-PATTERNS.md
@.planning/phases/01-serving-foundation-profile-schema/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- What Plan 02 produced that this plan consumes -->

emmy_serve.profile.schema         — ServingConfig, HarnessConfig, ProfileManifest, ProfileYaml, EngineConfig
emmy_serve.profile.loader         — load_serving(path), load_harness(path), load_profile(bundle_dir) -> (ServingConfig, HarnessConfig, ProfileRef), load_profile_manifest(path)
emmy_serve.profile.hasher         — hash_bundle(bundle_dir) -> "sha256:<64-hex>"
emmy_serve.profile.immutability   — validate_bundle(bundle_dir, fix_hash, strict) -> int
emmy_serve.diagnostics.atomic     — write_bytes_atomic, write_text_atomic, write_json_atomic, append_jsonl_atomic
emmy_serve.diagnostics.layout     — EmmyRunLayout(base_dir, run_id, kind), new_run_id()
emmy_serve.cli                    — main(['profile', 'validate', ...])

From RESEARCH.md (verbatim to copy):
  §7.1 lines 1006-1023 — wait_for_vllm
  §7.2 lines 1034-1058 — run_sp_ok + SP_OK_SYSTEM_PROMPT
  §7.3 lines 1066-1127 — read_file.json + run_tool_call
  §7.4 lines 1134-1153 — run_generate
  §7.5 lines 1156-1180 — D-06 bundle file list
  §7.6 lines 1181-1215 — CanaryResult (8 fields) + package exports
  §14 lines 1891-1947 — start_emmy.sh full contract
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement emmy.canary package + boot.probe + D-06 bundle</name>
  <files>
    emmy_serve/canary/__init__.py,
    emmy_serve/canary/sp_ok.py,
    emmy_serve/canary/tool_call.py,
    emmy_serve/canary/generate.py,
    emmy_serve/canary/logging.py,
    emmy_serve/canary/replay.py,
    emmy_serve/canary/tool_schemas/read_file.json,
    emmy_serve/boot/__init__.py,
    emmy_serve/boot/probe.py,
    emmy_serve/diagnostics/bundle.py
  </files>
  <read_first>
    - /data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-RESEARCH.md §7.1 lines 1005-1030, §7.2 lines 1034-1058, §7.3 lines 1061-1127, §7.4 lines 1134-1153, §7.5 lines 1156-1180, §7.6 lines 1181-1215, §7.7 lines 1217-1224, §10.3 lines 1639-1665 (replay)
    - /data/projects/emmy/emmy_serve/profile/loader.py (ProfileRef fields)
    - /data/projects/emmy/emmy_serve/diagnostics/atomic.py (append_jsonl_atomic, write_json_atomic, write_text_atomic)
    - /data/projects/emmy/emmy_serve/diagnostics/layout.py (EmmyRunLayout kind="boot-failure")
    - /data/projects/emmy/tests/unit/test_canary.py (the contract this module satisfies)
  </read_first>
  <behavior>
    - `run_sp_ok(base_url, served_model_name)` returns (True, text) when response contains `[SP_OK]`; temperature=0.0, max_tokens=32
    - `run_tool_call(base_url, served_model_name, tool_schema)` returns (True, msg) only when exactly one `tool_calls` entry, name=="read_file", arguments JSON has `path`
    - `run_generate(base_url, served_model_name)` returns (True, data, elapsed_s) when finish_reason in ('length', 'stop') AND len(content) > 50
    - `CanaryResult` has 8 fields: check, ok, elapsed_ms, profile_id, profile_version, profile_hash, served_model_name, ts, + optional response_excerpt
    - `log_canary_event(jsonl_path, result)` appends one JSON line via append_jsonl_atomic
    - `wait_for_vllm(base_url, timeout_s=300, interval_s=0.5)` polls /v1/models; returns parsed JSON on 200; raises TimeoutError with last_err after timeout
    - `write_boot_failure_bundle(layout, profile_ref, check, reason, ...)` writes all 7 files listed in §7.5
  </behavior>
  <action>
**1. `emmy_serve/boot/probe.py`** — copy RESEARCH.md §7.1 lines 1006-1023 verbatim:

```python
"""Poll /v1/models until 200 OK or timeout. Per RESEARCH.md §7.1."""
from __future__ import annotations
import httpx, time


def wait_for_vllm(base_url: str, timeout_s: int = 300, interval_s: float = 0.5) -> dict:
    """Poll /v1/models until 200 OK or timeout. Returns parsed JSON."""
    deadline = time.monotonic() + timeout_s
    last_err = None
    while time.monotonic() < deadline:
        try:
            r = httpx.get(f'{base_url}/v1/models', timeout=5.0)
            if r.status_code == 200:
                return r.json()
            last_err = f'HTTP {r.status_code}: {r.text[:200]}'
        except httpx.HTTPError as e:
            last_err = str(e)
        time.sleep(interval_s)
    raise TimeoutError(f'/v1/models did not respond in {timeout_s}s; last error: {last_err}')
```

**2. `emmy_serve/boot/__init__.py`:**
```python
from .probe import wait_for_vllm
__all__ = ['wait_for_vllm']
```

**3. `emmy_serve/canary/tool_schemas/read_file.json`** — Option A per §7.7; copy RESEARCH.md §7.3 lines 1067-1085 verbatim:
```json
{
  "type": "function",
  "function": {
    "name": "read_file",
    "description": "Read the contents of a file at the given absolute path.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute filesystem path of the file to read."
        }
      },
      "required": ["path"],
      "additionalProperties": false
    }
  }
}
```

**4. `emmy_serve/canary/sp_ok.py`** — copy RESEARCH.md §7.2 verbatim:
```python
"""SP_OK canary (D-07) — EXACT payload per RESEARCH.md §7.2."""
from __future__ import annotations
import httpx

SP_OK_SYSTEM_PROMPT = (
    "When the user says 'ping' you must reply with the exact literal text "
    "[SP_OK] and nothing else."
)
SP_OK_USER_MESSAGE = "ping"
SP_OK_ASSERTION_SUBSTR = "[SP_OK]"


def run_sp_ok(base_url: str, served_model_name: str) -> tuple[bool, str]:
    """Returns (ok, full_response_text)."""
    payload = {
        "model": served_model_name,
        "messages": [
            {"role": "system", "content": SP_OK_SYSTEM_PROMPT},
            {"role": "user", "content": SP_OK_USER_MESSAGE},
        ],
        "temperature": 0.0,
        "max_tokens": 32,
        "stream": False,
    }
    r = httpx.post(f'{base_url}/v1/chat/completions', json=payload, timeout=60.0)
    r.raise_for_status()
    text = r.json()['choices'][0]['message']['content'] or ''
    return (SP_OK_ASSERTION_SUBSTR in text), text
```

**5. `emmy_serve/canary/tool_call.py`** — copy RESEARCH.md §7.3 lines 1088-1127 verbatim + helper:
```python
"""Tool-call smoke (D-08) — RESEARCH.md §7.3."""
from __future__ import annotations
import json
from pathlib import Path
import httpx

TOOL_CALL_SYSTEM_PROMPT = (
    "You have one tool available: read_file(path: string). "
    "When the user asks you to call a tool, call it. Do not explain."
)
TOOL_CALL_USER_MESSAGE = "call the tool read_file with path=/tmp/nothing.txt"


def load_default_tool_schema() -> dict:
    """Load read_file schema from emmy_serve/canary/tool_schemas/read_file.json (§7.7 Option A)."""
    p = Path(__file__).parent / "tool_schemas" / "read_file.json"
    return json.loads(p.read_text(encoding="utf-8"))


def run_tool_call(base_url: str, served_model_name: str, tool_schema: dict) -> tuple[bool, dict]:
    payload = {
        "model": served_model_name,
        "messages": [
            {"role": "system", "content": TOOL_CALL_SYSTEM_PROMPT},
            {"role": "user", "content": TOOL_CALL_USER_MESSAGE},
        ],
        "tools": [tool_schema],
        "tool_choice": "auto",
        "temperature": 0.0,
        "max_tokens": 128,
        "stream": False,
    }
    r = httpx.post(f'{base_url}/v1/chat/completions', json=payload, timeout=60.0)
    r.raise_for_status()
    msg = r.json()['choices'][0]['message']
    tcs = msg.get('tool_calls') or []
    if len(tcs) != 1:
        return False, msg
    tc = tcs[0]
    if tc.get('function', {}).get('name') != 'read_file':
        return False, msg
    try:
        args = json.loads(tc['function']['arguments'])
    except (json.JSONDecodeError, KeyError, TypeError):
        return False, msg
    if 'path' not in args:
        return False, msg
    return True, msg
```

**6. `emmy_serve/canary/generate.py`** — copy RESEARCH.md §7.4 verbatim:
```python
"""100-token generation smoke (RESEARCH.md §7.4)."""
from __future__ import annotations
import time
import httpx


def run_generate(base_url: str, served_model_name: str) -> tuple[bool, dict, float]:
    payload = {
        "model": served_model_name,
        "messages": [{"role": "user", "content": "Count from 1 to 100, one number per line."}],
        "temperature": 0.0,
        "max_tokens": 100,
        "stream": False,
    }
    t0 = time.monotonic()
    r = httpx.post(f'{base_url}/v1/chat/completions', json=payload, timeout=120.0)
    elapsed = time.monotonic() - t0
    r.raise_for_status()
    data = r.json()
    out = data['choices'][0]['message']['content'] or ''
    finish = data['choices'][0].get('finish_reason')
    ok = bool(out) and finish in ('length', 'stop') and len(out) > 50
    return ok, data, elapsed
```

**7. `emmy_serve/canary/logging.py`:**
```python
"""Canary event logging — RESEARCH.md §7.6."""
from __future__ import annotations
from dataclasses import dataclass, asdict
from pathlib import Path
from ..diagnostics.atomic import append_jsonl_atomic


@dataclass(frozen=True)
class CanaryResult:
    check: str
    ok: bool
    elapsed_ms: int
    profile_id: str
    profile_version: str
    profile_hash: str
    served_model_name: str
    ts: str
    response_excerpt: str = ''


def log_canary_event(jsonl_path: Path, result: CanaryResult) -> None:
    append_jsonl_atomic(jsonl_path, asdict(result))
```

**8. `emmy_serve/canary/replay.py`** — copy RESEARCH.md §10.3 lines 1639-1665 (Plan 05 consumes):
```python
"""50-turn session replay (D-11). Measures wire format round-trips."""
from __future__ import annotations
import json
from pathlib import Path
import httpx


def chat_completions(base_url: str, served_model_name: str, history: list, *, tools: list | None = None) -> dict:
    payload = {
        "model": served_model_name,
        "messages": history,
        "temperature": 0.0,
        "max_tokens": 256,
        "stream": False,
    }
    if tools:
        payload["tools"] = tools
        payload["tool_choice"] = "auto"
    r = httpx.post(f'{base_url}/v1/chat/completions', json=payload, timeout=120.0)
    r.raise_for_status()
    return r.json()


def run_replay(base_url: str, served_model_name: str, session_path: Path, tools: list | None = None) -> None:
    history: list[dict] = []
    turns = [json.loads(l) for l in session_path.read_text().splitlines() if l.strip()]
    i = 0
    while i < len(turns):
        turn = turns[i]
        if turn['role'] == 'user':
            history.append({'role': 'user', 'content': turn['content']})
            resp = chat_completions(base_url, served_model_name, history, tools=tools)
            msg = resp['choices'][0]['message']
            history.append(msg)
            if turn.get('_expected_tool_call') is not None:
                assert msg.get('tool_calls'), f"Turn {turn['turn']}: expected tool_call, got {msg}"
            i += 1
        elif turn['role'] == 'tool':
            history.append({
                'role': 'tool',
                'tool_call_id': turn['tool_call_id'],
                'content': turn['content'],
            })
            i += 1
        else:
            i += 1
```

**9. `emmy_serve/canary/__init__.py`:**
```python
from .sp_ok import run_sp_ok, SP_OK_SYSTEM_PROMPT, SP_OK_USER_MESSAGE, SP_OK_ASSERTION_SUBSTR
from .tool_call import run_tool_call, TOOL_CALL_SYSTEM_PROMPT, TOOL_CALL_USER_MESSAGE, load_default_tool_schema
from .generate import run_generate
from .logging import CanaryResult, log_canary_event
from .replay import run_replay

__all__ = [
    'run_sp_ok', 'run_tool_call', 'run_generate', 'run_replay',
    'SP_OK_SYSTEM_PROMPT', 'SP_OK_USER_MESSAGE', 'SP_OK_ASSERTION_SUBSTR',
    'TOOL_CALL_SYSTEM_PROMPT', 'TOOL_CALL_USER_MESSAGE', 'load_default_tool_schema',
    'CanaryResult', 'log_canary_event',
]
```

**10. `emmy_serve/diagnostics/bundle.py`** — D-06 per RESEARCH.md §7.5:
```python
"""D-06 boot-failure diagnostic bundle writer."""
from __future__ import annotations
import os, subprocess
from pathlib import Path
from .atomic import write_json_atomic, write_text_atomic
from .layout import EmmyRunLayout
from ..profile.loader import ProfileRef


def write_boot_failure_bundle(
    layout: EmmyRunLayout,
    *,
    profile_ref: ProfileRef,
    check: str,
    reason: str,
    prompt_text: str = "",
    response_text: str = "",
    response_json: dict | None = None,
    metrics_snapshot: str = "",
) -> None:
    """Write all 7 files per §7.5."""
    assert layout.kind == "boot-failure", f"expected kind=boot-failure, got {layout.kind}"
    layout.run_dir.mkdir(parents=True, exist_ok=True)
    write_json_atomic(layout.check_path, {"check": check, "reason": reason})
    write_json_atomic(layout.profile_snapshot_path, {
        "id": profile_ref.id,
        "version": profile_ref.version,
        "hash": profile_ref.hash,
        "path": str(profile_ref.path),
    })
    write_text_atomic(layout.prompt_path, prompt_text)
    write_text_atomic(layout.response_path, response_text)
    if response_json is not None:
        write_json_atomic(layout.run_dir / "response.json", response_json)
    env = {k: v for k, v in os.environ.items()
           if k.startswith(("EMMY_", "VLLM_", "HF_", "TRANSFORMERS_"))}
    write_json_atomic(layout.env_path, env)
    try:
        out = subprocess.check_output(
            ["docker", "logs", "--tail", "5000", "emmy-serve"],
            stderr=subprocess.STDOUT, timeout=30,
        ).decode(errors="replace")
    except Exception as e:
        out = f"(docker logs failed: {e})"
    write_text_atomic(layout.docker_logs_path, out)
    write_text_atomic(layout.metrics_snapshot_path, metrics_snapshot or "")
```

**Run after writing:**
```bash
cd /data/projects/emmy && uv run pytest tests/unit/test_canary.py -x -v
```
  </action>
  <verify>
    <automated>cd /data/projects/emmy && uv run pytest tests/unit/test_canary.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /data/projects/emmy/emmy_serve/canary/sp_ok.py && grep -q 'When the user says .ping.' /data/projects/emmy/emmy_serve/canary/sp_ok.py`
    - `grep -q 'SP_OK_ASSERTION_SUBSTR = "\[SP_OK\]"' /data/projects/emmy/emmy_serve/canary/sp_ok.py`
    - `grep -q '"temperature": 0.0' /data/projects/emmy/emmy_serve/canary/sp_ok.py`
    - `grep -q 'def run_tool_call' /data/projects/emmy/emmy_serve/canary/tool_call.py`
    - `grep -q 'def load_default_tool_schema' /data/projects/emmy/emmy_serve/canary/tool_call.py`
    - `grep -q 'def run_generate' /data/projects/emmy/emmy_serve/canary/generate.py`
    - `grep -q 'finish in ' /data/projects/emmy/emmy_serve/canary/generate.py`
    - `grep -q 'class CanaryResult' /data/projects/emmy/emmy_serve/canary/logging.py`
    - `grep -q 'def log_canary_event' /data/projects/emmy/emmy_serve/canary/logging.py`
    - `test -f /data/projects/emmy/emmy_serve/canary/tool_schemas/read_file.json && python3 -c "import json; d = json.load(open('/data/projects/emmy/emmy_serve/canary/tool_schemas/read_file.json')); assert d['function']['name'] == 'read_file'"`
    - `test -f /data/projects/emmy/emmy_serve/boot/probe.py && grep -q 'def wait_for_vllm' /data/projects/emmy/emmy_serve/boot/probe.py && grep -q 'timeout_s: int = 300' /data/projects/emmy/emmy_serve/boot/probe.py`
    - `test -f /data/projects/emmy/emmy_serve/diagnostics/bundle.py && grep -q 'def write_boot_failure_bundle' /data/projects/emmy/emmy_serve/diagnostics/bundle.py`
    - `cd /data/projects/emmy && uv run pytest tests/unit/test_canary.py -x` exits 0 (all 3 subtests green)
    - `cd /data/projects/emmy && uv run python -c "from emmy_serve.canary import run_sp_ok, run_tool_call, run_generate, CanaryResult, log_canary_event, load_default_tool_schema, SP_OK_SYSTEM_PROMPT; from emmy_serve.boot.probe import wait_for_vllm; from emmy_serve.diagnostics.bundle import write_boot_failure_bundle; print('imports ok')"` prints "imports ok"
  </acceptance_criteria>
  <done>emmy.canary package shipped with exact payloads from RESEARCH.md §7.2/7.3/7.4; CanaryResult has 8 fields; log_canary_event uses atomic append; D-06 bundle writer writes all 7 files; wait_for_vllm probe function matches §7.1 verbatim; test_canary.py all-green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement emmy_serve.boot.runner (render_docker_args CLI) + scripts/smoke_test.py</name>
  <files>emmy_serve/boot/runner.py, scripts/smoke_test.py</files>
  <read_first>
    - /data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-RESEARCH.md §1 lines 401-487 (canonical docker run invocation), §14 lines 1891-1947 (start_emmy.sh contract — runner fits into the flag-array pattern)
    - /data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-PATTERNS.md Pattern D (docker-run orchestrator shape)
    - /data/projects/setup_local_opencode/scripts/start_unified.sh (first 80 lines — flag-array pattern)
    - /data/projects/emmy/emmy_serve/profile/schema.py (ServingConfig fields → docker CLI flags mapping)
    - /data/projects/emmy/tests/unit/test_docker_run_build.py (assertions on rendered flag list)
  </read_first>
  <behavior>
    - `render_docker_args(profile_path: Path, run_dir: Path, port: int, airgap: bool = False) -> list[str]` returns the list of args to pass to docker after `docker run --name emmy-serve --detach`. It does NOT include `docker run` itself.
    - Includes: `--gpus all`, `--shm-size 8g`, `-v /data/models:/models:ro`, `-v /data/hf-cache:/hf-cache:ro`, `-p {port}:8000`
    - Includes env flags: `-e VLLM_NO_USAGE_STATS=1 -e DO_NOT_TRACK=1 -e VLLM_LOAD_FORMAT=fastsafetensors -e VLLM_FLASHINFER_MOE_BACKEND=latency -e VLLM_DISABLE_COMPILE_CACHE=1 -e HF_HUB_OFFLINE=1 -e TRANSFORMERS_OFFLINE=1 -e HF_HOME=/hf-cache`
    - Includes `--network none` when `airgap=True`, else `--network host` (or `-p {port}:8000` with default bridge; see §1)
    - Returns entries suitable for `subprocess.run([docker, 'run', '--name', 'emmy-serve', '--detach'] + render_docker_args(...) + [image_ref, 'vllm', 'serve', ...])`
    - Image ref is `nvcr.io/nvidia/vllm@{digest}` where digest comes from serving.yaml.engine.container_image_digest
    - vLLM CLI flags (after image ref): `vllm serve {model_path} --host {host} --port 8000 --max-model-len {N} --served-model-name {name} --gpu-memory-utilization {X} --load-format fastsafetensors --kv-cache-dtype fp8 --enable-prefix-caching --enable-chunked-prefill --max-num-batched-tokens {N} --quantization fp8 --tool-call-parser qwen3_coder --enable-auto-tool-choice --attention-backend flashinfer`
    - CLI `render-docker-args` subcommand: `python -m emmy_serve.boot.runner render-docker-args --profile PATH --run-dir PATH --port 8002` prints space-joined args to stdout (shell captures via `$(...)`); exit 0
  </behavior>
  <action>
**1. `emmy_serve/boot/runner.py`:**

```python
"""Render docker run args from serving.yaml. Used by start_emmy.sh."""
from __future__ import annotations
import argparse, shlex, sys
from pathlib import Path
from ..profile.loader import load_serving


def render_docker_args(
    profile_path: Path,
    run_dir: Path,
    port: int,
    *,
    airgap: bool = False,
    models_mount: str = "/data/models",
    hf_cache_mount: str = "/data/hf-cache",
) -> list[str]:
    """Render the full docker run arg list (excluding 'docker run --name emmy-serve --detach' prefix
    and excluding the image ref + vllm CLI — those are emitted separately).

    Returns: list of argv parts that render the docker-level flags.
    """
    serving = load_serving(profile_path / "serving.yaml")
    engine = serving.engine

    args: list[str] = [
        "--gpus", "all",
        "--shm-size", "8g",
        "-v", f"{models_mount}:/models:ro",
        "-v", f"{hf_cache_mount}:/hf-cache:ro",
    ]
    if airgap:
        args += ["--network", "none"]
    else:
        args += ["-p", f"{port}:{engine.port}"]

    # Env vars — ServingConfig.env enforces airgap policy at schema layer
    env = serving.env
    args += [
        "-e", f"VLLM_NO_USAGE_STATS={env.VLLM_NO_USAGE_STATS}",
        "-e", f"DO_NOT_TRACK={env.DO_NOT_TRACK}",
        "-e", f"VLLM_LOAD_FORMAT={env.VLLM_LOAD_FORMAT}",
        "-e", f"VLLM_FLASHINFER_MOE_BACKEND={env.VLLM_FLASHINFER_MOE_BACKEND}",
        "-e", f"VLLM_DISABLE_COMPILE_CACHE={env.VLLM_DISABLE_COMPILE_CACHE}",
        "-e", f"HF_HUB_OFFLINE={env.HF_HUB_OFFLINE}",
        "-e", f"TRANSFORMERS_OFFLINE={env.TRANSFORMERS_OFFLINE}",
        "-e", "HF_HOME=/hf-cache",
    ]
    return args


def render_vllm_cli_args(profile_path: Path) -> list[str]:
    """Render the vllm serve CLI flags (after the image ref in docker run ... IMAGE vllm serve ...)."""
    serving = load_serving(profile_path / "serving.yaml")
    e = serving.engine

    cli = [
        "vllm", "serve", e.model,
        "--host", e.host,
        "--port", str(e.port),
        "--max-model-len", str(e.max_model_len),
        "--served-model-name", e.served_model_name,
        "--gpu-memory-utilization", f"{e.gpu_memory_utilization:.3f}",
        "--load-format", e.load_format,
        "--kv-cache-dtype", e.kv_cache_dtype,
        "--max-num-batched-tokens", str(e.max_num_batched_tokens),
        "--quantization", e.quantization,
    ]
    if e.enable_prefix_caching:
        cli.append("--enable-prefix-caching")
    if e.enable_chunked_prefill:
        cli.append("--enable-chunked-prefill")
    if e.tool_call_parser:
        cli += ["--tool-call-parser", e.tool_call_parser]
    if e.enable_auto_tool_choice:
        cli.append("--enable-auto-tool-choice")
    if e.attention_backend:
        cli += ["--attention-backend", e.attention_backend]
    return cli


def render_image_ref(profile_path: Path) -> str:
    """Return 'nvcr.io/nvidia/vllm@sha256:<64-hex>' from serving.yaml.engine.container_image_digest."""
    serving = load_serving(profile_path / "serving.yaml")
    return f"{serving.engine.container_image.split(':')[0]}@{serving.engine.container_image_digest}"


def _cmd_render_docker_args(args) -> int:
    parts = render_docker_args(
        Path(args.profile), Path(args.run_dir), args.port, airgap=args.airgap,
    )
    print(" ".join(shlex.quote(p) for p in parts))
    return 0


def _cmd_render_vllm_cli(args) -> int:
    parts = render_vllm_cli_args(Path(args.profile))
    print(" ".join(shlex.quote(p) for p in parts))
    return 0


def _cmd_render_image_ref(args) -> int:
    print(render_image_ref(Path(args.profile)))
    return 0


def main(argv: list[str] | None = None) -> int:
    p = argparse.ArgumentParser(prog='emmy_serve.boot.runner')
    sub = p.add_subparsers(dest='cmd', required=True)

    d = sub.add_parser('render-docker-args')
    d.add_argument('--profile', required=True)
    d.add_argument('--run-dir', default='runs/boot')
    d.add_argument('--port', type=int, default=8002)
    d.add_argument('--airgap', action='store_true')
    d.set_defaults(_handler=_cmd_render_docker_args)

    v = sub.add_parser('render-vllm-cli')
    v.add_argument('--profile', required=True)
    v.set_defaults(_handler=_cmd_render_vllm_cli)

    i = sub.add_parser('render-image-ref')
    i.add_argument('--profile', required=True)
    i.set_defaults(_handler=_cmd_render_image_ref)

    args = p.parse_args(argv)
    return args._handler(args)


if __name__ == '__main__':
    sys.exit(main())
```

**2. `scripts/smoke_test.py`** — orchestrates wait_for_vllm + 3 canaries + D-06 rollback on failure:

```python
#!/usr/bin/env python3
"""Phase 1 smoke-test orchestrator. Invoked by start_emmy.sh after docker run."""
from __future__ import annotations
import argparse, sys, time
from pathlib import Path
from datetime import datetime, timezone

from emmy_serve.boot.probe import wait_for_vllm
from emmy_serve.canary import (
    run_sp_ok, run_tool_call, run_generate, load_default_tool_schema,
    CanaryResult, log_canary_event,
)
from emmy_serve.profile.loader import load_profile
from emmy_serve.diagnostics.bundle import write_boot_failure_bundle
from emmy_serve.diagnostics.layout import EmmyRunLayout, new_run_id


def _now_iso() -> str:
    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")


def main(argv: list[str] | None = None) -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--base-url", required=True)
    ap.add_argument("--profile", required=True)
    ap.add_argument("--run-dir", required=True, help="Target dir for canary logs (success case)")
    ap.add_argument("--fail-dir", default="runs", help="Base dir for boot-failure bundle")
    args = ap.parse_args(argv)

    profile_path = Path(args.profile)
    run_dir = Path(args.run_dir)
    run_dir.mkdir(parents=True, exist_ok=True)

    serving, _, profile_ref = load_profile(profile_path)
    served_model = serving.engine.served_model_name

    def _fail(check: str, reason: str, prompt: str, response_text: str, response_json: dict | None = None) -> int:
        run_id = new_run_id()
        layout = EmmyRunLayout(base_dir=Path(args.fail_dir) / "boot-failures", run_id=run_id, kind="boot-failure")
        write_boot_failure_bundle(
            layout,
            profile_ref=profile_ref,
            check=check,
            reason=reason,
            prompt_text=prompt,
            response_text=response_text,
            response_json=response_json,
        )
        print(f"BOOT REJECTED ({check}): {reason}", file=sys.stderr)
        print(f"See: {layout.run_dir}", file=sys.stderr)
        return 1

    # --- 1. wait for /v1/models ---
    try:
        wait_for_vllm(args.base_url, timeout_s=300, interval_s=0.5)
    except TimeoutError as e:
        return _fail("wait_for_vllm", str(e), "", "")

    # --- 2. SP_OK canary (D-07) ---
    t0 = time.monotonic()
    try:
        ok, resp = run_sp_ok(args.base_url, served_model)
    except Exception as e:
        return _fail("sp_ok", f"exception: {e}", "(ping)", str(e))
    elapsed_ms = int((time.monotonic() - t0) * 1000)
    log_canary_event(
        run_dir / "canary.jsonl",
        CanaryResult(
            check="sp_ok", ok=ok, elapsed_ms=elapsed_ms,
            profile_id=profile_ref.id, profile_version=profile_ref.version, profile_hash=profile_ref.hash,
            served_model_name=served_model, ts=_now_iso(), response_excerpt=resp[:200],
        ),
    )
    if not ok:
        return _fail("sp_ok", f"[SP_OK] not found in response", "ping", resp)

    # --- 3. tool_call canary (D-08) ---
    t0 = time.monotonic()
    try:
        ok, msg = run_tool_call(args.base_url, served_model, load_default_tool_schema())
    except Exception as e:
        return _fail("tool_call", f"exception: {e}", "", str(e))
    elapsed_ms = int((time.monotonic() - t0) * 1000)
    log_canary_event(
        run_dir / "canary.jsonl",
        CanaryResult(
            check="tool_call", ok=ok, elapsed_ms=elapsed_ms,
            profile_id=profile_ref.id, profile_version=profile_ref.version, profile_hash=profile_ref.hash,
            served_model_name=served_model, ts=_now_iso(), response_excerpt=str(msg)[:200],
        ),
    )
    if not ok:
        return _fail("tool_call", "tool_call did not parse correctly", "", str(msg), response_json=msg if isinstance(msg, dict) else None)

    # --- 4. 100-token generation ---
    t0 = time.monotonic()
    try:
        ok, data, dur = run_generate(args.base_url, served_model)
    except Exception as e:
        return _fail("generate", f"exception: {e}", "", str(e))
    elapsed_ms = int((time.monotonic() - t0) * 1000)
    content = data['choices'][0]['message']['content']
    tokens_out = data.get("usage", {}).get("completion_tokens", 0)
    tok_per_s = tokens_out / dur if dur > 0 else 0.0
    log_canary_event(
        run_dir / "canary.jsonl",
        CanaryResult(
            check="generate", ok=ok, elapsed_ms=elapsed_ms,
            profile_id=profile_ref.id, profile_version=profile_ref.version, profile_hash=profile_ref.hash,
            served_model_name=served_model, ts=_now_iso(),
            response_excerpt=f"tok/s={tok_per_s:.1f} finish={data['choices'][0].get('finish_reason')}",
        ),
    )
    if not ok:
        return _fail("generate", f"generate not ok (tok/s={tok_per_s:.1f})", "Count 1-100", content or "")

    # Emit SC-1 throughput signal for start_emmy.sh to surface in the ready banner
    print(f"smoke ok: tok/s={tok_per_s:.2f} tokens_out={tokens_out}")
    return 0


if __name__ == '__main__':
    sys.exit(main())
```
chmod +x `scripts/smoke_test.py`.

**Run after writing:**
```bash
cd /data/projects/emmy && uv run pytest tests/unit/test_docker_run_build.py -x -v
```
  </action>
  <verify>
    <automated>cd /data/projects/emmy && uv run pytest tests/unit/test_docker_run_build.py -x -v && uv run python -m emmy_serve.boot.runner render-docker-args --profile profiles/qwen3.6-35b-a3b/v1 --run-dir /tmp/boot --port 8002 | grep -q fastsafetensors && uv run python -m emmy_serve.boot.runner render-vllm-cli --profile profiles/qwen3.6-35b-a3b/v1 | grep -q enable-prefix-caching</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /data/projects/emmy/emmy_serve/boot/runner.py && grep -q "def render_docker_args" /data/projects/emmy/emmy_serve/boot/runner.py && grep -q "def render_vllm_cli_args" /data/projects/emmy/emmy_serve/boot/runner.py && grep -q "def render_image_ref" /data/projects/emmy/emmy_serve/boot/runner.py`
    - `grep -q "\\-\\-network.*none" /data/projects/emmy/emmy_serve/boot/runner.py`
    - `grep -q "fastsafetensors" /data/projects/emmy/emmy_serve/boot/runner.py`
    - `grep -q "enable-prefix-caching" /data/projects/emmy/emmy_serve/boot/runner.py`
    - `grep -q "enable-chunked-prefill" /data/projects/emmy/emmy_serve/boot/runner.py`
    - `grep -q "tool-call-parser" /data/projects/emmy/emmy_serve/boot/runner.py`
    - `test -x /data/projects/emmy/scripts/smoke_test.py && grep -q "wait_for_vllm" /data/projects/emmy/scripts/smoke_test.py && grep -q "run_sp_ok" /data/projects/emmy/scripts/smoke_test.py && grep -q "run_tool_call" /data/projects/emmy/scripts/smoke_test.py && grep -q "run_generate" /data/projects/emmy/scripts/smoke_test.py && grep -q "write_boot_failure_bundle" /data/projects/emmy/scripts/smoke_test.py`
    - `cd /data/projects/emmy && uv run python -m emmy_serve.boot.runner render-image-ref --profile profiles/qwen3.6-35b-a3b/v1` prints a string starting with `nvcr.io/nvidia/vllm@sha256:`
    - `cd /data/projects/emmy && uv run pytest tests/unit/test_docker_run_build.py -x` exits 0 (all 5 subtests green)
  </acceptance_criteria>
  <done>render_docker_args + render_vllm_cli_args + render_image_ref callable and emit correct flags; scripts/smoke_test.py orchestrates wait + 3 canaries + D-06 on failure; canary.jsonl gets 3 rows on success.</done>
</task>

<task type="auto">
  <name>Task 3: Implement scripts/start_emmy.sh (REPRO-01 one-command contract)</name>
  <files>scripts/start_emmy.sh</files>
  <read_first>
    - /data/projects/emmy/.planning/phases/01-serving-foundation-profile-schema/01-RESEARCH.md §14 lines 1891-1947 (full start_emmy.sh contract — copy verbatim + adjust paths)
    - /data/projects/setup_local_opencode/scripts/start_unified.sh (prior-repo orchestrator shape)
    - /data/projects/emmy/tests/unit/test_start_script.py (shellcheck + digest-match + exit-code-literals assertions)
  </read_first>
  <behavior>
    - `./scripts/start_emmy.sh [--profile PATH] [--port N] [--airgap]` runs the full boot flow
    - Pre-flight: docker info, nvidia-smi, python3 available, /data/models exists, /data/hf-cache exists → exit 4 on any missing
    - Validates profile (`scripts/validate_profile.py` + `scripts/hash_profile.py --check`) → exit 2 on failure
    - Reads digest from serving.yaml via a short python snippet; if digest != pulled image digest, exit 3
    - Renders docker args via `python -m emmy_serve.boot.runner render-docker-args/render-image-ref/render-vllm-cli`
    - `docker run -d --name emmy-serve <docker-args> <image-ref> <vllm-cli-args>`
    - Invokes `scripts/smoke_test.py`; on non-zero → bundle already written by smoke_test, `docker stop emmy-serve`, exit 1
    - On success: prints ready banner + cold-start elapsed + throughput from smoke output
    - Exit codes: 0 ready, 1 boot rejected, 2 schema invalid, 3 digest mismatch, 4 prereq missing
  </behavior>
  <action>
Create `scripts/start_emmy.sh` per RESEARCH.md §14 contract. Make executable.

```bash
#!/usr/bin/env bash
# start_emmy.sh — Phase 1 one-command contract (REPRO-01)
#
# Usage:
#   ./scripts/start_emmy.sh [--profile profiles/qwen3.6-35b-a3b/v1] [--port 8002] [--airgap]
#
# Exit codes:
#   0 — vLLM is up, smoke test passed, ready for harness
#   1 — boot rejected (diagnostic bundle in runs/boot-failures/)
#   2 — profile schema invalid
#   3 — container digest mismatch
#   4 — prerequisite missing (docker, nvidia-smi, python3, model dir, HF cache)

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

PROFILE="profiles/qwen3.6-35b-a3b/v1"
PORT=8002
AIRGAP=0

while [[ $# -gt 0 ]]; do
  case "$1" in
    --profile) PROFILE="$2"; shift 2 ;;
    --port) PORT="$2"; shift 2 ;;
    --airgap) AIRGAP=1; shift ;;
    -h|--help) sed -n '1,14p' "$0"; exit 0 ;;
    *) echo "unknown arg: $1" >&2; exit 4 ;;
  esac
done

RUN_ID="$(date -u +'%Y%m%dT%H%M%SZ')-$(head -c 6 /dev/urandom | xxd -p | head -c 6)"
RUN_DIR="runs/${RUN_ID}-boot"
mkdir -p "$RUN_DIR"
BOOT_START=$(date +%s)

echo "emmy: starting profile=${PROFILE} port=${PORT} airgap=${AIRGAP} run_id=${RUN_ID}" >&2

# --- 1. Pre-flight (exit 4 on any missing prereq) ---
if ! command -v docker >/dev/null 2>&1; then
  echo "ERROR (prereq): docker not installed" >&2; exit 4
fi
if ! docker info >/dev/null 2>&1; then
  echo "ERROR (prereq): cannot connect to Docker daemon (try: sudo usermod -aG docker \$USER)" >&2; exit 4
fi
if ! command -v nvidia-smi >/dev/null 2>&1; then
  echo "ERROR (prereq): nvidia-smi not installed" >&2; exit 4
fi
if ! command -v uv >/dev/null 2>&1; then
  echo "ERROR (prereq): uv not installed" >&2; exit 4
fi
if [[ ! -d /data/models ]]; then
  echo "ERROR (prereq): /data/models not mounted" >&2; exit 4
fi
if [[ ! -d /data/hf-cache ]] && [[ ! -d "${HOME}/.cache/huggingface" ]]; then
  echo "ERROR (prereq): HF cache dir not found (tried /data/hf-cache and ~/.cache/huggingface)" >&2; exit 4
fi

# --- 2. Validate profile (exit 2 on schema invalid / hash mismatch) ---
if ! uv run emmy profile validate "$PROFILE" >&2; then
  echo "ERROR (schema/hash): profile failed validation" >&2; exit 2
fi

# --- 3. Resolve image ref from profile (exit 3 on digest issue) ---
IMAGE_REF="$(uv run python -m emmy_serve.boot.runner render-image-ref --profile "$PROFILE")"
if [[ -z "$IMAGE_REF" ]]; then
  echo "ERROR (digest): could not render image ref" >&2; exit 3
fi
echo "emmy: image ref = $IMAGE_REF" >&2

# Assert local image matches the digest (pulled once by operator per REPRO-01)
if ! docker inspect "$IMAGE_REF" >/dev/null 2>&1; then
  echo "ERROR (digest): local image not found; run 'docker pull nvcr.io/nvidia/vllm:26.03.post1-py3' first" >&2
  exit 3
fi

# --- 4. Stop any existing emmy-serve container ---
docker stop emmy-serve >/dev/null 2>&1 || true
docker rm emmy-serve >/dev/null 2>&1 || true

# --- 5. Build docker args + vllm cli args ---
DOCKER_ARGS_STR="$(uv run python -m emmy_serve.boot.runner render-docker-args \
  --profile "$PROFILE" --run-dir "$RUN_DIR" --port "$PORT" \
  $([ "$AIRGAP" = "1" ] && echo "--airgap" || echo ""))"
VLLM_CLI_STR="$(uv run python -m emmy_serve.boot.runner render-vllm-cli --profile "$PROFILE")"

# --- 6. docker run (detached) ---
# shellcheck disable=SC2086
# (we need word-splitting here; the renderer uses shlex.quote for safety)
eval docker run --name emmy-serve --detach $DOCKER_ARGS_STR "$IMAGE_REF" $VLLM_CLI_STR \
  > "$RUN_DIR/docker-run.log" 2>&1

echo "emmy: container started; running smoke test" >&2

# --- 7. Smoke test (exit 1 on failure) ---
BASE_URL="http://127.0.0.1:${PORT}"
if [[ "$AIRGAP" = "1" ]]; then
  # airgap mode: smoke test runs via docker exec inside the --network none container
  BASE_URL="http://127.0.0.1:8000"  # container-internal port
  if ! docker exec emmy-serve python3 /app/smoke_host.py --base-url "$BASE_URL" --profile "$PROFILE" --run-dir "$RUN_DIR" 2>&1 | tee -a "$RUN_DIR/smoke.log"; then
    # D-06 bundle already written by smoke_test.py into runs/boot-failures/
    docker stop emmy-serve >/dev/null 2>&1 || true
    echo "BOOT REJECTED — see runs/boot-failures/" >&2
    exit 1
  fi
else
  if ! uv run python scripts/smoke_test.py --base-url "$BASE_URL" --profile "$PROFILE" --run-dir "$RUN_DIR" 2>&1 | tee -a "$RUN_DIR/smoke.log"; then
    docker stop emmy-serve >/dev/null 2>&1 || true
    echo "BOOT REJECTED — see runs/boot-failures/" >&2
    exit 1
  fi
fi

# --- 8. Ready banner ---
BOOT_END=$(date +%s)
ELAPSED=$((BOOT_END - BOOT_START))
THROUGHPUT="$(grep 'smoke ok:' "$RUN_DIR/smoke.log" | tail -1 | sed 's/.*tok\/s=//' | awk '{print $1}')"
echo "emmy-serve ready — profile ${PROFILE} on ${BASE_URL} (cold-start ${ELAPSED}s, ${THROUGHPUT:-?} tok/s)"
exit 0
```

Make executable: `chmod +x scripts/start_emmy.sh`.

Run shellcheck to verify syntax:
```bash
cd /data/projects/emmy
shellcheck scripts/start_emmy.sh 2>&1 | head -30   # should be clean (exit 0) or only warnings we've disabled
```

**Run unit tests:**
```bash
cd /data/projects/emmy && uv run pytest tests/unit/test_start_script.py -x -v
```
  </action>
  <verify>
    <automated>cd /data/projects/emmy && uv run pytest tests/unit/test_start_script.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -x /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'set -euo pipefail' /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'exit 1' /data/projects/emmy/scripts/start_emmy.sh && grep -q 'exit 2' /data/projects/emmy/scripts/start_emmy.sh && grep -q 'exit 3' /data/projects/emmy/scripts/start_emmy.sh && grep -q 'exit 4' /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'render-docker-args' /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'render-image-ref' /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'smoke_test.py' /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'emmy profile validate' /data/projects/emmy/scripts/start_emmy.sh`
    - `grep -q 'runs/boot-failures' /data/projects/emmy/scripts/start_emmy.sh`
    - `if command -v shellcheck >/dev/null 2>&1; then shellcheck /data/projects/emmy/scripts/start_emmy.sh; else echo "shellcheck not installed — skipping"; fi` exits 0
    - `cd /data/projects/emmy && bash -n scripts/start_emmy.sh` exits 0 (syntax valid even without running)
    - `cd /data/projects/emmy && ./scripts/start_emmy.sh --help` prints usage and exits 0
  </acceptance_criteria>
  <done>start_emmy.sh matches RESEARCH.md §14 contract; shellcheck passes; exit codes 0/1/2/3/4 all present; validator call + digest-from-profile read + smoke-test orchestration + D-06 rollback path all wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Operator: pull NGC image, capture digest, first-boot + integration tests on DGX Spark</name>
  <what-built>start_emmy.sh + smoke_test.py + emmy.canary + D-06 bundle are all implemented. This checkpoint is the human-gated step that (a) captures the NGC image digest on first pull (one-time; clears the `test_digest_not_placeholder` xfail), (b) runs the integration test suite against the real Spark, (c) confirms SC-1 (≥60 tok/s) and SC-3 (three-check smoke) are met end-to-end.</what-built>
  <how-to-verify>
On DGX Spark:

```bash
cd /data/projects/emmy

# --- 1. Pull the NGC image and capture the digest (one-time) ---
docker pull nvcr.io/nvidia/vllm:26.03.post1-py3
DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' nvcr.io/nvidia/vllm:26.03.post1-py3 | sed 's/.*@//')
echo "Captured digest: $DIGEST"

# --- 2. Write the digest into serving.yaml ---
python3 <<EOF
import yaml, pathlib
p = pathlib.Path("profiles/qwen3.6-35b-a3b/v1/serving.yaml")
d = yaml.safe_load(p.read_text())
d['engine']['container_image_digest'] = "$DIGEST"
p.write_text(yaml.safe_dump(d, sort_keys=False))
EOF

# --- 3. Recompute the profile hash (immutable-layer-1 re-commit) ---
uv run emmy profile hash profiles/qwen3.6-35b-a3b/v1/ --write
uv run emmy profile validate profiles/qwen3.6-35b-a3b/v1/
# Expect exit 0 (schema valid + hash matches) — placeholder sentinel cleared

# --- 4. Remove the xfail on test_digest_not_placeholder ---
# Edit tests/unit/test_container_digest.py and delete the
# @pytest.mark.xfail(reason="Plan 03 operator task...") decorator on test_digest_not_placeholder

uv run pytest tests/unit/test_container_digest.py -x -v
# Expect: both test_digest_format_valid and test_digest_not_placeholder PASSED

# --- 5. First boot test ---
./scripts/start_emmy.sh --profile profiles/qwen3.6-35b-a3b/v1 --port 8002
# Expect:
#   - container starts
#   - smoke test runs (SP_OK echo found, tool_call parsed, 100-token generation succeeds)
#   - ready banner printed: "emmy-serve ready — profile profiles/qwen3.6-35b-a3b/v1 on http://127.0.0.1:8002 (cold-start Ns, Xtoks tok/s)"
#   - exit code 0
# The ready banner MUST show tok/s ≥ 60.

# --- 6. Run integration test suite ---
uv run pytest tests/integration/test_boot.py -x -v --run-integration
# Expect all GREEN:
#   - test_models_endpoint
#   - test_throughput_floor (≥60 tok/s assertion)
#   - test_extra_body_passthrough
#   - test_cold_start_time (<240s)
#   - test_smoke_all_three

# --- 7. Test the D-06 rollback path (inject a failure) ---
docker stop emmy-serve

# Temporarily break the SP_OK assertion via a profile edit (simulate regression):
# Copy v1 -> v1-broken, mess with the system prompt, verify boot fails loud:
cp -r profiles/qwen3.6-35b-a3b/v1 /tmp/v1-broken
echo "nonsense system prompt" > /tmp/v1-broken/prompts/system.md
uv run emmy profile hash /tmp/v1-broken --write   # recompute so schema passes
# Expect: start_emmy.sh exits 1 with "BOOT REJECTED"
./scripts/start_emmy.sh --profile /tmp/v1-broken --port 8002 || true
ls runs/boot-failures/*-boot-failure/ | head   # expect: check.json, profile.json, prompt.txt, response.txt, env.json, docker-logs.txt, metrics-snapshot.txt

rm -rf /tmp/v1-broken
docker stop emmy-serve >/dev/null 2>&1 || true

# --- 8. Commit the digest + cleared xfail ---
git add profiles/qwen3.6-35b-a3b/v1/serving.yaml profiles/qwen3.6-35b-a3b/v1/profile.yaml tests/unit/test_container_digest.py
git commit -m "feat(01-03): capture NGC digest + clear digest xfail"

# Optional: reboot for Plan 04's consumption
./scripts/start_emmy.sh
```
  </how-to-verify>
  <resume-signal>Type "boot green" once: (a) the digest is written into serving.yaml + profile.yaml.hash is recomputed, (b) `uv run pytest tests/unit/test_container_digest.py` exits 0 with test_digest_not_placeholder PASSED (xfail removed), (c) `./scripts/start_emmy.sh` exits 0 with a ready banner showing tok/s ≥ 60, (d) `uv run pytest tests/integration/test_boot.py --run-integration` exits 0, (e) the D-06 failure-path demonstration produced a populated runs/boot-failures/ dir with all 7 files.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell → docker daemon | start_emmy.sh requires docker-group membership (not root); verified in pre-flight |
| serving.yaml → running container env | Env vars are passed via -e flags; ServingConfig schema enforces VLLM_NO_USAGE_STATS="1" + HF_HUB_OFFLINE="1" |
| NGC digest source of truth | serving.yaml is the single source; start_emmy.sh reads from it only (no second hardcoded copy) |
| smoke test → runs/ | Failure bundle path written under runs/boot-failures/<iso>/ via atomic writers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | NGC image drift (tag moves, digest changes silently) | mitigate | start_emmy.sh does NOT `docker pull` at boot; it asserts the locally-present image matches the digest in serving.yaml via `docker inspect "$IMAGE_REF"`. Exit 3 on mismatch. Operator step in Task 4 captures digest once, writes to serving.yaml, immutability Layer 1 enforces subsequent change → v2. |
| T-03-02 | Tampering | start_emmy.sh mutation bypassing profile validator | mitigate | Script invokes `uv run emmy profile validate` as first step (exit 2 on failure); `shellcheck` in test_start_script.py catches syntactic drift; digest-match test (`test_digest_match`) asserts start_emmy.sh doesn't hardcode a digest (must read from serving.yaml). |
| T-03-03 | SP-delivery-failure | Silent system-prompt drop (Pitfall #6 — Critical; prior-repo Phase 3 incident) | mitigate | SP_OK canary runs on every boot; `[SP_OK]` substring check with literal trigger word "ping" (chosen for low-collision per Pitfall #2 in RESEARCH.md §Common Pitfalls). Fail-loud → D-06 bundle → docker stop → exit 1. Additionally, the bundle preserves the assembled system prompt + model response for post-mortem. |
| T-03-04 | Tampering | Tool-call-parser mismatch causing silent wire-format bug | mitigate | tool_call canary exercises the qwen3_coder parser with a minimal deterministic read_file schema; failure fails the boot (not just a warning). |
| T-03-05 | DoS | Infinite poll loop if vLLM never comes up | mitigate | wait_for_vllm has 300s hard timeout; raises TimeoutError → D-06 bundle → docker stop. |
| T-03-06 | Information disclosure | D-06 bundle may contain model response excerpts with sensitive data | accept | runs/ is gitignored; local-only. Phase 7 publication story will apply redaction. |
| T-03-07 | DoS | Concurrent start_emmy.sh invocations conflicting on `emmy-serve` name | mitigate | `docker stop emmy-serve || true` + `docker rm emmy-serve || true` at step 4; name is fixed. For parallel profiles, Phase 4 will parameterize. |

**Air-gap-bypass mitigation:** start_emmy.sh supports `--airgap` flag (sets `--network none` via render_docker_args). Plan 05 wires this into CI.

**Profile-tampering mitigation:** Layer-1 validator runs as the first pre-flight step; any edit to v1/ without hash recompute exits 2.

**Container-drift mitigation:** Task 4 operator step is the one and only place where the digest is written (after the initial pull); subsequent starts load from serving.yaml and fail-fast on mismatch.

**SP_OK-canary mitigation:** Fully implemented and wired into boot gate; D-07 prompt + D-08 tool_call + 100-token generate all run before the ready banner.
</threat_model>

<verification>
- `uv run pytest tests/unit/test_canary.py tests/unit/test_docker_run_build.py tests/unit/test_start_script.py -x` exits 0
- `uv run pytest tests/unit/test_container_digest.py -x` exits 0 (after operator step clears xfail)
- `./scripts/start_emmy.sh --help` exits 0 with usage text
- `./scripts/start_emmy.sh` (operator step on Spark) exits 0 with ready banner, tok/s ≥ 60
- `uv run pytest tests/integration/test_boot.py --run-integration` exits 0
- D-06 bundle demonstration: runs/boot-failures/<iso>-boot-failure/ populated with check.json, profile.json, prompt.txt, response.txt, env.json, docker-logs.txt, metrics-snapshot.txt
</verification>

<success_criteria>
1. `emmy.canary` package shipped and importable (EVAL-07)
2. `scripts/start_emmy.sh` implements RESEARCH.md §14 contract verbatim including exit codes 0/1/2/3/4
3. Boot-time smoke test gates on SP_OK + tool_call + 100-token generate; any failure yields a D-06 bundle and `docker stop` + exit 1
4. SC-1 satisfied: one-command boot on DGX Spark with measured tok/s ≥ 60
5. SC-3 satisfied: three-check smoke test is the gate, with fail-loud + rollback + diagnostic bundle
6. `test_digest_not_placeholder` cleared (operator captured real digest)
</success_criteria>

<output>
After completion, create `.planning/phases/01-serving-foundation-profile-schema/01-03-SUMMARY.md` documenting:
- Captured NGC digest value + the `docker inspect` command used
- Measured cold-start time + 100-token tok/s on the first boot
- D-06 bundle layout confirmed (with the demonstration failure excerpt)
- Which integration tests ran + their results
- Operator TODO for Plan 04: leave container running (or `./scripts/start_emmy.sh` again) before running the finder
</output>
